import type { Technology } from '../../types/content'

const javascript: Technology = {
  id: 'javascript',
  name: 'JavaScript',
  icon: 'JS',
  color: '#f7df1e',
  tagline: 'The language of the web — single-threaded, event-driven, prototype-based.',
  category: 'language',

  overview: {
    what: 'JavaScript is a high-level, single-threaded, garbage-collected, prototype-based, multi-paradigm language with a non-blocking event loop concurrency model. It runs in browsers (V8, SpiderMonkey, JavaScriptCore) and on servers (Node.js, Deno, Bun).',
    why: 'Created in 1995 by Brendan Eich at Netscape to make web pages interactive without round-trips to the server. It evolved from a scripting glue language into the only language natively executed by every browser.',
    problemsSolved: [
      'Client-side interactivity without page reloads (DOM manipulation, events)',
      'Asynchronous I/O without threads — one language for UI and network concurrency',
      'Universal runtime: one language across browser, server, mobile (React Native), desktop (Electron)',
    ],
    whenToUse: [
      'Any browser-based UI — there is no alternative for DOM scripting',
      'I/O-heavy servers: APIs, gateways, real-time apps (chat, live dashboards)',
      'Full-stack teams that want one language end to end',
    ],
    whenNotToUse: [
      'CPU-bound workloads (video encoding, ML training) — the single thread blocks; use worker threads or another language',
      'Systems demanding strict memory control or hard real-time guarantees',
      'Heavy numeric computing — no integer types beyond Number/BigInt, no SIMD ergonomics',
    ],
  },

  internals: [
    {
      id: 'execution-context',
      title: 'Execution Context & Call Stack',
      summary: 'Every piece of JS runs inside an execution context. Contexts are created in two phases — memory allocation, then execution — and managed on the call stack.',
      details: [
        'When a script loads, the engine creates the Global Execution Context (GEC). Each function invocation creates a new Function Execution Context pushed onto the call stack.',
        'Phase 1 (creation): the engine scans the code, allocates memory for variables (var → undefined, let/const → uninitialized "temporal dead zone") and stores whole function declarations. This is what people call hoisting.',
        'Phase 2 (execution): code runs line by line, assignments happen, functions are invoked (pushing new contexts).',
        'When a function returns, its context pops off the stack. Stack overflow happens when recursion never terminates.',
        'Each context holds: a Variable Environment, a Lexical Environment (scope chain reference), and the `this` binding.',
      ],
      diagram: ['Script loads', 'Global Execution Context created', 'Memory phase: vars → undefined, functions stored', 'Execution phase: line-by-line run', 'Function call → new context pushed', 'Function returns → context popped'],
      code: {
        lang: 'js',
        code: `console.log(a)      // undefined (var hoisted)\nconsole.log(b)      // ReferenceError (TDZ)\ngreet()             // "hi" (function declaration hoisted)\n\nvar a = 1\nlet b = 2\nfunction greet() { console.log('hi') }`,
        caption: 'Hoisting is a consequence of the creation phase, not a magic "lifting" of code.',
      },
    },
    {
      id: 'event-loop',
      title: 'Event Loop, Microtasks & Macrotasks',
      summary: 'JS is single-threaded; concurrency comes from the host (browser/Node) plus a loop that drains queues between stack runs. Microtasks (promises) always beat macrotasks (timers).',
      details: [
        'The call stack runs synchronous code. Async APIs (setTimeout, fetch, DOM events) are handled by the host environment, not the JS engine.',
        'When an async operation completes, its callback is queued: promise callbacks and queueMicrotask go to the microtask queue; timers, I/O and UI events go to the macrotask (task) queue.',
        'The event loop: when the stack empties, drain the ENTIRE microtask queue first (including microtasks queued by microtasks), then take ONE macrotask, then drain microtasks again, then (in browsers) possibly render.',
        'This is why a resolved promise callback always runs before setTimeout(fn, 0), and why an infinite microtask chain can starve rendering.',
        'async/await is syntax over promises: everything after `await` is scheduled as a microtask continuation.',
      ],
      diagram: ['Call Stack executes sync code', 'Async work handed to Web APIs / libuv', 'Completion → callback queued', 'Stack empty?', 'Drain ALL microtasks (promises)', 'Run ONE macrotask (timer/IO)', 'Render (browser)', 'Loop'],
      code: {
        lang: 'js',
        code: `console.log('1')\nsetTimeout(() => console.log('2'), 0)\nPromise.resolve().then(() => console.log('3'))\nconsole.log('4')\n// Output: 1, 4, 3, 2 — microtasks before macrotasks`,
        caption: 'The classic output-prediction interview question.',
      },
    },
    {
      id: 'memory',
      title: 'Memory Heap & Garbage Collection',
      summary: 'Objects live on the heap; the engine reclaims memory via generational mark-and-sweep GC. Leaks are usually accidental references, not GC failures.',
      details: [
        'Primitives (on the stack/inline) vs objects, arrays, functions, closures (heap-allocated).',
        'V8 uses generational GC: new objects go to the young generation (fast scavenge collections); survivors are promoted to the old generation (slower mark-sweep-compact).',
        'Reachability is the rule: anything reachable from roots (global object, current stack, active closures) is kept.',
        'Common leak sources: forgotten timers/intervals, detached DOM nodes still referenced from JS, ever-growing caches/maps, closures capturing large structures.',
        'WeakMap/WeakRef hold references that do NOT prevent collection — ideal for metadata caches keyed by objects.',
      ],
      diagram: ['Allocation → Young Generation', 'Scavenge GC (frequent, fast)', 'Survivors promoted → Old Generation', 'Mark → Sweep → Compact (less frequent)', 'Unreachable memory reclaimed'],
    },
    {
      id: 'scope-closures',
      title: 'Lexical Scope & Closures',
      summary: 'Scope is determined by where code is written, not where it runs. A closure is a function bundled with its lexical environment, keeping outer variables alive after the outer function returns.',
      details: [
        'Each function carries a hidden reference to the Lexical Environment where it was DEFINED. Variable lookups walk this scope chain outward.',
        'When an inner function survives its outer function (returned, stored, passed as callback), the engine keeps the captured variables on the heap instead of destroying them — that is a closure.',
        'Closures power data privacy (module pattern), factory functions, memoization, debounce/throttle, and React/Vue hooks.',
        'Classic pitfall: `var` in a loop shares one binding across all iterations; `let` creates a fresh binding per iteration.',
        'Closures can leak memory if a long-lived callback captures a large object it no longer needs.',
      ],
      code: {
        lang: 'js',
        code: `function counter() {\n  let count = 0            // lives on the heap, owned by the closure\n  return () => ++count\n}\nconst inc = counter()\ninc() // 1\ninc() // 2 — count survives because inc still references it`,
      },
    },
    {
      id: 'prototypes',
      title: 'Prototypes & Inheritance',
      summary: 'JS objects delegate property lookups to a chain of prototype objects ending at null. Classes are syntax sugar over this mechanism.',
      details: [
        'Every object has an internal [[Prototype]] link (accessed via Object.getPrototypeOf or __proto__). Property reads walk the chain until found or null.',
        'Functions have a `.prototype` property; `new Fn()` creates an object whose [[Prototype]] is Fn.prototype.',
        '`class` / `extends` compile down to prototype wiring — there are no classical classes at runtime.',
        'Writes do NOT walk the chain: assigning a property creates/shadows it on the instance itself.',
        'Method lookup cost is why engines use hidden classes and inline caches; changing object shapes dynamically can deoptimize hot code.',
      ],
      diagram: ['instance', '[[Prototype]] → Constructor.prototype', '[[Prototype]] → Object.prototype', '[[Prototype]] → null'],
    },
    {
      id: 'this-binding',
      title: '`this` Binding Rules',
      summary: '`this` is decided at call time by HOW a function is called — except arrow functions, which inherit it lexically.',
      details: [
        'Default binding: plain call → undefined in strict mode, globalThis otherwise.',
        'Implicit binding: obj.method() → this is obj (the object left of the dot at call time).',
        'Explicit binding: call/apply set this for one call; bind returns a permanently-bound new function.',
        '`new` binding: this is the freshly created object.',
        'Arrow functions have NO own this — they capture it from the enclosing scope at definition time. That is why they fix the "lost this" callback problem and why they cannot be constructors.',
        'Precedence: new > bind > implicit > default.',
      ],
    },
    {
      id: 'promises-async',
      title: 'Promises & async/await Internals',
      summary: 'A promise is a state machine (pending → fulfilled/rejected) with subscriber queues. async/await compiles to promise chains with suspension points.',
      details: [
        'States are one-way: once settled, a promise never changes. .then returns a NEW promise, enabling chaining.',
        'Callbacks registered via .then/.catch/.finally run as microtasks, never synchronously — even on an already-resolved promise.',
        'An async function returns a promise immediately; `await` suspends the function, and the remainder resumes as a microtask when the awaited promise settles.',
        'Combinators: Promise.all (fail-fast), allSettled (never rejects), race (first to settle), any (first to fulfill).',
        'Unhandled rejections crash Node processes (since v15) — always terminate chains with .catch or try/catch around await.',
      ],
    },
    {
      id: 'modules',
      title: 'Modules: ESM vs CommonJS',
      summary: 'ESM is static (analyzable, tree-shakeable, async-loaded, live bindings); CommonJS is dynamic (synchronous, value copies).',
      details: [
        'ESM imports are hoisted, statically analyzed before execution — enabling tree shaking and circular-dependency handling via live bindings.',
        'CommonJS require() executes synchronously at runtime and exports a value snapshot (for primitives).',
        'ESM in browsers loads asynchronously over the network; bundlers (Vite, webpack) flatten modules for production.',
        'Node supports both: .mjs / "type": "module" for ESM. Interop pitfalls: default export mismatch, no __dirname in ESM.',
      ],
    },
  ],

  keywords: [
    {
      id: 'event-loop',
      term: 'Event Loop',
      importance: 5,
      whyAsked: 'It is the single best discriminator between developers who use async code and those who understand it. Nearly every JS interview includes an output-prediction question.',
      explanation: 'The event loop coordinates the call stack with task queues. When the stack is empty, it drains all microtasks (promise callbacks), then runs one macrotask (timer, I/O), then repeats. JS itself never blocks waiting for I/O — the host environment does the waiting and queues callbacks.',
      realWorldExample: 'A spinner that never animates because a heavy synchronous loop blocks the stack — the rendering step of the loop never gets a turn.',
      commonMistakes: [
        'Saying "setTimeout(fn, 0) runs immediately" — it runs after the current stack AND all microtasks',
        'Believing promises run on another thread — executor code is synchronous; only the callbacks are deferred',
        'Confusing Node\'s multi-phase loop (timers, poll, check) with the simpler browser model',
      ],
      followUps: ['What is the output of mixed setTimeout/promise code?', 'What is the difference between microtasks and macrotasks?', 'How does Node\'s event loop differ from the browser\'s?', 'What is starvation and how can microtasks cause it?'],
      diagram: ['Call Stack', 'Web APIs (host)', 'Microtask Queue ← promises', 'Macrotask Queue ← timers/IO', 'Event Loop ties them together'],
    },
    {
      id: 'closure',
      term: 'Closure',
      importance: 5,
      whyAsked: 'Closures underpin almost every JS pattern — modules, hooks, debounce, currying. Interviewers use it to test understanding of memory and scope, not just syntax.',
      explanation: 'A closure is a function that retains access to its lexical scope even when executed outside that scope. The engine keeps the captured variables alive on the heap as long as the inner function is reachable.',
      realWorldExample: 'A debounce utility: the timer ID lives in a closure between calls. Event handler factories in loops. Private state in composables/hooks.',
      commonMistakes: [
        'Defining it as just "a function inside a function" — the key is the persistent reference to outer variables',
        'The var-in-loop bug: all callbacks log the final value because they share one binding',
        'Not knowing closures can cause memory leaks when long-lived listeners capture large objects',
      ],
      followUps: ['How do closures relate to memory leaks?', 'Implement a counter / once() / memoize with closures', 'Why does var in a for-loop break callbacks and how does let fix it?'],
      diagram: ['Outer function runs', 'Inner function defined → captures scope', 'Outer returns', 'Captured variables stay on heap', 'Inner call still reads/writes them'],
    },
    {
      id: 'hoisting',
      term: 'Hoisting & TDZ',
      importance: 4,
      whyAsked: 'Quick test of whether you understand the two-phase execution model rather than memorized rules.',
      explanation: 'During context creation, declarations are registered before code runs: var → initialized to undefined, function declarations → fully stored, let/const/class → registered but uninitialized (Temporal Dead Zone — accessing them throws ReferenceError until the declaration line executes).',
      commonMistakes: ['Saying let/const "are not hoisted" — they are hoisted but not initialized', 'Forgetting function expressions/arrow functions follow variable rules, not function-declaration rules'],
      followUps: ['Why does typeof x throw for let but return undefined for var?', 'What happens with function declarations inside blocks?'],
    },
    {
      id: 'promise',
      term: 'Promise',
      importance: 5,
      whyAsked: 'Async correctness is daily production work — error handling, parallelism, and combinator choice separate seniors from juniors.',
      explanation: 'A promise represents an eventual value: pending → fulfilled or rejected, immutable once settled. .then returns a new promise so chains transform values; thrown errors propagate down the chain to the nearest .catch.',
      realWorldExample: 'Promise.all to parallelize independent API calls; allSettled for a dashboard where partial failure is acceptable.',
      commonMistakes: ['Nesting .then like callbacks instead of returning from the chain', 'Forgetting that Promise.all rejects on the FIRST failure', 'await in a loop when calls are independent (sequential instead of parallel)'],
      followUps: ['Implement Promise.all from scratch', 'all vs allSettled vs race vs any?', 'How are unhandled rejections detected?'],
    },
    {
      id: 'async-await',
      term: 'async/await',
      importance: 5,
      whyAsked: 'Tests whether you know it is promise sugar with microtask suspension — and the parallelism mistakes it invites.',
      explanation: 'An async function always returns a promise. `await` unwraps a promise by suspending the function; the rest of the function resumes as a microtask. try/catch works across awaits, giving synchronous-looking error handling.',
      commonMistakes: ['Sequential awaits for independent work — use Promise.all', 'forEach(async ...) — it does not await anything; use for...of or map + Promise.all', 'Missing try/catch causing unhandled rejections'],
      followUps: ['What does await actually do to the function execution?', 'How do you run N promises with a concurrency limit?'],
    },
    {
      id: 'this',
      term: 'this Keyword',
      importance: 4,
      whyAsked: 'Reveals whether you understand call-site binding vs lexical capture — a constant source of real bugs in callbacks and classes.',
      explanation: 'this is resolved at call time by four rules (new > explicit bind > implicit obj.method > default). Arrow functions are the exception: they capture this lexically at definition and ignore call-site rules.',
      commonMistakes: ['Passing obj.method as a callback and losing the binding', 'Using arrow functions as object methods expecting this to be the object', 'Believing bind can rebind an arrow function'],
      followUps: ['Implement Function.prototype.bind', 'What is this in a class field arrow vs prototype method?'],
    },
    {
      id: 'debounce-throttle',
      term: 'Debounce & Throttle',
      importance: 4,
      whyAsked: 'The most common practical coding question — combines closures, timers, and performance reasoning.',
      explanation: 'Debounce delays execution until activity stops for N ms (search-as-you-type). Throttle guarantees at most one execution per N ms window (scroll handlers). Both use a closure to persist timer state between calls.',
      realWorldExample: 'Debounce an autocomplete API call; throttle a scroll-position tracker to one update per 100ms.',
      followUps: ['Implement both from scratch', 'Leading vs trailing edge options?', 'Which one for a resize handler and why?'],
    },
    {
      id: 'shallow-deep-copy',
      term: 'Shallow vs Deep Copy',
      importance: 4,
      whyAsked: 'Mutation bugs from shared references are among the most common production defects, especially in state management.',
      explanation: 'Spread/Object.assign copy one level — nested objects remain shared references. Deep copies duplicate the whole graph: structuredClone (handles cycles, Dates, Maps) is the modern answer; JSON.parse(JSON.stringify()) loses functions, undefined, Dates, and breaks on cycles.',
      commonMistakes: ['Recommending JSON round-trip without naming its data-loss caveats', 'Not knowing structuredClone exists', 'Deep-copying when an immutable update of one path is all that is needed'],
      followUps: ['Implement deepClone handling cycles', 'Why is deep copy expensive and how do immutable libraries avoid it (structural sharing)?'],
    },
    {
      id: 'equality-coercion',
      term: '== vs === & Coercion',
      importance: 3,
      whyAsked: 'Quick correctness check; also opens the door to type-coercion edge cases.',
      explanation: '=== compares type and value with no conversion. == applies coercion rules first (null == undefined is true; numbers vs strings convert; objects call valueOf/toString). Always default to ===; the only idiomatic == is `x == null` to match both null and undefined.',
      followUps: ['What does [] == ![] evaluate to and why?', 'NaN === NaN? How to check NaN properly?'],
    },
    {
      id: 'map-filter-reduce',
      term: 'map / filter / reduce',
      importance: 4,
      whyAsked: 'Daily-use tools; reduce especially shows functional thinking and is the basis of many transform questions.',
      explanation: 'map transforms 1:1 into a new array; filter keeps matching items; reduce folds an array into any single value (number, object, grouped map). None mutate the source. reduce can implement the other two.',
      commonMistakes: ['Using map for side effects (use forEach)', 'Forgetting reduce\'s initial value, breaking on empty arrays', 'Chaining map+filter over huge arrays where one reduce pass would do'],
      followUps: ['Implement groupBy with reduce', 'Implement map using reduce', 'When is a plain for-loop better?'],
    },
    {
      id: 'event-delegation',
      term: 'Event Delegation & Propagation',
      importance: 3,
      whyAsked: 'Tests DOM fundamentals: capture/bubble phases and performance-minded handler design.',
      explanation: 'Events travel capture (document → target) then bubble (target → document). Delegation attaches ONE listener on an ancestor and uses event.target to identify which child fired — fewer listeners, works for dynamically added children.',
      followUps: ['stopPropagation vs preventDefault vs stopImmediatePropagation?', 'Which events do not bubble (focus, blur)?'],
    },
    {
      id: 'currying',
      term: 'Currying & Partial Application',
      importance: 3,
      whyAsked: 'A favorite closure-application coding question; also appears in functional codebases and middleware patterns.',
      explanation: 'Currying transforms f(a,b,c) into f(a)(b)(c) — each call returns a closure remembering previous arguments. Partial application fixes some arguments and returns a function for the rest. Used for configuration-first APIs and reusable specialized functions.',
      followUps: ['Implement curry(fn) generically using fn.length', 'Write sum(1)(2)(3)() style chains'],
    },
  ],

  cheatSheets: [
    {
      id: 'var-let-const',
      title: 'var vs let vs const',
      rows: [
        { concept: 'var', description: 'Function-scoped, hoisted to undefined, re-declarable, attaches to globalThis at top level. Avoid in modern code.' },
        { concept: 'let', description: 'Block-scoped, TDZ until declaration, reassignable, fresh binding per loop iteration.' },
        { concept: 'const', description: 'Block-scoped, TDZ, no reassignment — but object contents stay mutable. Default choice.' },
      ],
    },
    {
      id: 'array-methods',
      title: 'Array methods quick reference',
      rows: [
        { concept: 'map', description: '1:1 transform → new array', code: '[1,2].map(x => x*2) // [2,4]' },
        { concept: 'filter', description: 'Keep matches → new array', code: '[1,2,3].filter(x => x>1) // [2,3]' },
        { concept: 'reduce', description: 'Fold to single value', code: '[1,2,3].reduce((a,x)=>a+x,0) // 6' },
        { concept: 'find / findIndex', description: 'First match (value / index) or undefined / -1' },
        { concept: 'some / every', description: 'Boolean: at least one / all pass' },
        { concept: 'flat / flatMap', description: 'Flatten nesting / map then flatten one level' },
        { concept: 'slice vs splice', description: 'slice copies (pure); splice mutates (insert/remove)' },
        { concept: 'sort', description: 'MUTATES; lexicographic by default — pass (a,b)=>a-b for numbers. Use toSorted() for a copy.' },
      ],
    },
    {
      id: 'comparisons',
      title: 'Classic comparisons',
      rows: [
        { concept: 'null vs undefined', description: 'undefined = not assigned (default state); null = intentional absence (assigned by you). null == undefined is true, === is false.' },
        { concept: 'spread vs rest', description: 'Same ... syntax, opposite roles: spread expands (call site / literals), rest collects (parameters / destructuring).' },
        { concept: '== vs ===', description: '=== no coercion (use it). == coerces; only idiomatic use is x == null.' },
        { concept: 'call vs apply vs bind', description: 'call(this, a, b) invokes now; apply(this, [a,b]) invokes now with array; bind(this) returns new bound function.' },
        { concept: 'forEach vs for...of', description: 'forEach: no break, no await support. for...of: break/continue/await work.' },
        { concept: 'Map vs Object', description: 'Map: any key type, insertion order, .size, no prototype pollution. Object: JSON-friendly, literal syntax.' },
        { concept: 'localStorage vs sessionStorage vs cookie', description: '~5MB persistent / per-tab session / ~4KB sent with every request (use httpOnly for auth).' },
      ],
    },
    {
      id: 'async-cheat',
      title: 'Async patterns',
      rows: [
        { concept: 'Microtask order', description: 'sync → ALL microtasks (promises) → one macrotask (timer) → repeat' },
        { concept: 'Promise.all', description: 'Parallel, fail-fast on first rejection', code: 'const [a,b] = await Promise.all([fa(), fb()])' },
        { concept: 'Promise.allSettled', description: 'Parallel, never rejects — array of {status, value|reason}' },
        { concept: 'Promise.race', description: 'First to SETTLE wins (fulfil or reject) — timeouts' },
        { concept: 'Promise.any', description: 'First to FULFILL wins; AggregateError if all reject' },
        { concept: 'Sequential vs parallel', description: 'await in loop = sequential; map to promises + Promise.all = parallel' },
        { concept: 'Cancellation', description: 'AbortController + signal — fetch(url, { signal })' },
      ],
    },
  ],

  questions: [
    {
      id: 'q-event-loop-output',
      level: 'intermediate',
      question: 'Predict the output: console.log(1); setTimeout(()=>console.log(2)); Promise.resolve().then(()=>console.log(3)); console.log(4);',
      answer: '1, 4, 3, 2. Synchronous code runs first (1, 4). When the stack empties, the microtask queue drains — the promise callback logs 3. Only then does the event loop take the macrotask, logging 2.',
      deepDive: 'If the .then callback itself queued another promise callback, that would ALSO run before the timeout — the microtask queue is drained completely, including newly added microtasks. This is why microtask loops can starve rendering and timers.',
      followUps: ['Where does queueMicrotask fit?', 'What changes with async/await versions of the same code?', 'What about setImmediate / process.nextTick in Node?'],
    },
    {
      id: 'q-closure-loop',
      level: 'beginner',
      question: 'Why does `for (var i = 0; i < 3; i++) setTimeout(() => console.log(i))` print 3,3,3 — and how do you fix it?',
      answer: 'var creates ONE function-scoped binding shared by all three closures. By the time the timers fire (after the loop), i is 3. Fix: use let (fresh block-scoped binding per iteration), or capture the value with an IIFE parameter.',
      followUps: ['What exactly does let do differently in a for-loop?', 'How else could you fix it without let?'],
    },
    {
      id: 'q-closure-def',
      level: 'beginner',
      question: 'What is a closure and where have you used one in real code?',
      answer: 'A closure is a function that retains access to variables from its defining (lexical) scope even after that scope has finished executing. The engine keeps captured variables alive on the heap. Real uses: debounce/throttle (timer state), memoization caches, module-pattern private state, event handler factories, Vue composables / React hooks.',
      deepDive: 'Mention the memory angle: captured variables are heap-allocated and live as long as the function is reachable — which is both the feature (persistent private state) and the risk (leaks via long-lived listeners capturing large objects).',
      followUps: ['Can closures cause memory leaks?', 'Implement memoize.'],
    },
    {
      id: 'q-promise-all-impl',
      level: 'advanced',
      question: 'Implement Promise.all from scratch.',
      answer: 'Return a new Promise; iterate the input, Promise.resolve each item, store results by index, count completions, resolve when count equals length, reject immediately on any rejection. Handle the empty-array case by resolving immediately.',
      realWorldExample: 'Variants of this question: implement allSettled (never reject, wrap outcomes), race (settle on first), or a concurrency-limited pool — know all four.',
      followUps: ['Why store by index instead of pushing?', 'Now add a concurrency limit of N.'],
    },
    {
      id: 'q-this-rules',
      level: 'intermediate',
      question: 'How is `this` determined? Why does passing obj.method as a callback break it?',
      answer: 'this is bound at CALL time: new binding > explicit (call/apply/bind) > implicit (obj.method()) > default (undefined in strict mode). Passing obj.method detaches the function from obj — when the framework later calls it as a plain function, implicit binding is gone. Fix with obj.method.bind(obj) or an arrow wrapper () => obj.method().',
      followUps: ['How do arrow functions differ?', 'What does bind return and can you re-bind it?'],
    },
    {
      id: 'q-var-tdz',
      level: 'beginner',
      question: 'What is the Temporal Dead Zone?',
      answer: 'The region between entering a scope and the let/const declaration line. The variable IS hoisted (registered in the environment) but uninitialized — any access throws ReferenceError. This catches use-before-declaration bugs that var silently allowed by returning undefined.',
      followUps: ['Why does typeof also throw inside the TDZ?'],
    },
    {
      id: 'q-deep-copy',
      level: 'intermediate',
      question: 'How do you deep-clone an object, and what is wrong with JSON.parse(JSON.stringify(obj))?',
      answer: 'Modern answer: structuredClone(obj) — handles nested objects, Dates, Maps, Sets, cycles. The JSON round-trip drops functions, undefined and symbols, turns Dates into strings, loses Map/Set, and throws on circular references. For partial updates, prefer immutable spread of just the changed path over full deep clones.',
      followUps: ['Implement deepClone recursively with cycle handling (WeakMap)', 'What can structuredClone NOT copy? (functions, DOM nodes)'],
    },
    {
      id: 'q-event-delegation',
      level: 'intermediate',
      question: 'You have a list with 10,000 rows, each needing a click handler. What do you do?',
      answer: 'Event delegation: one listener on the list container; use event.target.closest(\'[data-row-id]\') to identify the row. Bubbling carries the event up. Benefits: one listener instead of 10,000 (memory), and dynamically added rows work automatically.',
      followUps: ['Which events do not bubble and how do you delegate them (focusin/focusout)?', 'How would you also virtualize the rendering?'],
    },
    {
      id: 'q-async-parallel',
      level: 'advanced',
      question: 'This code awaits three independent API calls sequentially. What is wrong and how do you fix it? What if one may fail?',
      answer: 'Three awaits in a row serialize the latency (t1+t2+t3). Start all three first, then await: const [a,b,c] = await Promise.all([fa(), fb(), fc()]) — total time becomes max(t1,t2,t3). If partial failure is acceptable, use Promise.allSettled and handle each outcome.',
      followUps: ['How do you limit concurrency to N for 100 calls?', 'How does fail-fast of Promise.all interact with the other in-flight requests? (they continue, results ignored — use AbortController)'],
    },
    {
      id: 'q-memory-leak',
      level: 'expert',
      question: 'Users report the app gets slower the longer the tab stays open. How do you find and fix a JS memory leak?',
      answer: 'DevTools Memory panel: take heap snapshots across the suspected interaction, compare retained objects, look at retainer chains. Performance panel with the memory checkbox shows a sawtooth that trends upward. Usual suspects: uncleared setInterval, event listeners on window/document never removed, detached DOM nodes referenced from closures, unbounded caches. Fixes: cleanup in unmount hooks, AbortController for listener groups, WeakMap for object-keyed caches.',
      followUps: ['What is a detached DOM node?', 'How does WeakMap help vs Map?'],
    },
    {
      id: 'q-prototype-chain',
      level: 'advanced',
      question: 'What happens when you access obj.foo? Walk through the lookup.',
      answer: 'Engine checks own properties of obj; if absent, follows [[Prototype]] to the constructor\'s prototype object, then its prototype, until Object.prototype, then null → undefined. Writes do not walk the chain — they create an own property that shadows inherited ones. Methods like toString resolve this way, and class syntax just wires these links.',
      followUps: ['hasOwnProperty vs in operator?', 'How do hidden classes/inline caches make this fast?'],
    },
    {
      id: 'q-es6-features',
      level: 'beginner',
      question: 'Which ES6+ features do you use daily?',
      answer: 'let/const, arrow functions, template literals, destructuring, spread/rest, default params, modules (import/export), classes, promises + async/await, optional chaining ?., nullish coalescing ??, Map/Set. Strong answers attach a use-case to each rather than listing.',
      followUps: ['?? vs || difference?', 'When is ?. dangerous? (masking real bugs)'],
    },
  ],

  codingQuestions: [
    {
      id: 'code-debounce',
      title: 'Implement debounce',
      level: 'intermediate',
      problem: 'Write debounce(fn, delay): calling the returned function repeatedly executes fn only once, delay ms after the LAST call. Preserve this and arguments.',
      solution: {
        lang: 'js',
        code: `function debounce(fn, delay) {\n  let timer = null\n  return function (...args) {\n    clearTimeout(timer)\n    timer = setTimeout(() => fn.apply(this, args), delay)\n  }\n}`,
      },
      explanation: 'The timer ID persists in the closure between calls. Each call cancels the pending execution and schedules a new one — only the final call in a burst survives. A regular function + fn.apply(this, args) preserves the caller\'s this (the arrow inside inherits it).',
      timeComplexity: 'O(1) per call',
      spaceComplexity: 'O(1)',
      alternatives: ['Leading-edge option: fire immediately, then suppress for delay ms', 'Add cancel() and flush() methods on the returned function'],
    },
    {
      id: 'code-throttle',
      title: 'Implement throttle',
      level: 'intermediate',
      problem: 'Write throttle(fn, interval): the returned function executes fn at most once per interval, regardless of call frequency.',
      solution: {
        lang: 'js',
        code: `function throttle(fn, interval) {\n  let last = 0\n  return function (...args) {\n    const now = Date.now()\n    if (now - last >= interval) {\n      last = now\n      fn.apply(this, args)\n    }\n  }\n}`,
      },
      explanation: 'Timestamp comparison gates execution to one call per window (leading edge). A timer-based variant can guarantee a trailing call with the latest arguments — mention both in interviews.',
      timeComplexity: 'O(1) per call',
      spaceComplexity: 'O(1)',
      alternatives: ['Timer-based trailing-edge version', 'Hybrid leading+trailing (lodash default)'],
    },
    {
      id: 'code-promise-all',
      title: 'Implement Promise.all',
      level: 'advanced',
      problem: 'Implement promiseAll(promises) matching native behavior: resolves with results in input order, rejects on first rejection, handles non-promise values and empty arrays.',
      solution: {
        lang: 'js',
        code: `function promiseAll(items) {\n  return new Promise((resolve, reject) => {\n    const results = new Array(items.length)\n    let remaining = items.length\n    if (remaining === 0) return resolve(results)\n    items.forEach((item, i) => {\n      Promise.resolve(item).then(value => {\n        results[i] = value\n        if (--remaining === 0) resolve(results)\n      }, reject)\n    })\n  })\n}`,
      },
      explanation: 'Results stored by index (completion order ≠ input order). A counter — not results.length — detects completion. Promise.resolve normalizes plain values. Any rejection calls reject immediately; promise state machines ignore later settle attempts.',
      timeComplexity: 'O(n)',
      spaceComplexity: 'O(n)',
      alternatives: ['allSettled: wrap each in .then(v=>({status:"fulfilled",value:v}), r=>({status:"rejected",reason:r}))', 'Concurrency-limited pool with a shared cursor'],
    },
    {
      id: 'code-flatten',
      title: 'Flatten a nested array',
      level: 'beginner',
      problem: 'Write flatten(arr, depth = Infinity) that flattens nested arrays to the given depth without using Array.prototype.flat.',
      solution: {
        lang: 'js',
        code: `function flatten(arr, depth = Infinity) {\n  return arr.reduce((acc, item) =>\n    Array.isArray(item) && depth > 0\n      ? acc.concat(flatten(item, depth - 1))\n      : acc.concat(item), [])\n}`,
      },
      explanation: 'Recursive reduce: arrays recurse with decremented depth, scalars append. An iterative stack-based version avoids recursion limits on very deep nesting.',
      timeComplexity: 'O(n) total elements',
      spaceComplexity: 'O(d) recursion depth',
      alternatives: ['Iterative with an explicit stack', 'Generator-based lazy flattening'],
    },
    {
      id: 'code-deep-clone',
      title: 'Deep clone with cycle handling',
      level: 'advanced',
      problem: 'Implement deepClone(obj) supporting objects, arrays, Date, and circular references.',
      solution: {
        lang: 'js',
        code: `function deepClone(value, seen = new WeakMap()) {\n  if (value === null || typeof value !== 'object') return value\n  if (seen.has(value)) return seen.get(value)\n  if (value instanceof Date) return new Date(value)\n  const copy = Array.isArray(value) ? [] : {}\n  seen.set(value, copy)\n  for (const key of Object.keys(value)) {\n    copy[key] = deepClone(value[key], seen)\n  }\n  return copy\n}`,
      },
      explanation: 'The WeakMap maps original → copy. Registering the copy BEFORE recursing breaks cycles: when the cycle comes back around, the partially built copy is returned instead of recursing forever.',
      timeComplexity: 'O(n) properties',
      spaceComplexity: 'O(n)',
      alternatives: ['structuredClone (native, also handles Map/Set/TypedArrays)', 'Extend for Map/Set/RegExp by adding instanceof branches'],
    },
    {
      id: 'code-memoize',
      title: 'Implement memoize',
      level: 'intermediate',
      problem: 'Write memoize(fn) that caches results by arguments, so repeated calls with the same args skip recomputation.',
      solution: {
        lang: 'js',
        code: `function memoize(fn) {\n  const cache = new Map()\n  return function (...args) {\n    const key = JSON.stringify(args)\n    if (!cache.has(key)) {\n      cache.set(key, fn.apply(this, args))\n    }\n    return cache.get(key)\n  }\n}`,
      },
      explanation: 'The Map lives in the closure. JSON key serialization is the simple approach — discuss its limits (object key order, functions, large args). For single-object args, a WeakMap allows GC of unused entries.',
      timeComplexity: 'O(1) average per cached call (+ key serialization)',
      spaceComplexity: 'O(unique inputs) — discuss eviction (LRU)',
      alternatives: ['Trie of nested Maps keyed per-argument (no serialization)', 'LRU cache to bound memory'],
    },
    {
      id: 'code-curry',
      title: 'Implement curry',
      level: 'advanced',
      problem: 'Write curry(fn) so that curry(sum)(1)(2)(3), curry(sum)(1, 2)(3) and curry(sum)(1, 2, 3) all work for a 3-arg sum.',
      solution: {
        lang: 'js',
        code: `function curry(fn) {\n  return function curried(...args) {\n    if (args.length >= fn.length) {\n      return fn.apply(this, args)\n    }\n    return (...next) => curried.apply(this, [...args, ...next])\n  }\n}`,
      },
      explanation: 'fn.length is the declared arity. Each partial call returns a closure accumulating arguments; once enough are collected, the original runs. Note fn.length ignores rest/default params — a known limitation worth mentioning.',
      timeComplexity: 'O(1) per call',
      spaceComplexity: 'O(arity) accumulated args',
      alternatives: ['Placeholder support (lodash _ )', 'Infinite curry with valueOf/toString termination: sum(1)(2)(3)+0'],
    },
    {
      id: 'code-event-emitter',
      title: 'Implement an EventEmitter',
      level: 'advanced',
      problem: 'Build a class with on(event, cb), off(event, cb), once(event, cb), emit(event, ...args).',
      solution: {
        lang: 'js',
        code: `class EventEmitter {\n  #listeners = new Map()\n  on(event, cb) {\n    if (!this.#listeners.has(event)) this.#listeners.set(event, new Set())\n    this.#listeners.get(event).add(cb)\n    return this\n  }\n  off(event, cb) {\n    this.#listeners.get(event)?.delete(cb)\n    return this\n  }\n  once(event, cb) {\n    const wrap = (...args) => { this.off(event, wrap); cb(...args) }\n    return this.on(event, wrap)\n  }\n  emit(event, ...args) {\n    this.#listeners.get(event)?.forEach(cb => cb(...args))\n    return this\n  }\n}`,
      },
      explanation: 'Map of event → Set of callbacks gives O(1) add/remove and prevents duplicates. once wraps the callback so it unsubscribes itself before running — the wrapper must be what is registered, or off cannot find it.',
      timeComplexity: 'on/off O(1); emit O(listeners)',
      spaceComplexity: 'O(events × listeners)',
      alternatives: ['Snapshot listeners before emit to tolerate mutation during dispatch', 'Wildcard events, error-event semantics like Node'],
    },
  ],

  scenarios: [
    {
      id: 'sc-slow-page',
      title: 'A page loads slowly — walk me through your debugging process',
      situation: 'Users complain a production page takes 8+ seconds to become interactive. You own the fix.',
      approach: [
        'Reproduce with DevTools Network tab (throttled): check TTFB vs download vs waterfall — is it the server, the payload, or the client?',
        'Run Lighthouse for LCP/TBT/CLS breakdown and the opportunities list (render-blocking resources, unused JS).',
        'Analyze the bundle (rollup-plugin-visualizer / webpack-bundle-analyzer): find the heavy dependencies; code-split routes and lazy-load below-the-fold components.',
        'Performance tab recording: find long tasks (>50ms) blocking the main thread; defer or chunk them.',
        'Check API timing: parallelize independent calls, add caching headers/CDN, paginate large responses.',
        'Verify images: modern formats (WebP/AVIF), responsive srcset, lazy loading.',
      ],
      keyTakeaways: ['Measure before optimizing — name the tool and the metric', 'Distinguish network-bound vs CPU-bound vs render-bound', 'Mention a verification step: re-run Lighthouse, compare Core Web Vitals'],
    },
    {
      id: 'sc-50k-list',
      title: 'Render a 50,000-row list without freezing the browser',
      situation: 'A data-heavy dashboard must display tens of thousands of rows with smooth scrolling.',
      approach: [
        'Virtualize: render only the ~30 rows in the viewport plus a buffer; absolute-position them inside a spacer div of total height (vue-virtual-scroller / @tanstack/virtual).',
        'Paginate or infinite-scroll at the API level — never ship 50k records in one response.',
        'Use event delegation for row interactions (one listener, not 50k).',
        'Debounce search/filter inputs; move heavy filtering to a Web Worker or the server.',
        'Avoid layout thrashing: batch DOM reads/writes; use transform for row positioning.',
      ],
      keyTakeaways: ['Virtualization is the headline answer — explain the spacer + window technique', 'The DOM is the bottleneck: ~50k nodes ≈ memory + layout cost, regardless of framework'],
    },
    {
      id: 'sc-race-condition',
      title: 'Search results flicker to stale data — fix the race condition',
      situation: 'Fast typists see results for an older query overwrite results for the latest one, because responses arrive out of order.',
      approach: [
        'Identify the race: request A (slow) and request B (fast) resolve out of order; last-write-wins corrupts state.',
        'Best fix: AbortController — abort the in-flight request before issuing a new one.',
        'Alternative: sequence tokens — stamp each request, ignore responses whose token is not the latest.',
        'Add debounce so bursts of keystrokes issue fewer requests in the first place.',
        'Mention framework hygiene: cancel on unmount to avoid setting state on dead components.',
      ],
      keyTakeaways: ['Name the pattern: stale-response race', 'AbortController is the production-grade answer; token comparison is the framework-agnostic fallback'],
    },
    {
      id: 'sc-third-party-script',
      title: 'A third-party script tanks your performance score',
      situation: 'Marketing added an analytics/chat widget; TBT jumped 800ms and LCP regressed.',
      approach: [
        'Quantify: Performance tab → attribute long tasks to the third-party domain; Lighthouse third-party report.',
        'Load it async/defer, or better, after first interaction (facade pattern: fake chat button that loads the real widget on click).',
        'Move beacon-style scripts to a Web Worker via Partytown if compatible.',
        'Self-host critical scripts with proper caching if licensing allows; set resource hints (preconnect) for required origins.',
        'Establish a performance budget in CI so regressions are caught at PR time.',
      ],
      keyTakeaways: ['Facade/lazy-init pattern is the senior answer', 'Performance budgets make the fix durable, not one-off'],
    },
  ],

  mentalModels: [
    {
      id: 'mm-execution',
      title: 'Execution Context Mental Model',
      flow: ['Script starts', 'Global Execution Context created', 'Memory phase: declarations registered', 'Execution phase: code runs', 'Function call → new context on stack', 'Return → context popped'],
      explanation: [
        'Picture JS as a stack of boxes. Each box (context) holds variables, a link to its outer scope, and a this. Only the top box runs.',
        'The two-phase creation (memory, then execution) explains hoisting without any magic: declarations are simply known before execution begins.',
        'Recursion depth = box count. Stack overflow = too many boxes.',
      ],
      whatBreaksWithoutIt: 'Without contexts and the stack, there would be no scope isolation, no recursion, no way to resume the caller after a function returns.',
      interviewTip: 'Anchor every hoisting/TDZ/scope answer in "creation phase vs execution phase" — it converts memorized rules into one coherent story.',
    },
    {
      id: 'mm-event-loop',
      title: 'Event Loop Mental Model',
      flow: ['Call Stack', 'Web APIs / libuv (host does the waiting)', 'Callback Queues: micro + macro', 'Event Loop: stack empty?', 'Drain ALL microtasks', 'Run ONE macrotask', 'Render', 'Repeat'],
      explanation: [
        'JS is a single cook (the stack). Orders that take time (timers, network) are handed to kitchen staff (host APIs). Finished orders queue at two windows: VIP (microtasks) and regular (macrotasks).',
        'The cook always clears the entire VIP window before taking ONE order from the regular window.',
        'Rendering is another customer that only gets served between loop turns — block the cook and the UI freezes.',
      ],
      whatBreaksWithoutIt: 'Without the loop, any network call or timer would freeze the page; without microtask priority, promise chains would interleave unpredictably with events.',
      interviewTip: 'For output questions, narrate three columns aloud: stack, microtask queue, macrotask queue. Walking the model beats recalling the answer.',
    },
    {
      id: 'mm-closure',
      title: 'Closure Mental Model',
      flow: ['Outer function runs', 'Inner function created → backpack packed with outer variables', 'Outer function returns', 'Backpack persists on heap', 'Inner function still reads/writes backpack'],
      explanation: [
        'Every function carries a backpack (its lexical environment). The backpack is packed where the function is DEFINED, not where it is called.',
        'Variables in the backpack survive as long as the function does — the stack frame dies, the backpack does not.',
        'Two functions created in the same scope share one backpack — mutating through one is visible to the other.',
      ],
      whatBreaksWithoutIt: 'No private state, no debounce/memoize, no module pattern, no hooks/composables — every callback would lose its context the moment its parent returned.',
      interviewTip: 'The shared-backpack point (two closures over the same scope share state) is what separates a great answer — demo with a getter/setter pair.',
    },
    {
      id: 'mm-prototype',
      title: 'Prototype Chain Mental Model',
      flow: ['obj.foo lookup', 'Own properties?', '[[Prototype]] → Constructor.prototype', '[[Prototype]] → Object.prototype', 'null → undefined'],
      explanation: [
        'Think "delegation, not copying": an object that lacks a property asks its prototype, which asks its prototype.',
        'Methods exist ONCE on the prototype and are shared by all instances — that is the memory win over per-instance copies.',
        'Writes never delegate: assigning creates an own property that shadows the inherited one.',
      ],
      whatBreaksWithoutIt: 'Every instance would need its own copy of every method; instanceof, class inheritance and built-in methods like toString would not exist.',
      interviewTip: 'Say "delegation" early — it signals you understand prototypes are live links, unlike class-copy inheritance in Java.',
    },
    {
      id: 'mm-async-await',
      title: 'async/await Mental Model',
      flow: ['async fn called', 'Runs synchronously until first await', 'await → function SUSPENDS, returns promise to caller', 'Awaited promise settles', 'Rest of fn resumes as microtask'],
      explanation: [
        'await is a bookmark: the engine saves your place, leaves, and comes back via the microtask queue when the value is ready.',
        'The caller is never blocked — it receives a pending promise immediately.',
        'Three sequential awaits = three suspensions = summed latency. Start promises first, await later, to overlap them.',
      ],
      whatBreaksWithoutIt: 'You would be back to .then chains and pyramid callbacks; try/catch could not span async boundaries.',
      interviewTip: 'Demonstrate the parallelism fix unprompted (const p1 = fa(); const p2 = fb(); await p1; await p2) — it is the most common real-world async bug.',
    },
    {
      id: 'mm-rendering',
      title: 'Browser Rendering Pipeline Mental Model',
      flow: ['HTML → DOM', 'CSS → CSSOM', 'DOM + CSSOM → Render Tree', 'Layout (geometry)', 'Paint (pixels)', 'Composite (layers on GPU)'],
      explanation: [
        'Changing geometry (width, top, font-size) re-runs Layout → Paint → Composite: a reflow — the expensive path.',
        'Changing color/background skips layout: repaint only. Changing transform/opacity can skip both: composite-only — the cheap path, hence "animate transform, not top/left".',
        'Layout thrashing = alternating DOM writes and reads (offsetHeight) in a loop, forcing synchronous reflow each iteration. Batch reads, then writes.',
      ],
      whatBreaksWithoutIt: 'Without understanding the pipeline you cannot explain why a 60fps animation is possible with transform but janky with left, or what will-change does.',
      interviewTip: 'Drop the terms reflow, repaint, composite, and layout thrashing with one-line definitions — that vocabulary is exactly what is being probed.',
    },
  ],

  revision: [
    { id: 'r-exec', topic: 'Execution Context', importance: 5, thirtySecond: 'Every script/function runs in a context created in two phases: memory allocation (declarations registered) then execution. Contexts stack up on the call stack; top one runs.', twoMinute: 'On load, the Global Execution Context is created. Phase 1 registers var (undefined), let/const (TDZ), and full function declarations — this IS hoisting. Phase 2 executes line by line. Each call pushes a Function Execution Context holding its variable environment, scope-chain link, and this; returns pop it. Stack overflow = unbounded recursion.', deepDive: 'Each context = Variable Environment + Lexical Environment + this binding. The lexical link is set by WHERE the function was defined, enabling closures. Engines optimize contexts heavily (hidden classes, stack-allocated frames) but the spec model is what interviews test.', followUps: ['Explain hoisting via the two phases', 'What is in a context?'] },
    { id: 'r-callstack', topic: 'Call Stack', importance: 4, thirtySecond: 'LIFO stack of execution contexts. Sync code only. One stack = single-threaded = anything slow blocks everything, including rendering.', twoMinute: 'Each invocation pushes a frame; return pops it. Stack traces are snapshots of it. Because there is exactly one stack, a 2-second synchronous loop freezes clicks, timers and paint — the motivation for async APIs and workers. Max depth exceeded = RangeError.', deepDive: 'Async callbacks always run on an EMPTY stack — that is why their stack traces are short and why errors thrown in callbacks cannot be caught by an outer try/catch that has already popped. async/await restores logical traces via promise machinery.', followUps: ['Why can\'t try/catch catch a setTimeout error?'] },
    { id: 'r-heap', topic: 'Memory Heap & GC', importance: 3, thirtySecond: 'Objects live on the heap; V8 reclaims unreachable ones via generational mark-and-sweep. Leaks = accidental reachability.', twoMinute: 'Young generation gets new allocations (fast scavenges); survivors promote to old generation (mark-sweep-compact). Roots: globals, stack, active closures. Leak suspects: intervals, listeners, detached DOM nodes, unbounded caches. WeakMap/WeakRef do not prevent collection.', deepDive: 'Heap snapshots + retainer chains in DevTools locate leaks. Shape stability (hidden classes) and avoiding delete keep objects fast. Promotion-heavy churn causes GC pauses — object pooling helps in hot paths.', followUps: ['How do you find a leak in DevTools?', 'Map vs WeakMap for caches?'] },
    { id: 'r-scope', topic: 'Lexical Scope', importance: 4, thirtySecond: 'Scope is fixed by where code is WRITTEN. Lookups walk outward through enclosing scopes at definition site — call site is irrelevant.', twoMinute: 'Function/block scopes nest; each function stores a link to its defining environment. This static (lexical) resolution is what makes closures possible and predictable, and contrasts with this, which IS call-site dependent. let/const give block scoping; var ignores blocks.', deepDive: 'The scope chain is decided at parse time; engines resolve most lookups to fixed slots. eval and with defeat this (deoptimization) — why they are banned in strict mode.', followUps: ['Lexical vs dynamic scoping?', 'Why is this not lexical (except arrows)?'] },
    { id: 'r-closure', topic: 'Closure', importance: 5, thirtySecond: 'A function that keeps access to its outer variables after the outer function has returned. Powers private state, debounce, memoize, hooks.', twoMinute: 'Inner functions capture their defining environment by reference, not copy. If the inner function outlives the outer call, captured variables move to the heap and persist. Two closures from one scope share state. The var-in-loop bug: one shared binding; let fixes it with per-iteration bindings.', deepDive: 'Engines capture only variables actually referenced (mostly) but a single context object may pin siblings — a subtle leak source. Long-lived listeners capturing large objects = classic production leak. Closures + IIFE = module pattern, the pre-ESM encapsulation tool.', followUps: ['Implement once()/memoize', 'Closure memory leak example?'] },
    { id: 'r-hoisting', topic: 'Hoisting & TDZ', importance: 4, thirtySecond: 'Declarations are registered before execution: var → undefined, function declarations → usable, let/const → TDZ (ReferenceError until the declaration line).', twoMinute: 'Hoisting falls out of the creation phase. Function declarations hoist fully (call them above their definition); function expressions follow their variable\'s rules. TDZ makes use-before-declare a loud error instead of a silent undefined.', deepDive: 'typeof is safe on truly undeclared names but THROWS in the TDZ — a favorite trick question. Class declarations are also TDZ-bound.', followUps: ['typeof x where x is let-declared below?'] },
    { id: 'r-prototype', topic: 'Prototype Chain', importance: 4, thirtySecond: 'Property reads delegate up [[Prototype]] links until found or null. Methods live once on the prototype, shared by all instances. class is sugar over this.', twoMinute: 'new Fn() links the instance to Fn.prototype. Reads walk the chain; writes shadow locally. extends wires prototype-to-prototype. instanceof checks chain membership. Object.create(proto) builds delegation directly.', deepDive: 'Lookup cost is optimized with hidden classes + inline caches; megamorphic call sites deopt. Mutating Object.prototype is pollution (security issue). __proto__ vs Object.getPrototypeOf — use the latter.', followUps: ['hasOwnProperty vs in?', 'Prototype pollution attack?'] },
    { id: 'r-eventloop', topic: 'Event Loop', importance: 5, thirtySecond: 'Stack empties → drain ALL microtasks (promises) → one macrotask (timer/IO) → maybe render → repeat. Promises always beat timers.', twoMinute: 'Host APIs do the waiting; completions queue callbacks. Microtask queue has absolute priority and drains fully, including newly queued microtasks — infinite chains starve rendering. setTimeout(fn,0) means "after current stack + all microtasks", minimum ~4ms nested. Node adds phases (timers, poll, check) plus process.nextTick ahead of promises.', deepDive: 'requestAnimationFrame runs before paint, ideal for visual updates. Long tasks >50ms hurt INP; chunk with setTimeout/scheduler.yield. In Node, setImmediate (check phase) vs setTimeout(0) ordering depends on context — inside I/O callbacks setImmediate wins.', followUps: ['Output-prediction drills', 'nextTick vs queueMicrotask?'] },
    { id: 'r-promise', topic: 'Promise', importance: 5, thirtySecond: 'One-way state machine: pending → fulfilled/rejected. .then returns a NEW promise (chaining); callbacks always run as microtasks. Errors propagate to the nearest catch.', twoMinute: 'Executor runs synchronously; settlement is permanent. Chain transforms values; returning a promise from .then adopts its state. Combinators: all (fail-fast), allSettled, race, any. Unhandled rejections are process-fatal in modern Node.', deepDive: 'Thenable assimilation, the microtask timing of .then on resolved promises, and implementing all/retry/timeout from scratch are the standard deep-end questions.', followUps: ['Implement Promise.all / retry with backoff'] },
    { id: 'r-async', topic: 'async/await', importance: 5, thirtySecond: 'Sugar over promises: async fn returns a promise; await suspends and resumes as a microtask. try/catch works across awaits.', twoMinute: 'Code before the first await is synchronous. Each await yields control to the caller — the function is re-entered later. Pitfalls: sequential awaits for independent work (use Promise.all), forEach(async) not awaiting, missing error handling.', deepDive: 'Under the hood: generator-like state machine. Top-level await in ESM blocks dependent module evaluation. Concurrency pools (N at a time) are the senior follow-up.', followUps: ['Make 3 sequential awaits parallel', 'Implement a concurrency limiter'] },
    { id: 'r-this', topic: 'this / call / apply / bind', importance: 4, thirtySecond: 'Call-site decides: new > bind > obj.method() > plain call (undefined in strict). Arrows capture this lexically and ignore all of it.', twoMinute: 'call/apply invoke with explicit this (args listed vs array); bind returns a permanently bound function (cannot rebind, ignored by new... mostly). Detached methods lose this — fix via bind or arrow wrapper. Class fields as arrows = auto-bound handlers (per-instance cost).', deepDive: 'Implementing bind (with new-awareness) is a classic. Arrow this is set at DEFINITION; bind on arrows is a no-op for this. getters/setters and event handlers each have specific this rules worth citing.', followUps: ['Implement bind', 'this in arrow class field vs method?'] },
    { id: 'r-map-reduce', topic: 'map / filter / reduce', importance: 4, thirtySecond: 'map transforms, filter selects, reduce folds to anything. None mutate. reduce implements the others.', twoMinute: 'Each takes (item, index, array). reduce(acc, item) needs an initial value for safety on empty arrays. Use cases: groupBy, counting, flattening via reduce; chains read declaratively but iterate multiple times — single reduce or loop for hot paths.', deepDive: 'Holes in sparse arrays are skipped; async callbacks inside map produce promise arrays (pair with Promise.all). Transducers solve multi-pass overhead conceptually.', followUps: ['groupBy with reduce', 'map with reduce'] },
    { id: 'r-debounce', topic: 'Debounce & Throttle', importance: 4, thirtySecond: 'Debounce: run once after the burst stops (search input). Throttle: at most once per interval (scroll). Both persist timer state in closures.', twoMinute: 'Debounce resets a timer per call; only the last survives. Throttle gates by timestamp or timer; leading/trailing variants decide first/last call behavior. Choose by intent: final value (debounce) vs steady sampling (throttle).', deepDive: 'Production extras: cancel/flush methods, maxWait (lodash debounce), preserving this/args via apply, rAF-throttling for visual handlers.', followUps: ['Code both in 3 minutes', 'Resize handler — which one?'] },
    { id: 'r-copy', topic: 'Shallow vs Deep Copy', importance: 4, thirtySecond: 'Spread copies one level; nested objects stay shared. structuredClone for true deep copy; JSON round-trip loses Dates/functions/undefined and breaks on cycles.', twoMinute: 'Shallow: {...obj}, Object.assign, slice. Deep: structuredClone (cycles, Dates, Map/Set ok; functions/DOM not). Manual recursive clone needs a WeakMap for cycles. In state management, prefer immutable updates of changed paths over wholesale deep copies.', deepDive: 'Structural sharing (Immer/persistent data structures) gives immutability without copy cost. Know why deep equality checks are expensive and how memoization interacts with reference identity.', followUps: ['Write deepClone with cycles', 'What can structuredClone not handle?'] },
    { id: 'r-equality', topic: '== vs === / null vs undefined', importance: 3, thirtySecond: '=== always (no coercion). == coerces; only x == null is idiomatic (matches null + undefined). undefined = unassigned; null = intentional absence.', twoMinute: 'Coercion rules: null==undefined true; string↔number converts; objects valueOf/toString. NaN !== NaN — use Number.isNaN or Object.is. Object.is differs from === on NaN and ±0.', deepDive: 'The [] == ![] → true walkthrough demonstrates the full algorithm and is a popular curveball: ![] is false, [] → \'\' → 0, false → 0.', followUps: ['Explain [] == ![]', 'Object.is vs ===?'] },
    { id: 'r-spread', topic: 'Spread vs Rest', importance: 3, thirtySecond: 'Same ... token, opposite directions: spread EXPANDS iterables/objects at call/literal sites; rest COLLECTS leftovers in params/destructuring.', twoMinute: 'Spread: clone-one-level, merge objects (later wins), pass arrays as args. Rest: variadic functions, "everything else" in destructuring. Rest must be last; spread of objects copies own enumerable props only.', deepDive: 'Object spread ignores prototypes and getters become values (evaluated once). Spreading large arrays into fn args can blow the stack — use loops/apply limits awareness.', followUps: ['Clone vs reference pitfalls with spread?'] },
    { id: 'r-storage', topic: 'Web Storage & Workers', importance: 3, thirtySecond: 'localStorage persists (~5MB, sync, string-only); sessionStorage per tab; cookies (~4KB) ride every request — auth belongs in httpOnly cookies. Web Workers run JS off the main thread via message passing.', twoMinute: 'localStorage is synchronous — large reads block the main thread; never store tokens where XSS can read them. Workers have no DOM access; communicate via postMessage/structured clone; SharedArrayBuffer for shared memory. Service workers intercept network (offline/PWA).', deepDive: 'IndexedDB for structured/large data (async). Worker offloading patterns: parsing huge JSON, image processing, search indexing. Comlink ergonomics. Origin isolation requirements for SAB.', followUps: ['Where to store a JWT and why?', 'What can a worker not do?'] },
  ],
}

export default javascript
