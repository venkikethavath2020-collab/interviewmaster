import type { Technology } from '../../types/content'

const aws: Technology = {
  id: 'aws',
  name: 'AWS',
  icon: 'Aw',
  color: '#ff9900',
  tagline: 'The dominant cloud — compute, storage, networking and managed services billed by the second.',
  category: 'cloud',

  overview: {
    what: 'Amazon Web Services is a cloud platform offering 200+ on-demand services — compute (EC2, ECS, Lambda), storage (S3, EBS), databases (RDS, DynamoDB), networking (VPC, Route53, CloudFront) and glue (SQS, SNS, IAM, CloudWatch) — provisioned via API and billed for what you use.',
    why: 'It replaced capital-expense data centers with operational-expense infrastructure: provision a server in seconds instead of procuring hardware in months, scale elastically with traffic, and pay only for consumption. For interviews, AWS fluency signals you can take code to production, not just write it.',
    problemsSolved: [
      'Capacity planning: auto scaling replaces guessing peak load and over-buying hardware',
      'Undifferentiated heavy lifting: managed databases, queues and load balancers replace self-operated infrastructure',
      'Global reach: regions, availability zones and edge locations give low latency and disaster isolation without building data centers',
      'Reliability primitives: multi-AZ, replication, and health-checked load balancing make high availability a configuration choice',
    ],
    whenToUse: [
      'Production workloads needing elasticity, managed services, and a deep ecosystem (the default choice for most product companies)',
      'Spiky or unpredictable traffic where paying for idle hardware is wasteful',
      'Teams that want to outsource database failover, queue durability, and certificate management',
    ],
    whenNotToUse: [
      'Tiny static sites or hobby projects where a simpler PaaS/host is cheaper and faster to operate',
      'Workloads with strict data-residency or air-gap requirements that the available regions cannot satisfy',
      'Steady, predictable, massive compute where owned hardware (or a cheaper cloud) wins on cost at scale',
    ],
  },

  internals: [
    {
      id: 'request-flow',
      title: 'Anatomy of a Request: Route53 → CloudFront → ALB → Compute → RDS',
      summary: 'The canonical interview question: trace a browser request through DNS, CDN, load balancer, application tier and database. Every layer adds caching, security, or availability.',
      details: [
        'DNS: the browser resolves app.example.com via Route53, which returns the CloudFront distribution domain (alias record). Route53 routing policies (latency, failover, weighted) decide WHICH endpoint when there are several.',
        'CDN: CloudFront serves cached static assets from the nearest edge location. Cache misses and dynamic paths (/api/*) are forwarded to the origin over the AWS backbone. TLS usually terminates here first (ACM certificate).',
        'Load balancer: the Application Load Balancer in public subnets receives origin traffic, evaluates listener rules (path/host based), and forwards to a target group of healthy EC2 instances or ECS tasks in private subnets.',
        'Compute: the application server (EC2/ECS/Fargate task) executes business logic. It reaches the database via a VPC-internal DNS endpoint; security groups allow only ALB→app and app→DB traffic.',
        'Database: RDS in private subnets answers the query. Reads can be offloaded to read replicas; the multi-AZ standby exists purely for failover, not load.',
        'Response: data flows back app → ALB → CloudFront (which may cache it if headers allow) → browser. Each hop is a place to add caching, compression, WAF rules, or timeouts.',
      ],
      diagram: ['Browser', 'Route53 (DNS resolution)', 'CloudFront edge (cache hit? serve : forward)', 'ALB in public subnets (listener rules, health checks)', 'EC2 / ECS tasks in private subnets', 'RDS primary (multi-AZ standby, read replicas)', 'Response back up the chain'],
    },
    {
      id: 'iam-model',
      title: 'IAM: Identities, Policies & the Evaluation Logic',
      summary: 'Every API call to AWS is authenticated (who) and authorized (allowed?). IAM policies are JSON documents evaluated with explicit-deny-wins logic; roles eliminate long-lived credentials.',
      details: [
        'Identities: users (humans, long-lived credentials — discouraged), groups (policy attachment points for users), and roles (assumable identities issuing temporary credentials via STS — the right answer for almost everything).',
        'Policies are JSON: Effect (Allow/Deny), Action (s3:GetObject), Resource (ARN), optional Condition (IP, MFA, tags). Identity-based policies attach to who; resource-based policies (S3 bucket policies, SQS queue policies) attach to what.',
        'Evaluation: start at implicit deny → an explicit Allow in any applicable policy grants access → an explicit Deny anywhere overrides everything. Permission boundaries and SCPs (Organizations) cap the maximum possible permissions.',
        'Roles vs access keys: an EC2 instance profile or ECS task role delivers auto-rotated temporary credentials via the metadata service — nothing to leak, nothing to rotate. Access keys in code/env files are the #1 cause of AWS account compromise.',
        'Least privilege in practice: scope Action and Resource narrowly (s3:GetObject on one bucket prefix, not s3:* on *), use conditions, and review with IAM Access Analyzer which flags external access and unused permissions.',
        'Cross-account access: a role in account B trusts account A (trust policy); a principal in A calls sts:AssumeRole — the standard pattern for CI/CD deploying into multiple accounts.',
      ],
      diagram: ['API call with credentials', 'Authenticate: who is the principal?', 'Gather all applicable policies (identity + resource + boundary + SCP)', 'Explicit Deny anywhere? → DENY', 'Explicit Allow present? → ALLOW', 'Otherwise → implicit DENY'],
      code: {
        lang: 'json',
        code: `{\n  "Version": "2012-10-17",\n  "Statement": [{\n    "Effect": "Allow",\n    "Action": ["s3:GetObject", "s3:PutObject"],\n    "Resource": "arn:aws:s3:::app-uploads/users/*",\n    "Condition": { "Bool": { "aws:SecureTransport": "true" } }\n  }]\n}`,
        caption: 'Least privilege: specific actions, one prefix of one bucket, TLS required.',
      },
    },
    {
      id: 's3-internals',
      title: 'S3: Storage Classes, Presigned URLs & Access Control',
      summary: 'S3 is object storage with 11-nines durability: flat key/value over HTTP, lifecycle-tiered storage classes, and three overlapping access-control mechanisms interviewers love to probe.',
      details: [
        'Model: buckets (globally unique names) hold objects (key → bytes + metadata, up to 5TB). There are no real folders — prefixes simulate them. Durability comes from replicating objects across ≥3 AZs.',
        'Storage classes trade retrieval cost/latency for storage price: Standard (hot), Standard-IA and One Zone-IA (infrequent, retrieval fee), Glacier Instant/Flexible/Deep Archive (archival, minutes-to-hours restore), and Intelligent-Tiering (automatic movement). Lifecycle rules transition and expire objects automatically.',
        'Access control layers: Block Public Access (account/bucket kill switch — leave ON), bucket policies (resource-based JSON, the modern tool), and IAM identity policies. ACLs are legacy — disable them with bucket-owner-enforced object ownership.',
        'Presigned URLs: a principal with access signs a URL granting time-limited rights to one object — the standard pattern for direct browser uploads/downloads without proxying bytes through your servers or making anything public.',
        'Versioning keeps every object version; deletes just add a delete marker, so accidental deletion and overwrite are recoverable. Pair with MFA delete or Object Lock for compliance. Replication (CRR/SRR) requires versioning.',
        'Static hosting: keep the bucket PRIVATE and front it with CloudFront using Origin Access Control (OAC) — CloudFront signs origin requests and the bucket policy allows only that distribution. Public website hosting mode is the legacy, no-TLS approach.',
      ],
      diagram: ['Client requests upload', 'Backend (has IAM rights) generates presigned PUT URL, 5 min expiry', 'Client uploads bytes directly to S3', 'S3 event → SQS/Lambda for post-processing', 'Download path: CloudFront + OAC reads private bucket'],
    },
    {
      id: 'compute-spectrum',
      title: 'Compute Spectrum: EC2 vs ECS/Fargate vs Lambda',
      summary: 'The core tradeoff is control vs operational burden vs cost model: VMs you patch, containers AWS schedules, and functions that exist only while running.',
      details: [
        'EC2: raw VMs. You choose instance type, AMI, and patch the OS. Maximum control (GPUs, custom kernels, long-lived daemons), maximum ops. Billed per second while running — idle time costs money.',
        'ECS: container orchestration. Task definitions describe containers; services keep N tasks running behind an ALB. EC2 launch type = you manage the host fleet; Fargate = AWS runs each task on serverless capacity — no hosts to patch, slightly higher per-vCPU price.',
        'Lambda: functions triggered by events (API Gateway, SQS, S3, schedules). Scales from zero to thousands of concurrent executions automatically; billed per request + GB-ms. Constraints: 15-minute max, limited package size, cold starts, no local persistent state.',
        'Cold starts: a new Lambda execution environment must initialize runtime + code (tens of ms to seconds, worst with VPC + large bundles + JVM). Mitigations: provisioned concurrency, smaller bundles, keeping initialization outside the handler.',
        'Decision heuristic: steady high-utilization or special hardware → EC2 (with Reserved/Savings Plans); team ships containers and wants knobs without server patching → Fargate; spiky/event-driven/glue workloads → Lambda.',
        'Cost crossover: Lambda is cheapest at low/bursty volume and can become more expensive than always-on containers at sustained high throughput — interviewers reward mentioning this inversion.',
      ],
      diagram: ['More control / more ops', 'EC2 (you patch the VM)', 'ECS on EC2 (you patch hosts, AWS schedules containers)', 'Fargate (AWS owns hosts, you own containers)', 'Lambda (AWS owns everything but your function)', 'Less ops / less control'],
    },
    {
      id: 'vpc-networking',
      title: 'VPC: Subnets, Security Groups, NACLs & NAT',
      summary: 'A VPC is your private network slice: public subnets route to an Internet Gateway, private subnets reach out via NAT, and two firewall layers (stateful SGs, stateless NACLs) gate traffic.',
      details: [
        'A VPC spans a region with a CIDR block (e.g. 10.0.0.0/16); subnets carve it per AZ. A subnet is "public" only because its route table sends 0.0.0.0/0 to an Internet Gateway; private subnets have no such route.',
        'Standard topology: ALB + NAT Gateway in public subnets; app servers and databases in private subnets across ≥2 AZs. Nothing with a database should ever be directly internet-reachable.',
        'NAT Gateway lets private instances initiate outbound connections (package updates, external APIs) while remaining unreachable inbound. It is per-AZ, billed hourly + per-GB — a notorious surprise cost.',
        'Security groups: stateful, instance/ENI-level, allow rules only; return traffic is automatically permitted. Best practice: reference other SGs ("DB SG allows 5432 from App SG"), not CIDR ranges.',
        'NACLs: stateless, subnet-level, ordered allow AND deny rules, evaluated in both directions (you must open ephemeral ports for return traffic). Used sparingly — e.g., to blanket-deny a hostile CIDR.',
        'VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for other services) keep AWS API traffic off the public internet and off the NAT bill.',
      ],
      diagram: ['Internet', 'Internet Gateway', 'Public subnets: ALB, NAT Gateway', 'Private subnets: ECS/EC2 app tier (SG: allow from ALB SG)', 'Private subnets: RDS (SG: allow 5432 from App SG)', 'Outbound from private tier → NAT → IGW'],
    },
    {
      id: 'rds-ha',
      title: 'RDS: Multi-AZ vs Read Replicas, Backups & Failover',
      summary: 'The most-asked database distinction: multi-AZ is synchronous replication for availability (one endpoint, automatic failover); read replicas are asynchronous copies for read scaling.',
      details: [
        'Multi-AZ: a synchronous standby in another AZ. It serves NO traffic; on primary failure, RDS flips the DNS of the single endpoint to the standby in ~1–2 minutes. Zero data loss (sync commit), no application changes needed beyond reconnect logic.',
        'Read replicas: asynchronous copies (same region or cross-region) with their OWN endpoints. They scale reads and serve reporting, but replication lag means recently written data may be missing — never read-your-own-writes from a replica.',
        'Promotion: a replica can be manually promoted to a standalone primary (disaster recovery, region migration) — but because replication is async, promotion can lose the last moments of writes.',
        'Backups: automated daily snapshots + transaction logs enable point-in-time recovery within the retention window (up to 35 days). Manual snapshots persist until deleted. Restores always create a NEW instance — practice that runbook before you need it.',
        'Connection management: serverless or high-concurrency tiers exhaust DB connections; RDS Proxy pools and shares connections, and smooths failovers by holding client connections open.',
        'Aurora variant: storage is replicated 6 ways across 3 AZs and decoupled from compute; replicas share the storage volume (lag in ms) and failover is faster — worth name-dropping as the managed-MySQL/Postgres evolution.',
      ],
      diagram: ['App writes → primary endpoint', 'Synchronous replication → multi-AZ standby (invisible, failover only)', 'Asynchronous replication → read replicas (own endpoints, lag)', 'Failure detected → DNS flips to standby (~60–120s)', 'Reads can target replicas; writes always primary'],
    },
    {
      id: 'cloudfront-caching',
      title: 'CloudFront & Edge Caching',
      summary: 'CloudFront caches responses at 400+ edge locations keyed by path plus whatever the cache policy includes. Getting the cache key and invalidation story right is the whole game.',
      details: [
        'Request flow: viewer → nearest edge → (hit? serve) → regional edge cache → origin (S3, ALB, any HTTP server). Origin fetches ride the AWS backbone, which alone improves dynamic latency.',
        'Cache key: by default the URL path; cache policies add selected headers, query strings, cookies. Including too much (all cookies) fragments the cache to uselessness; too little serves the wrong variant to users.',
        'TTLs: origin Cache-Control headers drive freshness, bounded by policy min/max/default TTL. Best practice for assets: immutable content-hashed filenames (app.3f9c.js) with max-age=31536000 — deploys change the URL, so invalidation is rarely needed.',
        'Invalidation: explicit purge by path (/* wildcard allowed) — takes effect in seconds-to-minutes, first 1,000 paths/month free. A crutch if you rely on it routinely; versioned URLs are the durable fix.',
        'Dynamic content benefits too: TLS termination at the edge, connection reuse to origin, optional caching of short-TTL API responses, and edge compute (CloudFront Functions for header rewrites, Lambda@Edge for heavier logic).',
        'Security at the edge: ACM certificate for HTTPS, AWS WAF attachment for OWASP rules and rate limiting, signed URLs/cookies for paid private content, and OAC so S3 origins stay private.',
      ],
      diagram: ['Viewer request', 'Nearest edge location — cache hit? serve', 'Miss → regional edge cache', 'Miss → origin (S3 via OAC / ALB)', 'Response cached per cache policy + TTL', 'Subsequent viewers served from edge'],
    },
    {
      id: 'messaging-decoupling',
      title: 'SQS & SNS: Decoupling, Fan-out and Dead Letter Queues',
      summary: 'Queues buffer work between producers and consumers so neither needs the other alive or fast. SQS is pull-based 1:1 work distribution; SNS is push-based 1:many fan-out; DLQs catch poison messages.',
      details: [
        'SQS standard queues: at-least-once delivery, best-effort ordering, near-unlimited throughput. FIFO queues: exactly-once processing and strict order within a message group, at lower throughput. Consumers poll (use long polling to cut empty-receive cost).',
        'Visibility timeout: a received message becomes invisible while a consumer works; if the consumer crashes and never deletes it, the message reappears for redelivery. Set it longer than worst-case processing time — and make handlers idempotent because redelivery WILL happen.',
        'Dead letter queue: after maxReceiveCount failed attempts, SQS moves the message to a DLQ instead of retrying forever. Alarm on DLQ depth; inspect, fix, and redrive — this is how you debug poison messages without losing data.',
        'SNS: topics push one published message to many subscribers (SQS queues, Lambda, HTTP, email). The fan-out pattern — SNS topic feeding multiple SQS queues — gives each downstream service its own durable, independently consumed copy.',
        'Why decouple: the checkout API enqueues "send email, update analytics, sync CRM" and returns in 50ms; workers process asynchronously at their own pace. A downstream outage queues up instead of failing checkouts, and the queue absorbs traffic spikes.',
        'EventBridge is the richer sibling: an event bus with content-based rule routing, schema registry and SaaS integrations — mention it when the interviewer pushes past SNS.',
      ],
      diagram: ['Producer (API) publishes', 'SNS topic', 'Fan-out → SQS queue per consumer service', 'Workers poll, process, delete', 'Failure × maxReceiveCount → DLQ', 'Alarm on DLQ depth → inspect & redrive'],
    },
    {
      id: 'observability-cloudwatch',
      title: 'CloudWatch: Metrics, Logs, Alarms',
      summary: 'CloudWatch is the default telemetry plane: services emit metrics, agents and Lambda ship logs, alarms watch thresholds and trigger auto scaling or notifications.',
      details: [
        'Metrics: time-series datapoints in namespaces (CPUUtilization, ALB TargetResponseTime, SQS ApproximateNumberOfMessagesVisible). EC2 memory and disk are NOT default metrics — you need the CloudWatch agent. Custom metrics via PutMetricData or the cheaper Embedded Metric Format in logs.',
        'Logs: log groups → streams; Lambda and ECS (awslogs driver) ship automatically, EC2 needs the agent. Logs Insights gives an ad-hoc query language over them. Set retention per group — "never expire" is a silent cost leak.',
        'Alarms: a metric (or math expression) breaching a threshold for N evaluation periods changes alarm state, which can notify SNS, trigger auto scaling policies, or run automated actions. Composite alarms combine several to cut noise.',
        'Useful patterns: alarm on p99 latency not average (averages hide pain), alarm on ALB 5xx rate, on SQS queue depth and message age, on Lambda errors and throttles, and a billing alarm on estimated charges in every account.',
        'Tracing: X-Ray (or OpenTelemetry) stitches a request across ALB → service → DB → queue with a trace ID — the answer when "logs per service" is not enough to find the slow hop.',
        'Interview framing: metrics tell you THAT something is wrong, logs tell you WHY, traces tell you WHERE in the chain. Alarms turn the first into action.',
      ],
    },
  ],

  keywords: [
    {
      id: 'kw-iam-role',
      term: 'IAM Role vs Access Keys',
      importance: 5,
      whyAsked: 'Leaked access keys are the most common AWS breach vector; interviewers check whether you reach for roles and temporary credentials by default.',
      explanation: 'A role is an assumable identity that issues short-lived credentials via STS. EC2 instance profiles, ECS task roles and Lambda execution roles deliver and rotate these automatically — code never holds a secret. Access keys are static credentials that must be stored, rotated, and inevitably get committed to git.',
      realWorldExample: 'A CI pipeline uses GitHub OIDC federation to assume a deploy role for 15 minutes — no AWS secret stored in CI at all.',
      commonMistakes: ['Baking access keys into AMIs, .env files or frontend bundles', 'Using one over-privileged "admin" role for every service', 'Forgetting roles also need least-privilege policies — assuming "role" automatically means "safe"'],
      followUps: ['How does an EC2 instance actually obtain role credentials? (metadata service / IMDSv2)', 'How would you grant a partner account access to one bucket?', 'What is a permission boundary?'],
    },
    {
      id: 'kw-least-privilege',
      term: 'Least Privilege',
      importance: 5,
      whyAsked: 'It is the organizing principle of cloud security; concrete examples separate people who have written policies from those who have read about them.',
      explanation: 'Grant the minimum actions on the minimum resources with conditions where possible. IAM evaluation defaults to deny; explicit deny always wins. In practice: specific actions instead of service:*, ARNs instead of *, conditions like aws:SecureTransport, and periodic pruning with Access Analyzer.',
      commonMistakes: ['s3:* on Resource: * "to make it work" and never tightening it', 'Confusing identity policies with resource policies (S3 bucket policy) — access can come from either', 'Not knowing explicit Deny overrides any Allow'],
      followUps: ['Walk through IAM evaluation order', 'Identity-based vs resource-based policies?', 'How do SCPs interact with IAM policies?'],
      diagram: ['Implicit deny (default)', 'Collect identity + resource policies', 'Explicit Deny? → blocked', 'Explicit Allow? → permitted', 'Else → denied'],
    },
    {
      id: 'kw-presigned-url',
      term: 'S3 Presigned URL',
      importance: 4,
      whyAsked: 'The standard answer to "how do users upload files without your server proxying bytes or the bucket going public" — a daily pattern in product companies.',
      explanation: 'A URL signed with a principal\'s credentials granting time-limited access to one operation on one object (GET or PUT). The bucket stays fully private; authorization is enforced at signing time by your backend, and the signature expires (minutes, not days).',
      realWorldExample: 'Profile-photo upload: API validates the user, returns a presigned PUT for uploads/{userId}/avatar.jpg with a 5-minute expiry and content-type condition; the browser uploads straight to S3; an S3 event triggers a resize Lambda.',
      commonMistakes: ['Hours-long expiries "for convenience"', 'Signing with an over-privileged identity — the URL can do whatever the signer can on that key', 'Not constraining content type/size on uploads'],
      followUps: ['Presigned URL vs CloudFront signed URL — when each?', 'How do you cap upload size? (POST policy conditions)', 'What happens if the signer\'s credentials are revoked?'],
    },
    {
      id: 'kw-multi-az-replica',
      term: 'Multi-AZ vs Read Replica',
      importance: 5,
      whyAsked: 'The single most common AWS database question — it tests whether you separate availability from scalability.',
      explanation: 'Multi-AZ: synchronous standby, invisible to the app, one endpoint, automatic failover — buys AVAILABILITY. Read replica: asynchronous copy with its own endpoint, serves reads with possible lag — buys READ SCALE. Production systems typically use both.',
      commonMistakes: ['Claiming the multi-AZ standby serves read traffic (it does not)', 'Reading a user\'s own just-written data from a lagging replica', 'Thinking replica promotion is lossless — async replication can drop tail writes'],
      followUps: ['How long is multi-AZ failover and what does the app see?', 'How does Aurora change this picture?', 'Where do you point analytics queries?'],
      diagram: ['Writes → primary', 'Sync → standby (failover only)', 'Async → replicas (reads, lag)', 'Primary dies → DNS flips to standby'],
    },
    {
      id: 'kw-sg-vs-nacl',
      term: 'Security Group vs NACL',
      importance: 4,
      whyAsked: 'A crisp two-axis comparison (stateful/stateless, instance/subnet) that quickly reveals whether VPC knowledge is real.',
      explanation: 'Security groups are stateful, attached to ENIs/instances, allow-only — return traffic is automatically permitted. NACLs are stateless, attached to subnets, ordered allow/deny rules — return traffic needs explicit ephemeral-port rules. SGs are the daily tool; NACLs are the coarse subnet backstop.',
      realWorldExample: 'DB security group allows 5432 only from the app tier\'s security group ID — no CIDR ranges, so the rule survives instance churn and auto scaling.',
      commonMistakes: ['Opening SSH (22) to 0.0.0.0/0 instead of using SSM Session Manager', 'Forgetting NACLs are stateless and blocking return traffic', 'Allowing by CIDR where an SG reference is cleaner'],
      followUps: ['Can a security group deny traffic? (no — absence of allow)', 'How would you block one abusive IP? (NACL deny or WAF)', 'What is an ENI?'],
    },
    {
      id: 'kw-alb-nlb',
      term: 'ALB vs NLB',
      importance: 4,
      whyAsked: 'Standard L7-vs-L4 comparison that anchors the whole load-balancing and target-group conversation.',
      explanation: 'ALB operates at layer 7: HTTP-aware routing by path/host/header, WebSockets, OIDC auth, WAF attachment — the default for web apps and APIs. NLB operates at layer 4: TCP/UDP passthrough, millions of connections, static IPs per AZ, ultra-low latency, source-IP preservation — for non-HTTP protocols, PrivateLink, or fixed-IP requirements.',
      commonMistakes: ['Choosing NLB "for performance" when ALB latency is fine and L7 features are needed', 'Not knowing ALB IPs change (use the DNS name; use NLB if a customer firewall needs fixed IPs)', 'Ignoring target group health checks — the actual availability mechanism'],
      followUps: ['How do health checks interact with auto scaling?', 'What are connection draining / deregistration delays?', 'Where does TLS terminate in each case?'],
    },
    {
      id: 'kw-auto-scaling',
      term: 'Auto Scaling',
      importance: 4,
      whyAsked: 'Elasticity is the cloud\'s core promise; interviewers probe whether you can configure it without flapping or scaling on the wrong signal.',
      explanation: 'An Auto Scaling Group keeps N healthy instances across AZs and adjusts N via policies: target tracking (hold CPU at 60% or ALB requests-per-target at X — the sane default), step scaling on alarms, and scheduled scaling for known peaks. ECS services scale task counts the same way; Lambda scales per-request implicitly.',
      realWorldExample: 'Scale a worker fleet on SQS queue depth per instance rather than CPU — queue backlog is the truthful demand signal for async workloads.',
      commonMistakes: ['Scaling web tiers on CPU when the bottleneck is connections or latency', 'No scale-in protection or cooldowns → thrashing', 'Forgetting the warm-up time: scaling reacts in minutes, so capacity headroom still matters for sudden spikes'],
      followUps: ['Scale-out vs scale-up?', 'How do lifecycle hooks help with graceful shutdown?', 'How does predictive scaling work?'],
    },
    {
      id: 'kw-route53-policies',
      term: 'Route53 Routing Policies',
      importance: 3,
      whyAsked: 'DNS-level traffic steering is the top of every architecture; failover and weighted routing show up in DR and canary-deployment discussions.',
      explanation: 'Beyond simple records: weighted (split traffic by percentage — canaries), latency-based (send users to the lowest-latency region), failover (primary/secondary with health checks — DR), geolocation (compliance/localization), and multivalue (poor-man\'s LB). Alias records map zone apexes to AWS resources free of charge.',
      commonMistakes: ['Using CNAME at the zone apex (invalid — use alias)', 'Forgetting DNS TTL means failover is not instant for clients that cached the old answer', 'Confusing latency-based routing with geolocation (proximity ≠ measured latency)'],
      followUps: ['How does Route53 health checking decide failover?', 'How would you canary 5% of traffic to a new region?'],
    },
    {
      id: 'kw-dlq',
      term: 'Dead Letter Queue',
      importance: 4,
      whyAsked: 'It distinguishes engineers who have operated async systems — everyone else discovers poison messages in production.',
      explanation: 'A queue receiving messages that failed processing maxReceiveCount times. Without one, a malformed message redelivers forever, burning compute and blocking FIFO groups. With one, failures park safely: alarm on DLQ depth, inspect the payload, fix the bug, redrive back to the source queue.',
      realWorldExample: 'A payment webhook with an unexpected schema crashes the consumer; after 5 receives it lands in the DLQ, an alarm pages, and the other 50k messages keep flowing.',
      commonMistakes: ['No alarm on the DLQ — messages rot silently until expiry', 'Setting maxReceiveCount to 1, sending transient failures (DB blip) straight to DLQ', 'Forgetting handlers must be idempotent because at-least-once delivery means retries'],
      followUps: ['How do you redrive DLQ messages?', 'How does visibility timeout interact with retries?', 'What is a poison message?'],
    },
    {
      id: 'kw-secrets-mgmt',
      term: 'Secrets Manager vs Parameter Store',
      importance: 4,
      whyAsked: 'Every app has DB passwords and API keys; where they live and how they rotate is a routine production-readiness question.',
      explanation: 'Both store encrypted values retrieved via IAM-authorized API calls. Secrets Manager adds automatic rotation (built-in Lambdas for RDS), cross-account access and per-secret pricing. Parameter Store (SSM) is free for standard parameters, great for config + modest secret needs (SecureString with KMS), no native rotation. Either beats env files and code.',
      commonMistakes: ['Putting secrets in plaintext env vars in task definitions or .env in the repo', 'Fetching the secret on every request instead of caching with a TTL', 'Granting secretsmanager:GetSecretValue on * rather than specific ARNs'],
      followUps: ['How does RDS rotation work without downtime? (two active users / versioned secret)', 'How do ECS tasks inject secrets at startup?', 'Role of KMS underneath both?'],
    },
    {
      id: 'kw-oac',
      term: 'CloudFront OAC',
      importance: 3,
      whyAsked: 'It is the modern answer to "serve a private S3 bucket through a CDN" — and it exposes whether your S3 hosting knowledge is current.',
      explanation: 'Origin Access Control makes CloudFront sign its requests to S3 with SigV4; the bucket policy allows s3:GetObject only when the request comes from your specific distribution. The bucket stays private with Block Public Access on, all access flows through CloudFront (HTTPS, caching, WAF), and direct bucket URLs return 403. OAC replaces the older OAI.',
      followUps: ['Why is S3 website-hosting mode inferior? (public bucket, HTTP only)', 'How do SPA routes get handled? (403/404 → /index.html custom error response)', 'OAC vs signed URLs — which problem does each solve?'],
    },
    {
      id: 'kw-cost-model',
      term: 'Cost Awareness',
      importance: 4,
      whyAsked: 'Senior engineers are expected to know what their architecture costs; "how would you reduce the bill" is now a standard interview question.',
      explanation: 'The big levers: compute pricing models (on-demand vs Savings Plans/Reserved ~40-70% off for steady load vs Spot ~70-90% off for interruptible work), data transfer (cross-AZ and egress charges, NAT Gateway per-GB), storage tiering (S3 lifecycle, gp3 over gp2), and rightsizing (most fleets are over-provisioned). Visibility first: Cost Explorer, tags, budgets and billing alarms.',
      realWorldExample: 'A team cut 30% off their bill by moving batch workers to Spot, adding S3 lifecycle rules to Glacier, and replacing NAT-routed S3 traffic with a free gateway VPC endpoint.',
      commonMistakes: ['Optimizing instance prices while ignoring data-transfer and NAT charges', 'Buying reservations before rightsizing (locking in waste)', 'No tags → no idea which team/feature spends what'],
      followUps: ['Spot interruption handling?', 'Savings Plans vs Reserved Instances?', 'Why is cross-AZ traffic a cost concern for chatty microservices?'],
    },
  ],

  cheatSheets: [
    {
      id: 'cs-compute',
      title: 'Compute options at a glance',
      rows: [
        { concept: 'EC2', description: 'VMs you patch and size. Max control (GPUs, daemons). Pay while running. Steady high-utilization workloads.' },
        { concept: 'ECS on EC2', description: 'Containers scheduled by AWS on hosts you manage. Cheapest containers at scale; you own host patching.' },
        { concept: 'Fargate', description: 'Containers on serverless capacity — no hosts. Pay per task vCPU/RAM-second. The default for containerized services.' },
        { concept: 'Lambda', description: 'Event-driven functions, scale-to-zero, 15-min cap, cold starts. Pay per request + GB-ms. Spiky/glue workloads.' },
        { concept: 'Rule of thumb', description: 'Steady → EC2/ECS+Savings Plan; containerized product services → Fargate; event-driven/bursty → Lambda.' },
      ],
    },
    {
      id: 'cs-storage-db',
      title: 'Storage & database picks',
      rows: [
        { concept: 'S3', description: 'Objects (files, backups, static assets). 11-nines durable, lifecycle to IA/Glacier, presigned URLs for direct access.' },
        { concept: 'EBS', description: 'Block volume attached to one EC2 instance — its disk. gp3 default; snapshots to S3.' },
        { concept: 'EFS', description: 'Shared NFS filesystem mounted by many instances/tasks simultaneously.' },
        { concept: 'RDS', description: 'Managed Postgres/MySQL etc. Multi-AZ = availability; read replicas = read scale; PITR backups.' },
        { concept: 'Aurora', description: 'AWS-built MySQL/Postgres: 6-way replicated shared storage, ms-lag replicas, faster failover.' },
        { concept: 'DynamoDB', description: 'Serverless key-value at any scale, single-digit-ms. Design around access patterns; no ad-hoc joins.' },
        { concept: 'ElastiCache', description: 'Managed Redis/Memcached — sessions, hot keys, computed-result caching in front of RDS.' },
      ],
    },
    {
      id: 'cs-networking',
      title: 'Networking & delivery reference',
      rows: [
        { concept: 'Public vs private subnet', description: 'Public = route table has 0.0.0.0/0 → IGW. Private = no IGW route; outbound via NAT. DBs/app servers go private.' },
        { concept: 'SG vs NACL', description: 'SG: stateful, instance-level, allow-only. NACL: stateless, subnet-level, allow+deny, ordered rules.' },
        { concept: 'ALB vs NLB', description: 'ALB: L7, path/host routing, WAF, WebSockets. NLB: L4, TCP/UDP, static IPs, extreme throughput.' },
        { concept: 'NAT Gateway', description: 'Outbound-only internet for private subnets. Hourly + per-GB — use S3/DynamoDB gateway endpoints to dodge it.' },
        { concept: 'Route53 policies', description: 'Weighted (canary), latency (multi-region), failover (DR + health checks), geolocation (compliance).' },
        { concept: 'CloudFront', description: 'Edge cache keyed by path + policy-selected headers/query/cookies. OAC for private S3; invalidate sparingly, version URLs instead.' },
      ],
    },
    {
      id: 'cs-ops-security',
      title: 'Ops & security quick hits',
      rows: [
        { concept: 'IAM evaluation', description: 'Implicit deny → explicit Allow grants → explicit Deny overrides everything. Roles + STS over access keys, always.' },
        { concept: 'Secrets', description: 'Secrets Manager (rotation, RDS integration) or SSM Parameter Store (free, config + SecureString). Never env files in git.' },
        { concept: 'SQS essentials', description: 'Visibility timeout > processing time; idempotent consumers; DLQ with alarm; long polling on.' },
        { concept: 'CloudWatch alarms', description: 'Alarm on p99 latency, 5xx rate, queue depth/age, Lambda errors, DLQ depth, estimated charges.' },
        { concept: 'Cost levers', description: 'Savings Plans for steady compute, Spot for batch, S3 lifecycle, gp3, rightsizing, kill idle NAT/EIP/old snapshots.' },
        { concept: 'Access to servers', description: 'SSM Session Manager instead of SSH + bastion + open port 22 — audited, no inbound rules.' },
      ],
    },
  ],

  questions: [
    {
      id: 'q-request-flow',
      level: 'intermediate',
      question: 'Walk me through what happens when a user hits https://app.example.com on an AWS-hosted app.',
      answer: 'Route53 resolves the domain (alias to CloudFront). CloudFront serves cached static assets from the nearest edge; API/dynamic requests forward to the origin ALB over the AWS backbone. The ALB, in public subnets, applies listener rules and forwards to healthy ECS tasks/EC2 instances in private subnets. The app queries RDS (also private) via security-group-restricted access, possibly hitting ElastiCache first. The response returns through ALB and CloudFront, which may cache it per headers.',
      deepDive: 'Strong answers name where TLS terminates (CloudFront, again at ALB), where WAF sits, why the app tier has no public IPs, and what each layer contributes: DNS routing policy, edge caching, health-checked load balancing, SG-enforced isolation, multi-AZ database failover.',
      followUps: ['Where would you add caching at each layer?', 'What fails if one AZ goes down?', 'Where does auto scaling plug in?'],
    },
    {
      id: 'q-iam-user-role-policy',
      level: 'beginner',
      question: 'Explain IAM users, groups, roles and policies in plain terms.',
      answer: 'Users are long-lived identities for humans (better: SSO federation). Groups bundle users so policies attach once. Roles are identities anything can assume temporarily — EC2, Lambda, another account — receiving auto-expiring credentials from STS. Policies are JSON documents (Effect/Action/Resource/Condition) defining permissions; they attach to identities or to resources like S3 buckets.',
      deepDive: 'Evaluation: default deny; any applicable Allow grants; any explicit Deny overrides. Mention that services should use roles, not embedded access keys, and that least privilege means narrow actions and resource ARNs.',
      followUps: ['Why are roles safer than access keys?', 'What is a trust policy on a role?'],
    },
    {
      id: 'q-s3-classes',
      level: 'beginner',
      question: 'What S3 storage classes exist and how do lifecycle rules use them?',
      answer: 'Standard for hot data; Standard-IA/One Zone-IA for infrequently accessed data (cheaper storage, retrieval fee, One Zone has no multi-AZ redundancy); Glacier Instant/Flexible/Deep Archive for archival at decreasing cost and increasing restore time; Intelligent-Tiering moves objects automatically based on access. Lifecycle rules transition objects between classes by age and expire them — e.g., logs to IA at 30 days, Glacier at 90, deleted at 365.',
      followUps: ['When is Intelligent-Tiering worth its monitoring fee?', 'What are minimum storage-duration charges on IA/Glacier?'],
    },
    {
      id: 'q-ec2-lambda-choice',
      level: 'intermediate',
      question: 'When would you choose EC2 over Lambda, and Fargate over both?',
      answer: 'EC2: long-running, steady-high-utilization, special hardware (GPU), custom OS needs, or cost-optimized with Savings Plans. Lambda: event-driven, spiky, short tasks (<15 min), scale-to-zero economics, minimal ops. Fargate is the middle path: your team ships containers, runs continuously behind an ALB, no 15-minute cap or cold-start concerns, and nobody patches hosts.',
      deepDive: 'Mention the cost inversion: Lambda is cheapest at low volume but at sustained high throughput an always-on Fargate/EC2 service becomes cheaper per request. Also cold starts: VPC-attached, large-bundle Lambdas pay the worst startup penalty.',
      followUps: ['How do you mitigate Lambda cold starts?', 'What does ECS EC2 launch type still buy over Fargate? (price at scale, daemonsets, GPUs)'],
    },
    {
      id: 'q-vpc-design',
      level: 'intermediate',
      question: 'Design the VPC for a standard three-tier web app.',
      answer: 'One VPC (10.0.0.0/16) across at least two AZs. Per AZ: a public subnet (ALB, NAT Gateway) and private subnets (app tier, data tier). Route tables: public subnets → IGW; private → NAT for outbound only. Security groups chained by reference: ALB SG allows 443 from the internet; app SG allows app-port from ALB SG; DB SG allows 5432 from app SG. No public IPs on app or DB. Add gateway endpoints for S3/DynamoDB to skip NAT costs, and SSM Session Manager instead of SSH bastions.',
      followUps: ['Why two AZs minimum?', 'When would you touch NACLs at all?', 'How do private instances pull OS updates?'],
    },
    {
      id: 'q-multi-az-vs-replica',
      level: 'intermediate',
      question: 'RDS multi-AZ vs read replicas — what does each solve?',
      answer: 'Multi-AZ solves availability: a synchronous standby in another AZ, invisible to the app, takes over via DNS flip in ~1–2 minutes on failure — zero data loss, no read scaling. Read replicas solve read throughput: asynchronous copies with separate endpoints serving SELECTs and analytics, with replication lag. Use both: multi-AZ for HA, replicas for scale; never serve read-your-own-writes from a replica.',
      followUps: ['What can a cross-region replica be used for? (DR, latency, promotion)', 'How does the app handle failover gracefully? (reconnect logic, RDS Proxy)'],
    },
    {
      id: 'q-sg-statefulness',
      level: 'beginner',
      question: 'Why is a security group called "stateful" and a NACL "stateless"?',
      answer: 'A security group tracks connections: if inbound 443 is allowed, the response packets are automatically allowed out — no outbound rule needed. A NACL evaluates every packet independently against ordered rules in each direction; allowing inbound 443 does nothing for the response, so you must also allow outbound ephemeral ports (1024–65535). That is why SGs are the everyday tool and NACLs are reserved for subnet-level blanket rules.',
      followUps: ['Can SGs express deny? How do you block a single IP?', 'What are ephemeral ports?'],
    },
    {
      id: 'q-sqs-exactly-once',
      level: 'advanced',
      question: 'SQS standard queues deliver at-least-once. How do you build a correct system on top of that?',
      answer: 'Make consumers idempotent: derive an idempotency key from the message (order ID, event ID), record processed keys in DynamoDB/Redis with a conditional write, and skip duplicates. Set visibility timeout above worst-case processing time so messages are not double-processed by overlap. Use a DLQ after N attempts for poison messages, alarm on its depth, and redrive after fixing. FIFO queues offer exactly-once processing and ordering if throughput limits allow — but idempotency is still the robust answer because producers can also retry.',
      deepDive: 'Distinguish delivery semantics from processing semantics: the network can never guarantee exactly-once delivery end-to-end; exactly-once EFFECT is achieved at the consumer via idempotency. This framing is the senior answer.',
      followUps: ['How does a conditional write make dedup atomic?', 'What breaks if visibility timeout is shorter than processing time?'],
    },
    {
      id: 'q-cloudfront-cache-key',
      level: 'advanced',
      question: 'Your CloudFront hit ratio is 40%. How do you diagnose and improve it?',
      answer: 'Check the cache policy: forwarding all query strings, cookies or headers fragments the cache — every variant is a separate key. Trim the key to what actually changes responses. Check origin Cache-Control headers: no-cache or tiny max-age defeats edge caching; set long TTLs with content-hashed asset filenames. Check for cache-busting query params added by clients. Verify errors are not being cached or, conversely, that 4xx/5xx have short error TTLs. Measure with the CloudFront cache statistics report and x-cache response headers.',
      followUps: ['Why are versioned URLs better than invalidations?', 'How would you cache authenticated API responses safely (or should you)?'],
    },
    {
      id: 'q-asg-signals',
      level: 'advanced',
      question: 'What metric should auto scaling react to, and why is CPU often the wrong answer?',
      answer: 'Scale on the metric that tracks actual demand and saturation for your workload. CPU works for CPU-bound services but misleads for I/O-bound ones — a web tier can be drowning in slow downstream calls at 20% CPU. Better signals: ALB RequestCountPerTarget or p99 latency for web tiers; SQS queue depth (or message age) per worker for async fleets; for ECS, target tracking on service-level metrics. Add scheduled scaling for known peaks and headroom because scale-out takes minutes.',
      followUps: ['Target tracking vs step scaling?', 'How do you prevent scale-in from killing instances mid-request? (draining, lifecycle hooks)'],
    },
    {
      id: 'q-secrets-flow',
      level: 'intermediate',
      question: 'How should an ECS service get its database password?',
      answer: 'Store it in Secrets Manager (or SSM SecureString). Reference the secret ARN in the task definition\'s secrets block — ECS injects it as an env var at task start, fetched with the task execution role. The task role gets GetSecretValue on that one ARN only. For rotation, Secrets Manager\'s RDS integration rotates the password with a Lambda; the app re-fetches on auth failure or caches with a short TTL. Nothing lands in the image, the repo, or the task definition itself.',
      followUps: ['Task role vs task execution role?', 'How does rotation avoid downtime? (alternating users strategy)'],
    },
    {
      id: 'q-route53-failover',
      level: 'expert',
      question: 'Design DNS-level disaster recovery for a two-region deployment.',
      answer: 'Primary region serves traffic; standby region runs a scaled-down (pilot light / warm standby) copy. Route53 failover routing: primary record with a health check probing a deep health endpoint (one that exercises DB connectivity, not just a 200 from the LB); secondary record points at the standby region. On sustained health-check failure, Route53 answers with the secondary. Keep record TTLs low (30–60s) so clients re-resolve quickly. Replicate data continuously: cross-region RDS replica (promote on failover), S3 CRR, and infrastructure-as-code to scale the standby up.',
      deepDive: 'Discuss RTO/RPO honestly: async replication means promotion can lose tail writes (RPO > 0); DNS caching and client behavior mean failover is minutes, not instant. Mention failback complexity and testing the runbook with game days — DR that is never exercised does not exist.',
      followUps: ['Pilot light vs warm standby vs active-active?', 'Why must the health check be deeper than the load balancer?'],
    },
    {
      id: 'q-cloudwatch-alarm-design',
      level: 'intermediate',
      question: 'Which CloudWatch alarms would you set up for a typical web service, and on what?',
      answer: 'User-facing: ALB 5xx rate and p99 TargetResponseTime (not averages — they hide pain). Capacity: ASG/ECS CPU and memory, healthy host count below threshold. Async: SQS ApproximateAgeOfOldestMessage and DLQ depth > 0. Database: RDS CPU, free storage, connections, replica lag. Serverless: Lambda errors, throttles, duration nearing timeout. Account hygiene: estimated-charges billing alarm. Route alarms to SNS → on-call; use composite alarms to reduce noise.',
      followUps: ['Why p99 over average?', 'What is alarm fatigue and how do composite alarms help?'],
    },
  ],

  codingQuestions: [
    {
      id: 'code-least-priv-policy',
      title: 'Write a least-privilege IAM policy',
      level: 'intermediate',
      problem: 'An ECS task must read objects under uploads/ in bucket app-user-content, write objects under processed/, and read one secret. Write the task role policy.',
      solution: {
        lang: 'json',
        code: `{\n  "Version": "2012-10-17",\n  "Statement": [\n    {\n      "Sid": "ReadUploads",\n      "Effect": "Allow",\n      "Action": "s3:GetObject",\n      "Resource": "arn:aws:s3:::app-user-content/uploads/*"\n    },\n    {\n      "Sid": "WriteProcessed",\n      "Effect": "Allow",\n      "Action": "s3:PutObject",\n      "Resource": "arn:aws:s3:::app-user-content/processed/*"\n    },\n    {\n      "Sid": "ReadDbSecret",\n      "Effect": "Allow",\n      "Action": "secretsmanager:GetSecretValue",\n      "Resource": "arn:aws:secretsmanager:us-east-1:123456789012:secret:prod/app/db-??????"\n    }\n  ]\n}`,
        caption: 'Specific actions, prefix-scoped ARNs, no wildcards on Resource: *.',
      },
      explanation: 'Each statement grants one capability on one resource scope. GetObject and PutObject are split because read and write target different prefixes. The secret ARN includes the random six-character suffix wildcard Secrets Manager appends. Notably absent: s3:*, ListBucket (not needed for known keys), and any Resource: "*". Interviewers look for whether you reach for wildcards under pressure.',
      timeComplexity: 'N/A — policy evaluation is constant per request',
      spaceComplexity: 'N/A',
      alternatives: ['Add a Condition restricting s3 actions to aws:SecureTransport: true', 'Use ABAC: tag-based conditions (aws:ResourceTag) when many similar buckets exist'],
    },
    {
      id: 'code-presigned-upload',
      title: 'Presigned upload endpoint',
      level: 'intermediate',
      problem: 'Write an Express handler that returns a presigned PUT URL letting the authenticated user upload an avatar (max 5 min expiry, key scoped to their user ID, content type constrained).',
      solution: {
        lang: 'ts',
        code: `import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3'\nimport { getSignedUrl } from '@aws-sdk/s3-request-presigner'\n\nconst s3 = new S3Client({}) // credentials come from the task/instance role\n\napp.post('/api/avatar-upload-url', requireAuth, async (req, res) => {\n  const contentType = req.body.contentType\n  if (!['image/jpeg', 'image/png', 'image/webp'].includes(contentType)) {\n    return res.status(400).json({ error: 'unsupported content type' })\n  }\n  // Key derived from the SESSION user — client cannot choose the path\n  const key = \`avatars/\${req.user.id}/avatar\`\n  const command = new PutObjectCommand({\n    Bucket: 'app-user-content',\n    Key: key,\n    ContentType: contentType,\n  })\n  const url = await getSignedUrl(s3, command, { expiresIn: 300 })\n  res.json({ url, key })\n})`,
        caption: 'Authorization happens at signing time; S3 only verifies the signature.',
      },
      explanation: 'The three security decisions: the key comes from the server-side session (an attacker cannot overwrite another user\'s avatar), the content type is allow-listed and baked into the signature (the upload must match), and the expiry is short. The SDK signs with role credentials — no static keys. Size limits require a presigned POST with policy conditions, worth mentioning as the follow-up.',
      timeComplexity: 'O(1) — signing is local HMAC computation, no AWS round-trip',
      spaceComplexity: 'O(1)',
      alternatives: ['Presigned POST with content-length-range condition to cap file size', 'S3 event → Lambda to validate/resize after upload (never trust client bytes)'],
    },
    {
      id: 'code-architecture-as-code',
      title: 'Three-tier stack in Terraform (shape, not boilerplate)',
      level: 'advanced',
      problem: 'Sketch the Terraform resources for ALB → ECS Fargate service → RDS with correct security group chaining. Focus on the wiring that interviews probe, not provider boilerplate.',
      solution: {
        lang: 'hcl',
        code: `resource "aws_security_group" "alb" {\n  vpc_id = aws_vpc.main.id\n  ingress { from_port = 443, to_port = 443, protocol = "tcp", cidr_blocks = ["0.0.0.0/0"] }\n  egress  { from_port = 0, to_port = 0, protocol = "-1", cidr_blocks = ["0.0.0.0/0"] }\n}\n\nresource "aws_security_group" "app" {\n  vpc_id = aws_vpc.main.id\n  ingress {\n    from_port = 3000, to_port = 3000, protocol = "tcp"\n    security_groups = [aws_security_group.alb.id]   # only the ALB\n  }\n}\n\nresource "aws_security_group" "db" {\n  vpc_id = aws_vpc.main.id\n  ingress {\n    from_port = 5432, to_port = 5432, protocol = "tcp"\n    security_groups = [aws_security_group.app.id]   # only the app tier\n  }\n}\n\nresource "aws_ecs_service" "app" {\n  launch_type     = "FARGATE"\n  desired_count   = 2\n  network_configuration {\n    subnets          = aws_subnet.private[*].id     # no public IPs\n    security_groups  = [aws_security_group.app.id]\n    assign_public_ip = false\n  }\n  load_balancer {\n    target_group_arn = aws_lb_target_group.app.arn\n    container_name   = "app"\n    container_port   = 3000\n  }\n}\n\nresource "aws_db_instance" "main" {\n  engine                 = "postgres"\n  multi_az               = true\n  publicly_accessible    = false\n  db_subnet_group_name   = aws_db_subnet_group.private.name\n  vpc_security_group_ids = [aws_security_group.db.id]\n  backup_retention_period = 7\n}`,
        caption: 'The load-bearing lines: SG references chained tier-to-tier, private subnets, multi_az, no public IPs.',
      },
      explanation: 'What the interviewer checks: security groups reference each other by ID rather than CIDR (rules survive scaling and IP churn), the service runs in private subnets with assign_public_ip false, the DB is multi-AZ with backups and not publicly accessible, and desired_count ≥ 2 across AZs for availability. Everything else is boilerplate.',
      timeComplexity: 'N/A — infrastructure definition',
      spaceComplexity: 'N/A',
      alternatives: ['CDK/CloudFormation equivalents — the SG-chaining pattern is identical', 'Add an aws_appautoscaling_target + policy for ECS service auto scaling'],
    },
  ],

  scenarios: [
    {
      id: 'sc-host-fullstack',
      title: 'Host a full-stack app on AWS end to end (the classic)',
      situation: 'You have a Vue/React SPA, a Node API, and Postgres. Design the production AWS deployment — the most common AWS interview question asked of full-stack engineers.',
      approach: [
        'Frontend: build artifacts to a PRIVATE S3 bucket; CloudFront in front with OAC, ACM certificate, and a custom error response mapping 403/404 → /index.html for SPA routing. Route53 alias for the domain.',
        'API: containerize the Node app → ECR; run as an ECS Fargate service (≥2 tasks across 2 AZs) in private subnets behind an ALB. Either api.example.com via Route53, or route /api/* through the same CloudFront distribution to dodge CORS entirely.',
        'Database: RDS Postgres, multi-AZ, private subnets, SG allowing 5432 only from the app SG, 7–35 day PITR backups. Credentials in Secrets Manager, injected into the task definition.',
        'Networking: VPC with public subnets (ALB, NAT) and private subnets (tasks, RDS) in two AZs; gateway endpoint for S3.',
        'Scaling & ops: ECS service auto scaling on request count per target; CloudWatch alarms on ALB 5xx, p99 latency, RDS connections; logs via the awslogs driver; WAF on CloudFront.',
        'CI/CD: pipeline builds the image, pushes to ECR, updates the ECS service (rolling deploy); frontend sync to S3 + targeted CloudFront invalidation of index.html only (hashed assets need none).',
      ],
      keyTakeaways: ['Static and dynamic split at the CDN is the architecture headline', 'Say "private subnets, SG chaining, secrets injected, multi-AZ" unprompted — that is the seniority signal', 'Serving SPA + API from one CloudFront distribution eliminates CORS — a memorable differentiator'],
    },
    {
      id: 'sc-s3-leak',
      title: 'An S3 bucket with customer data was found public — respond',
      situation: 'A security researcher emails: a bucket holding customer documents is listable and readable by anyone. You are on point.',
      approach: [
        'Contain immediately: enable Block Public Access on the bucket (and account), remove the offending bucket policy/ACL grants. Minutes matter; do this before investigating.',
        'Assess exposure: S3 server access logs / CloudTrail data events — what was accessed, by which IPs, since when? Determine if data was actually exfiltrated, not just exposed.',
        'Find the cause: CloudTrail PutBucketPolicy/PutBucketAcl history — who changed it, when, via what (console, IaC, script)? Check whether other buckets share the misconfiguration (Access Analyzer, Config rules).',
        'Notify: security team, legal/compliance — data-breach notification laws (GDPR 72h) may apply depending on contents and confirmed access.',
        'Remediate structurally: account-level Block Public Access ON, bucket-owner-enforced object ownership (ACLs disabled), Access Analyzer alerts on external access, an SCP denying s3:PutBucketPolicy that opens public access, default encryption.',
        'Prevent recurrence: IaC review for bucket modules, a CI policy check (e.g., a rule failing any public-read bucket), and a postmortem covering why the change passed review.',
      ],
      keyTakeaways: ['Order matters: contain → assess → root-cause → notify → harden', 'CloudTrail is your forensic record — know it exists before the incident', 'Account-level Block Public Access + SCP makes the whole class of incident impossible, not just this instance'],
    },
    {
      id: 'sc-traffic-spike',
      title: 'Marketing launches a campaign tomorrow — prepare for 20x traffic',
      situation: 'Your steady 500-RPS app must survive a one-day spike to ~10,000 RPS with no architecture rewrite time.',
      approach: [
        'Shift load to the edge: maximize CloudFront caching — long TTLs on assets, cache short-TTL (30–60s) responses for anonymous/landing pages; the cheapest RPS is the one that never reaches origin.',
        'Pre-scale instead of reactive-scale: scheduled scaling to raise ECS/ASG minimums BEFORE the spike — autoscaling reacts in minutes and the spike front will outrun it.',
        'Protect the database (the hardest layer to scale fast): add/verify ElastiCache for hot reads, route read traffic to replicas, check max_connections vs task count, add RDS Proxy if connection counts are the risk.',
        'Check service quotas: ALB and account limits, Lambda concurrency, NAT Gateway bandwidth — quota tickets take time.',
        'Add safety valves: WAF rate limiting per IP against abuse, sensible timeouts and circuit breakers on downstream calls, a static "high demand" fallback page on CloudFront as the last resort.',
        'Watch it live: dashboard with p99 latency, 5xx, healthy hosts, DB connections/CPU, cache hit ratio; alarms pre-tuned; someone on point during the window.',
      ],
      keyTakeaways: ['Pre-scaling + edge caching beat reactive autoscaling for predictable spikes', 'The database is the usual bottleneck — caching and replicas, not bigger app fleets, save you', 'Load-test before the event; quotas and connection limits fail silently until they don\'t'],
    },
    {
      id: 'sc-cost-reduction',
      title: 'The AWS bill doubled — find and cut the waste',
      situation: 'Finance flags that monthly spend went from $20k to $40k over two quarters with no matching traffic growth. Reduce it without harming reliability.',
      approach: [
        'Visibility first: Cost Explorer grouped by service, then by tag/account — find the top movers. No tags? That is finding #1; enforce a tagging policy.',
        'Compute: rightsizing recommendations (Compute Optimizer) — over-provisioned instances are the norm; buy Savings Plans for the steady baseline (~40-65% off); move interruptible batch/CI to Spot (~70-90% off); kill dev/staging overnight with schedules.',
        'Storage: S3 lifecycle rules (IA at 30d, Glacier for archives, expire incomplete multipart uploads), Storage Lens for cold buckets, gp2→gp3 EBS migration (~20% cheaper), delete unattached volumes and ancient snapshots.',
        'Network: NAT Gateway per-GB charges — add S3/DynamoDB gateway endpoints (free) and check for chatty cross-AZ traffic between services; egress-heavy workloads may belong behind CloudFront (cheaper per GB than raw EC2 egress).',
        'Database: idle/oversized RDS instances, old manual snapshots, reserved instances for steady DBs, Aurora Serverless v2 for spiky dev workloads.',
        'Make it stick: AWS Budgets with alerts per team, anomaly detection on, cost review in sprint rituals, and billing alarms in every account.',
      ],
      keyTakeaways: ['Measure → rightsize → THEN commit (Savings Plans on waste locks in waste)', 'The sneaky line items: NAT per-GB, cross-AZ transfer, unattached EBS, log groups with infinite retention', 'Cost is an architecture property — bake review into the process or it regrows'],
    },
  ],

  mentalModels: [
    {
      id: 'mm-layered-request',
      title: 'The Layered Request Path',
      flow: ['Route53: which endpoint?', 'CloudFront: can the edge answer?', 'ALB: which healthy target?', 'App tier: business logic', 'Cache/queue: can I avoid or defer work?', 'RDS: source of truth'],
      explanation: [
        'Every AWS web architecture is the same six-layer funnel; each layer exists to answer one question and to absorb load before the next.',
        'Work gets cheaper the higher it stops: an edge cache hit costs microcents, a database query holds a connection and locks. Push every possible answer up the funnel.',
        'Each layer is also a control point: DNS for region steering, edge for WAF/TLS, ALB for routing/health, app for authz, DB for consistency.',
      ],
      whatBreaksWithoutIt: 'Without the funnel picture you answer design questions service-by-service instead of as a system, and you miss the standard fixes (cache higher, decouple with queues, protect the DB).',
      interviewTip: 'Whatever the design question, draw this spine first and hang specifics on it — interviewers recognize the shape immediately and you never freeze on a blank whiteboard.',
    },
    {
      id: 'mm-shared-responsibility',
      title: 'Shared Responsibility: OF the Cloud vs IN the Cloud',
      flow: ['AWS secures: physical DCs, hypervisor, managed-service internals', 'Line shifts per service model', 'You secure: IAM, network rules, OS patches (EC2), data, encryption config, application code'],
      explanation: [
        'AWS secures the cloud (facilities, hardware, the RDS engine\'s patching); you secure what you put IN it (who can access, how networks are segmented, whether the bucket is public).',
        'The line moves with abstraction: on EC2 you patch the OS; on Fargate AWS does; on Lambda you only own code, dependencies and IAM. More managed = less of the line on your side.',
        'Nearly every famous cloud breach (public buckets, leaked keys, over-broad roles) was on the CUSTOMER side of the line — config, not AWS infrastructure.',
      ],
      whatBreaksWithoutIt: 'You either over-trust ("AWS handles security") and leave buckets public, or under-trust and waste effort re-securing what AWS already guarantees.',
      interviewTip: 'Name the model and immediately give the EC2-vs-Lambda contrast of who patches what — it shows you understand the line moves rather than reciting a slogan.',
    },
    {
      id: 'mm-pets-cattle',
      title: 'Pets vs Cattle',
      flow: ['Pet: named server, hand-configured, nursed when sick', 'Cattle: numbered instance from an image, replaced when sick', 'ASG/ECS assume cattle', 'State exiled to S3/RDS/ElastiCache', 'Any instance can die at any time, nobody notices'],
      explanation: [
        'Cloud elasticity only works if instances are disposable: built from AMIs/containers, configured by automation, holding no unique state.',
        'Everything stateful gets externalized — sessions to Redis or signed tokens, uploads to S3, data to RDS. The instance becomes a pure compute unit.',
        'This is WHY auto scaling, rolling deploys, and Spot instances are possible: each assumes any node can be terminated and replaced without ceremony.',
      ],
      whatBreaksWithoutIt: 'Local sessions log users out on every scale-in; files saved to instance disk vanish; auto scaling becomes dangerous instead of routine; deploys require careful per-server surgery.',
      interviewTip: 'When asked "what changes when moving an app to AWS", lead with statelessness — it is the prerequisite every other cloud benefit hangs off.',
    },
    {
      id: 'mm-blast-radius',
      title: 'Blast Radius & Failure Domains',
      flow: ['Instance fails → ASG replaces it', 'AZ fails → multi-AZ ALB/RDS absorb it', 'Region fails → Route53 failover + replicated data', 'Account compromised → multi-account + SCPs contain it'],
      explanation: [
        'Design by asking "when X fails, what is the blast radius?" — then add redundancy one level above each failure domain you must survive.',
        'AZs are the workhorse: independent power/network within a region, low-latency links between — which is why "two AZs minimum" is reflexive for ALB targets, ASGs, and RDS.',
        'Cost scales with the domain you defend against: multi-AZ is cheap and default; multi-region is expensive and bought only when the business case (RTO/RPO) justifies it.',
      ],
      whatBreaksWithoutIt: 'Single-AZ deployments take total outages from routine AZ events; or conversely you gold-plate multi-region onto an internal tool that tolerates a day of downtime.',
      interviewTip: 'State the failure domain you are defending against and the cost of the next level up — "multi-AZ yes, multi-region only if the business needs sub-hour RTO" is a senior sentence.',
    },
    {
      id: 'mm-async-buffer',
      title: 'The Queue as a Shock Absorber',
      flow: ['Producer enqueues, returns fast', 'Queue buffers (durable, elastic depth)', 'Consumers drain at their own pace', 'Failures retry → DLQ catches poison', 'Queue depth = honest demand signal → autoscale workers'],
      explanation: [
        'A queue converts "caller waits for downstream" into "caller hands off and moves on" — coupling in time is broken, so a slow or dead consumer no longer fails the producer.',
        'The buffer absorbs spikes: 10x traffic becomes queue depth, not dropped requests; workers scale on depth and catch up.',
        'The contract costs something: at-least-once delivery forces idempotent consumers, and eventual processing means the caller cannot read the result synchronously.',
      ],
      whatBreaksWithoutIt: 'Synchronous chains fail as one unit (checkout dies because the email service is down) and spikes become 5xx storms instead of temporary backlog.',
      interviewTip: 'Whenever a design includes "and then we send email / update analytics / call a partner API", interrupt yourself with "this goes behind a queue" — it is the most reliable architecture-points move there is.',
    },
  ],

  revision: [
    { id: 'r-request-flow', topic: 'Request flow: Route53 → CloudFront → ALB → app → RDS', importance: 5, thirtySecond: 'DNS resolves via Route53; CloudFront serves cached content at the edge; misses hit the ALB in public subnets; healthy app targets in private subnets handle logic; RDS (private, multi-AZ) is the source of truth.', twoMinute: 'Route53 routing policies pick the endpoint. CloudFront terminates TLS, caches by path + policy-selected keys, carries WAF. The ALB applies path/host rules to target groups with health checks. App tier holds no state (sessions in Redis/tokens, files in S3). RDS multi-AZ for failover, replicas for reads. Security groups chain each hop: internet→ALB, ALB→app, app→DB.', deepDive: 'Be ready to add layers on demand: ElastiCache before RDS, SQS behind the app for async work, S3+presigned URLs for uploads, X-Ray for tracing the whole path. Know what each layer contributes (caching, routing, isolation, availability) and what fails when an AZ dies: ALB and ASG span AZs, RDS fails over — the system degrades, not dies.', whiteboard: ['Browser → Route53 → CloudFront → ALB → ECS/EC2 → RDS', 'public subnets: ALB+NAT | private: app+DB', 'SG chain: 443→ALB, app-port→app, 5432→DB'], followUps: ['Where does TLS terminate?', 'What happens when an AZ fails?'] },
    { id: 'r-iam', topic: 'IAM: roles, policies, evaluation', importance: 5, thirtySecond: 'Policies are JSON (Effect/Action/Resource/Condition). Evaluation: implicit deny → Allow grants → explicit Deny overrides all. Services use roles (temporary STS credentials), never access keys.', twoMinute: 'Users for humans (better: SSO), groups for attachment, roles for everything programmatic — EC2 instance profile, ECS task role, Lambda execution role deliver auto-rotated credentials. Resource policies (bucket/queue) grant from the resource side. Least privilege: narrow actions, ARN-scoped resources, conditions. Cross-account = trust policy + sts:AssumeRole.', deepDive: 'Permission boundaries cap a principal\'s maximum; SCPs cap whole accounts in Organizations. Access Analyzer finds external and unused access. Know the access-key horror story (committed to git → cryptomining) and the fix: roles, OIDC federation for CI, IMDSv2 enforced. Trust policy (who can assume) vs permission policy (what they can do) is a favorite distinction.', followUps: ['Trust policy vs permission policy?', 'How does CI deploy without stored AWS keys? (OIDC)'], whiteboard: ['deny (default) → Allow? → Deny overrides', 'role = temp creds via STS'] },
    { id: 'r-s3', topic: 'S3: classes, presigned URLs, access control', importance: 5, thirtySecond: 'Object storage, 11-nines durable. Classes: Standard → IA → Glacier tiers via lifecycle. Keep buckets private; presigned URLs for time-limited direct access; CloudFront OAC for serving.', twoMinute: 'Block Public Access stays on; bucket policies (not ACLs) for resource-side grants. Presigned URL = your backend authorizes, signs, hands the client a short-lived URL for one object — uploads bypass your servers. Versioning recovers deletes/overwrites and enables replication. Static hosting: private bucket + CloudFront OAC + 403→index.html for SPAs.', deepDive: 'Storage-class economics: IA/Glacier charge retrieval and minimum durations — wrong tiering costs more, Intelligent-Tiering automates the decision. Presigned POST adds size/type policy conditions PUT cannot. S3 events → Lambda/SQS drive processing pipelines. Strong read-after-write consistency since 2020 — stale knowledge here dates you.', followUps: ['Presigned PUT vs POST?', 'Why OAC over public website hosting?'], whiteboard: ['client → API (auth) → presigned URL → S3 direct'] },
    { id: 'r-compute', topic: 'EC2 vs ECS/Fargate vs Lambda', importance: 5, thirtySecond: 'Control/ops spectrum: EC2 = VMs you patch; Fargate = containers, AWS owns hosts; Lambda = functions, scale-to-zero, 15-min cap, cold starts. Steady → EC2/ECS, services → Fargate, event-driven → Lambda.', twoMinute: 'EC2 wins for special hardware and cheap steady compute (Savings Plans). Fargate is the product-service default: container contract, no host patching, per-task pricing. Lambda wins for spiky/glue: pay per invocation, but mind cold starts (provisioned concurrency mitigates), package limits, and the cost inversion at sustained high throughput where always-on becomes cheaper.', deepDive: 'ECS pieces: task definition (containers, CPU/RAM, roles), service (desired count + ALB), cluster. Task role (app permissions) vs execution role (pull image, fetch secrets, write logs). Lambda concurrency model: one request per environment instance; throttles at account concurrency limits — know reserved vs provisioned concurrency.', followUps: ['Task role vs execution role?', 'When does Lambda become more expensive than Fargate?'] },
    { id: 'r-vpc', topic: 'VPC: subnets, SG vs NACL, NAT', importance: 5, thirtySecond: 'Public subnet = route to IGW; private = outbound via NAT only. SGs: stateful, instance-level, allow-only, reference other SGs. NACLs: stateless, subnet-level, rare. DBs and app servers go private.', twoMinute: 'Standard layout: per-AZ public (ALB, NAT) + private (app, data) subnets across ≥2 AZs. SG chaining by reference is the idiom: DB allows 5432 from app SG. NAT Gateway = outbound-only for private tiers, hourly + per-GB cost — gateway endpoints for S3/DynamoDB dodge it. No SSH: SSM Session Manager.', deepDive: 'Statefulness is the exam point: SGs auto-allow return traffic; NACLs need explicit ephemeral-port rules both directions. SGs cannot deny — blocking an IP needs NACL or WAF. Interface endpoints (PrivateLink) keep service traffic private. Know why a "private" DB with a public IP and open SG is the classic misconfiguration.', followUps: ['Why can\'t an SG block an IP?', 'How do private instances reach the internet?'], whiteboard: ['IGW → public (ALB, NAT) → private (app) → private (DB)'] },
    { id: 'r-rds', topic: 'RDS: multi-AZ, replicas, backups', importance: 5, thirtySecond: 'Multi-AZ = synchronous standby, auto-failover, availability — serves no reads. Read replicas = async, own endpoints, read scaling with lag. PITR backups up to 35 days; restores create new instances.', twoMinute: 'Failover flips DNS in ~1–2 min — apps need reconnect logic (or RDS Proxy). Replicas can be cross-region and promoted (DR), but async means possible tail-write loss. Never serve read-your-own-writes from a replica. Aurora: shared 6-way storage, ms-lag replicas, faster failover — the upgrade narrative.', deepDive: 'Backups = daily snapshot + transaction logs → point-in-time recovery; manual snapshots persist. Connection exhaustion under serverless/high concurrency → RDS Proxy pools. Encryption at rest via KMS must be set at creation (or snapshot-copy-restore to add). Performance Insights for query-level diagnosis.', followUps: ['What does the app experience during failover?', 'Why might a replica read return stale data?'] },
    { id: 'r-cloudfront', topic: 'CloudFront & caching strategy', importance: 4, thirtySecond: 'Edge cache at 400+ locations. Cache key = path + whatever the policy includes; origin Cache-Control drives TTLs. Content-hashed filenames + long max-age beat invalidations. OAC keeps S3 origins private.', twoMinute: 'Over-broad cache keys (all cookies/queries) destroy hit ratio; too narrow serves wrong variants. Dynamic traffic still gains: edge TLS, backbone transit, connection reuse. WAF, signed URLs/cookies, and edge functions attach here. Invalidate index.html on deploy; hashed assets never need it.', deepDive: 'Hit-ratio diagnosis: cache statistics, x-cache headers, policy audit. Error caching TTLs prevent hammering a down origin but can pin failures — tune low. Lambda@Edge vs CloudFront Functions: weight, runtime, price. Multi-behavior distributions route /api/* to ALB and /* to S3 — one domain, no CORS.', followUps: ['Why did the hit ratio crater after adding a cookie to the cache key?', 'SPA + API on one distribution — how?'] },
    { id: 'r-lb-scaling', topic: 'ALB vs NLB & auto scaling', importance: 4, thirtySecond: 'ALB = L7 (path/host routing, WAF, WebSockets) — the web default. NLB = L4 (TCP/UDP, static IPs, extreme scale). ASGs/ECS scale on target tracking; pick a metric that reflects real demand, not just CPU.', twoMinute: 'Health checks gate traffic: unhealthy targets are pulled, ASG replaces them. Target tracking (requests-per-target, queue depth per worker) is the sane default; scheduled scaling for known peaks because reactive scaling lags by minutes. Deregistration delay/connection draining prevents killing in-flight requests on scale-in.', deepDive: 'CPU misleads I/O-bound services — latency or RPS per target track saturation better. Lifecycle hooks run cleanup before termination. NLB preserves source IP and supports PrivateLink — the reasons to choose it beyond raw throughput. Cross-zone load balancing semantics differ between ALB (on) and NLB (off by default, costs to enable).', followUps: ['Scale a worker fleet — on what metric?', 'What stops scale-in from dropping requests?'] },
    { id: 'r-messaging', topic: 'SQS/SNS: decoupling & DLQs', importance: 4, thirtySecond: 'SQS = pull-based work queue (at-least-once, idempotent consumers required); SNS = push fan-out to many subscribers. DLQ catches messages failing N times — alarm on it. Queues absorb spikes and outages.', twoMinute: 'Visibility timeout hides in-flight messages; crash → redelivery, so handlers must be idempotent (conditional-write dedup keys). FIFO gives ordering + exactly-once processing at lower throughput. SNS→multiple SQS = fan-out, each consumer gets a durable copy. Queue depth is the honest autoscaling signal for workers.', deepDive: 'Exactly-once EFFECT is a consumer-side achievement (idempotency), not a delivery guarantee — the distinction is the senior talking point. Long polling cuts cost/latency. DLQ workflow: alarm → inspect → fix → redrive. EventBridge adds content-based routing and schema registry when topics outgrow SNS.', followUps: ['Why must consumers be idempotent?', 'Design order-processing with retries and poison handling'] },
    { id: 'r-observability', topic: 'CloudWatch: metrics, logs, alarms', importance: 4, thirtySecond: 'Metrics say THAT something broke, logs say WHY, traces (X-Ray) say WHERE. Alarm on p99 latency, 5xx, queue age, DLQ depth, Lambda errors — and estimated charges in every account.', twoMinute: 'EC2 memory/disk need the agent; ECS/Lambda log automatically. Logs Insights queries log groups ad hoc; set retention or pay forever. Alarms drive SNS notifications and auto scaling. Composite alarms cut noise. Dashboards per service with the user-facing signals first.', deepDive: 'Averages hide tail pain — percentiles or bust. Embedded Metric Format emits custom metrics through logs cheaply. Alarm design is an art: evaluation periods and datapoints-to-alarm trade detection speed vs flapping. X-Ray/OpenTelemetry trace IDs across ALB→service→queue→DB answer "which hop is slow" definitively.', followUps: ['Why p99 not average?', 'How do you find which service in a chain is slow?'] },
    { id: 'r-secrets', topic: 'Secrets Manager vs Parameter Store', importance: 3, thirtySecond: 'Both store encrypted values behind IAM. Secrets Manager: automatic rotation, RDS integration, per-secret cost. Parameter Store: free tier, config + SecureString, no native rotation. Never secrets in code, images or plain env vars.', twoMinute: 'ECS injects secrets at task start from either store via the execution role; Lambda reads at init and caches. Rotation without downtime uses alternating users or versioned secrets with re-fetch-on-auth-failure. Scope GetSecretValue to specific ARNs. KMS encrypts underneath both — key policies are a second authorization layer.', deepDive: 'The decision is rotation and cross-account needs vs price: heavy secret counts on Parameter Store, rotating DB credentials on Secrets Manager. Audit access via CloudTrail. The anti-patterns to name: .env in git, secrets in task-definition plaintext, frontend bundles containing API keys (anything shipped to a browser is public).', followUps: ['How does zero-downtime rotation work?', 'Why is caching the secret in the app important?'] },
    { id: 'r-cost', topic: 'Cost awareness & optimization', importance: 4, thirtySecond: 'Levers: pricing models (Savings Plans/Reserved for steady, Spot for interruptible), rightsizing, S3 lifecycle, gp3, and the sneaky costs — NAT per-GB, cross-AZ transfer, egress, infinite log retention. Visibility via tags + Cost Explorer first.', twoMinute: 'Order matters: measure (Cost Explorer, tags) → rightsize (Compute Optimizer) → commit (Savings Plans ~40-65%) → Spot for batch (~70-90%). Storage: lifecycle to IA/Glacier, delete unattached EBS and stale snapshots. Network: gateway endpoints kill NAT charges for S3/DynamoDB; CloudFront egress is cheaper than EC2 egress. Schedule dev environments off overnight.', deepDive: 'Spot needs interruption tolerance (2-min warning, checkpointing, capacity-optimized allocation). Savings Plans (flexible, compute-wide) vs RIs (instance-family-specific, marketplace-resalable). Budgets + anomaly detection make cost regression visible at PR-time-equivalent speed. Architecture choices ARE cost choices: chatty cross-AZ microservices and per-request Lambda at high steady volume are design-level bills.', followUps: ['Savings Plans vs Reserved Instances?', 'Name three costs that surprise teams'] },
  ],
}

export default aws
