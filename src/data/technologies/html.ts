import type { Technology } from '../../types/content'

const html: Technology = {
  id: 'html',
  name: 'HTML',
  icon: 'H5',
  color: '#e34f26',
  tagline: 'The document language of the web — structure, semantics, and the browser\'s entry point.',
  category: 'frontend',

  overview: {
    what: 'HTML (HyperText Markup Language) is the declarative language that defines the structure and meaning of web content. The browser parses it into the DOM — the live object tree that JavaScript manipulates, CSS styles, and assistive technologies read.',
    why: 'Created by Tim Berners-Lee in 1991 to link scientific documents. HTML5 (2014, now a living standard) turned it from a document format into an application platform: semantic elements, native form validation, media, storage, workers, and web components.',
    problemsSolved: [
      'Universal, accessible content structure that any browser, crawler, or screen reader can interpret',
      'Declarative UI primitives (forms, media, dialogs, details/summary) that work without JavaScript',
      'A stable contract between content, styling (CSS), behavior (JS), and machines (SEO, accessibility tree)',
    ],
    whenToUse: [
      'Every web page and web app — HTML is the non-negotiable foundation; the question is how well you use it',
      'Prefer native elements (button, dialog, details, input types) before reaching for JS-rebuilt widgets',
      'Server-rendered or static content where SEO, accessibility, and first paint matter',
    ],
    whenNotToUse: [
      'Do not abuse HTML for layout (tables-for-layout era) — structure in HTML, presentation in CSS',
      'Do not rebuild native controls (div-as-button) — you lose keyboard, focus, and accessibility for free',
      'Canvas/WebGL scenes — pixel-level rendering is not a document; but still wrap it in semantic HTML',
    ],
  },

  internals: [
    {
      id: 'parsing-dom',
      title: 'HTML Parsing & DOM Construction',
      summary: 'The browser parses HTML as a forgiving, streaming state machine: bytes → characters → tokens → nodes → DOM tree. Parsing is incremental and error-tolerant by spec.',
      details: [
        'Bytes arrive over the network and are decoded to characters using the declared encoding (meta charset must appear in the first 1024 bytes, or the browser may re-parse).',
        'The tokenizer emits start tags, end tags, text, and comments; the tree builder assembles them into DOM nodes using insertion modes defined by the spec.',
        'HTML parsing is error-tolerant by specification — unclosed tags, mis-nesting, and missing elements are repaired deterministically (e.g., the parser auto-creates tbody inside table). Every browser repairs identically; this is why "invalid" HTML still renders.',
        'Parsing is streaming: the browser starts building the DOM and rendering before the full document arrives — a key reason server-streamed HTML feels fast.',
        'A classic (non-async) script tag PAUSES the parser: the script may document.write or read the DOM, so the parser must stop, fetch, and execute it before continuing.',
        'The preload scanner is a second lightweight parser that races ahead during such blocks, discovering img/script/link URLs and starting their downloads early.',
      ],
      diagram: ['Bytes (network)', 'Characters (decoding)', 'Tokens (tokenizer)', 'Nodes (tree builder)', 'DOM Tree', 'script without defer/async → parser PAUSES', 'Preload scanner keeps fetching ahead'],
      code: {
        lang: 'html',
        code: '<!-- Parser repair in action: you write this... -->\n<table>\n  <tr><td>cell</td></tr>\n</table>\n\n<!-- ...the DOM actually contains this: -->\n<table>\n  <tbody>\n    <tr><td>cell</td></tr>\n  </tbody>\n</table>',
        caption: 'The spec defines exact repair rules — querySelector(\'table > tr\') fails because tbody was inserted.',
      },
    },
    {
      id: 'critical-rendering-path',
      title: 'Critical Rendering Path',
      summary: 'The sequence from HTML bytes to pixels: DOM + CSSOM → render tree → layout → paint → composite. Render-blocking resources on this path delay first paint.',
      details: [
        'DOM construction (from HTML) and CSSOM construction (from CSS) happen in parallel, but RENDERING needs both — the browser will not paint with incomplete CSSOM (to avoid a flash of unstyled content).',
        'CSS is render-blocking: a stylesheet in head blocks first paint until downloaded and parsed. JS is parser-blocking: a sync script halts DOM construction — and worse, it also waits for any pending CSSOM (because scripts may read computed styles).',
        'Render tree = DOM × CSSOM, containing only visible nodes (display:none is excluded). Layout computes geometry; paint rasterizes; compositing assembles layers on the GPU.',
        'Optimizations map directly to this model: inline critical CSS, defer non-critical CSS (media="print" trick or preload), use defer/async on scripts, preload key resources, minimize DOM depth.',
        'Core Web Vitals connect here: LCP suffers from render-blocking resources and slow hero images; CLS from layout shifts caused by media without dimensions; INP from long main-thread tasks.',
        'Always set width/height (or aspect-ratio) on images — the browser reserves space during layout, preventing CLS when the image loads.',
      ],
      diagram: ['HTML → DOM', 'CSS → CSSOM (render-blocking)', 'JS (parser-blocking, waits on CSSOM)', 'DOM + CSSOM → Render Tree', 'Layout (geometry)', 'Paint (pixels)', 'Composite (GPU layers)', 'First Paint'],
    },
    {
      id: 'script-loading',
      title: 'Script Loading: async, defer, module',
      summary: 'Three loading strategies decide whether a script blocks parsing, when it executes, and whether order is preserved. This is one of the most asked HTML internals questions.',
      details: [
        'Plain <script src>: parser stops, script downloads AND executes, then parsing resumes — the worst case for page speed. Historic reason scripts went at the bottom of body.',
        'defer: downloads in parallel with parsing; executes after parsing completes, BEFORE DOMContentLoaded, in document order. Safe default for scripts that touch the DOM.',
        'async: downloads in parallel; executes the moment it arrives, pausing the parser, with NO order guarantee. Right for independent scripts (analytics) — wrong for interdependent app code.',
        'type="module": deferred by default (plus dependencies fetched), runs in strict mode, scoped, executes once even if included twice. async on a module makes it execute as soon as the graph is ready.',
        'Inline scripts cannot be deferred with the defer attribute (it is ignored) — but inline type="module" IS deferred.',
        'DOMContentLoaded fires when parsing is done and deferred scripts have run; load fires when all resources (images, stylesheets) are in.',
      ],
      diagram: ['Parser running', 'plain script → STOP, fetch, execute, resume', 'defer → fetch in parallel, run after parse (in order)', 'async → fetch in parallel, run on arrival (any order)', 'DOMContentLoaded after deferred scripts', 'load after all resources'],
      code: {
        lang: 'html',
        code: '<script src="app.js" defer></script>      <!-- DOM-dependent app code -->\n<script src="analytics.js" async></script> <!-- independent, order-free -->\n<script type="module" src="main.js"></script> <!-- deferred by default -->',
        caption: 'defer = ordered + after parse; async = unordered + on arrival; module = defer semantics + ESM.',
      },
    },
    {
      id: 'forms-validation',
      title: 'Forms & the Constraint Validation API',
      summary: 'Native forms give you submission, serialization, keyboard handling, and validation for free. The Constraint Validation API bridges declarative rules and custom JS logic.',
      details: [
        'Declarative constraints: required, pattern, min/max, minlength/maxlength, step, and type itself (email, url, number) — the browser validates on submit and blocks invalid submission.',
        'Each control exposes .validity (a ValidityState with flags like valueMissing, typeMismatch, patternMismatch), .checkValidity(), .reportValidity(), and .setCustomValidity(msg) for custom rules.',
        'CSS hooks: :valid, :invalid, :user-invalid (only after interaction — fixes the "form screams red on load" problem), :required, :placeholder-shown.',
        'novalidate on the form (or formnovalidate on a submit button) disables native UI when you want full custom presentation, while still letting you use the API programmatically.',
        'FormData(formEl) serializes named controls in one line — name attributes are the contract; controls without name are not submitted.',
        'Correct input types (email, tel, number, date) drive mobile keyboards and built-in parsing; autocomplete attributes (e.g. autocomplete="email", "one-time-code") power browser autofill.',
      ],
      code: {
        lang: 'js',
        code: 'const form = document.querySelector(\'form\')\nconst pwd = form.elements.namedItem(\'password\')\nconst confirm = form.elements.namedItem(\'confirm\')\n\nconfirm.addEventListener(\'input\', () => {\n  confirm.setCustomValidity(\n    confirm.value !== pwd.value ? \'Passwords do not match\' : \'\'\n  )\n})\n\nform.addEventListener(\'submit\', (e) => {\n  if (!form.checkValidity()) {\n    e.preventDefault()\n    form.reportValidity() // shows native bubbles\n  }\n})',
        caption: 'setCustomValidity(\'\') clears the error — forgetting the empty-string reset is the classic bug.',
      },
    },
    {
      id: 'storage-apis',
      title: 'Browser Storage APIs',
      summary: 'Cookies, localStorage, sessionStorage, and IndexedDB differ in size, lifetime, scope, sync/async behavior, and whether they ride on HTTP requests — choosing wrongly is a security and performance bug.',
      details: [
        'Cookies (~4KB): sent with EVERY matching HTTP request — that is their purpose (server-side sessions) and their cost (request bloat). Flags matter: HttpOnly (JS cannot read — XSS protection), Secure (HTTPS only), SameSite=Lax/Strict (CSRF defense).',
        'localStorage (~5MB): synchronous, string-only key/value, persists until cleared, shared across all tabs of the same origin. Sync API means large reads/writes block the main thread.',
        'sessionStorage: same API, but scoped to a single tab and cleared when the tab closes. Duplicating a tab copies the data; new tabs start empty.',
        'IndexedDB: asynchronous, transactional, object-store database for structured data and large blobs — the right home for offline data, caches, and anything beyond a few KB.',
        'All web storage is origin-scoped (scheme + host + port). The storage event fires in OTHER tabs when localStorage changes — a cheap cross-tab sync channel.',
        'Auth guidance interviewers probe: tokens in localStorage are readable by any XSS payload; HttpOnly cookies are not. Defense in depth: HttpOnly + Secure + SameSite + short-lived tokens.',
      ],
      diagram: ['Need server to see it on every request? → Cookie (HttpOnly/Secure/SameSite)', 'Small client-only data, persistent? → localStorage', 'Per-tab, throwaway? → sessionStorage', 'Structured / large / offline? → IndexedDB'],
    },
    {
      id: 'web-components',
      title: 'Web Components: Custom Elements, Shadow DOM, Templates',
      summary: 'Three native specs compose into framework-free, encapsulated components: custom elements (lifecycle), shadow DOM (style/DOM isolation), and template/slot (inert markup + projection).',
      details: [
        'Custom elements: class extending HTMLElement registered via customElements.define(\'my-tag\', Cls). Name must contain a hyphen. Lifecycle callbacks: connectedCallback (inserted), disconnectedCallback (removed), attributeChangedCallback (observed attributes changed).',
        'Shadow DOM: el.attachShadow({ mode: \'open\' }) creates an isolated subtree. Outside CSS does not style it; its CSS does not leak out. Inherited properties (color, font) still flow in; CSS custom properties pierce the boundary deliberately — that is the theming API.',
        'template element: parsed but inert markup (no image fetches, no script run) cloned with content.cloneNode(true). slot elements project light-DOM children into the shadow tree.',
        'Events from inside shadow DOM are retargeted: event.target appears as the host element to outside listeners; composed: true lets custom events cross the boundary.',
        'Form-associated custom elements (ElementInternals) let components participate in native forms and constraint validation.',
        'Trade-offs vs frameworks: no rendering/reactivity model included, SSR is harder (declarative shadow DOM helps), but they are framework-agnostic and ideal for design systems shared across stacks.',
      ],
      code: {
        lang: 'js',
        code: 'class UserBadge extends HTMLElement {\n  static observedAttributes = [\'name\']\n  constructor() {\n    super()\n    this.attachShadow({ mode: \'open\' }).innerHTML =\n      \'<style>span{color:teal}</style><span></span>\'\n  }\n  attributeChangedCallback(attr, oldV, newV) {\n    this.shadowRoot.querySelector(\'span\').textContent = newV\n  }\n}\ncustomElements.define(\'user-badge\', UserBadge)\n// <user-badge name="Prasanth"></user-badge>',
        caption: 'Hyphenated tag name and observedAttributes are the two details interviewers check.',
      },
    },
    {
      id: 'iframes-browsing-contexts',
      title: 'Iframes & Browsing Contexts',
      summary: 'An iframe is a nested browsing context with its own document, origin, and event loop relationship. The sandbox attribute and postMessage define its security contract.',
      details: [
        'Each iframe loads a full document with its own DOM, CSSOM, and JS global. Same-origin frames can touch each other\'s DOM directly; cross-origin frames cannot — the same-origin policy walls them off.',
        'sandbox applies a deny-all policy you selectively relax: allow-scripts, allow-forms, allow-popups, allow-same-origin. Never combine allow-scripts with allow-same-origin for untrusted content — the frame could remove its own sandbox.',
        'Cross-origin communication is postMessage only: sender calls frame.contentWindow.postMessage(data, targetOrigin); receiver MUST validate event.origin before trusting the data.',
        'Clickjacking defense: the embedded site controls embedding via CSP frame-ancestors (modern) or X-Frame-Options: DENY/SAMEORIGIN (legacy) — the embedder cannot grant itself permission.',
        'Performance cost: each iframe is a separate document load and (often) a separate process; lazy-load below-the-fold iframes with loading="lazy".',
        'Legit use cases: third-party widgets (payments, maps, video embeds), ad isolation, and origin-isolating untrusted content — Stripe uses iframes so card data never touches your origin.',
      ],
    },
  ],

  keywords: [
    {
      id: 'semantic-html',
      term: 'Semantic HTML',
      importance: 5,
      whyAsked: 'It is the fastest filter between div-soup developers and engineers who understand that markup is an API consumed by browsers, screen readers, and crawlers.',
      explanation: 'Semantic elements (header, nav, main, article, section, aside, footer, button, time) describe MEANING, not appearance. Browsers map them to accessibility-tree roles and keyboard behavior; search engines use them for content extraction. A button gives you focus, Enter/Space activation, and role="button" for free — a div gives you nothing.',
      realWorldExample: 'Replacing a clickable div with button instantly fixed keyboard navigation and screen-reader announcement in an audit — zero JS changes needed.',
      commonMistakes: [
        'Saying semantics is "for readability" — the primary consumers are machines: the accessibility tree and crawlers',
        'Using section as a styling wrapper — section needs a heading and a thematic purpose; otherwise use div',
        'div onclick instead of button — losing focusability, keyboard activation, and the implicit role',
      ],
      followUps: ['Difference between section, article, and div?', 'What does the browser give you for free with button vs div?', 'How does semantic HTML affect SEO concretely?'],
    },
    {
      id: 'aria',
      term: 'ARIA & the Accessibility Tree',
      importance: 5,
      whyAsked: 'Accessibility is a legal requirement at most product companies; interviewers test whether you know the first rule of ARIA — prefer native elements — and how the accessibility tree works.',
      explanation: 'The browser derives an accessibility tree from the DOM (roles, names, states) and exposes it to screen readers. ARIA attributes patch that tree when native semantics fall short: role defines what a thing is, aria-label/aria-labelledby name it, aria-expanded/aria-hidden/aria-live describe state. ARIA changes ONLY the tree — it adds zero behavior; you still wire keyboard handling yourself.',
      realWorldExample: 'aria-live="polite" on a search-results count region makes a screen reader announce "42 results" after filtering — without it, the update is silent.',
      commonMistakes: [
        'Believing role="button" makes a div behave like a button — it only announces as one; tabindex and key handlers are still on you',
        'Slapping ARIA on everything — the first rule of ARIA is do not use ARIA when a native element exists',
        'Using aria-hidden="true" on focusable content — creates invisible-but-tabbable ghost elements',
      ],
      followUps: ['What is the first rule of ARIA?', 'aria-label vs aria-labelledby vs aria-describedby?', 'How do you make a custom dropdown accessible end to end?'],
      diagram: ['DOM', 'Browser computes role/name/state per node', 'Accessibility Tree', 'Platform API (e.g. UIA/AX)', 'Screen reader announces'],
    },
    {
      id: 'async-defer',
      term: 'async vs defer',
      importance: 5,
      whyAsked: 'A two-attribute question that exposes whether you understand parser blocking and the critical rendering path — asked at nearly every frontend interview.',
      explanation: 'Both download in parallel with parsing. defer executes after parsing finishes, in document order, before DOMContentLoaded — safe for DOM-dependent, interdependent code. async executes immediately on arrival (pausing the parser), in network order — only for independent scripts. A plain script blocks parsing for download + execution.',
      realWorldExample: 'App bundles get defer; a third-party analytics snippet gets async because nothing depends on it and it depends on nothing.',
      commonMistakes: [
        'Saying async "does not block" — it does not block during DOWNLOAD, but execution still pauses the parser',
        'Using async on order-dependent scripts (library + plugin) — intermittent breakage by network race',
        'Not knowing type="module" scripts are deferred by default',
      ],
      followUps: ['When does DOMContentLoaded fire relative to defer?', 'What happens with defer on an inline script? (ignored)', 'Why did old guides put scripts at the end of body?'],
    },
    {
      id: 'critical-rendering-path-kw',
      term: 'Critical Rendering Path',
      importance: 4,
      whyAsked: 'Performance questions at product companies start here — it tests whether your optimization advice derives from a model or from cargo-culted tips.',
      explanation: 'The pipeline from HTML to pixels: DOM + CSSOM → render tree → layout → paint → composite. CSS blocks rendering; sync JS blocks parsing (and waits on CSSOM). Optimizing means shortening this path: inline critical CSS, defer JS, preload key assets, set image dimensions.',
      realWorldExample: 'Moving a 200KB blocking stylesheet to preload + critical-CSS inline cut First Contentful Paint by over a second on 3G profiles.',
      commonMistakes: ['Not knowing WHY sync JS waits for CSSOM (scripts may read computed styles)', 'Treating "minify everything" as the answer instead of removing render-blocking resources from the path'],
      followUps: ['Why is CSS render-blocking but images are not?', 'How do preload/preconnect fit in?', 'Which Core Web Vital does each blocking resource hurt?'],
    },
    {
      id: 'meta-tags-seo',
      term: 'Meta Tags & SEO',
      importance: 4,
      whyAsked: 'Product companies care about organic acquisition; the question checks if you know which tags machines actually read and which are folklore.',
      explanation: 'The tags that matter: title (the ranking + click-through workhorse), meta description (snippet text, not a ranking factor), meta viewport (mobile rendering — required), canonical link (duplicate-content consolidation), robots (index/noindex), Open Graph/Twitter cards (social previews), and JSON-LD structured data (rich results). meta keywords has been dead since 2009.',
      realWorldExample: 'Adding rel="canonical" across paginated/filtered product URLs consolidated duplicate-content signals and recovered category-page rankings.',
      commonMistakes: ['Mentioning meta keywords as relevant', 'Forgetting viewport meta, then wondering why mobile renders at 980px desktop width', 'Not knowing structured data (JSON-LD) is what powers rich snippets'],
      followUps: ['How do crawlers handle client-side rendered SPAs?', 'What is a canonical URL and when do you need it?', 'Open Graph vs JSON-LD — who reads each?'],
    },
    {
      id: 'storage-comparison',
      term: 'localStorage vs sessionStorage vs Cookies',
      importance: 4,
      whyAsked: 'A daily practical decision with security consequences — interviewers steer it toward "where do you store a JWT?"',
      explanation: 'Cookies (~4KB) ride every request and support HttpOnly/Secure/SameSite — the only XSS-resistant client storage. localStorage (~5MB) persists across tabs/sessions, synchronous, JS-readable. sessionStorage is per-tab, cleared on close. IndexedDB for large structured data, async.',
      realWorldExample: 'Auth token in an HttpOnly SameSite cookie; UI preferences (theme, collapsed sidebar) in localStorage; multi-step wizard draft in sessionStorage.',
      commonMistakes: ['Recommending localStorage for tokens without acknowledging XSS exposure', 'Forgetting cookies are sent with every request — payload tax on every API call', 'Not knowing the storage event enables cross-tab sync'],
      followUps: ['Where do you store a JWT and why?', 'What does SameSite actually prevent?', 'How would you sync logout across tabs?'],
    },
    {
      id: 'doctype-quirks',
      term: 'DOCTYPE & Quirks Mode',
      importance: 3,
      whyAsked: 'A quick history-meets-internals check: do you know what that first line actually switches?',
      explanation: '<!DOCTYPE html> tells the browser to render in standards mode. Without it, browsers emulate 1990s bugs (quirks mode): the old IE box model (width includes padding/border), different inline-image baselines, and case-insensitive weirdness. It is a rendering-mode switch, not version metadata.',
      commonMistakes: ['Calling it "the HTML version declaration" and stopping there — the interviewer wants quirks mode', 'Not being able to name one concrete quirks-mode difference (box model is the safe answer)'],
      followUps: ['Name a concrete difference in quirks mode', 'How do you detect the mode at runtime? (document.compatMode)'],
    },
    {
      id: 'resource-hints',
      term: 'Resource Hints (preload, prefetch, preconnect)',
      importance: 4,
      whyAsked: 'Distinguishes engineers who can tune real loading waterfalls from those who only know "make it smaller".',
      explanation: 'preload: fetch a critical resource for THIS page at high priority (hero image, font — fonts need crossorigin). prefetch: low-priority fetch for a LIKELY NEXT page, used from cache later. preconnect: perform DNS + TCP + TLS handshake early for a known third-party origin. dns-prefetch is the cheaper DNS-only fallback.',
      realWorldExample: 'preload on the LCP hero image plus preconnect to the image CDN moved LCP from 4.1s to 2.3s — no asset got smaller, the waterfall got flatter.',
      commonMistakes: ['Confusing preload (this page, high priority) with prefetch (next page, idle priority)', 'Preloading fonts without crossorigin — the preload is wasted and the font downloads twice', 'Preloading everything — unprioritized preloads compete with genuinely critical resources'],
      followUps: ['Why does a font preload need crossorigin even for same-origin fonts?', 'When is preconnect better than preload?'],
    },
    {
      id: 'shadow-dom',
      term: 'Shadow DOM',
      importance: 3,
      whyAsked: 'Web components appear in design-system and micro-frontend discussions; shadow DOM tests your grasp of encapsulation boundaries in the platform itself.',
      explanation: 'A scoped subtree attached to a host element. Outside selectors cannot reach in; inside styles cannot leak out. Inherited properties (font, color) and CSS custom properties DO cross the boundary — custom properties are the intended theming API. Events are retargeted so outside code sees the host as event.target.',
      realWorldExample: 'A design-system date picker shipped as a web component renders identically inside React, Vue, and a legacy jQuery admin — its styles cannot be clobbered by any host page CSS.',
      commonMistakes: ['Claiming "no styles cross the boundary" — inheritance and custom properties do', 'Confusing shadow DOM (encapsulation) with virtual DOM (a framework diffing strategy) — they are unrelated'],
      followUps: ['How do you theme a shadow-DOM component from outside?', 'open vs closed shadow roots?', 'What is event retargeting?'],
    },
    {
      id: 'iframe-sandbox',
      term: 'iframe sandbox & postMessage',
      importance: 3,
      whyAsked: 'Third-party embeds (payments, ads, widgets) are everywhere; this checks whether you can integrate untrusted content safely.',
      explanation: 'sandbox turns an iframe into a deny-everything context; each allow-* token restores one capability. Cross-origin frames can only talk via postMessage — the receiver must check event.origin, and the sender should pass an explicit targetOrigin rather than "*".',
      commonMistakes: ['allow-scripts + allow-same-origin on untrusted content — the frame can script itself out of the sandbox', 'postMessage listeners that skip the event.origin check — an open door for any site to inject messages'],
      followUps: ['How does Stripe keep card data off your origin?', 'frame-ancestors vs X-Frame-Options?'],
    },
    {
      id: 'lazy-loading',
      term: 'Lazy Loading (loading="lazy", srcset)',
      importance: 4,
      whyAsked: 'Images dominate page weight; native lazy loading and responsive images are the highest-ROI HTML-level performance tools.',
      explanation: 'loading="lazy" on img/iframe defers fetching until near the viewport — one attribute, no JS. srcset + sizes let the browser pick the right resolution per device; picture handles art direction and format fallback (AVIF/WebP → JPEG). Pair with width/height to avoid CLS.',
      realWorldExample: 'Adding loading="lazy" to below-the-fold product images cut initial page weight by 60% on a listing page with no code changes.',
      commonMistakes: ['Lazy-loading the LCP hero image — it delays your biggest paint; eager-load (or preload) above-the-fold media', 'Using srcset without sizes for non-full-width images — the browser assumes 100vw and over-downloads'],
      followUps: ['Why should the LCP image never be lazy?', 'srcset w-descriptors vs x-descriptors?', 'When do you still need IntersectionObserver?'],
    },
    {
      id: 'data-attributes',
      term: 'data-* Attributes',
      importance: 3,
      whyAsked: 'A small topic that reveals practical DOM habits — state in markup, event delegation hooks, JS–CSS handshakes.',
      explanation: 'data-* attributes store custom data on elements, exposed as el.dataset.camelCase (data-user-id → dataset.userId). They are the standard hook for event delegation (read the id off the clicked row), test selectors (data-testid), and CSS state styling ([data-state="open"]).',
      commonMistakes: ['Stuffing large JSON blobs into data attributes — they live in the DOM as strings; keep real state in JS', 'Using non-standard made-up attributes instead — fails validation and risks future HTML collisions'],
      followUps: ['How does dataset name conversion work?', 'data-* vs aria-* — when each?'],
    },
  ],

  cheatSheets: [
    {
      id: 'script-loading-cheat',
      title: 'Script loading strategies',
      rows: [
        { concept: '<script src>', description: 'Blocks parser for download + execution. Avoid.', code: '<script src="a.js"></script>' },
        { concept: 'defer', description: 'Parallel download; runs after parse, in order, before DOMContentLoaded. Default choice.', code: '<script src="a.js" defer></script>' },
        { concept: 'async', description: 'Parallel download; runs on arrival, any order. Independent scripts only.', code: '<script src="ga.js" async></script>' },
        { concept: 'type="module"', description: 'Defer semantics by default; strict mode; deduped; own scope.', code: '<script type="module" src="m.js"></script>' },
        { concept: 'preload script', description: 'Fetch early, execute later when you insert it.', code: '<link rel="preload" href="a.js" as="script">' },
        { concept: 'DOMContentLoaded vs load', description: 'DCL: DOM parsed + deferred scripts ran. load: all images/styles done.' },
      ],
    },
    {
      id: 'semantic-elements-cheat',
      title: 'Semantic elements quick reference',
      rows: [
        { concept: 'main', description: 'The unique primary content — exactly one visible per page; screen-reader landmark.' },
        { concept: 'article', description: 'Self-contained, independently distributable unit (card, post, comment) — makes sense in an RSS feed.' },
        { concept: 'section', description: 'Thematic grouping that deserves a heading. No heading? Probably a div.' },
        { concept: 'nav / aside', description: 'Major navigation block / tangential content (sidebar, pull-quote). Both are landmarks.' },
        { concept: 'header / footer', description: 'Intro / closing content of page OR of any article/section — can appear multiple times.' },
        { concept: 'button vs a', description: 'button = performs an action; a href = navigates somewhere. Never a div for either.' },
        { concept: 'figure + figcaption', description: 'Media with a caption programmatically tied to it.' },
        { concept: 'time datetime', description: 'Machine-readable dates for crawlers.', code: '<time datetime="2026-06-11">today</time>' },
      ],
    },
    {
      id: 'storage-cheat',
      title: 'Storage APIs comparison',
      rows: [
        { concept: 'Cookie', description: '~4KB; sent with every request; HttpOnly/Secure/SameSite flags; the only XSS-shielded option. Server sessions, auth.' },
        { concept: 'localStorage', description: '~5MB; persistent; shared across same-origin tabs; SYNCHRONOUS (blocks main thread); string-only. Preferences.' },
        { concept: 'sessionStorage', description: 'Same API; per-tab; cleared on tab close. Wizard drafts, per-tab state.' },
        { concept: 'IndexedDB', description: 'Large quota; async; transactional; stores objects/blobs. Offline data, caches.' },
        { concept: 'Cache API', description: 'Request/Response storage used by service workers for offline assets.' },
        { concept: 'storage event', description: 'Fires in OTHER tabs on localStorage change — cross-tab logout sync.', code: 'window.addEventListener(\'storage\', onChange)' },
      ],
    },
    {
      id: 'seo-head-cheat',
      title: 'Head / SEO essentials',
      rows: [
        { concept: 'title', description: 'Top ranking + click-through element; unique per page, ~60 chars.' },
        { concept: 'meta description', description: 'Snippet text (CTR), not a ranking factor; ~155 chars.' },
        { concept: 'meta viewport', description: 'Mandatory for mobile rendering.', code: '<meta name="viewport" content="width=device-width, initial-scale=1">' },
        { concept: 'canonical', description: 'Consolidates duplicate URLs to one indexed version.', code: '<link rel="canonical" href="https://x.com/p">' },
        { concept: 'meta robots', description: 'Per-page crawler directives.', code: '<meta name="robots" content="noindex, follow">' },
        { concept: 'Open Graph', description: 'Social share previews (og:title, og:image) — read by social crawlers, not Google ranking.' },
        { concept: 'JSON-LD', description: 'Structured data for rich results.', code: '<script type="application/ld+json">{...}</script>' },
        { concept: 'meta charset', description: 'Must sit in the first 1024 bytes.', code: '<meta charset="utf-8">' },
      ],
    },
    {
      id: 'a11y-cheat',
      title: 'Accessibility quick wins',
      rows: [
        { concept: 'alt text', description: 'Describe function, not appearance; decorative images get alt="" (NOT a missing alt).' },
        { concept: 'label for', description: 'Every input needs a programmatic label — for/id pairing or wrapping.', code: '<label for="em">Email</label><input id="em">' },
        { concept: 'Heading order', description: 'One h1; never skip levels; headings are how screen-reader users scan.' },
        { concept: 'Focus visibility', description: 'Never outline:none without a replacement; use :focus-visible.' },
        { concept: 'aria-live', description: 'Announce dynamic updates: polite (wait) vs assertive (interrupt).' },
        { concept: 'Skip link', description: 'First focusable element jumps keyboard users past the nav to #main.' },
        { concept: 'tabindex', description: '0 = in tab order; -1 = focusable by JS only; positive = never (breaks natural order).' },
      ],
    },
  ],

  questions: [
    {
      id: 'q-semantic-why',
      level: 'beginner',
      question: 'What is semantic HTML and why does it matter beyond "clean code"?',
      answer: 'Semantic elements describe meaning, and three machine consumers depend on it: (1) the accessibility tree — screen readers get landmarks, roles, and keyboard behavior for free; (2) search engines — better content extraction and eligibility for rich results; (3) the browser itself — native behavior like form submission and button activation. Semantics is an API contract, not styling preference.',
      realWorldExample: 'nav, main, and footer landmarks let screen-reader users jump directly between page regions — div equivalents are invisible to that navigation.',
      followUps: ['section vs article vs div?', 'What do you lose with div onclick vs button?'],
    },
    {
      id: 'q-async-defer',
      level: 'beginner',
      question: 'Explain the difference between async and defer on a script tag.',
      answer: 'Both download in parallel with HTML parsing. defer waits to execute until parsing completes, preserves document order, and runs before DOMContentLoaded — use it for app code that touches the DOM or depends on other scripts. async executes the moment the download finishes, pausing the parser, with no ordering guarantee — only for fully independent scripts like analytics.',
      deepDive: 'A plain script blocks parsing for download AND execution, and additionally waits for pending CSSOM because it might read computed styles. type="module" scripts are deferred by default. defer on inline scripts is ignored.',
      followUps: ['What if two async scripts depend on each other?', 'Why does a sync script wait for CSS?'],
    },
    {
      id: 'q-doctype',
      level: 'beginner',
      question: 'What does <!DOCTYPE html> actually do?',
      answer: 'It switches the browser into standards mode. Omitting it triggers quirks mode, where browsers emulate legacy bugs — most famously the old IE box model where width includes padding and border. It is a rendering-mode switch, not a version number; document.compatMode reports "CSS1Compat" (standards) vs "BackCompat" (quirks).',
      followUps: ['Name one concrete quirks-mode behavior.', 'Why is the HTML5 doctype so short compared to HTML4?'],
    },
    {
      id: 'q-block-parsing',
      level: 'intermediate',
      question: 'Walk through what happens when the parser encounters <script src="app.js"> in the middle of the body.',
      answer: 'Parsing halts. The browser fetches app.js (if not already grabbed by the preload scanner), waits for any pending CSSOM (the script may read computed styles), executes it, then resumes parsing. Everything below the script — content, images — is invisible until this completes. The preload scanner mitigates the network cost by scanning ahead for other URLs, but execution blocking remains.',
      deepDive: 'This is why document.write is so destructive — a parser-inserted sync script can inject markup at the current parse position, so the parser cannot proceed speculatively past it. async/defer scripts are forbidden from using document.write effectively.',
      followUps: ['What is the preload scanner?', 'How do defer/async change this picture?'],
    },
    {
      id: 'q-crp-optimize',
      level: 'intermediate',
      question: 'Your page shows a blank screen for 3 seconds before anything paints. Using the critical rendering path, what do you check?',
      answer: 'First paint needs DOM + CSSOM, so look for render-blocking resources: (1) large or numerous stylesheets in head — inline the critical CSS, load the rest non-blocking; (2) synchronous scripts — add defer/async; (3) slow TTFB — server/CDN issue, not markup; (4) font loading hiding text — font-display: swap; (5) missing preconnect/preload for critical third-party assets. Confirm with the DevTools Performance panel and the Coverage tab for unused CSS.',
      realWorldExample: 'A single blocking Google Fonts stylesheet on a slow connection is a classic 1–2s blank-screen cause; preconnect + font-display: swap fixes it.',
      followUps: ['Why does the browser refuse to paint before CSSOM is ready?', 'How would you measure improvement? (FCP/LCP in Lighthouse)'],
    },
    {
      id: 'q-form-validation',
      level: 'intermediate',
      question: 'How does native HTML form validation work, and how do you add a custom rule like "passwords must match"?',
      answer: 'Declarative constraints (required, pattern, type, min/max) are checked automatically on submit; invalid forms do not submit and the browser shows a message on the first invalid control. For custom rules, use the Constraint Validation API: call setCustomValidity(\'message\') on the control when the rule fails and setCustomValidity(\'\') when it passes — a non-empty custom validity makes the control invalid like any built-in failure. checkValidity() tests silently; reportValidity() tests and shows UI.',
      deepDive: 'Style with :user-invalid rather than :invalid to avoid flagging untouched fields on page load. For fully custom UI, set novalidate on the form, keep using the API programmatically, and wire aria-describedby + aria-invalid so the errors are announced.',
      followUps: ['What is in ValidityState?', 'How do you keep custom error UI accessible?'],
    },
    {
      id: 'q-storage-jwt',
      level: 'intermediate',
      question: 'Where should you store an auth token in the browser, and why?',
      answer: 'Preferred: an HttpOnly, Secure, SameSite cookie — JavaScript cannot read it, so XSS cannot exfiltrate it, and SameSite blunts CSRF. localStorage is readable by ANY script that executes on your origin, so one XSS hole leaks every user\'s token. If an SPA must hold a token in JS (e.g., third-party API), keep it in memory, keep it short-lived, and pair it with a refresh token in an HttpOnly cookie.',
      deepDive: 'Cookies reintroduce CSRF, so mention the pairing: SameSite=Lax/Strict plus CSRF tokens for state-changing requests. This "name the trade-off on both sides" structure is what interviewers reward.',
      followUps: ['How does SameSite=Lax differ from Strict?', 'How do you log out across all open tabs? (storage event or BroadcastChannel)'],
    },
    {
      id: 'q-img-cls',
      level: 'intermediate',
      question: 'Why do images without width and height attributes cause layout shift, and what is the modern fix?',
      answer: 'Without dimensions, the browser allocates zero height during layout; when the image arrives, everything below it jumps — that is CLS. With width and height attributes, the browser computes the aspect ratio up front and reserves the correct space even when CSS later makes the image fluid (width: 100%; height: auto). For responsive art, aspect-ratio in CSS does the same job.',
      followUps: ['How does this interact with srcset?', 'What other elements commonly cause CLS? (ads, embeds, late-loading fonts)'],
    },
    {
      id: 'q-spa-seo',
      level: 'advanced',
      question: 'Your content is rendered entirely client-side by a SPA. What are the SEO implications and how do you fix them?',
      answer: 'Crawlers receive a near-empty HTML shell; Google can execute JS but rendering is deferred to a second-wave queue (slower, budget-limited), and many other crawlers and social scrapers run no JS at all. Fixes in order of strength: server-side rendering or static generation for indexable routes (Nuxt/Next), so full HTML arrives in the initial response; correct per-route title/meta/canonical; clean crawlable hrefs in real anchor tags; structured data server-rendered; sitemap.xml. Prerendering services are the legacy fallback.',
      deepDive: 'Also mention soft-404s (SPA returns 200 with "not found" UI — return real 404 status server-side) and that Open Graph scrapers (WhatsApp, Slack, Twitter) never run JS, so social previews REQUIRE server-rendered meta tags.',
      followUps: ['SSR vs SSG vs ISR — when each?', 'How do you verify what Googlebot actually sees? (URL Inspection in Search Console)'],
    },
    {
      id: 'q-custom-dropdown-a11y',
      level: 'advanced',
      question: 'Design teams want a fully custom-styled dropdown. Walk through making it accessible.',
      answer: 'First answer: can select with modern styling primitives do the job? Native wins on every platform. If truly custom: a button trigger with aria-haspopup="listbox" and aria-expanded; the popup as role="listbox" with role="option" children and aria-selected; keyboard support — Down/Up to move, Enter/Space to select, Esc to close, type-ahead to jump; focus management via roving tabindex or aria-activedescendant; close on outside click and on blur. Then test with an actual screen reader, not just a checklist.',
      deepDive: 'The senior signal is citing the ARIA Authoring Practices Guide (APG) listbox/combobox patterns rather than inventing semantics, and acknowledging that combobox (with filtering) is a different, harder pattern than listbox.',
      followUps: ['roving tabindex vs aria-activedescendant?', 'How would you test this? (NVDA/VoiceOver + keyboard-only pass)'],
    },
    {
      id: 'q-shadow-vs-virtual',
      level: 'advanced',
      question: 'Compare shadow DOM with virtual DOM. When would you ship a web component instead of a framework component?',
      answer: 'They solve unrelated problems. Shadow DOM is a BROWSER encapsulation boundary: scoped styles and DOM hidden from the page. Virtual DOM is a FRAMEWORK rendering strategy: an in-memory tree diffed to minimize real DOM writes. Ship web components when consumers span frameworks — design systems used by React, Vue, and legacy pages alike — or for embeddable third-party widgets. Inside a single-framework app, framework components win on DX, reactivity, and SSR.',
      followUps: ['How does SSR work with shadow DOM? (declarative shadow DOM)', 'How do custom elements interop with Vue/React props and events?'],
    },
    {
      id: 'q-third-party-iframe',
      level: 'expert',
      question: 'You must embed an untrusted third-party widget. Design the isolation and communication architecture.',
      answer: 'Isolate in a cross-origin iframe — the same-origin policy is the strongest browser boundary. Apply sandbox with minimal grants (allow-scripts but NOT allow-same-origin for untrusted code — together they let the frame unsandbox itself). Constrain with an allow attribute (Permissions Policy) so it cannot use camera/geolocation. Communicate exclusively via postMessage with a versioned message schema: always pass an explicit targetOrigin when sending, always validate event.origin and message shape when receiving. Add CSP frame-src on the host to whitelist the embed origin, lazy-load the iframe, and reserve its layout box to prevent CLS.',
      deepDive: 'Mention the payment-industry precedent: Stripe Elements runs card fields inside Stripe-origin iframes so the host page (and its XSS surface) never touches card data — that architecture is the answer template.',
      followUps: ['What exactly goes wrong with allow-scripts + allow-same-origin?', 'How does the embedded site protect itself from clickjacking? (frame-ancestors)'],
    },
    {
      id: 'q-html-streaming',
      level: 'expert',
      question: 'How does HTML streaming improve perceived performance, and what constraints does it impose on your markup and architecture?',
      answer: 'Because parsing is incremental, the browser renders HTML as bytes arrive — a server that flushes the head plus above-the-fold shell early gets first paint before the slow data-dependent body is even generated. The browser begins fetching CSS/JS referenced in the flushed head in parallel with server work. Constraints: status code and headers are committed at first flush (no late redirects or 404s); slow content must be ordered last or filled via out-of-order streaming (slots swapped in by small inline scripts — the technique behind React 18 streaming SSR); blocking head resources still gate first paint, so the streamed shell must keep its critical path tiny.',
      followUps: ['How does early flushing interact with CDN/proxy buffering?', 'How do React/Vue SSR streaming implementations swap suspended content into place?'],
    },
  ],

  codingQuestions: [
    {
      id: 'code-accessible-form',
      title: 'Accessible signup form with custom validation',
      level: 'intermediate',
      problem: 'Build a signup form (email, password, confirm password) using native validation plus a custom "passwords must match" rule, with errors that screen readers announce.',
      solution: {
        lang: 'html',
        code: '<form id="signup" novalidate>\n  <label for="email">Email</label>\n  <input id="email" name="email" type="email" required\n         autocomplete="email" aria-describedby="email-err">\n  <p id="email-err" role="alert"></p>\n\n  <label for="pwd">Password</label>\n  <input id="pwd" name="pwd" type="password" required minlength="8"\n         autocomplete="new-password" aria-describedby="pwd-err">\n  <p id="pwd-err" role="alert"></p>\n\n  <label for="confirm">Confirm password</label>\n  <input id="confirm" name="confirm" type="password" required\n         autocomplete="new-password" aria-describedby="confirm-err">\n  <p id="confirm-err" role="alert"></p>\n\n  <button type="submit">Sign up</button>\n</form>\n\n<script>\n  const form = document.getElementById(\'signup\')\n  const pwd = form.elements.pwd\n  const confirm = form.elements.confirm\n\n  confirm.addEventListener(\'input\', () => {\n    confirm.setCustomValidity(\n      confirm.value && confirm.value !== pwd.value\n        ? \'Passwords do not match\' : \'\'\n    )\n  })\n\n  form.addEventListener(\'submit\', (e) => {\n    e.preventDefault()\n    let firstInvalid = null\n    for (const field of form.querySelectorAll(\'input\')) {\n      const err = document.getElementById(field.id + \'-err\')\n      const bad = !field.checkValidity()\n      err.textContent = bad ? field.validationMessage : \'\'\n      field.setAttribute(\'aria-invalid\', String(bad))\n      if (bad && !firstInvalid) firstInvalid = field\n    }\n    if (firstInvalid) { firstInvalid.focus(); return }\n    console.log(Object.fromEntries(new FormData(form)))\n  })\n</script>',
      },
      explanation: 'novalidate disables native bubbles so we render our own UI, but checkValidity()/validationMessage still do all the rule-checking. setCustomValidity wires the match rule into the same machinery. Each error lives in a role="alert" region tied via aria-describedby, so screen readers announce it; aria-invalid marks state; focus jumps to the first failure. Correct autocomplete tokens are a free UX and accessibility win.',
      timeComplexity: 'O(n) fields per submit',
      spaceComplexity: 'O(1)',
      alternatives: ['Pure native UI: drop novalidate and let reportValidity() show browser bubbles', 'Style states with :user-invalid for zero-JS visual feedback'],
    },
    {
      id: 'code-web-component',
      title: 'Build a counter web component',
      level: 'advanced',
      problem: 'Create a <click-counter start="5"> custom element with shadow DOM, an observed attribute, and a custom event that crosses the shadow boundary.',
      solution: {
        lang: 'js',
        code: 'class ClickCounter extends HTMLElement {\n  static observedAttributes = [\'start\']\n  #count = 0\n\n  constructor() {\n    super()\n    const root = this.attachShadow({ mode: \'open\' })\n    root.innerHTML =\n      \'<style>\' +\n      \'  button { font: inherit; padding: .5em 1em; cursor: pointer; }\' +\n      \'  :host([disabled]) button { opacity: .5; pointer-events: none; }\' +\n      \'</style>\' +\n      \'<button type="button">Count: 0</button>\'\n    this.#btn = root.querySelector(\'button\')\n  }\n\n  #btn\n\n  connectedCallback() {\n    this.#btn.addEventListener(\'click\', this.#onClick)\n    this.#render()\n  }\n\n  disconnectedCallback() {\n    this.#btn.removeEventListener(\'click\', this.#onClick)\n  }\n\n  attributeChangedCallback(name, oldV, newV) {\n    if (name === \'start\') {\n      this.#count = Number(newV) || 0\n      this.#render()\n    }\n  }\n\n  #onClick = () => {\n    this.#count++\n    this.#render()\n    this.dispatchEvent(new CustomEvent(\'count-change\', {\n      detail: { count: this.#count },\n      bubbles: true,\n      composed: true, // crosses the shadow boundary\n    }))\n  }\n\n  #render() {\n    this.#btn.textContent = \'Count: \' + this.#count\n  }\n}\n\ncustomElements.define(\'click-counter\', ClickCounter)\n// <click-counter start="5"></click-counter>',
      },
      explanation: 'Covers every checkpoint interviewers look for: hyphenated name, observedAttributes + attributeChangedCallback for attribute reactivity, listener cleanup in disconnectedCallback, :host styling, and composed: true on the custom event — without it the event stops at the shadow root and outside listeners never see it.',
      timeComplexity: 'O(1) per interaction',
      spaceComplexity: 'O(1) per instance',
      alternatives: ['Reflect count to an attribute for CSS hooks', 'ElementInternals to make it form-associated', 'template element + cloneNode for many instances'],
    },
    {
      id: 'code-lazy-images',
      title: 'Lazy-load images with IntersectionObserver fallback',
      level: 'intermediate',
      problem: 'Implement image lazy loading: native loading="lazy" where supported, IntersectionObserver fallback otherwise, no layout shift.',
      solution: {
        lang: 'html',
        code: '<img data-src="photo.jpg" src="placeholder.svg"\n     width="800" height="600" alt="Team offsite, 2026" loading="lazy">\n\n<script>\n  const imgs = document.querySelectorAll(\'img[data-src]\')\n\n  if (\'loading\' in HTMLImageElement.prototype) {\n    // Native path: just swap in the real URL; browser defers the fetch\n    imgs.forEach(img => { img.src = img.dataset.src })\n  } else {\n    const io = new IntersectionObserver((entries, observer) => {\n      for (const entry of entries) {\n        if (!entry.isIntersecting) continue\n        const img = entry.target\n        img.src = img.dataset.src\n        img.removeAttribute(\'data-src\')\n        observer.unobserve(img) // load once, stop watching\n      }\n    }, { rootMargin: \'200px 0px\' }) // start fetching 200px early\n    imgs.forEach(img => io.observe(img))\n  }\n</script>',
      },
      explanation: 'Feature-detect native lazy loading first — it is free and the browser tunes the distance heuristics. The fallback observes each image and swaps data-src → src when it nears the viewport; rootMargin pre-fetches before the user arrives so images never pop in late. width/height reserve layout space, preventing CLS. unobserve avoids leaking observers.',
      timeComplexity: 'O(1) per intersection callback entry',
      spaceComplexity: 'O(n) observed images',
      alternatives: ['Low-quality placeholder (LQIP/blur-up) instead of a generic SVG', 'content-visibility: auto for whole below-fold sections', 'Never lazy-load the LCP image — preload it instead'],
    },
    {
      id: 'code-dialog-modal',
      title: 'Accessible modal with the native dialog element',
      level: 'intermediate',
      problem: 'Implement a modal dialog with focus management, Esc handling, backdrop click-to-close, and background scroll lock — using the platform, not a div stack.',
      solution: {
        lang: 'html',
        code: '<button id="open">Delete account</button>\n\n<dialog id="confirm" aria-labelledby="confirm-title">\n  <h2 id="confirm-title">Delete account?</h2>\n  <p>This action is permanent.</p>\n  <form method="dialog">\n    <button value="cancel">Cancel</button>\n    <button value="confirm" autofocus>Delete</button>\n  </form>\n</dialog>\n\n<script>\n  const dialog = document.getElementById(\'confirm\')\n  const opener = document.getElementById(\'open\')\n\n  opener.addEventListener(\'click\', () => {\n    dialog.showModal()                  // top layer + focus trap + Esc, free\n    document.body.style.overflow = \'hidden\'\n  })\n\n  dialog.addEventListener(\'click\', (e) => {\n    if (e.target === dialog) dialog.close(\'cancel\') // backdrop click\n  })\n\n  dialog.addEventListener(\'close\', () => {\n    document.body.style.overflow = \'\'\n    opener.focus()                      // return focus to the trigger\n    if (dialog.returnValue === \'confirm\') console.log(\'deleting...\')\n  })\n</script>',
      },
      explanation: 'showModal() supplies what div modals reimplement badly: rendering in the top layer (no z-index wars), inert background (real focus trap), Esc-to-close, and ::backdrop styling. form method="dialog" closes the dialog and sets returnValue without JS handlers per button. The two manual responsibilities left are scroll lock and returning focus to the opener on close — interviewers specifically listen for the focus-return step.',
      timeComplexity: 'O(1)',
      spaceComplexity: 'O(1)',
      alternatives: ['Pre-dialog era answer: role="dialog" + aria-modal + manual focus trap — know it to compare', 'Popover API for non-modal overlays (menus, tooltips)'],
    },
    {
      id: 'code-tabs-aria',
      title: 'Accessible tabs with roving tabindex',
      level: 'advanced',
      problem: 'Build a tab interface with correct ARIA roles and the roving-tabindex keyboard pattern (arrow keys move between tabs, Tab key exits the tablist).',
      solution: {
        lang: 'html',
        code: '<div role="tablist" aria-label="Account settings">\n  <button role="tab" id="tab-1" aria-controls="panel-1"\n          aria-selected="true" tabindex="0">Profile</button>\n  <button role="tab" id="tab-2" aria-controls="panel-2"\n          aria-selected="false" tabindex="-1">Security</button>\n  <button role="tab" id="tab-3" aria-controls="panel-3"\n          aria-selected="false" tabindex="-1">Billing</button>\n</div>\n<div role="tabpanel" id="panel-1" aria-labelledby="tab-1" tabindex="0">...</div>\n<div role="tabpanel" id="panel-2" aria-labelledby="tab-2" tabindex="0" hidden>...</div>\n<div role="tabpanel" id="panel-3" aria-labelledby="tab-3" tabindex="0" hidden>...</div>\n\n<script>\n  const tablist = document.querySelector(\'[role="tablist"]\')\n  const tabs = [...tablist.querySelectorAll(\'[role="tab"]\')]\n\n  function activate(tab) {\n    for (const t of tabs) {\n      const selected = t === tab\n      t.setAttribute(\'aria-selected\', String(selected))\n      t.tabIndex = selected ? 0 : -1   // roving tabindex\n      document.getElementById(t.getAttribute(\'aria-controls\'))\n        .hidden = !selected\n    }\n    tab.focus()\n  }\n\n  tablist.addEventListener(\'click\', (e) => {\n    const tab = e.target.closest(\'[role="tab"]\')\n    if (tab) activate(tab)\n  })\n\n  tablist.addEventListener(\'keydown\', (e) => {\n    const i = tabs.indexOf(document.activeElement)\n    if (i === -1) return\n    if (e.key === \'ArrowRight\') activate(tabs[(i + 1) % tabs.length])\n    if (e.key === \'ArrowLeft\') activate(tabs[(i - 1 + tabs.length) % tabs.length])\n    if (e.key === \'Home\') activate(tabs[0])\n    if (e.key === \'End\') activate(tabs[tabs.length - 1])\n  })\n</script>',
      },
      explanation: 'The roving tabindex is the heart of the pattern: exactly one tab has tabindex="0" at any time, so the Tab key enters the widget once and exits to the panel — arrow keys handle movement inside, which matches how screen-reader users expect composite widgets to behave. aria-controls/aria-labelledby bind tabs to panels in both directions; hidden (not CSS classes) keeps inactive panels out of the accessibility tree. Click handling uses event delegation on the tablist.',
      timeComplexity: 'O(n) tabs per activation',
      spaceComplexity: 'O(1)',
      alternatives: ['aria-activedescendant variant (focus stays on the container)', 'Automatic activation on arrow-focus vs manual (Enter) — APG documents both'],
    },
  ],

  scenarios: [
    {
      id: 'sc-a11y-remediation',
      title: 'The app failed an accessibility audit before an enterprise deal — fix it systematically',
      situation: 'A WCAG 2.1 AA audit returned 120 violations across the product; the contract depends on remediation within a quarter.',
      approach: [
        'Triage by user impact, not violation count: blockers first (keyboard traps, unlabeled form fields, missing button semantics), then perceivability (contrast, alt text), then polish.',
        'Fix classes of bugs, not instances: one shared Button/Input component fixed correctly repairs hundreds of call sites — audit the design system before the pages.',
        'Replace fake interactive elements: div onclick → button, div modal → dialog, restoring keyboard and screen-reader behavior platform-wide.',
        'Add landmarks and heading hierarchy (header/nav/main/footer, single h1, no skipped levels) so screen-reader navigation works structurally.',
        'Wire dynamic announcements: aria-live for async results/toasts; manage focus on route changes and modal open/close.',
        'Make it durable: axe-core checks in CI, eslint a11y rules, and a manual keyboard + NVDA/VoiceOver pass in the release checklist — automated tools catch only ~30–40% of issues.',
      ],
      keyTakeaways: ['Lead with "fix the design system, not the pages" — it shows systems thinking', 'Native elements eliminate violation categories wholesale', 'Name the limit of automated scanners and the need for manual testing'],
    },
    {
      id: 'sc-seo-collapse',
      title: 'Organic traffic dropped after migrating to a client-rendered SPA — diagnose and recover',
      situation: 'After replacing a server-rendered site with a CSR React/Vue app, rankings and indexed pages fell over six weeks.',
      approach: [
        'Confirm what crawlers see: Search Console URL Inspection + "view rendered HTML" — typically an empty app shell with no content, titles, or meta.',
        'Check the basics broken by the migration: changed URLs without 301 redirects, soft-404s (200 status with "not found" UI), lost canonical tags, missing sitemap.',
        'Restore server-rendered HTML for indexable routes: SSR or SSG (Nuxt/Next) so the initial response contains full content and per-route title/meta/canonical.',
        'Server-render Open Graph/Twitter tags — social and chat scrapers execute no JS at all, so previews were also broken.',
        'Reinstate structured data (JSON-LD) for product/article rich results, and ensure internal links are real anchor hrefs, not onClick handlers.',
        'Monitor recovery: index coverage and impressions in Search Console; expect weeks, not days, for re-crawl.',
      ],
      keyTakeaways: ['The diagnosis tool is "what HTML does the crawler receive" — everything follows from that', 'Googlebot renders JS late and budgeted; other scrapers never do', 'Redirect mapping is the silent killer in migrations — always mention 301s'],
    },
    {
      id: 'sc-lcp-regression',
      title: 'LCP is 5.2s on mobile — find the HTML-level wins before touching the backend',
      situation: 'Field data (CrUX) shows poor LCP on product pages; the LCP element is the hero image.',
      approach: [
        'Identify the LCP element and its loading chain in DevTools Performance — discover the hero is loading="lazy" (self-inflicted delay) and only referenced in CSS background, hiding it from the preload scanner.',
        'Make the hero a real img with fetchpriority="high" and a preload hint; remove lazy from above-the-fold media.',
        'Shrink delivery: AVIF/WebP via picture, srcset/sizes so mobiles stop downloading desktop pixels, preconnect to the image CDN.',
        'Clear render-blocking obstacles ahead of the image: inline critical CSS, defer scripts, font-display: swap.',
        'Set width/height on all media to stabilize layout (CLS) while the image streams in.',
        'Verify with Lighthouse lab runs plus field data over the next CrUX window; set a performance budget in CI to prevent regression.',
      ],
      keyTakeaways: ['Lazy-loading the LCP image is the #1 self-inflicted LCP wound', 'CSS background images are invisible to the preload scanner — real img tags win', 'Always close with measurement: lab (Lighthouse) AND field (CrUX) data'],
    },
  ],

  mentalModels: [
    {
      id: 'mm-streaming-parser',
      title: 'The Parser as an Assembly Line',
      flow: ['Bytes stream in', 'Tokenizer cuts tokens', 'Tree builder assembles DOM', 'Sync script = line STOPS', 'Preload scanner runs ahead grabbing parts', 'Rendering ships partial product early'],
      explanation: [
        'Picture a conveyor belt: HTML flows in one end, DOM nodes come off the other — continuously, not in one batch. The page can render the front of the document while the back is still downloading.',
        'A synchronous script is a worker who halts the belt: nothing past them is assembled until they finish. defer/async move that worker off the belt.',
        'The preload scanner is a runner who sprints down the unprocessed belt grabbing every URL — downloads start even while the line is stopped.',
      ],
      whatBreaksWithoutIt: 'Without the streaming model you cannot explain why scripts block content below them, why early-flushed HTML paints sooner, or what the preload scanner saves you from.',
      interviewTip: 'Say "parsing is streaming and error-tolerant by spec" early — those two adjectives signal you know the parser, then explain script blocking from the assembly-line picture.',
    },
    {
      id: 'mm-two-trees',
      title: 'Two Trees Must Meet Before Pixels',
      flow: ['HTML → DOM tree', 'CSS → CSSOM tree', 'Both ready? → Render tree', 'Layout → Paint → Composite', 'Any blocker on either tree delays first paint'],
      explanation: [
        'The browser refuses to paint until it has BOTH structure (DOM) and styling (CSSOM) — painting unstyled content then restyling would be worse than waiting.',
        'Every render-blocking resource is a delay on one of the two trees: stylesheets stall CSSOM; sync scripts stall DOM (and wait on CSSOM themselves).',
        'All classic performance advice is just pruning this graph: inline critical CSS (faster CSSOM), defer JS (unblock DOM), preload (start fetches sooner).',
      ],
      whatBreaksWithoutIt: 'Without the two-tree model, performance work is a checklist of memorized tips; with it, you can derive why each tip works and when it does not apply.',
      interviewTip: 'Whiteboard the two trees converging into the render tree — interviewers asking "why is CSS render-blocking" want exactly this picture.',
    },
    {
      id: 'mm-accessibility-tree',
      title: 'The Accessibility Tree: Your Second DOM',
      flow: ['DOM node', 'Browser computes role + name + state', 'Accessibility tree node', 'OS accessibility API', 'Screen reader speaks it'],
      explanation: [
        'Every page produces a parallel tree consumed by assistive tech. Semantic elements populate it richly for free; divs produce blank, meaningless nodes.',
        'ARIA is a patch language for THIS tree only — it edits announcements (role, name, state) but never adds focus, keyboard handling, or behavior.',
        'display:none and the hidden attribute remove nodes from this tree; aria-hidden removes them while leaving them visible — and opacity:0 removes them from neither.',
      ],
      whatBreaksWithoutIt: 'Without this model, ARIA looks like magic dust — developers add role="button" and wonder why Enter does nothing, or hide content visually and confuse screen-reader users.',
      interviewTip: 'Frame every accessibility answer as "what does the accessibility tree contain after my change" — and quote the first rule of ARIA: prefer native elements.',
    },
    {
      id: 'mm-forms-platform',
      title: 'Forms: the Platform Does the Work',
      flow: ['Named controls inside form', 'Declarative constraints (required, type, pattern)', 'Browser validates on submit', 'ValidityState + setCustomValidity for custom rules', 'FormData serializes by name'],
      explanation: [
        'A form is a declarative contract: name attributes define the payload, constraint attributes define the rules, and the browser supplies validation, messages in the user\'s locale, keyboard handling, and submission.',
        'The Constraint Validation API is the escape hatch, not a replacement — custom rules plug INTO native machinery via setCustomValidity rather than replacing it.',
        'Every layer you rebuild in JS (div buttons, manual serialization, hand-rolled validation) is platform behavior you now must maintain, localize, and make accessible yourself.',
      ],
      whatBreaksWithoutIt: 'Skip the platform and you hand-roll serialization, error display, focus management, and mobile keyboards — the bug surface of every JS-only form library reimplementation.',
      interviewTip: 'Open form answers with "native first, JS to enhance" and name one API from each layer (pattern → ValidityState → setCustomValidity) to show you know the full ladder.',
    },
    {
      id: 'mm-iframe-embassy',
      title: 'Iframes as Foreign Embassies',
      flow: ['Host page (your country)', 'iframe (embassy: foreign soil inside it)', 'Same-origin policy = the border', 'postMessage = diplomatic mail', 'sandbox = treaty limiting embassy powers'],
      explanation: [
        'A cross-origin iframe is foreign territory embedded in your page: it runs its own document under its own laws, and you cannot reach across the border to touch its DOM — nor it yours.',
        'postMessage is the official mail channel: anyone can send a letter, so the receiver must check the sender (event.origin) before acting on it.',
        'sandbox writes the treaty: start from zero capabilities and grant only what is needed — and never allow-scripts plus allow-same-origin for untrusted content, which lets the embassy rewrite its own treaty.',
      ],
      whatBreaksWithoutIt: 'Without origin-based isolation, any embedded ad or widget could read your DOM, your cookies, and your users\' data — the embassy model is why payment fields live in iframes.',
      interviewTip: 'Anchor iframe answers in the same-origin policy as the boundary, then name the two controlled crossings: postMessage (with origin checks) and the sandbox grant list.',
    },
  ],

  revision: [
    { id: 'r-semantic', topic: 'Semantic HTML', importance: 5, thirtySecond: 'Elements carry meaning for machines: accessibility tree, SEO, native behavior. button gives focus + keyboard + role for free; div gives nothing. main/nav/article/section are landmarks.', twoMinute: 'Three consumers: screen readers (landmarks, roles, heading navigation), crawlers (content extraction, rich results), the browser (form submission, focusability). article = self-contained unit; section = themed group needing a heading; div = no meaning, styling only. button acts, a navigates — never a clickable div. One visible main and one h1 per page, no skipped heading levels.', deepDive: 'Implicit ARIA role mapping: button → role button, a[href] → link, nav → navigation. Rebuilt controls must reimplement role + tabindex + keyboard + states by hand and usually miss some — which is why "use the native element" is the senior answer to most widget questions.', followUps: ['section vs article?', 'What exactly does button give you over div onclick?'] },
    { id: 'r-parsing', topic: 'Parsing & DOM Construction', importance: 4, thirtySecond: 'Streaming pipeline: bytes → tokens → nodes → DOM. Error-tolerant by spec (browsers repair identically). Sync scripts pause the parser; the preload scanner fetches ahead anyway.', twoMinute: 'The parser builds the DOM incrementally — pages render before fully downloading. Repair rules are specified: missing tbody inserted, mis-nesting fixed deterministically. A plain script halts parsing for download + execution (it might document.write); the preload scanner races ahead grabbing img/script/link URLs so the network is not idle during the stall.', deepDive: 'The script must also wait for pending CSSOM before executing, because it may read computed styles — so a slow stylesheet transitively blocks your script AND parsing below it. document.write from async scripts is ignored; from parser-inserted sync scripts it injects at the parse position.', followUps: ['Why does querySelector("table > tr") fail?', 'What does the preload scanner not see? (CSS background images, JS-injected URLs)'] },
    { id: 'r-crp', topic: 'Critical Rendering Path', importance: 5, thirtySecond: 'DOM + CSSOM → render tree → layout → paint → composite. CSS blocks rendering; sync JS blocks parsing and waits on CSSOM. Shorten the path: critical CSS inline, defer JS, preload key assets.', twoMinute: 'No paint until both trees exist — that is why CSS is render-blocking. Sync JS is worse: blocks DOM construction AND waits for CSSOM. Render tree holds only visible nodes; layout computes geometry; paint rasterizes; compositing assembles GPU layers. Optimizations map 1:1: fewer blocking bytes (inline critical CSS), unblock the parser (defer/async), start fetches earlier (preload/preconnect), reserve media space (width/height) for CLS.', deepDive: 'Tie to Core Web Vitals: render-blocking resources and slow hero delivery → LCP; dimension-less media and late fonts → CLS; long main-thread tasks → INP. Lab vs field: Lighthouse simulates, CrUX measures real users — quote both in answers.', followUps: ['Why exactly does JS wait for CSSOM?', 'Walk LCP from HTML attribute to metric.'] },
    { id: 'r-async-defer', topic: 'async vs defer', importance: 5, thirtySecond: 'Both download in parallel. defer: after parse, in order, before DOMContentLoaded — app code. async: on arrival, any order — analytics only. Modules are deferred by default.', twoMinute: 'Plain script: parser stops for download + execution. defer keeps order — safe for dependent bundles. async races the network — two async scripts with a dependency fail intermittently. defer on inline scripts is ignored; inline modules ARE deferred. DOMContentLoaded waits for deferred scripts; load waits for all resources.', deepDive: 'fetchpriority and preload tune fetch order independently of execution timing. Module graphs fetch dependencies in parallel and execute once even if referenced twice. The old "scripts at the bottom of body" pattern is the pre-defer workaround — recognize it as legacy.', followUps: ['async script that needs a library — what happens?', 'When does DOMContentLoaded fire?'] },
    { id: 'r-forms', topic: 'Forms & Validation', importance: 4, thirtySecond: 'Declarative constraints (required, type, pattern, min/max) validate on submit natively. Constraint Validation API: checkValidity, reportValidity, setCustomValidity, ValidityState. FormData serializes by name.', twoMinute: 'Controls without name are not submitted — name is the payload contract. Custom rules: setCustomValidity(msg) marks invalid, setCustomValidity(\'\') clears — forgetting the reset is the classic bug. novalidate disables native UI but keeps the API. Style with :user-invalid (post-interaction), not :invalid (fires on load). Correct input types drive mobile keyboards; autocomplete tokens drive autofill.', deepDive: 'Accessible custom errors: aria-describedby ties message to field, aria-invalid flags state, role="alert" announces, focus moves to first failure. ValidityState flags (valueMissing, patternMismatch, typeMismatch) tell you WHICH rule failed for tailored messages.', followUps: ['Implement password-match validation.', ':invalid vs :user-invalid?'] },
    { id: 'r-aria', topic: 'ARIA & Accessibility', importance: 5, thirtySecond: 'ARIA edits the accessibility tree (role, name, state) — zero behavior added. First rule: use native elements instead. aria-label names, aria-expanded states, aria-live announces updates.', twoMinute: 'Browser derives the accessibility tree from the DOM; screen readers consume it. role="button" announces but does not behave — tabindex + Enter/Space handlers are still on you. aria-hidden removes from the tree while visible; never put it on focusable elements. aria-live polite/assertive announces dynamic changes. Forms: every input programmatically labeled (label for / aria-label).', deepDive: 'Composite widgets follow APG patterns: roving tabindex (one tabbable element, arrows move within) or aria-activedescendant. Name computation precedence: aria-labelledby > aria-label > native label/content. Automated scanners catch ~30–40% of issues — manual keyboard and screen-reader passes are non-negotiable.', followUps: ['Make a div behave as a full button — every step.', 'aria-label vs aria-labelledby vs aria-describedby?'] },
    { id: 'r-meta-seo', topic: 'Meta Tags & SEO', importance: 4, thirtySecond: 'title (ranking + CTR), meta description (snippet only), viewport (mobile, mandatory), canonical (dedupe URLs), robots (index control), OG/Twitter (social previews), JSON-LD (rich results). meta keywords is dead.', twoMinute: 'charset must be in the first 1024 bytes. Canonical consolidates filtered/paginated duplicates. robots noindex per page; robots.txt blocks crawling (not indexing!) site-wide. Social scrapers run no JS — OG tags must be server-rendered. JSON-LD is Google\'s preferred structured-data format and powers rich snippets.', deepDive: 'CSR SPAs index late and poorly: Googlebot queues rendering; soft-404s (200 + "not found" UI) pollute the index. SSR/SSG for indexable routes is the structural fix. Migrations: missing 301 maps cause most traffic collapses. Verify reality with Search Console URL Inspection.', followUps: ['robots.txt vs meta noindex?', 'Why do WhatsApp previews break on a SPA?'] },
    { id: 'r-storage', topic: 'Storage APIs', importance: 4, thirtySecond: 'Cookie ~4KB rides every request, HttpOnly = XSS-proof — auth. localStorage ~5MB persistent, sync, JS-readable — preferences. sessionStorage per-tab. IndexedDB async for large/structured data.', twoMinute: 'All origin-scoped (scheme+host+port). localStorage is synchronous — big payloads block the main thread; string-only, so JSON round-trips everything. storage event fires in other tabs — cross-tab logout. Tokens in localStorage are one XSS away from theft; HttpOnly + Secure + SameSite cookies are the defensive default, paired with CSRF protection.', deepDive: 'SameSite Lax (default: blocks cross-site POST cookies, allows top-level GET nav) vs Strict (blocks all cross-site sends) vs None (requires Secure). IndexedDB: transactional, versioned schemas, stores blobs — the offline/PWA store alongside the Cache API. BroadcastChannel is the modern cross-tab message bus.', followUps: ['Where does a JWT go and why?', 'Sync logout across tabs — two ways.'] },
    { id: 'r-iframes', topic: 'Iframes & Sandboxing', importance: 3, thirtySecond: 'Nested browsing context with its own document and origin. Cross-origin frames are isolated; talk via postMessage with origin checks. sandbox = deny-all, grant back selectively.', twoMinute: 'Same-origin frames can script each other; cross-origin cannot — the same-origin policy is the wall. Never allow-scripts + allow-same-origin on untrusted content (frame can unsandbox itself). postMessage: explicit targetOrigin on send, validate event.origin on receive. Embedded sites block clickjacking via CSP frame-ancestors (or legacy X-Frame-Options).', deepDive: 'Permissions Policy (allow attribute) gates capabilities like camera/payment per frame. Iframes are heavy: separate document, often separate process — lazy-load with loading="lazy" and reserve dimensions for CLS. Payment providers (Stripe) use cross-origin iframes so card data never enters the host origin or its XSS surface.', followUps: ['Design a safe third-party widget embed.', 'Who sets frame-ancestors — embedder or embedded?'] },
    { id: 'r-web-components', topic: 'Web Components', importance: 3, thirtySecond: 'Custom elements (define + lifecycle callbacks) + shadow DOM (style/DOM isolation) + template/slot (inert markup + projection). Framework-agnostic; ideal for cross-stack design systems.', twoMinute: 'Hyphen required in tag names. Lifecycle: connectedCallback, disconnectedCallback, attributeChangedCallback (+ static observedAttributes). Shadow DOM blocks selectors both ways, but inheritance and CSS custom properties cross — custom properties ARE the theming API. Events need composed: true to escape the shadow root; targets are retargeted to the host.', deepDive: 'Declarative shadow DOM enables SSR. ElementInternals makes components form-participating with native validation. Compare honestly: no reactivity/render model included; inside one framework, native components rarely beat framework components — across frameworks, they win.', followUps: ['Theme a shadow component from outside.', 'Shadow DOM vs virtual DOM?'] },
    { id: 'r-resource-hints', topic: 'Resource Hints & Lazy Loading', importance: 4, thirtySecond: 'preload = critical asset, this page, high priority (fonts need crossorigin). prefetch = likely next page, idle priority. preconnect = early DNS+TCP+TLS. loading="lazy" defers offscreen img/iframe.', twoMinute: 'preload the LCP hero and critical fonts; fetchpriority="high" boosts fetch order. Never lazy-load above-the-fold/LCP media. srcset + sizes picks resolution per device; picture handles format fallback (AVIF/WebP) and art direction. CSS background images hide from the preload scanner — critical heroes belong in real img tags.', deepDive: 'Font preload without crossorigin double-downloads (font fetches are CORS-mode even same-origin). Over-preloading creates priority contention — budget 2–3. prefetch respects data-saver settings and may be skipped. IntersectionObserver with rootMargin remains the fallback/custom-trigger tool.', followUps: ['Why crossorigin on font preloads?', 'Why is lazy-loading the hero image harmful?'] },
    { id: 'r-doctype-quirks', topic: 'DOCTYPE, Quirks Mode & DCL/load', importance: 3, thirtySecond: '<!DOCTYPE html> switches standards mode; without it, quirks mode emulates legacy bugs (old box model). DOMContentLoaded = DOM parsed + deferred scripts done; load = all resources fetched.', twoMinute: 'Quirks mode concretes: width includes padding/border (old IE box model), different baseline handling. document.compatMode detects it. DOMContentLoaded is when DOM-dependent code can safely run — defer scripts execute just before it. load waits for images/styles/iframes; window metrics and image dimensions are final there.', deepDive: 'Frameworks effectively standardize on DCL-equivalent timing (mount after parse). For analytics on real interactivity, neither event suffices — use Core Web Vitals (LCP/INP) instead of load, which modern lazy-loaded pages fire early or misleadingly.', followUps: ['Detect quirks mode at runtime?', 'Which event do deferred scripts precede?'] },
  ],
}

export default html
