import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ──────────────────── 1. Fundamentals & Mental Model ────────────────────
  {
    slug: 'what-is-vue',
    name: 'What is Vue',
    category: 'fundamentals',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'A progressive, reactive, component-based JavaScript framework for building user interfaces.',
    explanation: [
      'Vue lets you describe UI declaratively as components whose template renders from reactive state, and the framework keeps the real DOM in sync as that state changes.',
      'It exists to occupy a middle ground between a tiny library and a heavyweight framework: an approachable HTML-based template layer on top of a fine-grained reactivity engine, so you can scale from a single sprinkled component to a full SPA.',
      'Interviewers open with it to check whether you can articulate Vue\'s identity — reactive, component-based, progressive — rather than reciting marketing words, and to set up follow-ups on reactivity and the virtual DOM.',
      'The common candidate mistake is describing Vue purely as "like React but with templates" instead of naming its distinctive pillars: a dependency-tracking reactivity system and an incrementally adoptable design.',
      'A senior answer connects the pieces: state is reactive, components compose the UI, and the compiler plus virtual DOM turn declarative templates into efficient, minimal DOM updates.',
    ],
    prerequisites: [],
    leadsTo: ['progressive-framework', 'declarative-rendering', 'reactivity-fundamentals'],
    questions: [
      'What makes Vue a "progressive" framework, and why does that matter for adoption?',
      'What are the core pillars of Vue: reactivity, components, and declarative rendering?',
      'How does Vue differ from React and Angular at a conceptual level?',
      'What does "reactive" mean in the context of Vue, in one sentence?',
      'Why might a team choose Vue over a lighter library like a hand-rolled jQuery setup or a heavier framework?',
    ],
  },
  {
    slug: 'progressive-framework',
    name: 'Progressive Framework',
    category: 'fundamentals',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Vue can be a single `<script>` drop-in on a page or a full SPA; you adopt it incrementally.',
    explanation: [
      'Progressive means the framework layers in: you can include Vue from a CDN to enhance one part of an existing page, then add single-file components, routing, state management, and a build step only as the project demands.',
      'It exists because rewrites are expensive — Vue was designed so a legacy server-rendered app can adopt reactivity in one widget without committing to an SPA architecture up front.',
      'Interviewers ask this to see whether you understand Vue\'s adoption story and can justify it on a real project, not just label it.',
      'The common mistake is conflating "progressive framework" with "progressive web app" (PWA) — they are unrelated concepts that happen to share a word.',
      'The senior nuance is that the incremental path has a ceiling: the CDN/no-build mode uses the runtime compiler and loses single-file-component ergonomics, so most production apps quickly graduate to a Vite build.',
    ],
    prerequisites: ['what-is-vue'],
    leadsTo: ['vite-vue'],
    questions: [
      'What does it mean that Vue is a "progressive" framework?',
      'How would you add Vue to an existing server-rendered legacy page without a build step?',
      'What do you give up by using the CDN drop-in mode versus a full build with single-file components?',
      'How is a "progressive framework" different from a progressive web app?',
      'Describe a migration path from a sprinkled-in Vue widget to a full single-page application.',
    ],
  },
  {
    slug: 'vue-instance-app',
    name: 'Application Instance',
    category: 'fundamentals',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      '`createApp(rootComponent)` creates an application instance you configure and then `.mount()` to the DOM.',
    explanation: [
      'In Vue 3 you call `createApp(RootComponent)` to get an app instance, register plugins, global components, and directives on it, then call `.mount(\'#app\')` to attach the root component tree to a real DOM container.',
      'This replaced Vue 2\'s `new Vue()` global so that each app instance is isolated — global config no longer leaks across multiple Vue apps on the same page, which matters for micro-frontends and widgets.',
      'Interviewers ask it to verify you know the modern bootstrapping API and the difference between the app instance (configuration) and the root component (UI), a distinction Vue 2 blurred.',
      'The common mistake is calling things like `Vue.use` or `Vue.component` globally (the Vue 2 style) instead of chaining `app.use()` / `app.component()` on the returned instance.',
      'A senior point is that `mount()` returns the root component instance, app-level config (errorHandler, provides, global properties) must be set before mounting, and you can create several independent apps in one page.',
    ],
    prerequisites: ['what-is-vue'],
    leadsTo: ['lifecycle-hooks'],
    questions: [
      'What does `createApp()` return, and how does it differ from Vue 2\'s `new Vue()`?',
      'Why did Vue 3 move from a global Vue object to per-application instances?',
      'In what order do you register plugins and global components relative to calling `.mount()`?',
      'How do you run two independent Vue applications on the same page?',
      'What is the difference between the application instance and the root component instance?',
    ],
  },
  {
    slug: 'declarative-rendering',
    name: 'Declarative Rendering',
    category: 'fundamentals',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'You describe the UI as a function of state; Vue keeps the DOM in sync as that state changes.',
    explanation: [
      'Instead of imperatively reading and mutating DOM nodes, you declare in a template what the UI should look like for the current state, and Vue\'s reactivity plus virtual DOM compute the minimal set of real DOM operations to get there.',
      'It exists because manual DOM manipulation does not scale: tracking which nodes need updating when state changes is error-prone, and a declarative model makes the rendered output a pure projection of state.',
      'Interviewers ask it to confirm you grasp the central mental model — "UI = f(state)" — which underlies why you almost never touch the DOM directly in idiomatic Vue.',
      'The common mistake is reaching for `document.querySelector` and manual mutation inside a Vue component, fighting the framework instead of changing reactive state and letting it re-render.',
      'The senior nuance is that declarative rendering depends on two systems working together: reactivity to detect what changed and the virtual-DOM diff to apply only the necessary patches.',
    ],
    prerequisites: ['what-is-vue'],
    leadsTo: ['template-syntax', 'virtual-dom'],
    questions: [
      'Explain the mental model "UI is a function of state" in Vue.',
      'What is the difference between declarative and imperative DOM updates?',
      'Why is reaching for `document.querySelector` inside a Vue component usually a smell?',
      'How does Vue know which parts of the DOM to update when state changes?',
      'What two underlying systems make declarative rendering work in Vue 3?',
    ],
  },
  {
    slug: 'vue2-vs-vue3',
    name: 'Vue 2 vs Vue 3',
    category: 'fundamentals',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Vue 3 brings Proxy-based reactivity, the Composition API, multiple root nodes, Fragments, and a faster, smaller runtime.',
    explanation: [
      'Vue 3 rewrote reactivity from Vue 2\'s `Object.defineProperty` getters/setters to ES Proxies, which removes the famous caveats (adding properties, array index assignment) and adds the Composition API for organizing logic by concern.',
      'It exists because Vue 2\'s reactivity could not intercept property additions or index writes without `Vue.set`, the runtime was larger and harder to tree-shake, and the Options API made cross-cutting logic reuse awkward (mixins).',
      'Interviewers use it as a knowledge-currency check and a migration-experience probe: a strong candidate names concrete improvements (Proxy reactivity, Fragments/multiple roots, Teleport, Suspense, better TypeScript, tree-shakable APIs) rather than vague "it\'s faster".',
      'The common mistake is reducing the difference to "Vue 3 has the Composition API" — the reactivity rewrite and its removal of caveats is the deeper, more interview-relevant change, and Composition API was even backported to Vue 2.7.',
      'The senior nuance is the trade-offs: Proxy reactivity drops IE11 support, and migrating large Options-API codebases is gradual — the two APIs coexist, so "Vue 3" does not force a rewrite.',
    ],
    prerequisites: ['what-is-vue'],
    leadsTo: ['options-vs-composition-api', 'proxy-reactivity'],
    questions: [
      'What are the most important differences between Vue 2 and Vue 3?',
      'Why did Vue 3 switch from `Object.defineProperty` to Proxy for reactivity, and what did that fix?',
      'Is the Composition API exclusive to Vue 3? What about Vue 2.7?',
      'What did Vue 3 gain by supporting multiple root nodes (Fragments)?',
      'What are the downsides or trade-offs of Vue 3 compared to Vue 2?',
    ],
  },
  {
    slug: 'options-vs-composition-api',
    name: 'Options API vs Composition API',
    category: 'fundamentals',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Two authoring styles: Options groups code by option type; Composition organizes code by logical concern.',
    explanation: [
      'The Options API spreads a feature across `data`, `methods`, `computed`, and `watch` blocks, while the Composition API lets you co-locate all the state, derived values, and side effects for one logical concern inside `setup` (typically `<script setup>`).',
      'Composition exists to solve logic reuse and large-component readability: related code lives together and can be extracted into composable functions, whereas the Options API scattered a feature across option buckets and relied on mixins for reuse.',
      'This is one of the most common Vue interview questions because it tests whether you understand not just syntax but the maintainability and reuse motivations behind Composition API.',
      'The common mistake is claiming one is strictly "better" — the honest answer is Composition shines for complex, reusable logic and TypeScript, while the Options API remains clear for small components and is more familiar to many teams.',
      'The senior nuance is that the two are not mutually exclusive at the framework level (both compile to the same instance), but mixing them in one component is discouraged; the real win of Composition is extracting `useXxx` composables, which the Options API cannot match cleanly.',
    ],
    prerequisites: ['vue2-vs-vue3'],
    leadsTo: ['setup-function', 'composables'],
    questions: [
      'What problem does the Composition API solve that the Options API struggled with?',
      'When would you still choose the Options API over the Composition API?',
      'How does the Composition API improve logic reuse compared to mixins?',
      'Do Options API and Composition API compile to different things at runtime?',
      'Why does the Composition API generally offer better TypeScript inference?',
    ],
  },

  // ──────────────────────── 2. Reactivity System ────────────────────────
  {
    slug: 'reactivity-fundamentals',
    name: 'Reactivity Fundamentals',
    category: 'reactivity',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Vue tracks which reactive state an effect reads, then re-runs that effect when the state is written.',
    explanation: [
      'At its core Vue reactivity is dependency tracking: while an effect (a render function, computed, or watcher) runs, Vue records every reactive property it reads, and on a later write to one of those properties it re-runs only the effects that depended on it.',
      'This exists so the framework can keep the DOM in sync automatically — you mutate state, and Vue knows exactly which renders and watchers to re-trigger without you wiring up update logic.',
      'Interviewers treat it as the single most important Vue topic because nearly every other behavior (computed caching, why `reactive` loses reactivity on destructure, batched re-renders) is a consequence of track/trigger.',
      'The common candidate mistake is describing reactivity as "Vue watches the whole object and diffs it" — there is no polling or deep diffing; it is precise getter-based tracking and setter-based triggering.',
      'A senior answer names the primitives: `track` runs inside reactive getters to register the currently-running effect as a dependency, and `trigger` runs inside setters to flush the effects registered for that key.',
    ],
    prerequisites: [],
    leadsTo: ['ref', 'reactive', 'computed'],
    questions: [
      'How does Vue know when to re-run a component\'s render function?',
      'Explain dependency tracking: what happens during a read versus a write?',
      'Does Vue poll or diff state to detect changes? Why not?',
      'What are "track" and "trigger" in Vue\'s reactivity system?',
      'Why is reactivity considered the foundation that computed, watch, and rendering all build on?',
    ],
  },
  {
    slug: 'ref',
    name: 'ref',
    category: 'reactivity',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'A reactive container whose `.value` property holds any value, primitive or object.',
    explanation: [
      '`ref(x)` returns an object with a single `.value` property; reads of `.value` are tracked and writes trigger dependent effects, which is how Vue makes even primitives (numbers, strings, booleans) reactive.',
      'It exists because Proxies cannot wrap primitives — you cannot make a bare number reactive — so Vue boxes the value in an object and intercepts the `.value` getter/setter to track and trigger.',
      'Interviewers ask about ref to test whether you understand why `.value` exists and when it is auto-unwrapped, which trips up many candidates.',
      'The common mistake is forgetting `.value` in `<script>` (refs only auto-unwrap in templates and as top-level template bindings, not inside JavaScript expressions or when nested in plain objects), leading to bugs like comparing a ref object instead of its value.',
      'The senior nuance is that a `ref` holding an object internally uses `reactive` for that object, so deep reactivity still applies, and refs are the preferred default in Composition API because they survive destructuring and pass cleanly between composables.',
    ],
    prerequisites: ['reactivity-fundamentals'],
    leadsTo: ['ref-vs-reactive', 'computed', 'template-refs'],
    questions: [
      'Why does `ref` require a `.value` accessor while `reactive` does not?',
      'When is a ref automatically unwrapped, and when must you write `.value`?',
      'How does `ref` make a primitive like a number reactive, given Proxies cannot wrap primitives?',
      'What happens when you pass an object to `ref` versus a primitive?',
      'Why is `ref` often preferred over `reactive` as the default choice in composables?',
    ],
  },
  {
    slug: 'reactive',
    name: 'reactive',
    category: 'reactivity',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Returns a deep reactive Proxy wrapper around an object, so nested property access is tracked.',
    explanation: [
      '`reactive(obj)` returns a Proxy that intercepts every property get and set, recursively making nested objects reactive too, so you read and write properties with normal dot syntax and Vue tracks them automatically.',
      'It exists to give object-shaped state ergonomic, `.value`-free access while still hooking into track/trigger through the Proxy traps.',
      'Interviewers ask about it to surface its limitations — it only works on objects/arrays/collections, not primitives, and the reactivity is tied to the Proxy identity.',
      'The classic mistake is destructuring a reactive object (`const { count } = state`) or reassigning the whole variable, both of which detach the value from the Proxy and silently lose reactivity.',
      'The senior nuance is that reactivity lives on the Proxy, not the raw object, so you must keep using the returned Proxy; mutating the original target or replacing the reference entirely breaks tracking, which is why many teams default to `ref`.',
    ],
    prerequisites: ['reactivity-fundamentals'],
    leadsTo: ['ref-vs-reactive', 'reactivity-caveats'],
    questions: [
      'What types can `reactive` wrap, and what happens if you pass a primitive?',
      'Why does destructuring a reactive object lose reactivity?',
      'Where does the reactivity actually live — on the original object or the returned Proxy?',
      'Is `reactive` deep or shallow by default? What are the implications?',
      'Why can reassigning a `reactive` variable wholesale break reactivity?',
    ],
  },
  {
    slug: 'ref-vs-reactive',
    name: 'ref vs reactive',
    category: 'reactivity',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'When to use each: `ref` for any value with `.value`; `reactive` for objects — and why `reactive` loses reactivity on destructure.',
    explanation: [
      '`ref` works for any value and is accessed via `.value`, while `reactive` only wraps objects and is accessed with plain property syntax; under the hood a `ref` to an object delegates to `reactive`, so they share the same tracking machinery.',
      'The decision matters because `reactive`\'s reactivity is bound to the Proxy: destructuring (`const { x } = state`) or reassigning the variable detaches the value, whereas a `ref` survives destructuring and reassignment because the reactivity rides on the `.value` accessor of a stable container.',
      'This is arguably the most-asked Vue reactivity question because it tests whether you understand the Proxy-vs-boxed-value distinction rather than just memorizing "use ref for primitives".',
      'The common mistake is the rule "ref for primitives, reactive for objects" stated without the caveat — many seniors default to `ref` for everything precisely so they never lose reactivity on destructure and so all state has uniform `.value` access.',
      'A senior nuance is that to safely pull reactive properties out of a `reactive` object you reach for `toRef`/`toRefs`, which convert each property into a connected `ref`, preserving the link the bare destructure would have broken.',
    ],
    prerequisites: ['ref', 'reactive'],
    leadsTo: ['toref-torefs'],
    questions: [
      'When would you choose `ref` over `reactive`, and vice versa?',
      'Why does `reactive` lose reactivity on destructure while `ref` does not?',
      'How is a `ref` holding an object implemented in terms of `reactive`?',
      'Some teams use `ref` for everything — what is the argument for that convention?',
      'How do you destructure a `reactive` object without losing reactivity?',
    ],
  },
  {
    slug: 'proxy-reactivity',
    name: 'Proxy Reactivity',
    category: 'reactivity',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'How Vue 3 uses ES Proxy plus track/trigger to implement fine-grained reactivity.',
    explanation: [
      'Vue 3 wraps reactive objects in an ES `Proxy` whose `get` trap calls `track` to register the currently active effect against that key, and whose `set`/`deleteProperty` traps call `trigger` to re-run the effects that depend on it.',
      'This replaced Vue 2\'s `Object.defineProperty`, which had to walk and convert every property up front and could not intercept property additions, deletions, or array index/length writes — the source of Vue 2\'s reactivity caveats.',
      'Interviewers ask it as a depth probe: a candidate who can describe the get/set traps, lazy per-access conversion, and the WeakMap-of-effects dependency store demonstrably understands Vue rather than just using it.',
      'The common mistake is saying "Vue 3 uses Proxies so everything is reactive" — Proxies remove most caveats but introduce their own (a Proxy is a different identity from the raw object, and primitives still cannot be proxied, which is why `ref` exists).',
      'The senior nuance is that conversion is lazy and on-demand (nested objects become reactive only when accessed), dependencies are stored in a `targetMap` (WeakMap → Map → Set of effects), and the trade-off for dropping `defineProperty` is no IE11 support.',
    ],
    prerequisites: ['reactive'],
    leadsTo: ['reactivity-caveats', 'reactivity-render-link'],
    questions: [
      'How does Vue 3 use a Proxy to implement reactivity? Describe the get and set traps.',
      'What could `Object.defineProperty` not do that Proxy can, and which caveats did that fix?',
      'Where and how does Vue store the mapping from reactive keys to effects?',
      'Is the reactive conversion eager or lazy in Vue 3? Why does it matter?',
      'What new limitations did Proxy-based reactivity introduce compared to Vue 2?',
    ],
  },
  {
    slug: 'computed',
    name: 'computed',
    category: 'reactivity',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Cached derived state that recomputes only when its reactive dependencies change.',
    explanation: [
      '`computed(getter)` returns a readonly ref whose value is lazily evaluated and then cached; it re-runs the getter only when one of the reactive dependencies it tracked actually changes, otherwise returning the memoized result.',
      'It exists to express derived state efficiently — unlike a method, which recomputes on every render, a computed memoizes and skips work when inputs are unchanged.',
      'Interviewers ask "computed vs method" to verify you understand caching: both can return the same value, but only computed avoids recomputation when nothing it depends on changed.',
      'The common mistake is performing side effects or async work inside a computed getter — computeds must be pure and synchronous; effects belong in `watch`/`watchEffect`, and a getter with side effects breaks caching assumptions.',
      'The senior nuance is that a computed can be writable via a `{ get, set }` object, its caching is dependency-driven (not time-based), and it tracks only what the getter reads, so referencing an untracked value will produce a stale cached result.',
    ],
    prerequisites: ['ref'],
    leadsTo: ['computed-caching'],
    questions: [
      'What is the difference between a `computed` and a method that returns the same value?',
      'When does a computed recompute, and when does it return a cached value?',
      'Why should you avoid side effects or async calls inside a computed getter?',
      'How do you create a writable computed, and when would you need one?',
      'Why might a computed return a stale value, and how is that related to dependency tracking?',
    ],
  },
  {
    slug: 'watch',
    name: 'watch',
    category: 'reactivity',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Runs a callback when one or more explicitly specified reactive sources change.',
    explanation: [
      '`watch(source, cb)` takes an explicit source (a ref, a getter, a reactive object, or an array of these) and runs the callback with the new and old values only when that source changes — it is lazy by default, not firing on setup.',
      'It exists for side effects that must react to specific state changes: fetching when an id changes, persisting to storage, or imperatively syncing with non-Vue code, where you need access to the previous value.',
      'Interviewers ask it to check that you can distinguish reacting to a known source (watch) from auto-tracking everything (watchEffect), and that you know its options.',
      'The common mistake is forgetting that watching a `reactive` object is deep by default (and gives you the same object as new and old), while watching a getter that returns a primitive is not deep, and that watching `props.x` directly may not fire unless you pass a getter `() => props.x`.',
      'The senior nuance is mastery of the options: `immediate` to run on setup, `deep` for nested object changes, `flush: \'post\'` to run after the DOM updates, and returning a cleanup via the `onCleanup` argument to cancel stale async work.',
    ],
    prerequisites: ['ref', 'reactive'],
    leadsTo: ['watch-vs-watcheffect'],
    questions: [
      'What sources can `watch` accept, and how do you watch a single prop reactively?',
      'Why does `watch` not fire immediately by default, and how do you make it?',
      'When watching a `reactive` object, why might the old and new values be the same?',
      'What does the `deep` option do, and when is it unnecessary?',
      'How do you cancel a stale async request inside a watcher when the source changes again?',
    ],
  },
  {
    slug: 'watcheffect',
    name: 'watchEffect',
    category: 'reactivity',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Automatically tracks the reactive dependencies it reads and runs the effect immediately and on every change.',
    explanation: [
      '`watchEffect(fn)` runs `fn` once immediately, recording every reactive value it reads during that run, then re-runs it whenever any of those tracked dependencies change — no explicit source list required.',
      'It exists for the common case where you want an effect to stay in sync with whatever state it happens to use, without manually enumerating sources the way `watch` requires.',
      'Interviewers ask it to test the contrast with `watch`: auto-tracked and eager versus explicit and lazy, and whether you understand the dependency-collection rule.',
      'The common mistake is expecting it to track a dependency that was only read inside a branch that did not execute, or inside an async callback after an `await` — only synchronously read reactive values before the first await are tracked.',
      'The senior nuance is that it gives you no previous value (use `watch` if you need old vs new), supports an `onCleanup` callback for cancelling side effects, and has `flush` timing variants (`watchPostEffect`, `watchSyncEffect`).',
    ],
    prerequisites: ['reactivity-fundamentals'],
    leadsTo: ['watch-vs-watcheffect'],
    questions: [
      'How does `watchEffect` know which dependencies to track?',
      'Why does `watchEffect` run immediately while `watch` does not?',
      'Why might a reactive value read after an `await` inside `watchEffect` not be tracked?',
      'Can `watchEffect` give you the previous value of a dependency? If not, what do you use?',
      'How do you clean up a side effect (like a timer) created inside `watchEffect`?',
    ],
  },
  {
    slug: 'watch-vs-watcheffect',
    name: 'watch vs watchEffect',
    category: 'reactivity',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'Explicit-source and lazy (`watch`) versus auto-tracked and eager (`watchEffect`).',
    explanation: [
      '`watch` requires you to name the source(s), runs lazily (only on change unless `immediate` is set), and hands the callback both new and old values; `watchEffect` auto-collects dependencies from what it reads, runs eagerly on setup, and has no access to previous values.',
      'The distinction exists to cover two needs: precise, value-comparing reactions to a known source (watch) versus a "keep this in sync with whatever it uses" effect (watchEffect).',
      'This is one of the most common reactivity interview questions because the choice has real consequences for correctness — using watchEffect where you need the old value, or watch where the source list is awkward, leads to bugs.',
      'The common mistake is treating them as interchangeable; the rule of thumb is: use `watch` when you need the previous value, want lazy behavior, or want to watch a specific source, and `watchEffect` when the effect just needs to track whatever it reads.',
      'The senior nuance is that both share `flush` timing and `onCleanup` semantics, but watchEffect\'s implicit tracking can accidentally over- or under-subscribe (branches, post-await reads), which is exactly why `watch`\'s explicit source is sometimes the safer choice.',
    ],
    prerequisites: ['watch', 'watcheffect'],
    leadsTo: [],
    questions: [
      'What are the key differences between `watch` and `watchEffect`?',
      'When would you choose `watch` over `watchEffect`, and vice versa?',
      'Which one gives you the previous value, and why does the other not?',
      'How does dependency tracking differ between explicit `watch` sources and `watchEffect`?',
      'What pitfalls does `watchEffect`\'s implicit tracking introduce that `watch` avoids?',
    ],
  },
  {
    slug: 'shallowref-shallowreactive',
    name: 'shallowRef & shallowReactive',
    category: 'reactivity',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Opt out of deep reactivity: track only the top level for performance or large/external structures.',
    explanation: [
      '`shallowRef` makes only the `.value` assignment reactive (mutating nested properties does not trigger), and `shallowReactive` makes only the object\'s own top-level properties reactive while leaving nested objects raw.',
      'They exist for performance and integration: deeply converting large objects, third-party class instances, or big lists is wasteful, and shallow variants avoid the cost of recursively proxying structures you will replace wholesale.',
      'Interviewers ask these to probe whether you know reactivity has a cost and how to opt out of it deliberately, a senior-level optimization signal.',
      'The common mistake is being surprised that mutating a nested property of a `shallowRef` does nothing — the fix is to replace `.value` with a new object (often combined with `triggerRef` to force an update) rather than mutating in place.',
      'The senior nuance is pairing these with `markRaw` to keep certain objects permanently non-reactive, and recognizing that a `shallowRef` holding a large immutable snapshot (e.g., from an external store) is a common performance pattern.',
    ],
    prerequisites: ['ref', 'reactive'],
    leadsTo: ['reactivity-performance'],
    questions: [
      'What is the difference between `shallowRef`/`shallowReactive` and their deep counterparts?',
      'When would you deliberately opt out of deep reactivity?',
      'Why does mutating a nested property of a `shallowRef` not trigger an update, and how do you force one?',
      'How do `shallowRef` and `markRaw` work together for large or external data?',
      'What performance problem do shallow reactivity APIs solve?',
    ],
  },
  {
    slug: 'toref-torefs',
    name: 'toRef & toRefs',
    category: 'reactivity',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Keep reactivity when destructuring a reactive object or props by converting properties into connected refs.',
    explanation: [
      '`toRef(obj, key)` creates a ref that stays linked to one property of a reactive source, and `toRefs(obj)` converts every property of a reactive object into such refs, so destructuring the result preserves the live connection.',
      'They exist precisely to solve the destructure problem: a bare `const { x } = reactive(...)` copies the current value and loses reactivity, while `toRefs` gives you refs that read and write back through to the original object.',
      'Interviewers ask this immediately after `ref-vs-reactive` because it is the practical fix — and because it comes up constantly when returning state from composables and when destructuring `props`.',
      'The common mistake is using `toRefs` on `props` and then forgetting that the refs are readonly, or destructuring props without `toRef`/`toRefs` and wondering why a later prop change does not propagate.',
      'The senior nuance is that `toRef` can also create a ref to a not-yet-existing property and (in Vue 3.3+) accept a getter, making it the idiomatic way for a composable to expose individual reactive props while keeping the parent\'s data flow intact.',
    ],
    prerequisites: ['ref-vs-reactive'],
    leadsTo: ['composables'],
    questions: [
      'What problem do `toRef` and `toRefs` solve?',
      'How do you destructure props without losing reactivity?',
      'What is the difference between `toRef` and `toRefs`?',
      'Why are refs created from `props` via `toRefs` readonly?',
      'Why is `toRefs` commonly used when returning state from a composable?',
    ],
  },
  {
    slug: 'readonly-reactivity',
    name: 'readonly',
    category: 'reactivity',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Creates a read-only Proxy of a reactive object so consumers can read but not mutate it.',
    explanation: [
      '`readonly(obj)` returns a deep read-only Proxy: reads are tracked as usual, but any write attempt is blocked and warns in development, while still reflecting changes made through the original writable source.',
      'It exists to enforce one-way data flow — for example, a store or `provide` can hand out a `readonly` view so downstream components consume state without being able to mutate it directly.',
      'Interviewers ask it to check whether you understand controlled state ownership and the provide/inject discipline of "only the owner mutates".',
      'The common mistake is assuming `readonly` clones the data or freezes it permanently — it is a live Proxy mirror, so the underlying writable object can still change and those changes are visible (and reactive) through the readonly view.',
      'The senior nuance is the difference from `Object.freeze` (readonly is reactive and deep but only guards the Proxy, not the raw object) and the existence of `shallowReadonly` for guarding only the top level.',
    ],
    prerequisites: ['reactive'],
    leadsTo: [],
    questions: [
      'What does `readonly` do, and how does it differ from `Object.freeze`?',
      'How is `readonly` used to enforce one-way data flow with provide/inject?',
      'If the underlying object changes, does a `readonly` view reflect it? Why?',
      'What is the difference between `readonly` and `shallowReadonly`?',
      'Does `readonly` make the original object immutable too? Explain.',
    ],
  },
  {
    slug: 'reactivity-caveats',
    name: 'Reactivity Caveats',
    category: 'reactivity',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Array index / new-property pitfalls in Vue 2 and how the Vue 3 Proxy rewrite fixes most of them.',
    explanation: [
      'In Vue 2, reactivity was installed per-property via `Object.defineProperty`, so adding a brand-new property or setting an array element by index (`arr[0] = x`) or changing `.length` was not detected — you needed `Vue.set`/`this.$set` or array methods like `splice`.',
      'These caveats existed because `defineProperty` can only intercept properties that exist at conversion time and cannot trap arbitrary additions, deletions, or index writes.',
      'Interviewers ask this both as a Vue 2 history question and as a "do you understand why Proxy was adopted" probe, often paired with proxy-reactivity.',
      'The common mistake is carrying Vue 2 habits into Vue 3 (reaching for `Vue.set`, which no longer exists) or, conversely, assuming Vue 3 has zero caveats — it still has subtler ones (replacing a destructured value, mutating a raw target, Map/Set requiring reactive collection handling, losing reactivity across reassignment).',
      'The senior nuance is naming exactly what Vue 3 fixed (property add/delete and array index/length writes now work because the Proxy traps them) versus what remains (primitives need `ref`, and reactivity is tied to Proxy identity, not the raw object).',
    ],
    prerequisites: ['reactive', 'proxy-reactivity'],
    leadsTo: [],
    questions: [
      'What were the array and object reactivity caveats in Vue 2, and why did they exist?',
      'How does Vue 3\'s Proxy-based system fix the new-property and array-index problems?',
      'Does Vue 3 have any remaining reactivity caveats? Name some.',
      'Why does `Vue.set` no longer exist in Vue 3?',
      'Why can adding a property to a plain (non-reactive) object never be reactive in either version?',
    ],
  },
  {
    slug: 'reactivity-transform',
    name: 'Reactivity Transform',
    category: 'reactivity',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 2,
    definition:
      'A deprecated, removed compiler experiment that let you use refs without `.value`; know its status, not its syntax.',
    explanation: [
      'Reactivity Transform was an experimental compile-time feature (macros like `$ref`, `$()`, `$$()`) that rewrote ref access so you could read and write reactive variables without typing `.value`.',
      'It existed to address a real ergonomic complaint — the friction and easy-to-forget nature of `.value` — by moving the unwrapping into the compiler instead of the runtime.',
      'Interviewers who mention it are usually testing whether you keep up with the ecosystem and can reason about why a feature was abandoned, not whether you memorized its syntax.',
      'The common mistake is recommending or relying on it in new code: it was marked experimental, then deprecated, and ultimately removed from core Vue because it introduced surprising scoping rules and a gap between written and executed code.',
      'The senior nuance is articulating the rejection reasoning — the implicit magic hurt readability and the mental model, and the team decided the explicit `.value` cost was preferable to compiler-hidden reactivity — so the correct answer is "do not use it; it is gone".',
    ],
    prerequisites: ['ref'],
    leadsTo: [],
    questions: [
      'What was Reactivity Transform, and what problem did it try to solve?',
      'What is the current status of Reactivity Transform in Vue?',
      'Why did the Vue team decide to deprecate and remove it?',
      'Would you use Reactivity Transform in a new project today? Why or why not?',
      'What does this feature\'s history teach about the trade-offs of compiler magic versus explicit APIs?',
    ],
    overlooked: true,
  },
]
