import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ── 7. Asynchronous JavaScript ─────────────────────────────────────────────
  {
    slug: 'sync-vs-async',
    name: 'Synchronous vs Asynchronous',
    category: 'async',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Synchronous code blocks the single thread until it finishes; asynchronous code hands waiting off to the host environment and resumes later via queued callbacks.',
    explanation: [
      'JavaScript executes on one thread with one call stack, so any synchronous operation monopolizes it: while a loop or computation runs, nothing else — clicks, rendering, timers — can happen.',
      'Asynchronous APIs (timers, fetch, file I/O) are delegated to the host (browser or Node), which performs the waiting off-thread and queues a callback for the event loop to run once the stack is free.',
      'This split exists because blocking the only thread on network or disk waits would freeze every page; the async model lets one thread serve thousands of concurrent operations.',
      'Interviewers use it as the entry question to gauge whether you understand that JS itself never runs two pieces of code at once — concurrency lives in the host, not the language.',
      'The classic candidate mistake is saying async code "runs in parallel" or "on another thread": the waiting is concurrent, but every callback still executes one at a time on the same thread.',
    ],
    prerequisites: ['call-stack'],
    leadsTo: ['event-loop'],
    questions: [
      'Why does a heavy synchronous loop freeze the entire page, including buttons that have nothing to do with it?',
      'JavaScript is single-threaded — so where does the "waiting" for a network request actually happen?',
      'Is asynchronous JavaScript the same as parallel or multi-threaded execution? Explain the difference.',
      'Give an example of a synchronous API and an asynchronous API in the browser, and explain what the caller experiences differently.',
      'How can a single-threaded language handle thousands of concurrent network requests?',
    ],
  },
  {
    slug: 'timers',
    name: 'setTimeout / setInterval',
    category: 'async',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Host-scheduled macrotasks where the delay is a minimum, not a guarantee — subject to clamping and background-tab throttling; setTimeout(fn, 0) is not immediate.',
    explanation: [
      'setTimeout registers a callback with the host; after at least the given delay it is placed in the macrotask queue, and it runs only when the call stack is empty and all pending microtasks have drained.',
      'The delay is therefore a floor, not a promise: a busy stack pushes execution later, nested timers get clamped to ~4ms minimums, and background tabs throttle timers to once per second or less.',
      'setInterval drifts because each tick is queued relative to the schedule, not to when the previous callback actually finished — the standard fix is a self-rescheduling setTimeout that computes the next delay.',
      'Interviewers ask about timers because "why is setTimeout(fn, 0) not immediate?" is the fastest probe of whether you actually understand task queuing rather than just the API.',
      'The common mistake is treating the delay as exact, or not knowing about clamping and throttling at all — section-6 territory that separates senior candidates from API users.',
    ],
    prerequisites: ['sync-vs-async'],
    leadsTo: ['macrotask-queue', 'event-loop'],
    questions: [
      'Why is setTimeout(fn, 0) not executed immediately? What is the minimum that must happen first?',
      'setInterval is drifting over time in your polling code — explain why and implement a drift-free alternative.',
      'What is timer clamping, and what happens to your timers when the tab goes to the background?',
      'Predict the output: console.log("a"); setTimeout(() => console.log("b"), 0); console.log("c");',
      'A setTimeout scheduled with 100ms fired after 2 seconds — list the possible reasons.',
    ],
  },
  {
    slug: 'event-loop',
    name: 'Event Loop',
    category: 'async',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'The coordinator of asynchronous execution: when the call stack empties, drain all microtasks, run one macrotask, possibly render (in browsers), and repeat.',
    explanation: [
      'The event loop is the host-level scheduler that bridges the single call stack and the queues of pending work: each turn it picks one macrotask, runs it to completion, then drains the entire microtask queue before rendering or taking the next task.',
      'It exists because JS has one thread — without a loop multiplexing queued callbacks onto that thread, asynchronous APIs would have no way to deliver results back into your code.',
      'Crucially, every callback runs on an empty stack: the synchronous code that scheduled it is long gone, which is why try/catch around setTimeout cannot catch errors thrown inside the callback.',
      'This is the single most-asked JS interview topic, almost always as output prediction mixing synchronous logs, promise .then callbacks, and setTimeout — it tests the layered mental model in one question.',
      'The classic mistake is treating all async as one queue: candidates who do not know that microtasks (promises) always run before the next macrotask (timers) get the ordering wrong every time.',
      'A second frequent slip is conflating the browser loop with Node\'s: Node has phased queues (timers, poll, check/setImmediate) and process.nextTick, while the browser interleaves rendering between tasks.',
    ],
    prerequisites: ['call-stack', 'timers'],
    leadsTo: ['microtask-queue', 'macrotask-queue', 'promises'],
    questions: [
      'Predict the output: console.log(1); setTimeout(() => console.log(2)); Promise.resolve().then(() => console.log(3)); console.log(4);',
      'Walk me through exactly what the event loop does in one full turn, including where rendering fits.',
      'Why does a promise callback always run before a setTimeout(0) callback scheduled at the same time?',
      'Why can\'t a try/catch block catch an error thrown inside a setTimeout callback?',
      'How does the browser event loop differ from the Node.js event loop?',
      'If the call stack is never empty, what happens to queued tasks and to rendering?',
    ],
  },
  {
    slug: 'macrotask-queue',
    name: 'Macrotask (Task) Queue',
    category: 'async',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'The queue for timers, I/O, and UI events; exactly ONE task runs per event-loop turn, with a full microtask drain and a possible render between tasks.',
    explanation: [
      'Macrotasks (spec name: tasks) are the coarse units of work — timer callbacks, click handlers, message events, I/O completions — and the loop dequeues exactly one per turn, never batches.',
      'Between any two macrotasks the engine drains the whole microtask queue and the browser gets an opportunity to render, which is why breaking work into macrotask-sized chunks keeps a page responsive.',
      'The one-per-turn rule exists to guarantee fairness: input events and rendering can interleave between your tasks instead of waiting for an arbitrary batch to finish.',
      'Interviewers ask it together with the microtask queue because the macro/micro distinction is what actually decides output order in every event-loop prediction question.',
      'The common mistake is assuming several queued setTimeout callbacks run back-to-back in one turn, or saying promises and timers share a single queue — both instantly reveal a shallow model.',
    ],
    prerequisites: ['event-loop'],
    leadsTo: ['microtask-queue'],
    questions: [
      'How many macrotasks can run before the browser gets a chance to paint?',
      'What kinds of callbacks go into the macrotask queue? Name at least four sources.',
      'Two setTimeout(0) callbacks and one promise .then are queued — in what order do they run, and how many loop turns does it take?',
      'Why does chunking a long computation with setTimeout keep the UI responsive, while chunking it with promises does not?',
      'Explain the difference between a task and a microtask in the HTML event-loop specification.',
    ],
  },
  {
    slug: 'microtask-queue',
    name: 'Microtask Queue',
    category: 'async',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'The queue for promise callbacks and queueMicrotask, drained COMPLETELY after each stack run — including microtasks queued during the drain itself.',
    explanation: [
      'Whenever the call stack empties — after the synchronous script, after each macrotask, even after each listener — the engine runs every pending microtask, and any microtask enqueued during that drain also runs before the loop moves on.',
      'Promise .then/.catch/.finally callbacks, await continuations, queueMicrotask, and MutationObserver callbacks are all microtasks; this guarantees promise reactions run "as soon as possible" with consistent, render-independent timing.',
      'The exhaustive drain is a double-edged sword: it gives promises predictable ordering, but a self-perpetuating microtask chain never yields, starving rendering and input entirely.',
      'Interviewers lean on this because ".then beats setTimeout(0), always" is the crux of every output-prediction question, and explaining why requires the full two-queue model.',
      'The classic mistake is thinking microtasks and macrotasks alternate one-for-one, or not knowing that newly queued microtasks are processed in the same drain — which is exactly how infinite microtask loops freeze a UI.',
    ],
    prerequisites: ['event-loop', 'promises'],
    leadsTo: ['event-loop-starvation'],
    questions: [
      'Why does Promise.resolve().then(fn) always run before setTimeout(fn, 0), no matter what?',
      'Predict the output of nested .then calls mixed with setTimeout: Promise.resolve().then(() => { console.log("p1"); Promise.resolve().then(() => console.log("p2")); }); setTimeout(() => console.log("t"));',
      'What happens if a microtask keeps queueing another microtask forever? How is that different from setTimeout recursion?',
      'Compare queueMicrotask, setTimeout(0), and requestAnimationFrame — where does each callback land in the loop?',
      'Name everything that creates a microtask besides promise callbacks.',
      'When exactly is the microtask queue drained relative to macrotasks and rendering?',
    ],
  },
  {
    slug: 'callback-hell',
    name: 'Callback Hell → Promises',
    category: 'async',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Deeply nested error-first callbacks causing rightward drift, manual error propagation, and inversion of control — the historical motivation for promises.',
    explanation: [
      'Before promises, sequencing async steps meant nesting each continuation inside the previous callback, with every level repeating error-first checks by hand — the "pyramid of doom".',
      'The deeper problem is inversion of control: you hand your continuation to someone else\'s code and must trust it to call you exactly once, never both success and error, and not swallow exceptions.',
      'Promises fix all three structurally: flat .then chains replace nesting, rejections propagate automatically to the nearest catch, and the one-way state machine guarantees a single settlement.',
      'Interviewers ask this to check whether you understand promises as a solution to concrete problems rather than just newer syntax — "what did promises actually solve?" is the real question.',
      'The common mistake is answering only "nesting/readability" and missing the trust and error-propagation arguments, which are the substantive part of the answer.',
    ],
    prerequisites: ['callbacks'],
    leadsTo: ['promises'],
    questions: [
      'What problems did promises actually solve over callbacks, beyond making code prettier?',
      'What is inversion of control in callback-based APIs, and why is it a trust problem?',
      'Refactor a three-level nested callback sequence into a flat promise chain.',
      'In Node-style error-first callbacks, how do errors propagate through three nested operations — and how does that compare to a promise chain?',
      'A third-party library calls your callback twice — how do promises make this class of bug impossible?',
    ],
  },
  {
    slug: 'promises',
    name: 'Promises',
    category: 'async',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'A one-way state machine (pending → fulfilled or rejected) whose .then returns a new promise; callbacks always run as microtasks and errors propagate to the nearest catch.',
    explanation: [
      'A promise is a placeholder for an eventual value with exactly one irreversible transition: pending to fulfilled or pending to rejected; once settled, its value and all late subscribers see the same result.',
      'The executor function runs synchronously at construction, but every .then/.catch/.finally callback is deferred to the microtask queue — even for an already-resolved promise — guaranteeing consistent async ordering.',
      'Each .then returns a brand-new promise resolved with the callback\'s return value (and flattened if that value is itself a promise/thenable), which is what makes flat chaining work; throws and rejections skip forward to the nearest rejection handler.',
      'Interviewers treat promises as the load-bearing async topic: state-machine questions test the model, "implement Promise.all" or a retry helper tests whether you can build with it, and chaining-vs-nesting tests practical fluency.',
      'The top candidate mistakes: claiming the executor is async (it is synchronous), forgetting to return inside .then so the chain loses the value, and nesting promises inside callbacks instead of returning them — recreating callback hell with extra steps.',
    ],
    prerequisites: ['callback-hell', 'microtask-queue'],
    leadsTo: ['async-await', 'promise-combinators'],
    questions: [
      'Is the promise executor function synchronous or asynchronous? Predict: new Promise(r => { console.log(1); r(); }).then(() => console.log(3)); console.log(2);',
      'Walk through the three promise states. Can a promise transition from fulfilled to rejected?',
      'Implement Promise.all from scratch — what must it do with order, rejection, and non-promise values?',
      'What does .then return, and what happens if the callback returns another promise?',
      'Where does an error thrown in the middle of a chain go if the .catch is three links later?',
      'Implement a retry(fn, attempts) helper that retries a promise-returning function.',
    ],
  },
  {
    slug: 'promise-combinators',
    name: 'Promise Combinators',
    category: 'async',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'The four static composers: all (fail-fast), allSettled (never rejects), race (first to settle wins), any (first to fulfill, AggregateError if all reject).',
    explanation: [
      'Each combinator takes an iterable of promises and returns a single promise with a distinct settlement policy: all rejects on the first rejection, allSettled always fulfills with per-item status objects, race adopts the first settlement of any kind, and any waits for the first fulfillment.',
      'They exist because hand-coordinating N concurrent operations with counters and flags is error-prone; the combinators encode the four common "wait for many" policies as one-liners.',
      'Internally they all subscribe to every input promise and resolve the output based on the policy — which is why Promise.all preserves input order in its result array regardless of completion order.',
      'Interviewers love scenario matching ("dashboard where one widget may fail — which combinator?") and implementation questions, because allSettled is trivial if you understand promises and impossible if you memorized APIs.',
      'The common mistake is reaching for Promise.all when partial failure is acceptable — one rejected request kills the whole dashboard — or confusing race (first settle, including rejection) with any (first fulfillment).',
    ],
    prerequisites: ['promises'],
    leadsTo: ['concurrency-patterns'],
    questions: [
      'A dashboard fires five independent requests and should render whatever succeeds — which combinator do you use and why not Promise.all?',
      'Explain the difference between Promise.race and Promise.any, including what each does with rejections.',
      'Implement Promise.allSettled using only Promise.all.',
      'Promise.all([p1, p2, p3]) — p2 rejects after 10ms while p1 and p3 are still pending. What happens to p1 and p3?',
      'How would you add a timeout to a fetch call using a combinator?',
      'What is an AggregateError and which combinator produces it?',
    ],
  },
  {
    slug: 'async-await',
    name: 'async / await',
    category: 'async',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Syntax over promises: an async function always returns a promise, await suspends the function and resumes it as a microtask, and try/catch works across awaits.',
    explanation: [
      'An async function runs synchronously until the first await; there it suspends, returns its (pending) promise to the caller, and the rest of the function is resumed as a microtask once the awaited promise settles — the function is paused, but the thread never is.',
      'Under the hood it is generator-style suspension over promise chaining: each await is roughly a .then with the remainder of the function as the callback, which is why awaiting a rejected promise throws a catchable exception at the await site.',
      'The syntax exists because long .then chains scatter control flow; await restores straight-line reading order, working try/catch, loops, and early returns over asynchronous code.',
      'Interviewers ask it constantly in two forms: "what does await actually do to execution?" (testing the suspension model) and "these three awaits run sequentially — make them parallel" (testing that you start promises before awaiting, e.g. Promise.all).',
      'The classic mistakes: believing await blocks the thread, accidentally serializing independent requests by awaiting each in turn, and using forEach(async ...) — forEach ignores the returned promises, so nothing is awaited and errors vanish.',
    ],
    prerequisites: ['promises'],
    leadsTo: ['async-error-handling', 'generators'],
    questions: [
      'What exactly happens to execution when the engine hits an await? Does the thread block?',
      'Predict the output: async function f() { console.log(1); await null; console.log(3); } f(); console.log(2);',
      'These three independent fetches are awaited one after another — refactor them to run in parallel and explain the timing difference.',
      'Why is array.forEach(async item => { await save(item) }) broken, and what do you use instead?',
      'What does an async function return if you return a plain value? If you throw?',
      'Is await Promise.resolve(1) free, or does it still yield to the microtask queue?',
    ],
  },
  {
    slug: 'async-error-handling',
    name: 'Async Error Handling',
    category: 'async',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'try/catch around await, .catch placement semantics, unhandledrejection, AggregateError, and error-cause chains for asynchronous code.',
    explanation: [
      'A rejected promise becomes a throw at its await site, so try/catch finally works for async code — but only if you actually await: forgetting the await means the rejection escapes the try block entirely.',
      'In .then chains, placement is semantics: .catch handles everything before it in the chain and recovers (the chain continues fulfilled), while a rejection with no handler anywhere fires the global unhandledrejection event.',
      'The second argument of .then(onFulfilled, onRejected) differs from .then().catch(): the inline rejection handler cannot catch errors thrown by its sibling onFulfilled — a subtle but frequently tested distinction.',
      'This is asked because error paths are where production async code actually fails: interviewers probe whether you know where an error surfaces, not just how to write the happy path.',
      'The common mistakes are fire-and-forget calls inside try/catch (the catch never fires because the promise was not awaited) and assuming .catch placed before the failing step can see the failure.',
    ],
    prerequisites: ['async-await'],
    leadsTo: ['global-error-handlers'],
    questions: [
      'Where does an error thrown in the third link of a promise chain go if there are two .catch handlers, one above and one below?',
      'Your try/catch around an async call never fires even though the promise rejects — what is the most likely bug?',
      'What is the difference between .then(onSuccess, onError) and .then(onSuccess).catch(onError)?',
      'What is the unhandledrejection event, and how do error-monitoring tools use it?',
      'What does .catch return — and what state is the chain in after it runs?',
      'How do error.cause and AggregateError help when wrapping low-level errors in an API client?',
    ],
  },
  {
    slug: 'fetch-api',
    name: 'Fetch API',
    category: 'async',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Promise-based HTTP: the promise rejects only on network failure (response.ok signals 4xx/5xx), body streams can be read once, and credentials modes control cookies.',
    explanation: [
      'fetch returns a promise that fulfills as soon as response headers arrive — it rejects only for network-level failures (DNS, offline, CORS block, abort), so a 404 or 500 is a fulfilled promise and must be checked via response.ok or response.status.',
      'The body is a one-shot stream: response.json() consumes it, and a second read throws "body already read" — clone the response first if two consumers need it.',
      'This design exists because fetch models the network transaction, not your application\'s success criteria; HTTP errors are valid, completed exchanges from the protocol\'s perspective.',
      'Interviewers ask it because the "fetch doesn\'t reject on 404" gotcha has caused real production bugs in nearly every team, and follow-ups (streaming, credentials, keepalive/sendBeacon for unload analytics) scale the question to senior depth.',
      'The classic mistake is wrapping fetch in try/catch and assuming HTTP errors land in the catch — they do not — or forgetting that response.json() is itself a second promise to await.',
    ],
    prerequisites: ['promises'],
    leadsTo: ['abortcontroller', 'cors'],
    questions: [
      'Why does fetch not reject on a 404, and how do you correctly detect HTTP errors?',
      'You call response.json() twice on the same response — what happens and how do you work around it?',
      'When exactly does the promise returned by fetch reject? List the cases.',
      'How do you send cookies with a cross-origin fetch, and what must the server do in return?',
      'How do you reliably send analytics when the user closes the tab — and why does plain fetch fail there?',
      'Write a fetchJson wrapper that converts HTTP errors into rejected promises with useful error info.',
    ],
  },
  {
    slug: 'abortcontroller',
    name: 'AbortController & Cancellation',
    category: 'async',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 4,
    overlooked: true,
    definition:
      'Signal-based cancellation for fetch, event listeners, and custom async work — the fix for stale-response races and unmount leaks.',
    explanation: [
      'An AbortController owns an AbortSignal; passing the signal to fetch (or addEventListener, or your own async code) and calling controller.abort() rejects the pending operation with an AbortError and lets the browser actually drop the network request.',
      'It exists because promises are not cancellable by design — without an external signal there is no way to tell in-flight work "stop", which is why search-as-you-type races and post-unmount setState leaks were endemic.',
      'The canonical pattern is one controller per request generation: abort the previous controller before issuing the next request, guaranteeing only the latest response can ever resolve.',
      'Beyond fetch, a single signal passed as { signal } to many addEventListener calls removes them all on one abort() — a clean teardown idiom most candidates have never seen.',
      'This is a flagship overlooked topic: interviewers use it to separate seniors who have fixed stale-response bugs from candidates who only ever awaited the happy path, and the common mistake is "solving" the race by checking a boolean flag while the wasted request still completes.',
    ],
    prerequisites: ['fetch-api'],
    leadsTo: ['concurrency-patterns', 'memory-leaks'],
    questions: [
      'A search box fires a request per keystroke and results sometimes arrive out of order — fix it properly with AbortController.',
      'How does aborting a fetch surface in your code — what rejects, and with what error?',
      'How can you remove ten different event listeners with a single call using an AbortSignal?',
      'Why can\'t promises be cancelled directly, and how does the signal pattern work around that?',
      'Implement a fetchWithTimeout using AbortSignal (bonus: AbortSignal.timeout).',
      'How do you distinguish an intentional abort from a real network failure in a catch block?',
    ],
  },
  {
    slug: 'concurrency-patterns',
    name: 'Concurrency Patterns',
    category: 'async',
    level: 'senior',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Composable async control: pools running N tasks at a time, timeouts via race, retries with exponential backoff and jitter, and sequential-vs-parallel composition.',
    explanation: [
      'These are the patterns built from promise primitives that production code actually needs: a worker-pool that keeps exactly N requests in flight, a race against a timer for timeouts, and retry loops whose delays grow exponentially with random jitter to avoid thundering herds.',
      'They exist because the naive extremes both fail — firing 100 requests at once with Promise.all exhausts connections and trips rate limits, while strictly sequential awaits are needlessly slow.',
      'A pool is just bookkeeping over promises: start N tasks, and whenever one settles, pull the next from the queue until empty — no library required, which is exactly why it is a popular implement-it question.',
      'Interviewers use "100 requests, max 5 concurrent" as a senior litmus test: it requires fluent promise composition under constraints, not API recall.',
      'The common mistakes are awaiting inside a plain for loop (concurrency of 1, not N), confusing batching (waves of 5 with idle gaps) with true pooling (always 5 in flight), and retrying without backoff or jitter.',
    ],
    prerequisites: ['promise-combinators'],
    leadsTo: ['abortcontroller'],
    questions: [
      'Run 100 requests with at most 5 in flight at any moment — implement the pool from scratch.',
      'Add a timeout to any promise-returning function. What happens to the underlying operation when the timeout wins?',
      'Implement retry with exponential backoff — why is jitter essential at scale?',
      'What is the difference between processing in batches of 5 and a true concurrency pool of 5?',
      'Given a list of dependent and independent async steps, how do you decide what runs sequentially vs in parallel?',
      'How would you combine a pool, per-request timeout, and retry into one robust request layer?',
    ],
  },
  {
    slug: 'event-loop-starvation',
    name: 'Event-Loop Starvation',
    category: 'async',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    overlooked: true,
    definition:
      'Heavy or infinite microtask chains and long tasks that block rendering and input; chunking and yielding back to the loop restore responsiveness.',
    explanation: [
      'Because the microtask queue is drained exhaustively before the loop proceeds, a promise chain that keeps scheduling more microtasks never lets the browser render or handle input — the page is frozen even though no single function is "long".',
      'The same symptom comes from long macrotasks: any task over ~50ms delays input handling and damages INP, since rendering and events can only interleave between tasks.',
      'The fix is yielding: break work into chunks and reschedule with a macrotask (setTimeout, or scheduler.yield where available) so rendering and input get a turn — which is precisely why setTimeout-based recursion does not freeze the UI while microtask recursion does.',
      'Interviewers use this as the differentiator between candidates who can recite "microtasks run first" and those who understand the consequence: an unbounded microtask drain starves everything else.',
      'The classic mistake is "fixing" a blocking loop by sprinkling await Promise.resolve() into it — that yields only to other microtasks, not to rendering, so the freeze remains.',
    ],
    prerequisites: ['microtask-queue'],
    leadsTo: ['long-task-scheduling'],
    questions: [
      'A recursive promise chain froze the UI, but the same recursion via setTimeout did not — explain precisely why.',
      'Why does await Promise.resolve() inside a heavy loop fail to unblock rendering?',
      'What counts as a "long task", and what user-facing metric does it damage?',
      'You must process a huge dataset on the main thread — show how you would chunk and yield to keep input responsive.',
      'How would you detect event-loop starvation in production?',
    ],
  },

  // ── 8. Iteration & Collections ─────────────────────────────────────────────
  {
    slug: 'array-methods',
    name: 'Array Methods',
    category: 'iteration-collections',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'map/filter/reduce/find/some/every/flat and the mutating vs non-mutating split (sort vs toSorted); reduce can build anything.',
    explanation: [
      'These higher-order methods iterate the array and invoke your callback per element: map transforms one-to-one, filter selects, reduce folds everything into a single accumulator — and since map/filter return new arrays, they chain cleanly.',
      'The mutating/non-mutating split is the practical trap: sort, reverse, and splice mutate in place (sort also coerces to strings by default), while the modern toSorted/toReversed/toSpliced return copies — mutation bugs in shared state are a constant source of production defects.',
      'They exist to replace index-juggling for loops with declarative intent, and they are the everyday application of first-class functions — which is why interviewers treat them as a fluency floor.',
      'Classic interview tasks are compositional: groupBy with reduce, implement map in terms of reduce, dedupe and transform pipelines — testing that you understand the accumulator pattern rather than memorizing signatures.',
      'Common mistakes: forgetting reduce\'s initial value (the first element silently becomes the accumulator, breaking empty arrays), using map where forEach is meant (discarding the result) and vice versa, and not knowing [1,10,2].sort() yields [1,10,2] order by string comparison.',
    ],
    prerequisites: ['callbacks'],
    leadsTo: ['higher-order-functions'],
    questions: [
      'Implement groupBy(arr, keyFn) using reduce.',
      'Implement map using only reduce.',
      'Which array methods mutate the original array? What are the modern non-mutating alternatives?',
      'Why does [1, 10, 2].sort() return [1, 10, 2], and how do you sort numbers correctly?',
      'What happens when you call reduce on an empty array without an initial value?',
      'When would you choose some/every over filter for a validity check, and why?',
    ],
  },
  {
    slug: 'map-vs-object',
    name: 'Map vs Object',
    category: 'iteration-collections',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Map offers any key type, guaranteed insertion order, .size, no prototype risk, and faster add/delete churn; Object wins for literals, fixed shapes, and JSON.',
    explanation: [
      'Object keys are coerced to strings (or symbols) and the object drags a prototype chain along; Map stores keys of any type — objects, functions, NaN — by identity, tracks size natively, and iterates in guaranteed insertion order.',
      'Map exists because using plain objects as dictionaries has sharp edges: a user-supplied key like "__proto__" or "constructor" collides with inherited properties — the prototype-pollution angle — while Map keys can never touch the prototype.',
      'Engines also optimize them differently: objects are tuned for fixed shapes (hidden classes), so heavy key add/delete churn deoptimizes them, whereas Map is built as a hash map for exactly that workload.',
      'Interviewers ask it to test whether you choose data structures deliberately; the security follow-up about attacker-controlled keys is what elevates the answer to senior level.',
      'The common mistake is defaulting to objects for everything and then hitting the gotchas: no easy size, string-coerced keys, prototype collisions, and JSON.stringify silently producing {} for a Map.',
    ],
    prerequisites: ['object-creation'],
    leadsTo: ['weakmap-weakset', 'json-serialization'],
    questions: [
      'When is Map clearly the better choice than a plain object? Give at least three concrete reasons.',
      'What happens when you use an object as a key in a plain object vs in a Map?',
      'How does using a plain object as a dictionary for user-supplied keys relate to prototype pollution?',
      'Why might an object with heavy key addition and deletion be slower than a Map in V8?',
      'You need to serialize a keyed collection to JSON — which structure do you pick and what conversion is needed for the other?',
    ],
  },
  {
    slug: 'set-operations',
    name: 'Set',
    category: 'iteration-collections',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'A collection of unique values with O(1) membership checks; powers the [...new Set(arr)] dedupe idiom and union/intersection patterns.',
    explanation: [
      'Set stores unique values using SameValueZero equality (so NaN equals NaN, but two distinct objects are always different entries) and offers constant-time add/has/delete backed by a hash table.',
      'It exists because uniqueness and membership with arrays cost O(n) per includes call — converting a lookup list to a Set turns an accidental O(n²) filter into O(n), a staple performance fix.',
      'Union, intersection, and difference fall out of combining Sets with spread and filter (with newer engines shipping native set methods), and [...new Set(arr)] is the canonical dedupe one-liner.',
      'Interviewers like the "dedupe objects by key" follow-up because it exposes the reference-equality limit: a Set of objects never deduplicates by content, so the answer is a Map keyed by the field.',
      'The common mistake is exactly that — expecting new Set(arrayOfObjects) to remove "duplicate" objects — or repeatedly using array.includes inside a loop where a Set lookup was the point.',
    ],
    prerequisites: ['array-methods'],
    leadsTo: ['map-vs-object'],
    questions: [
      'Dedupe an array of primitives in one line. Now dedupe an array of objects by id — why is Set alone not enough?',
      'What equality rule does Set use for membership? How does it treat NaN and -0?',
      'Implement union, intersection, and difference for two Sets.',
      'You call arr1.filter(x => arr2.includes(x)) on large arrays — what is the complexity and how does Set fix it?',
      'When would you choose a Set over an array even though you also need iteration?',
    ],
  },
  {
    slug: 'iterables-iterators',
    name: 'Iteration Protocols',
    category: 'iteration-collections',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    overlooked: true,
    definition:
      'Symbol.iterator returning an object with next() powers for...of, spread, and destructuring; any object can opt in to iteration.',
    explanation: [
      'The protocol has two halves: an iterable exposes [Symbol.iterator]() returning an iterator, and the iterator exposes next() returning { value, done } — for...of, spread, Array.from, destructuring, and Promise.all all just call this protocol.',
      'It exists so the language\'s iteration constructs work over one interface instead of special-casing each collection: arrays, strings, Map, Set, NodeLists, and your own objects all speak the same contract.',
      'Plain objects are deliberately not iterable — there is no single right order or unit (keys? values? entries?) — which is why for...of over an object throws and you reach for Object.entries instead.',
      'Generators are the practical shortcut: a function* implements both halves of the protocol for you, making custom and lazy/infinite sequences trivial to expose.',
      'This is an overlooked, protocol-level topic: interviewers use "make this object work with for...of" to find candidates who understand the mechanism, and the common mistake is conflating iterable with iterator or hand-rolling next() with the wrong { value, done } shape.',
    ],
    prerequisites: ['symbol'],
    leadsTo: ['generators'],
    questions: [
      'Make a custom range object work with for...of — write the [Symbol.iterator] implementation by hand.',
      'Why is a plain object not iterable while a Map is? How do you iterate an object\'s entries anyway?',
      'What is the difference between an iterable and an iterator? Can something be both?',
      'Which language features and APIs consume the iteration protocol under the hood?',
      'Rewrite your hand-rolled iterator using a generator — what does the generator handle for you?',
    ],
  },
  {
    slug: 'json-serialization',
    name: 'JSON & Serialization',
    category: 'iteration-collections',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'JSON.stringify/parse with replacer and reviver hooks; loses functions, undefined, and symbols; Dates become strings; circular references throw.',
    explanation: [
      'JSON.stringify walks the value and emits the JSON subset: functions, undefined, and symbol-keyed/valued properties are dropped (or nulled inside arrays), Dates serialize via toJSON into ISO strings, NaN/Infinity become null, and a circular reference throws TypeError.',
      'Because parse has no type information, round-trips are lossy: Date strings stay strings, Map and Set become {} , and BigInt does not serialize at all — it throws.',
      'The replacer and reviver hooks exist for exactly this gap: custom encoding on the way out and rehydration on the way in (e.g., tagging Maps, reviving date strings).',
      'Interviewers ask "what survives a round-trip?" because the JSON.parse(JSON.stringify(x)) deep-clone idiom is everywhere in legacy code and silently corrupts Dates, drops methods, and explodes on cycles — structuredClone is the modern answer for cloning.',
      'The common mistake is treating JSON round-tripping as a faithful deep copy, or being unable to explain why stringify of a Map yields "{}" and how to serialize it properly.',
    ],
    prerequisites: ['object-creation'],
    leadsTo: ['shallow-vs-deep-copy', 'bigint'],
    questions: [
      'What survives a JSON.parse(JSON.stringify(obj)) round-trip — and what is silently lost or transformed?',
      'How do you serialize a Map or Set to JSON and restore it on parse?',
      'What does JSON.stringify do with undefined, functions, NaN, Dates, and circular references respectively?',
      'What are the replacer and reviver parameters for? Show a practical use of each.',
      'Why does JSON.stringify({ n: 10n }) throw, and what are your options for BigInt-bearing API payloads?',
      'When should you use structuredClone instead of a JSON round-trip?',
    ],
  },
  {
    slug: 'typed-arrays',
    name: 'TypedArrays & ArrayBuffer',
    category: 'iteration-collections',
    level: 'expert',
    importance: 2,
    interviewFrequency: 1,
    realWorldUsage: 2,
    definition:
      'Fixed-type binary views (Uint8Array, Float32Array, DataView) over raw ArrayBuffer memory — the substrate for files, WebGL, WASM, and crypto.',
    explanation: [
      'An ArrayBuffer is a raw, fixed-length block of bytes with no read/write API of its own; TypedArrays and DataView are views that interpret those bytes as specific numeric types, and multiple views can alias the same buffer.',
      'They exist because regular arrays of boxed doubles cannot represent binary data efficiently or predictably — file contents, image pixels, audio samples, network protocols, WebGL buffers, and WASM memory all require contiguous typed bytes.',
      'Unlike normal arrays they are fixed-length and type-coercing on write, and DataView adds explicit endianness control for parsing binary file formats.',
      'They matter for workers too: ArrayBuffers can be transferred between threads zero-copy (and SharedArrayBuffer truly shared), making them the backbone of off-main-thread data processing.',
      'Rarely asked outside specialist roles, but interviewers probing graphics, media, or performance work expect you to know the buffer/view split; the common mistake is conflating the buffer (storage) with the view (interpretation).',
    ],
    prerequisites: ['array-methods'],
    leadsTo: ['web-workers'],
    questions: [
      'What is the relationship between an ArrayBuffer and a Uint8Array? Can two views share one buffer?',
      'When have you actually needed raw bytes in the browser? Name three APIs that hand you TypedArrays.',
      'How does a TypedArray differ from a normal array in length, element types, and write behavior?',
      'What is DataView for, and when does endianness matter in frontend code?',
      'How do you pass a large binary payload to a Web Worker without copying it?',
    ],
  },

  // ── 9. Modules ─────────────────────────────────────────────────────────────
  {
    slug: 'commonjs',
    name: 'CommonJS',
    category: 'modules',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Node\'s original module system: synchronous runtime require() and module.exports, where importers receive value snapshots, not live bindings.',
    explanation: [
      'require() is just a function call executed at runtime: it resolves the specifier, loads and runs the module synchronously (once — results are cached in require.cache), and returns whatever was assigned to module.exports.',
      'Because require is ordinary code, it can sit inside an if-block or take a computed path — which made CJS flexible on a server with synchronous file access, and impossible to load natively over the network in browsers.',
      'Exports are snapshots: an importer holds a reference to the exported value at require time, so a module reassigning its export later is invisible to existing importers — the key contrast with ESM live bindings.',
      'That same dynamism is why CJS cannot be statically tree-shaken: a bundler cannot prove which properties of module.exports are used when requires are conditional and exports are mutable objects — the canonical interview question on this topic.',
      'The common mistake is treating CJS and ESM as interchangeable syntax; candidates who cannot explain the snapshot-vs-live-binding or static-vs-dynamic distinction miss the entire point of the modules section.',
    ],
    prerequisites: ['iife'],
    leadsTo: ['esm-vs-cjs'],
    questions: [
      'Why can\'t CommonJS modules be statically tree-shaken, while ES modules can?',
      'What does require() actually do step by step, and what role does require.cache play?',
      'A CJS module reassigns module.exports after being required — what do existing importers see, and why?',
      'What is the difference between module.exports and exports, and what is the classic bug with reassigning exports?',
      'Why does require() being synchronous make CommonJS unsuitable for browsers?',
    ],
  },
  {
    slug: 'es-modules',
    name: 'ES Modules',
    category: 'modules',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'The standard module system: static, hoisted imports with live bindings, strict mode by default, async loading in browsers, and exactly one evaluation per module.',
    explanation: [
      'ESM loading has three phases — construction (fetch and parse the whole graph), instantiation (allocate memory and wire imports to exports), and evaluation (run module bodies) — which is why import declarations are hoisted and the dependency graph is known before any code runs.',
      'Exports are live bindings: an import is a read-only view of the exporting module\'s variable, so when a module updates an exported counter, every importer observes the new value — unlike CJS snapshots.',
      'Each module is evaluated exactly once per realm and cached, which makes every ES module a de-facto singleton — exporting an instance is the modern singleton pattern with zero ceremony.',
      'The static structure (no conditional or computed import declarations) is what enables tree-shaking and reliable static analysis; modules also run in strict mode automatically and load asynchronously in browsers.',
      'Interviewers ask "what is a live binding?" because it explains a family of real bugs and behaviors — circular-dependency survival, mutable counters across files — and the common mistake is describing imports as copied values or believing you can reassign an imported binding (it throws).',
    ],
    prerequisites: ['commonjs'],
    leadsTo: ['dynamic-imports', 'tree-shaking'],
    questions: [
      'What is a live binding? Show code where an importer observes a change to an exported variable.',
      'Why are ES modules singletons, and how does that replace the classic singleton pattern?',
      'Why must import declarations be at the top level with static string specifiers?',
      'What happens if two different files import the same module — how many times does it execute?',
      'Can you reassign an imported binding? What error do you get and why?',
      'Why are ES modules automatically in strict mode, and how do they load differently in the browser than classic scripts?',
    ],
  },
  {
    slug: 'esm-vs-cjs',
    name: 'ESM vs CJS',
    category: 'modules',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'The behavioral split: static vs dynamic structure, live bindings vs value copies, async vs sync loading, plus interop pitfalls like default exports and __dirname.',
    explanation: [
      'The differences are semantic, not cosmetic: ESM resolves its whole graph before evaluation with live, read-only bindings; CJS resolves at runtime, call by call, handing out snapshots of mutable export objects.',
      'Loading models diverge too — CJS is synchronous (fine on a server filesystem), ESM is async-capable by design (necessary for the network), which is why top-level await exists only in ESM.',
      'Interop is where teams bleed: importing CJS from ESM merges module.exports into a synthetic default unevenly across tools, named-export detection from CJS is heuristic, and ESM lacks __dirname/require (import.meta.url replaces them) — the "dual package hazard" can even load both copies of one library.',
      'Node decides which system a file uses via extension (.mjs/.cjs) and the nearest package.json "type" field, a mechanism behind a whole class of ERR_REQUIRE_ESM-style errors.',
      'Interviewers ask for "behavioral differences, not just syntax" precisely to filter out candidates who answer "import vs require"; the common mistake is exactly that surface-level answer, with no mention of bindings, timing, or tree-shaking consequences.',
    ],
    prerequisites: ['es-modules'],
    leadsTo: ['bundlers-module-resolution'],
    questions: [
      'List the key behavioral differences between ESM and CJS — beyond import/require syntax.',
      'What is the difference between live bindings and value snapshots? Demonstrate with a counter module in both systems.',
      'What interop traps have you hit importing a CJS package from ESM code (default exports, named exports)?',
      'How does Node decide whether a .js file is ESM or CJS?',
      'Why does __dirname not exist in ESM, and what do you use instead?',
      'What is the dual package hazard?',
    ],
  },
  {
    slug: 'dynamic-imports',
    name: 'Dynamic import()',
    category: 'modules',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'A promise-returning, runtime import of a module — the primitive behind route-level code splitting and conditional loading.',
    explanation: [
      'import(specifier) is expression syntax usable anywhere — inside conditions, handlers, with computed paths — that fetches, instantiates, and evaluates the module on demand and fulfills with its namespace object.',
      'It exists because fully static graphs force you to ship everything up front; dynamic import lets rarely used or route-specific code load only when needed, while the static graph remains analyzable for everything else.',
      'Bundlers treat each import() as a split point, emitting a separate chunk and turning the expression into a runtime chunk loader — this is literally what router lazy routes (() => import("./Page.vue")) compile to.',
      'Because it returns a promise, real code must handle the failure path: chunk-load errors after a deploy (stale hashed filenames) are a classic production incident that error boundaries and retry logic exist for.',
      'Interviewers ask "how does your router lazy-load pages under the hood?" to check you see through framework sugar; common mistakes are thinking import() of an already-loaded module re-executes it (the cache still applies) and forgetting that a default export arrives as namespace.default.',
    ],
    prerequisites: ['es-modules', 'promises'],
    leadsTo: ['code-splitting-lazy-loading'],
    questions: [
      'How does () => import("./AdminPage.vue") in a router config actually work, from bundler to runtime?',
      'What does the promise returned by import() resolve to, and how do you get at a default export?',
      'How is dynamic import() different from a hoisted static import in timing, placement, and analyzability?',
      'A user gets "Failed to fetch dynamically imported module" right after you deploy — what happened and how do you handle it?',
      'Does calling import() twice for the same module run it twice? Why or why not?',
      'When would you use dynamic import for something other than code splitting?',
    ],
  },
  {
    slug: 'top-level-await',
    name: 'Top-Level await',
    category: 'modules',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 2,
    overlooked: true,
    definition:
      'await at module scope — the module evaluates asynchronously and every importer\'s evaluation is blocked until it settles; convenient and dangerous.',
    explanation: [
      'TLA makes a module\'s evaluation itself asynchronous: the engine treats the module body like an async function, and any module importing it pauses its own evaluation until the awaited work settles.',
      'It exists to remove the old workarounds — exporting init() promises or wrapping module setup in IIFEs — for genuinely async initialization like loading config, WASM, or a database handle before exports are usable.',
      'The danger is graph-wide: the blocking propagates transitively up the import chain, so one slow awaited fetch deep in a dependency delays the startup of everything above it, invisibly.',
      'It also interacts badly with circular dependencies — a cycle through a TLA module can deadlock or surface unexpected evaluation orders — which is why it is paired with circular-dependencies in the doc.',
      'This is an overlooked, ESM-only feature (CJS cannot support it because require is synchronous); the common mistake is sprinkling TLA for convenience without realizing every importer now waits, turning a utility module into a startup bottleneck.',
    ],
    prerequisites: ['es-modules', 'async-await'],
    leadsTo: ['circular-dependencies'],
    questions: [
      'What does top-level await do to the module graph\'s evaluation order?',
      'Why is top-level await only possible in ES modules and not in CommonJS?',
      'A utility module awaits a config fetch at top level — what is the startup cost for the rest of the app?',
      'What problem did TLA solve compared to exporting an init() promise? When is the old pattern still better?',
      'How can top-level await interact badly with circular dependencies?',
    ],
  },
  {
    slug: 'tree-shaking',
    name: 'Tree Shaking',
    category: 'modules',
    level: 'senior',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Dead-export elimination enabled by ESM\'s static structure; defeated by side effects and namespace re-export patterns.',
    explanation: [
      'Because ESM imports and exports are static, a bundler can build the full dependency graph at compile time, mark which exports are actually referenced, and drop the rest — dead-code elimination at module granularity.',
      'It exists because libraries export far more than any app uses; without shaking, importing one function from a utility library ships the whole thing — the "one icon pulled in 2MB" problem.',
      'It is fragile by nature: the bundler must prove removal is safe, so module-level side effects (polyfills, CSS imports, registry mutations), CJS dependencies, and dynamic property access all force code to be retained.',
      'The sideEffects field in package.json is the library author\'s pledge that unimported modules can be dropped wholesale; "barrel" index files that re-export everything are the classic shaking-killer in app code.',
      'Interviewers ask the 2MB-icon question to test bundle-debugging instincts; the common mistake is believing tree shaking is automatic and infallible, rather than a static analysis you can (and regularly do) accidentally defeat.',
    ],
    prerequisites: ['es-modules'],
    leadsTo: ['bundlers-module-resolution'],
    questions: [
      'You import one icon and your bundle grows by 2MB — diagnose and fix it.',
      'What property of ES modules makes tree shaking possible, and why does it fail on CommonJS?',
      'What does "sideEffects": false in package.json promise the bundler, and what breaks if the promise is false?',
      'How do barrel files (index.ts re-exporting everything) interfere with tree shaking?',
      'What counts as a side effect that prevents a module from being dropped?',
      'How would you verify with a bundle analyzer that a dependency is actually being shaken?',
    ],
  },
  {
    slug: 'circular-dependencies',
    name: 'Circular Dependencies',
    category: 'modules',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    overlooked: true,
    definition:
      'Module cycles: ESM survives via hoisted live bindings (possibly hitting TDZ at access time); CJS returns partial exports — both cause subtle, intermittent undefineds.',
    explanation: [
      'In a cycle, some module must evaluate before its dependency has finished: CJS handles this by returning whatever has been assigned to module.exports so far — a partial snapshot frozen at require time — while ESM wires live bindings during instantiation so values that exist by access time resolve correctly.',
      'ESM\'s function declarations are hoisted and thus safe to call across a cycle, but accessing a let/const or class binding before the exporting module has evaluated it throws a ReferenceError — module-level TDZ.',
      'The failure mode is what makes this senior-signal: nothing crashes at the cycle itself; instead an import "is undefined only sometimes," depending on which module the app happened to load first.',
      'Cycles usually indicate a missing third module both sides should depend on; fixes are extracting shared code, inverting the dependency, or deferring the access into a function called after evaluation.',
      'This is an overlooked topic from the doc\'s differentiator list; the common mistake is hunting the bug in the importing file rather than recognizing the load-order signature of a cycle (and not knowing tools like madge can find them mechanically).',
    ],
    prerequisites: ['esm-vs-cjs'],
    leadsTo: ['top-level-await'],
    questions: [
      'An imported value is undefined only in some entry points but not others — walk through how a circular dependency causes this.',
      'How do CommonJS and ESM each behave when modules import each other in a cycle?',
      'Why is calling an imported function across a cycle often fine in ESM while reading an imported const throws?',
      'How would you detect circular dependencies in a large codebase, and what are three ways to break one?',
      'Module A needs a constant from B and B needs a function from A — restructure this without the cycle.',
    ],
  },
  {
    slug: 'bundlers-module-resolution',
    name: 'Bundlers & Resolution',
    category: 'modules',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'How Vite and webpack resolve specifiers, transform source, split chunks, and serve in dev — bundled graphs vs native ESM dev serving.',
    explanation: [
      'A bundler\'s pipeline is resolve → load → transform → chunk: bare specifiers like "vue" are mapped through node_modules using package.json fields ("exports", "module", "main") and aliases, sources pass through transforms (TS, JSX, Vue SFCs), and the graph is split into chunks at entry and dynamic-import boundaries.',
      'Bundling exists because raw module graphs do not survive contact with production: bare specifiers do not resolve in browsers, hundreds of waterfall requests are slow, and TS/SFC files need compilation — the bundler closes the gap between authoring and delivery.',
      'Vite\'s dev-mode trick is skipping the bundle: it serves your source as native ESM transformed on demand per request, pre-bundling only node_modules dependencies with esbuild — which is why dev startup is near-instant regardless of app size, while production builds still use Rollup for optimized chunks.',
      'webpack instead bundles up front and rebuilds incrementally, trading startup time for uniform dev/prod behavior; knowing this tradeoff is what the "what does Vite actually do differently in dev?" question is probing.',
      'The common mistake is treating the toolchain as magic: candidates who cannot explain how "vue" becomes a file path, or why a package\'s "exports" map breaks deep imports, struggle the moment a build fails.',
    ],
    prerequisites: ['tree-shaking', 'dynamic-imports'],
    leadsTo: ['code-splitting-lazy-loading'],
    questions: [
      'What does Vite actually do differently in dev mode compared to webpack, and why does it start so fast?',
      'Walk through how import { ref } from "vue" becomes an actual file on disk — which package.json fields participate?',
      'Why does Vite pre-bundle dependencies with esbuild in dev, and what problem does that solve?',
      'How does a bundler decide chunk boundaries, and how do you influence them?',
      'A deep import like "lib/dist/helper" worked last year and now throws — what changed in the package\'s resolution config?',
      'Why do dev and prod behave differently in Vite, and what class of bug does that create?',
    ],
  },
]
