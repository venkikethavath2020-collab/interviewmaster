import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ───────────────────────── 6. Lifecycle & Rendering ─────────────────────────
  {
    slug: 'lifecycle-hooks',
    name: 'Lifecycle Hooks',
    category: 'lifecycle-rendering',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Callbacks Vue runs at defined points in a component’s life — creation, mounting, updating, and unmounting.',
    explanation: [
      'A component moves through phases: it is created (state and computed set up), mounted (rendered into the real DOM), updated (re-rendered after reactive changes), and unmounted (removed and cleaned up).',
      'Hooks let you run code at the right moment: fetch data or read the DOM after `mounted`, and remove listeners/timers before `unmounted` to avoid leaks.',
      'They exist because side effects (network, DOM measurement, subscriptions) must be tied to a deterministic point in the component’s existence, not run arbitrarily during setup.',
      'The classic mistake is trying to read `$el`/DOM nodes in `created`/`setup` — the DOM does not exist yet; you must wait for `mounted`/`onMounted`.',
      'A senior nuance: cleanup symmetry matters — anything you start in mounted (intervals, observers, sockets) must be torn down in unmounted, or you leak memory and get duplicate handlers on re-mount.',
    ],
    prerequisites: ['vue-instance-app'],
    leadsTo: ['mounting-process', 'lifecycle-hooks-composition', 'keepalive'],
    questions: [
      'List the Vue 3 lifecycle hooks in order and what each is for.',
      'Why can’t you access the DOM in `created`/`setup`, and where should you?',
      'Where do you put a data fetch, and where do you clean up a subscription?',
      'What happens to lifecycle hooks when a component is wrapped in `<KeepAlive>`?',
      'How do Composition API lifecycle hooks (`onMounted`) map to Options API ones?',
    ],
  },
  {
    slug: 'mounting-process',
    name: 'Mounting Process',
    category: 'lifecycle-rendering',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'How an app goes from `createApp(root)` and `.mount(el)` to a live, reactive DOM tree.',
    explanation: [
      'Calling `createApp` builds an application instance; `.mount(selector)` compiles the root component’s template to a render function, runs it to produce a virtual DOM tree, and creates the real DOM from it.',
      'During this, reactive effects are registered so that future state changes will re-run the render function and patch the DOM.',
      'It exists as a distinct phase so the framework can do one-time setup (app config, plugins, provides) before any rendering happens.',
      'A common misconception is that `mounted` means children are also mounted in every case — parent `mounted` fires after its children mount, so order matters for DOM-dependent logic.',
      'Senior insight: in SSR the "mount" on the client is actually *hydration* — Vue reuses existing server-rendered DOM instead of recreating it, which changes the timing and the mismatch failure modes.',
    ],
    prerequisites: ['lifecycle-hooks', 'vue-instance-app'],
    leadsTo: ['virtual-dom', 'ssr-hydration'],
    questions: [
      'Walk through what happens between `createApp` and a visible UI.',
      'In what order do parent and child `mounted` hooks fire?',
      'How does mounting differ from hydration in SSR?',
      'When is the render effect first registered, and why does that matter?',
      'What setup must happen before `.mount()` (plugins, provides) and why?',
    ],
  },
  {
    slug: 'virtual-dom',
    name: 'Virtual DOM',
    category: 'lifecycle-rendering',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 3,
    definition:
      'An in-memory tree of VNodes that Vue diffs against the previous tree to compute minimal real-DOM updates.',
    explanation: [
      'Instead of touching the DOM directly on every change, Vue builds a lightweight JavaScript description of the UI (VNodes), and on update produces a new tree and "diffs" it against the old one.',
      'Only the differences are patched into the real DOM, because real DOM operations are expensive relative to comparing plain objects.',
      'It exists to make declarative rendering efficient — you describe the *what*, and the diff algorithm figures out the minimal *how*.',
      'A frequent misconception is that the virtual DOM is always faster than direct DOM manipulation; it is a trade-off that buys maintainability and batching, and Vue 3 adds compile-time hints (patch flags) to skip diffing static parts.',
      'Senior nuance: stable `key`s on lists let the diff match nodes correctly; without them Vue falls back to index-based patching that can corrupt component state and inputs.',
    ],
    prerequisites: ['declarative-rendering', 'v-for-key'],
    leadsTo: ['render-functions', 'reactivity-render-link', 'list-rendering-performance'],
    questions: [
      'What is the virtual DOM and why does Vue use one?',
      'Is the virtual DOM always faster than direct DOM manipulation? Defend your answer.',
      'How do Vue 3 patch flags / static hoisting make diffing cheaper?',
      'Why are stable keys critical to the diff algorithm?',
      'How does a reactive change end up as a DOM patch?',
    ],
  },
  {
    slug: 'render-functions',
    name: 'Render Functions',
    category: 'lifecycle-rendering',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Authoring a component’s output in JavaScript with `h()` (createVNode) instead of a template.',
    explanation: [
      'A render function returns VNodes built with `h(type, props, children)`, giving you the full power of JavaScript for highly dynamic output.',
      'Templates actually compile down to render functions, so this is the lower-level API the compiler targets.',
      'They exist for cases templates handle awkwardly — programmatically generated structures, wrapper components, or libraries that need maximal flexibility.',
      'The common mistake is reaching for render functions too early; templates are more optimizable (patch flags, hoisting) and more readable for the vast majority of components.',
      'Senior nuance: hand-written render functions opt out of many compile-time optimizations, so you trade some performance and tooling for flexibility.',
    ],
    prerequisites: ['virtual-dom'],
    leadsTo: ['jsx-in-vue'],
    questions: [
      'When would you use a render function instead of a template?',
      'What does `h()` do and what are its arguments?',
      'What optimizations do you lose by hand-writing render functions?',
      'How do templates relate to render functions under the hood?',
      'How would you render a dynamic heading level (h1–h6) with a render function?',
    ],
  },
  {
    slug: 'jsx-in-vue',
    name: 'JSX in Vue',
    category: 'lifecycle-rendering',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Using JSX/TSX as an alternative authoring syntax that compiles to render functions.',
    explanation: [
      'With the Babel/Vite JSX plugin you can write components in JSX, which transpiles to `h()` calls just like templates compile to render functions.',
      'It appeals to teams coming from React or to highly dynamic components where JS control flow is clearer than template directives.',
      'It exists as an option because Vue’s rendering layer is syntax-agnostic — templates, render functions, and JSX all produce VNodes.',
      'A misconception is that JSX is "more powerful" — it sacrifices Vue’s template compiler optimizations and some directive ergonomics (`v-model`, `v-for` become manual).',
      'Senior nuance: mixing JSX loses static hoisting/patch-flag optimizations, so prefer it only where the dynamism genuinely warrants it.',
    ],
    prerequisites: ['render-functions'],
    leadsTo: [],
    overlooked: true,
    questions: [
      'How does JSX work in Vue and what does it compile to?',
      'What do you give up by using JSX instead of templates?',
      'How do you express `v-model`/`v-for` in Vue JSX?',
      'When is JSX a reasonable choice in a Vue codebase?',
      'How does Vue JSX differ from React JSX?',
    ],
  },
  {
    slug: 'nexttick',
    name: 'nextTick',
    category: 'lifecycle-rendering',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'A way to wait until Vue has flushed pending reactive updates to the DOM before running code.',
    explanation: [
      'Vue batches reactive changes and applies DOM updates asynchronously on the next "tick", so the DOM is not updated synchronously right after you change state.',
      '`nextTick()` (returns a promise, or takes a callback) lets you run code *after* that flush — e.g. measuring an element or focusing a newly rendered input.',
      'It exists because synchronous DOM updates on every state change would be wasteful; batching coalesces many changes into one render.',
      'The classic bug is reading the DOM immediately after setting state and seeing the *old* value because the update has not been flushed yet.',
      'Senior nuance: `nextTick` resolves after the current update cycle’s DOM patch; understanding the flush timing (and that watchers can run pre/post-flush) is key to predictable DOM-dependent code.',
    ],
    prerequisites: ['reactivity-render-link'],
    leadsTo: ['mounting-process'],
    questions: [
      'Why is the DOM not updated immediately after you change a ref?',
      'What does `nextTick` do and when do you need it?',
      'Give a real scenario where forgetting `nextTick` causes a bug.',
      'How does update batching relate to `nextTick`?',
      'What is the difference between a pre-flush and post-flush watcher in this context?',
    ],
  },
  {
    slug: 'template-compilation',
    name: 'Template Compilation',
    category: 'lifecycle-rendering',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Vue compiles templates into optimized render functions with static hoisting and patch flags.',
    explanation: [
      'At build time (or runtime with the full build), the Vue compiler turns your template string into a JavaScript render function.',
      'It analyzes which parts are static vs dynamic, hoists static VNodes out of the render function, and tags dynamic nodes with "patch flags" so the runtime only diffs what can change.',
      'This exists to make the virtual DOM fast: the compiler does work once so the runtime does less work on every update.',
      'A misconception is that templates are "interpreted" each render — they are compiled once into a function that just executes.',
      'Senior nuance: these compile-time optimizations are exactly what hand-written render functions and JSX forfeit, which is the core perf trade-off between authoring styles.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['virtual-dom', 'v-once-v-memo'],
    questions: [
      'What does the Vue template compiler produce?',
      'What are patch flags and static hoisting, and why do they matter?',
      'Why are templates often faster than hand-written render functions?',
      'Does the template get re-parsed on every render? Explain.',
      'How does compilation enable Vue 3’s "compiler-informed" virtual DOM?',
    ],
  },
  {
    slug: 'reactivity-render-link',
    name: 'Reactivity → Render Link',
    category: 'lifecycle-rendering',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'How a reactive change schedules a batched re-render of the components that depend on it.',
    explanation: [
      'Each component’s render is wrapped in a reactive effect; while rendering, every reactive value it reads is tracked as a dependency.',
      'When one of those dependencies changes, the effect is marked dirty and queued; Vue flushes the queue asynchronously, re-running only the affected components’ render functions.',
      'This link exists so the UI stays in sync with state automatically and efficiently — only components that actually used the changed value re-render.',
      'A common misconception is that changing any state re-renders the whole app; Vue’s dependency tracking scopes updates to the precise components that read that state.',
      'Senior nuance: over-reactivity (huge reactive objects, unnecessary deep tracking) widens the dependency set and causes extra renders — `shallowRef`/`markRaw` and computed memoization are the levers to narrow it.',
    ],
    prerequisites: ['proxy-reactivity', 'virtual-dom'],
    leadsTo: ['nexttick', 'reactivity-performance'],
    questions: [
      'How does changing a `ref` cause a component to re-render?',
      'Does every state change re-render the whole app? Why or why not?',
      'Why are renders batched and flushed asynchronously?',
      'What is "over-reactivity" and how does it hurt render performance?',
      'How does Vue know which components depend on a given reactive value?',
    ],
  },

  // ───────────────────────── 7. Built-in Components & Features ─────────────────────────
  {
    slug: 'keepalive',
    name: 'KeepAlive',
    category: 'built-in',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'A built-in component that caches toggled components so their state is preserved instead of destroyed.',
    explanation: [
      'Wrapping dynamic or routed components in `<KeepAlive>` keeps their instances alive when hidden, so scroll position, form input, and fetched data survive toggling.',
      'Cached components fire `onActivated`/`onDeactivated` instead of being unmounted/remounted.',
      'It exists to avoid expensive re-creation and to provide tab-like UX where switching away and back should feel instant and stateful.',
      'A common mistake is forgetting that kept-alive components do NOT re-run `mounted`/setup on reactivation, so logic placed only in `mounted` won’t re-run — use `onActivated`.',
      'Senior nuance: KeepAlive holds memory for cached instances; use `include`/`exclude`/`max` to bound the cache and avoid leaking many heavy components.',
    ],
    prerequisites: ['dynamic-components', 'lifecycle-hooks'],
    leadsTo: [],
    questions: [
      'What problem does `<KeepAlive>` solve?',
      'Which lifecycle hooks fire for a kept-alive component instead of mounted/unmounted?',
      'How do you limit what gets cached and how much?',
      'Why might data not refresh when returning to a kept-alive tab, and how do you fix it?',
      'What are the memory implications of KeepAlive?',
    ],
  },
  {
    slug: 'teleport',
    name: 'Teleport',
    category: 'built-in',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'A built-in component that renders its children into a DOM node elsewhere in the document (e.g. body).',
    explanation: [
      '`<Teleport to="body">` moves rendered DOM out of the current component’s position while keeping it logically and reactively part of the component.',
      'It is the idiomatic fix for modals, tooltips, and dropdowns that must escape parent `overflow:hidden`/`z-index`/`transform` stacking contexts.',
      'It exists because CSS stacking and clipping are determined by DOM ancestry, which can trap overlay UI inside the wrong container.',
      'A common mistake is duplicating modal markup at the app root manually; Teleport keeps the markup co-located with its logic while relocating only the output.',
      'Senior nuance: teleported content still participates in the component tree for events and reactivity, and you can toggle `disabled` to teleport conditionally (e.g. only on desktop).',
    ],
    prerequisites: ['components-basics'],
    leadsTo: [],
    questions: [
      'What problem does `<Teleport>` solve and for which UI?',
      'Does teleported content still belong to the component (events/reactivity)?',
      'Why do modals often break without Teleport (z-index/overflow)?',
      'How do you conditionally disable teleporting?',
      'Where does the teleported DOM actually end up?',
    ],
  },
  {
    slug: 'suspense',
    name: 'Suspense',
    category: 'built-in',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'A built-in (experimental) component that coordinates loading states for async dependencies in a subtree.',
    explanation: [
      '`<Suspense>` lets you show a `#fallback` slot while async components or an async `setup()` in the `#default` slot are resolving, then swaps to the content.',
      'It centralizes "is this subtree still loading?" instead of every component managing its own spinner flag.',
      'It exists to make async UI declarative and to compose with async components and async setup.',
      'A common misconception is that it is stable/finished — it is still marked experimental, so interviewers want you to know its status and caveats.',
      'Senior nuance: error handling pairs with `onErrorCaptured`/error boundaries, and nested Suspense + transitions require care to avoid flicker.',
    ],
    prerequisites: ['async-components'],
    leadsTo: [],
    overlooked: true,
    questions: [
      'What does `<Suspense>` do and what are its slots?',
      'How does it interact with async components and async setup?',
      'Is Suspense production-stable? What is its status?',
      'How do you handle errors within a Suspense boundary?',
      'When would you choose Suspense over per-component loading flags?',
    ],
  },
  {
    slug: 'transition',
    name: 'Transition',
    category: 'built-in',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'A built-in component that applies enter/leave CSS or JS animations to a single element or component.',
    explanation: [
      '`<Transition>` adds well-defined CSS classes (`v-enter-from`, `v-enter-active`, `v-leave-to`, etc.) around insertion/removal so you can animate appearance and disappearance.',
      'It works with `v-if`, `v-show`, dynamic components, and route changes.',
      'It exists to make entrance/exit animation declarative without manually wiring class toggles and transitionend listeners.',
      'A common mistake is animating an element that is never actually inserted/removed (e.g. forgetting `v-if`), so no transition fires.',
      'Senior nuance: use JS hooks for physics/spring libraries, `mode="out-in"` to avoid overlap, and be mindful of layout thrash on large lists (prefer `transform`/`opacity`).',
    ],
    prerequisites: ['v-if-v-show'],
    leadsTo: ['transition-group'],
    questions: [
      'What classes does `<Transition>` apply and when?',
      'How do you animate between two elements without overlap (`mode`)?',
      'When would you use JS transition hooks instead of CSS?',
      'Why might a transition not fire at all?',
      'Which CSS properties are cheapest to animate and why?',
    ],
  },
  {
    slug: 'transition-group',
    name: 'TransitionGroup',
    category: 'built-in',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'A built-in component that animates insertion, removal, and reordering of items in a list.',
    explanation: [
      '`<TransitionGroup>` wraps a `v-for` list and animates items as they are added, removed, or moved, including smooth FLIP move animations via a `v-move` class.',
      'Unlike `<Transition>`, it renders a real wrapper element (or none with `tag`) and requires stable `key`s.',
      'It exists to make animated lists (reorderable boards, filtered results) declarative.',
      'A common mistake is missing or unstable `key`s, which breaks move animations and item identity.',
      'Senior nuance: FLIP animations are powered by transforms; very large lists still need virtualization, and move animations can be expensive if every item shifts.',
    ],
    prerequisites: ['transition', 'v-for'],
    leadsTo: [],
    questions: [
      'How does `<TransitionGroup>` differ from `<Transition>`?',
      'Why are stable keys mandatory here?',
      'What is the `v-move` class and how do FLIP animations work?',
      'How do you animate a filtered/sorted list reordering?',
      'What are the performance limits with large animated lists?',
    ],
  },
  {
    slug: 'built-in-special-attributes',
    name: 'Built-in Special Attributes',
    category: 'built-in',
    level: 'beginner',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Framework-reserved attributes like `key`, `ref`, and `is` that control identity, access, and component type.',
    explanation: [
      '`key` gives elements/components a stable identity for the diff algorithm; `ref` registers a template ref to access a node/instance; `is` selects which component/element to render.',
      'They are not normal HTML attributes — Vue intercepts them during compilation/patching.',
      'They exist to expose low-level rendering controls (identity, access, dynamic type) through the template.',
      'A common mistake is using array index as `key`, which defeats its purpose, or expecting `ref` to be populated before mount.',
      'Senior nuance: `is` enables `<component :is>` and the native-element workaround (`<component is="vue:...">`) in in-DOM templates; understanding when each special attribute is resolved is interview-relevant.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['v-for-key', 'dynamic-components', 'template-refs'],
    overlooked: true,
    questions: [
      'What do `key`, `ref`, and `is` each do?',
      'Why are these treated specially rather than as normal attributes?',
      'When is a `ref` actually populated?',
      'Why is an array index a poor `key`?',
      'How does `is` enable dynamic components?',
    ],
  },

  // ───────────────────────── 8. Routing (Vue Router) ─────────────────────────
  {
    slug: 'vue-router-basics',
    name: 'Vue Router Basics',
    category: 'routing',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'The official client-side router: a route table, `<router-view>` outlet, and `<router-link>` navigation.',
    explanation: [
      'Vue Router maps URL paths to components, renders the matched component inside `<router-view>`, and navigates via `<router-link>` without full page reloads.',
      'You create a router with a history mode (HTML5 history or hash) and a `routes` array, then install it as a plugin.',
      'It exists to turn a single-page app into something with real URLs, back/forward support, and deep-linkable views.',
      'A common mistake is using `<a href>` for internal links (causing full reloads) instead of `<router-link>`/programmatic navigation.',
      'Senior nuance: HTML5 history mode needs server fallback to `index.html`, or refreshes on deep routes 404 — a frequent production gotcha.',
    ],
    prerequisites: [],
    leadsTo: ['dynamic-routes', 'nested-routes', 'navigation-guards', 'programmatic-navigation'],
    questions: [
      'How does client-side routing differ from server-side routing?',
      'What do `<router-view>` and `<router-link>` do?',
      'What is the difference between history mode and hash mode?',
      'Why does refreshing a deep route 404 in history mode, and how do you fix it?',
      'How do you set up a basic router and install it?',
    ],
  },
  {
    slug: 'dynamic-routes',
    name: 'Dynamic Route Matching',
    category: 'routing',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Routes with path parameters (e.g. `/user/:id`) that match a pattern and expose params to the component.',
    explanation: [
      'A param segment like `:id` matches any value and is available as `route.params.id`.',
      'The same component instance is reused when only the param changes, so you watch the param (or use `beforeRouteUpdate`) to react.',
      'It exists so one route definition can serve many resources (every user, product, post) without enumerating them.',
      'The classic bug is fetching data only in `mounted`/`setup`: navigating `/user/1` → `/user/2` reuses the instance and never refetches unless you watch the param.',
      'Senior nuance: param changes do not remount by default; use a `:key` on `<router-view>` or watch params to force fresh state when needed.',
    ],
    prerequisites: ['vue-router-basics'],
    leadsTo: ['route-params-query'],
    questions: [
      'How do you define and read a route param?',
      'Why doesn’t the component refetch when only the param changes?',
      'How do you force a remount on param change?',
      'How do you match optional or repeatable params?',
      'How do you handle a 404 for an unknown id?',
    ],
  },
  {
    slug: 'nested-routes',
    name: 'Nested Routes',
    category: 'routing',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Child routes rendered inside a parent component’s own `<router-view>` to build layered layouts.',
    explanation: [
      'A route can have `children`; the parent renders shared layout/chrome and a nested `<router-view>` where the active child renders.',
      'This models real UI hierarchies — a settings page with sub-tabs, a dashboard with sections — sharing a layout while swapping inner content.',
      'It exists so URL structure mirrors component structure and shared parents aren’t re-created on child navigation.',
      'A common mistake is forgetting the nested `<router-view>` in the parent, so child routes match but render nothing.',
      'Senior nuance: index/default children, relative redirects, and shared data fetching in the parent are where nested routing gets subtle.',
    ],
    prerequisites: ['vue-router-basics'],
    leadsTo: [],
    questions: [
      'How do nested routes work and where do children render?',
      'Why might a child route match but show nothing?',
      'How do you define a default child route?',
      'How do parents and children share data/layout?',
      'When would you choose nesting over sibling routes?',
    ],
  },
  {
    slug: 'navigation-guards',
    name: 'Navigation Guards',
    category: 'routing',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Hooks that run before/after navigation to allow, redirect, or cancel route changes (auth, confirmations).',
    explanation: [
      'Guards come in global (`beforeEach`/`afterEach`), per-route (`beforeEnter`), and in-component (`beforeRouteEnter`/`Update`/`Leave`) flavors.',
      'A `beforeEach` guard can return `false` to cancel, a location to redirect, or nothing to proceed — the backbone of auth gating and "unsaved changes" prompts.',
      'They exist to centralize cross-cutting navigation logic instead of scattering checks in every component.',
      'The classic mistake is doing async auth checks without awaiting/returning properly, letting protected routes flash before redirect.',
      'Senior nuance: guard order and the resolution flow (global before → route → component, then global resolve → afterEach) matter; `beforeRouteEnter` can’t access `this` until `next(vm => ...)`.',
    ],
    prerequisites: ['vue-router-basics'],
    leadsTo: ['route-meta-lazy-loading'],
    questions: [
      'What are the three kinds of navigation guards?',
      'How do you protect a route behind authentication?',
      'What can a `beforeEach` guard return and what does each do?',
      'Why can’t you access `this` in `beforeRouteEnter`?',
      'Describe the full navigation resolution order.',
    ],
  },
  {
    slug: 'programmatic-navigation',
    name: 'Programmatic Navigation',
    category: 'routing',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Navigating from code via `router.push`, `router.replace`, and `router.go` instead of a link click.',
    explanation: [
      '`router.push(location)` adds a history entry; `router.replace` swaps the current one (no back entry); `router.go(n)` moves through history.',
      'You navigate after side effects — e.g. redirect to a dashboard after a successful login, or to a detail page after creating a record.',
      'It exists because not all navigation is a user clicking a link; flows often navigate as a result of logic.',
      'A common mistake is using `push` where `replace` belongs (e.g. after login), leaving a back-button trap to the login page.',
      'Senior nuance: `push` returns a promise that can reject on aborted/duplicated navigation; handle those to avoid unhandled rejections.',
    ],
    prerequisites: ['vue-router-basics'],
    leadsTo: [],
    questions: [
      'What is the difference between `push` and `replace`?',
      'When should you use `replace` instead of `push`?',
      'How do you pass params/query in programmatic navigation?',
      'What does the promise returned by `push` represent?',
      'How do you navigate back/forward programmatically?',
    ],
  },
  {
    slug: 'route-meta-lazy-loading',
    name: 'Route Meta & Lazy Loading',
    category: 'routing',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Attaching `meta` fields to routes and code-splitting route components with dynamic imports.',
    explanation: [
      'Route `meta` carries custom data (e.g. `requiresAuth`, page title, layout) read by guards and components.',
      'Defining a route `component: () => import(...)` makes it a separate lazy chunk loaded only when that route is visited.',
      'They exist to keep initial bundles small and to drive cross-cutting behavior (auth, titles, transitions) declaratively from the route table.',
      'A common mistake is eagerly importing every view, bloating the initial bundle and slowing first load.',
      'Senior nuance: combine meta + a global guard for auth/titles, and group related lazy routes into named chunks to balance request count vs size.',
    ],
    prerequisites: ['navigation-guards', 'async-components'],
    leadsTo: ['lazy-loading-components', 'bundle-optimization'],
    questions: [
      'What is route `meta` used for? Give examples.',
      'How do you lazy-load a route component and why?',
      'How do meta + guards combine for auth and page titles?',
      'What is the trade-off between many small chunks and few large ones?',
      'How would you set the document title per route?',
    ],
  },
  {
    slug: 'route-params-query',
    name: 'Route Params & Query',
    category: 'routing',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Reading and reacting to `route.params` (path segments) and `route.query` (querystring) reactively.',
    explanation: [
      '`params` come from path segments (`/user/:id`), `query` from the `?key=value` string; both are reactive on the current route object.',
      'You read them in setup via `useRoute()` and typically `watch` them to refetch or recompute when they change.',
      'They exist to make views configurable and shareable via URL — filters, pagination, search terms live in the query.',
      'A common mistake is treating query values as typed — they are always strings (or arrays), so coerce them deliberately.',
      'Senior nuance: keep source-of-truth state in the URL for shareable/back-button-friendly views, and debounce query updates for search-as-you-type to avoid history spam.',
    ],
    prerequisites: ['dynamic-routes'],
    leadsTo: [],
    questions: [
      'What is the difference between params and query?',
      'How do you reactively respond to a query change?',
      'Why are query values always strings, and how do you handle that?',
      'When should app state live in the URL query?',
      'How do you avoid flooding history when syncing a search box to the query?',
    ],
  },

  // ───────────────────────── 9. State Management ─────────────────────────
  {
    slug: 'state-management-overview',
    name: 'State Management Overview',
    category: 'state',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Choosing between local component state, provide/inject, shared composables, and a store like Pinia.',
    explanation: [
      'Not everything needs a store: local `ref`s suit component-private state, props/events handle parent-child, provide/inject covers deep trees, and a store fits truly global/shared domains.',
      'The decision is about scope and ownership — who reads/writes the state and how far it travels.',
      'It exists as a discipline because over-centralizing state (everything in a global store) creates coupling and re-render churn, while under-sharing causes prop drilling.',
      'A common mistake is reaching for a global store for state that only two sibling components share (a composable or lifted state is simpler).',
      'Senior nuance: prefer the smallest scope that works; escalate to Pinia when state is cross-cutting, persisted, or needs devtools/time-travel and shared actions.',
    ],
    prerequisites: [],
    leadsTo: ['pinia-basics', 'state-sharing-composables', 'provide-inject-state'],
    questions: [
      'How do you decide between local state, provide/inject, a composable, and a store?',
      'What are the downsides of putting everything in a global store?',
      'When is provide/inject the right tool?',
      'When does a shared composable beat Pinia?',
      'What signals that state should become global?',
    ],
  },
  {
    slug: 'pinia-basics',
    name: 'Pinia Basics',
    category: 'state',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'The official store library: `defineStore` with state, getters, and actions; usable in setup or option style.',
    explanation: [
      'Pinia stores are defined with `defineStore(id, ...)` and consumed via the returned `useXxxStore()` hook inside components/composables.',
      'State is reactive, getters are computed-like derived values, and actions hold logic (sync or async) — there are no Vuex-style mutations.',
      'It exists as Vue’s modern, TypeScript-friendly state solution with first-class devtools, modular stores, and SSR support.',
      'A common mistake is destructuring a store and losing reactivity — use `storeToRefs()` for state/getters while calling actions directly.',
      'Senior nuance: the setup-store syntax (a function returning refs/computeds/functions) composes naturally with composables and is the idiomatic modern form.',
    ],
    prerequisites: ['state-management-overview', 'composables'],
    leadsTo: ['pinia-stores-actions', 'pinia-vs-vuex'],
    questions: [
      'How do you define and use a Pinia store?',
      'What are state, getters, and actions in Pinia?',
      'Why does destructuring a store break reactivity, and what fixes it?',
      'How does the setup-store syntax differ from the options syntax?',
      'How does Pinia integrate with the Composition API and devtools?',
    ],
  },
  {
    slug: 'pinia-vs-vuex',
    name: 'Pinia vs Vuex',
    category: 'state',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Why Pinia replaced Vuex as the official store: simpler API, no mutations, better TypeScript, modular by default.',
    explanation: [
      'Vuex required mutations (the only way to change state) plus actions, plus namespaced modules — verbose and awkward to type.',
      'Pinia drops mutations (actions mutate state directly), is modular by design (each store is independent), and infers types cleanly.',
      'The change exists because Vuex’s ceremony added friction without proportional safety, and TypeScript support was painful.',
      'A common mistake is bringing Vuex habits (separate mutations, deeply nested module namespacing) into Pinia, which it neither needs nor encourages.',
      'Senior nuance: Pinia is now the recommended default; know the migration story (modules → independent stores, mutations folded into actions) for legacy codebases.',
    ],
    prerequisites: ['pinia-basics'],
    leadsTo: [],
    questions: [
      'Why did Vue move from Vuex to Pinia?',
      'What happened to mutations in Pinia?',
      'How does Pinia improve the TypeScript experience?',
      'How do Vuex modules map to Pinia?',
      'What would you watch out for migrating Vuex → Pinia?',
    ],
  },
  {
    slug: 'pinia-stores-actions',
    name: 'Pinia Stores & Actions',
    category: 'state',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Structuring stores: defining state/getters, writing sync and async actions, and composing stores.',
    explanation: [
      'Actions encapsulate business logic and async work (API calls), mutating state directly and optionally calling other stores.',
      'Getters derive values from state and are cached like computed; state holds the reactive source of truth.',
      'This structure exists to keep logic co-located with the data it touches and reusable across components.',
      'A common mistake is putting async logic in components and treating the store as a dumb bag of state, scattering the same logic everywhere.',
      'Senior nuance: stores can use other stores, handle loading/error state inside actions, and the loading/error/throw pattern keeps components thin and consistent.',
    ],
    prerequisites: ['pinia-basics'],
    leadsTo: ['project-structure'],
    questions: [
      'Where should async API calls live — component or store action? Why?',
      'How do getters differ from state, and are they cached?',
      'How can one store use another?',
      'How do you model loading/error state for an async action?',
      'How do you keep components thin when using Pinia?',
    ],
  },
  {
    slug: 'provide-inject-state',
    name: 'Provide/Inject for State',
    category: 'state',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Sharing app- or subtree-level reactive state via provide/inject without prop drilling or a library.',
    explanation: [
      'A parent (or the app) `provide`s reactive state and updater functions; descendants `inject` them, avoiding passing props through every level.',
      'It suits subtree-scoped shared state (a form context, a theme, a feature module) where a global store would be overkill.',
      'It exists as a lightweight DI mechanism built into Vue, complementing (not replacing) stores.',
      'A common mistake is providing plain non-reactive values and expecting updates, or mutating injected state directly from deep children without a controlled updater.',
      'Senior nuance: provide refs/reactive plus explicit setter functions and consider `readonly()` to enforce one-way data flow and prevent uncontrolled deep mutations.',
    ],
    prerequisites: ['provide-inject', 'state-management-overview'],
    leadsTo: [],
    questions: [
      'When is provide/inject a better fit than a store?',
      'How do you keep injected state reactive?',
      'How do you prevent descendants from mutating shared state uncontrollably?',
      'What are the risks of provide/inject for state at scale?',
      'How does this compare to React Context?',
    ],
  },
  {
    slug: 'state-sharing-composables',
    name: 'State Sharing via Composables',
    category: 'state',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Module-scoped reactive state defined once in a composable and shared across all importers.',
    explanation: [
      'If a composable creates its reactive state at module scope (outside the exported function), every component that imports it shares the same singleton state — a tiny store without a library.',
      'If instead it creates state *inside* the function, each caller gets its own instance — the distinction is crucial.',
      'This exists as a pragmatic middle ground between local state and a full store for modest global needs (current user, theme, a feature flag).',
      'A common mistake is accidentally creating module-scoped state when you wanted per-instance (or vice versa), causing surprising shared/leaked state.',
      'Senior nuance: module-scoped singletons are not SSR-safe by default (state leaks across requests on the server) — for SSR, scope via provide/inject or Pinia instead.',
    ],
    prerequisites: ['composables', 'state-management-overview'],
    leadsTo: [],
    overlooked: true,
    questions: [
      'How do you make a composable share one state across components?',
      'What determines whether composable state is shared vs per-instance?',
      'Why are module-scoped singletons unsafe for SSR?',
      'When would you use this instead of Pinia?',
      'How do you avoid accidental shared state?',
    ],
  },
]
