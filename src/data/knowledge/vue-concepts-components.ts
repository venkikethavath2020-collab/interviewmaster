import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ───────────────────────── 3. Templates & Directives ─────────────────────────
  {
    slug: 'template-syntax',
    name: 'Template Syntax',
    category: 'templates',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'HTML-based templates compiled to render functions.',
    explanation: [
      'Vue templates are valid HTML augmented with directives and mustache interpolation, and the compiler turns them into JavaScript render functions that produce a virtual-DOM tree at runtime.',
      'Because templates compile rather than run as strings, Vue can statically analyze them — hoisting static nodes, flagging which bindings are dynamic with patch flags — which is why template-authored components often out-perform hand-written render functions.',
      'The template is declarative: you describe what the DOM should look like for a given state, and the reactivity system re-runs the render function and patches only what changed when that state updates.',
      'Interviewers use this to check that you understand a template is not magic HTML but compiled code — a candidate who knows templates become render functions can reason about performance, SSR, and why some expressions are disallowed.',
      'A common mistake is treating the template as a place to run arbitrary statements; only single expressions are allowed in bindings, and side-effecting logic belongs in computed properties, methods, or watchers.',
    ],
    prerequisites: ['declarative-rendering'],
    leadsTo: ['interpolation-expressions', 'v-bind', 'template-compilation'],
    questions: [
      'What does a Vue template actually compile into, and why does that matter for performance?',
      'Why can you only put expressions, not statements, inside template bindings?',
      'How does compiling templates let Vue optimize updates that a string-based template engine cannot?',
      'When would you reach for a render function instead of a template?',
      'What is the relationship between a template, the virtual DOM, and the real DOM?',
    ],
  },
  {
    slug: 'interpolation-expressions',
    name: 'Interpolation & Expressions',
    category: 'templates',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition: 'Mustaches and in-template JS expressions.',
    explanation: [
      'Double-mustache syntax embeds a reactive value as text content, and Vue re-renders that text whenever any reactive dependency read inside the expression changes.',
      'The contents of a binding must be a single JavaScript expression that resolves to a value — ternaries and method calls are fine, but `if` statements, declarations, and assignments are not, because the compiler wraps them in a return.',
      'Expressions are evaluated in a sandboxed scope exposing the component instance plus a small allowlist of globals (Math, Date), so you cannot reach arbitrary window globals without explicitly exposing them.',
      'Interviewers ask about this to confirm you keep templates thin: heavy logic in an interpolation re-runs on every render and is hard to test, so derived values belong in computed properties.',
      'A frequent mistake is mutating state or calling impure functions inside an interpolation, which produces inconsistent renders and breaks Vue assumptions that render functions are pure reads of state.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['v-bind', 'class-style-bindings'],
    questions: [
      'What kinds of JavaScript are allowed inside a mustache binding, and what is forbidden?',
      'Why should you prefer a computed property over a complex expression in the template?',
      'Can you access `window` or arbitrary globals inside a template expression? Why or why not?',
      'How does Vue know to update interpolated text when the underlying data changes?',
      'What happens if you call a method that mutates state inside an interpolation?',
    ],
  },
  {
    slug: 'v-bind',
    name: 'v-bind',
    category: 'templates',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'Bind attributes/props to dynamic values.',
    explanation: [
      'v-bind, almost always written with the `:` shorthand, binds an HTML attribute or a component prop to a dynamic JavaScript expression so the DOM stays in sync with reactive state.',
      'Vue distinguishes attributes from props automatically: on a native element it sets the attribute or DOM property, while on a component it passes the value as a prop, and unmatched bindings fall through as attributes.',
      'You can bind an entire object with `v-bind="obj"` to spread many attributes at once, which is how wrapper components forward a bag of attributes to an inner element.',
      'Interviewers ask about v-bind to check you understand the attribute-versus-property distinction and how boolean attributes behave — binding `false` removes the attribute rather than setting it to the string "false".',
      'A common mistake is confusing a static attribute with a bound one: `href="url"` sets the literal string "url", whereas `:href="url"` evaluates the expression — forgetting the colon is a frequent beginner bug.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['class-style-bindings', 'v-model', 'props'],
    questions: [
      'What is the difference between `href="x"` and `:href="x"` in a template?',
      'How does Vue decide whether a bound value becomes an attribute or a DOM property?',
      'What does `v-bind="someObject"` do, and when is it useful?',
      'How are boolean attributes like `disabled` handled when you bind a falsy value?',
      'When you bind a value on a component versus a native element, what is the difference in behavior?',
    ],
  },
  {
    slug: 'v-if-v-show',
    name: 'v-if vs v-show',
    category: 'templates',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Conditional rendering vs CSS toggling; cost trade-off.',
    explanation: [
      'v-if conditionally creates and destroys elements and their component instances in the virtual DOM, so a falsy condition means the node is genuinely absent — no markup, no mounted children, no event listeners.',
      'v-show always renders the element and merely toggles its CSS `display` property, so the node and its component instance persist in the DOM and keep their state even while hidden.',
      'The trade-off is initial cost versus toggle cost: v-if is cheaper if the condition is rarely true (nothing is created up front) but pays mount/unmount cost on every toggle, while v-show pays full render cost once and toggles almost for free.',
      'Interviewers love this because it is a precise, checkable decision: use v-show for things toggled frequently like tabs or dropdowns, and v-if for conditions that rarely flip or whose subtree is expensive and seldom needed.',
      'A common mistake is using v-show expecting lifecycle hooks to fire on toggle — the component is never unmounted, so onMounted runs once and onUnmounted never runs until the parent is destroyed; also note v-if cannot be combined with v-for on the same element with predictable precedence.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['transition', 'list-rendering-performance'],
    questions: [
      'What is the practical difference between v-if and v-show, and when do you choose each?',
      'Which lifecycle hooks fire when you toggle a v-show element versus a v-if element?',
      'Why is v-if described as having higher toggle cost but lower initial cost than v-show?',
      'What happens to a hidden component instance under v-show — is its state preserved?',
      'Why is putting v-if and v-for on the same element discouraged?',
    ],
  },
  {
    slug: 'v-for',
    name: 'v-for',
    category: 'templates',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Render lists from arrays/objects.',
    explanation: [
      'v-for renders a block of template once per item in an array, object, number, or any iterable, exposing the item and an optional index (or key and index for objects) in the loop scope.',
      'Each iteration creates its own block scope, so the item alias is local to that render — closures created inside the loop, such as inline event handlers, correctly capture the per-iteration value.',
      'Vue reactivity tracks the source array, so array mutation methods like push and splice trigger re-renders, and replacing the array wholesale works too because the binding re-reads the reactive reference.',
      'Interviewers pair v-for with the key question almost every time: they want to confirm you provide a stable, unique key so the diff algorithm can match nodes across renders instead of patching by position.',
      'A common mistake is mutating an array by index assignment in Vue 2 (which was not reactive) — in Vue 3 the Proxy fixes this, but candidates should also know not to combine v-for with v-if on the same node and to avoid using the index as a key when the list reorders.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['v-for-key', 'list-rendering-performance'],
    questions: [
      'What can you iterate over with v-for besides arrays?',
      'How does v-for expose the index, and how does it differ when iterating an object?',
      'Why does each v-for iteration get its own scope, and how does that affect inline handlers?',
      'Which array operations trigger a re-render, and which historically did not in Vue 2?',
      'Why is combining v-for and v-if on the same element problematic?',
    ],
  },
  {
    slug: 'v-for-key',
    name: 'v-for key',
    category: 'templates',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Why stable keys matter for diffing/state.',
    explanation: [
      'The key attribute gives each v-for item a stable identity so Vue’s diff algorithm can match a virtual node to the same real node across renders instead of comparing purely by position.',
      'Without keys (or with index keys), Vue patches in place: when a list reorders or an item is inserted at the front, every node after the change is mutated and any component-local state or DOM state (focus, input value, animations) leaks to the wrong row.',
      'A correct key is a stable, unique identifier from the data itself — a database id — not the array index, because the index changes when items move and defeats the entire purpose of keying.',
      'Interviewers ask this because the symptom is subtle and production-grade: checkboxes that check the wrong row after a sort, inputs that keep the previous value, transitions that animate the wrong element — all classic missing-key bugs.',
      'The common mistake is using the index as a key to silence the warning; it removes the console message but reintroduces exactly the position-based patching bug the key was meant to prevent.',
    ],
    prerequisites: ['v-for'],
    leadsTo: ['virtual-dom', 'list-rendering-performance'],
    questions: [
      'Why does Vue require a key on v-for, and what does it actually do for the diff?',
      'What goes wrong if you use the array index as the key when the list can reorder?',
      'Give a concrete bug caused by a missing or wrong key.',
      'How does keying interact with component-local state inside a list item?',
      'When is using the index as a key actually acceptable?',
    ],
  },
  {
    slug: 'v-on-events',
    name: 'v-on Events',
    category: 'templates',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'Listen to DOM/component events.',
    explanation: [
      'v-on, written with the `@` shorthand, attaches a listener that runs a handler when a native DOM event or a custom component event fires, receiving the event object (or emitted payload) as an argument.',
      'You can pass a method reference (`@click="handler"`), which receives the event, or an inline expression (`@click="count++"`), and to pass both arguments and the event you use the special `$event` variable.',
      'On native elements v-on listens to DOM events; on a component it listens to events the child emits, which is the receiving half of the props-down-events-up data flow.',
      'Interviewers check that you know handlers should be lightweight and that listener setup/teardown is automatic — Vue removes listeners when the element unmounts, so there is no manual cleanup for template-bound handlers.',
      'A common mistake is invoking the handler instead of referencing it (`@click="handler()"` calls it immediately on render) unless you intentionally want an inline call passing arguments.',
    ],
    prerequisites: ['template-syntax'],
    leadsTo: ['event-modifiers', 'component-events'],
    questions: [
      'What is the difference between `@click="handler"` and `@click="handler()"`?',
      'How do you access the native event object when passing custom arguments to a handler?',
      'How does v-on behave differently on a native element versus a component?',
      'Do you need to manually remove listeners added with v-on? Why or why not?',
      'How does v-on fit into Vue’s props-down, events-up model?',
    ],
  },
  {
    slug: 'event-modifiers',
    name: 'Event Modifiers',
    category: 'templates',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition: 'Modifiers like .prevent, .stop, .once, and key modifiers.',
    explanation: [
      'Event modifiers are postfixes on v-on that encapsulate common event-handling boilerplate declaratively, so `.prevent` calls preventDefault, `.stop` calls stopPropagation, and `.self` only fires when the event targets the element itself.',
      'They exist so handler methods stay focused on logic rather than DOM plumbing: `@submit.prevent="save"` keeps the method free of `event.preventDefault()` and signals intent right in the template.',
      'Key and mouse modifiers like `.enter`, `.esc`, `.ctrl`, and `.left` filter which keystroke or button triggers the handler, and `.once` detaches the listener after the first call.',
      'Modifier order matters because they are applied in sequence — `@click.prevent.self` prevents default for all clicks but only runs the handler for clicks on the element itself, whereas `.self.prevent` differs subtly.',
      'Interviewers use these to gauge template fluency; a common mistake is reimplementing preventDefault or key checks manually in the method when a declarative modifier is cleaner and clearer.',
    ],
    prerequisites: ['v-on-events'],
    leadsTo: ['form-handling'],
    questions: [
      'What does `.prevent` do, and how does it compare to calling preventDefault in the handler?',
      'Explain the difference between `.stop` and `.self`.',
      'Why can the order of chained modifiers like `.prevent.self` change behavior?',
      'How do you only run a handler when the Enter key is pressed?',
      'What does the `.once` modifier do, and how is it different from a v-if guard?',
    ],
  },
  {
    slug: 'v-model',
    name: 'v-model',
    category: 'templates',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Two-way binding sugar over :value + @input.',
    explanation: [
      'v-model on a form input is syntactic sugar that binds the element’s value and listens for its input event, so writing `v-model="text"` expands to roughly `:value="text"` plus `@input="text = $event.target.value"`.',
      'Vue chooses the right property and event per input type: text uses value/input, checkboxes use checked/change, and selects bind to the selected option, so one directive covers every native control.',
      'Modifiers refine the binding — `.lazy` syncs on change instead of input, `.number` coerces to a number, and `.trim` strips whitespace — addressing the most common form-data hygiene needs declaratively.',
      'Interviewers ask this to confirm you understand it is not real two-way magic but a read plus an event handler, which is exactly the knowledge needed to implement v-model on a custom component.',
      'A common mistake is expecting v-model to deep-bind into an object or to work without an emitted event on a custom component; on native inputs the IME composition also means `.lazy` is sometimes needed for correct behavior.',
    ],
    prerequisites: ['v-bind', 'v-on-events'],
    leadsTo: ['v-model-components', 'form-handling'],
    questions: [
      'What does v-model expand to on a text input, and how does that change for a checkbox?',
      'What do the .lazy, .number, and .trim modifiers do?',
      'How does v-model know which property and event to use for different input types?',
      'Why is understanding v-model on native inputs a prerequisite for custom-component v-model?',
      'Is v-model truly two-way binding at the framework level? Explain.',
    ],
  },
  {
    slug: 'class-style-bindings',
    name: 'Class & Style Bindings',
    category: 'templates',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition: 'Dynamic class/style via objects/arrays.',
    explanation: [
      'Binding `:class` and `:style` accepts special object and array syntax beyond plain strings, so `:class="{ active: isActive }"` toggles a class based on a truthy value and `:class="[a, b]"` applies a list.',
      'Vue intelligently merges a bound class or style with any static `class`/`style` attribute on the same element, so you can keep base classes static and layer conditional ones dynamically.',
      'Style objects accept camelCase or kebab-case properties and arrays of style objects, and Vue auto-prefixes properties that need vendor prefixes, removing a chunk of manual cross-browser code.',
      'Interviewers use this to check you prefer the declarative object syntax over string concatenation, which is error-prone and harder to read when several conditions drive the class set.',
      'A common mistake is building class strings by hand with template literals when the object syntax expresses the same intent more clearly and merges correctly with static classes.',
    ],
    prerequisites: ['v-bind'],
    leadsTo: ['transition'],
    questions: [
      'What is the object syntax for :class, and how does it decide which classes apply?',
      'How does Vue merge a static class attribute with a bound :class?',
      'How do you bind multiple style objects to one element?',
      'Why prefer the object/array class syntax over building a string yourself?',
      'Does Vue handle vendor prefixing for bound styles? What does that save you?',
    ],
  },
  {
    slug: 'custom-directives',
    name: 'Custom Directives',
    category: 'templates',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition: 'Low-level DOM access via directive hooks.',
    explanation: [
      'A custom directive is an object of lifecycle hooks (mounted, updated, beforeUnmount, etc.) that receive the raw DOM element and a binding object, giving you a sanctioned escape hatch for direct DOM manipulation.',
      'They exist for behavior that is fundamentally about the element itself rather than data — autofocus, click-outside detection, tooltips, intersection observers, lazy-loading images — where a component would be overkill.',
      'Each hook gets the element plus `binding.value`, `binding.arg`, and `binding.modifiers`, so a single directive like `v-focus` or `v-tooltip:top.delay` can be configured declaratively from the template.',
      'Interviewers treat this as a senior-signal topic because most candidates have only ever consumed directives; knowing when a directive beats a component (no wrapper element, reusable across many tags) shows depth.',
      'The common mistake is reaching for a directive to manage application state or reactivity — that belongs in components and composables; directives are for imperative, element-level side effects, and you must clean up listeners and observers in the unmount hook to avoid leaks.',
    ],
    prerequisites: ['template-syntax', 'lifecycle-hooks'],
    leadsTo: ['composition-reuse-patterns'],
    questions: [
      'When would you write a custom directive instead of a component?',
      'What information does the binding object pass to directive hooks?',
      'Which directive hooks correspond to the component lifecycle, and where do you clean up?',
      'Implement a v-click-outside directive conceptually — what do you attach and tear down?',
      'Why are custom directives the wrong tool for managing application state?',
    ],
    overlooked: true,
  },
  {
    slug: 'template-refs',
    name: 'Template Refs',
    category: 'templates',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'A ref to access a DOM element or child component.',
    explanation: [
      'A template ref lets you grab a direct reference to a rendered DOM element or child component instance for imperative work the declarative template cannot express — focusing an input, measuring layout, integrating a third-party library.',
      'In script setup you declare a ref whose name matches the `ref="..."` attribute, and Vue populates its `.value` with the element after mount; before mount the value is null, which is why access belongs in onMounted, not setup body.',
      'On a child component the ref exposes the component instance, but with script setup the child must use defineExpose to make members public, reinforcing encapsulation by default.',
      'Interviewers ask this to verify you treat DOM access as a last resort and understand the timing — reading a template ref synchronously in setup or before nextTick after a data change yields stale or null values.',
      'A common mistake is using template refs to read or mutate data that should flow through props and events, or reading them too early; refs are for imperative DOM/instance access, not for state sharing.',
    ],
    prerequisites: ['ref'],
    leadsTo: ['lifecycle-hooks', 'nexttick'],
    questions: [
      'How do you obtain a reference to a DOM element in script setup, and when is its value available?',
      'Why is a template ref null in the setup body but populated in onMounted?',
      'How do you access a child component’s methods via a ref, and what does defineExpose change?',
      'After changing data that affects the DOM, why might you need nextTick before reading a ref?',
      'When is using a template ref the wrong choice compared to props and events?',
    ],
  },

  // ───────────────────────────── 4. Components ──────────────────────────────
  {
    slug: 'components-basics',
    name: 'Components Basics',
    category: 'components',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Reusable instances with their own state/template.',
    explanation: [
      'A component is a reusable, self-contained unit with its own template, state, and logic that can be instantiated many times, each instance keeping independent reactive state.',
      'This is why a component’s `data` (Options API) must be a function returning a fresh object — otherwise all instances would share one state object, a classic bug.',
      'Components compose into a tree with a single root app instance, and data flows one way down via props while changes flow up via emitted events, keeping data flow predictable.',
      'Interviewers open component rounds here to check you understand instances versus definitions and the unidirectional data-flow contract that makes Vue apps debuggable.',
      'A common mistake is treating components as global singletons or trying to mutate parent state directly from a child, both of which break the one-way data-flow model Vue depends on.',
    ],
    prerequisites: ['vue-instance-app'],
    leadsTo: ['props', 'single-file-components', 'slots'],
    questions: [
      'Why must a component’s data be a function rather than a plain object?',
      'How do multiple instances of the same component keep separate state?',
      'Describe Vue’s unidirectional data-flow model between parent and child.',
      'What is the difference between a component definition and a component instance?',
      'Why should a child not directly mutate data owned by its parent?',
    ],
  },
  {
    slug: 'single-file-components',
    name: 'Single-File Components',
    category: 'components',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'The .vue file: template, script, and style.',
    explanation: [
      'A single-file component packages a component’s template, JavaScript/TypeScript logic, and CSS in one `.vue` file with `<template>`, `<script>`, and `<style>` blocks, colocating everything that belongs together.',
      'A build step (Vite with the Vue plugin) compiles the SFC: the template becomes a render function, the script exports the component options or setup, and styles can be scoped to the component.',
      'Colocation is the point — one file is the unit of reuse and review, and `<style scoped>` keeps CSS from leaking across components without naming conventions like BEM.',
      'Interviewers ask this to confirm you know SFCs require tooling (they are not usable raw in the browser) and that `<script setup>` is the modern way to write the script block.',
      'A common mistake is thinking SFCs are runtime magic; they are a compile-time format, which is why you cannot just drop a `.vue` file into a plain HTML page without a build pipeline.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['script-setup', 'vue-style-options'],
    questions: [
      'What three blocks make up a single-file component, and what does each contribute?',
      'Why do SFCs require a build step instead of running directly in the browser?',
      'What does `<style scoped>` do, and what problem does it solve?',
      'How does `<script setup>` relate to a single-file component?',
      'What are the advantages of colocating template, logic, and style in one file?',
    ],
  },
  {
    slug: 'props',
    name: 'Props',
    category: 'components',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Pass data parent to child; one-way down.',
    explanation: [
      'Props are the declared inputs a component accepts from its parent, forming the downward half of Vue’s one-way data flow: the parent owns the value and passes it in via a binding.',
      'Props are read-only inside the child — Vue warns if you mutate a prop directly — because the parent re-passes the value on each render, so a child mutation would be silently overwritten and break the single source of truth.',
      'When a child needs to transform a prop, it derives a computed from it or copies it into local state on initialization; to change the parent’s value it emits an event and lets the parent update, completing the props-down-events-up loop.',
      'Interviewers ask about props to verify you respect immutability and understand reactivity caveats — destructuring props in setup loses reactivity unless you use toRefs or access them via the props object.',
      'A common mistake is mutating an object or array prop in place; while it does not trigger the warning the way reassignment does, it secretly mutates the parent’s data and creates action-at-a-distance bugs.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['prop-validation', 'v-model-components', 'attribute-fallthrough'],
    questions: [
      'Why are props read-only in the child, and what should you do when you need to change one?',
      'What happens if you mutate an object prop in place? Why is it dangerous?',
      'How do you keep reactivity when destructuring props in setup?',
      'Describe the full props-down, events-up cycle when a child needs to update parent data.',
      'How would you derive local, editable state from an incoming prop?',
    ],
  },
  {
    slug: 'prop-validation',
    name: 'Prop Validation',
    category: 'components',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition: 'Types, required, default, validators.',
    explanation: [
      'Prop validation lets you declare each prop’s expected type, whether it is required, a default value, and a custom validator function that returns true/false, turning the component’s contract into runtime-checked documentation.',
      'Vue checks these in development and warns on mismatches, catching wrong-shape data at the boundary instead of letting it cause a confusing failure deep inside the template.',
      'Object and array defaults must be returned from a factory function, for the same reason data is a function: a shared default reference would be reused across instances.',
      'Interviewers ask this to see that you design explicit component APIs; with TypeScript and `defineProps<{}>()` the type half becomes compile-time, but required/default/validator semantics still matter.',
      'A common mistake is omitting validation on shared library components, or assuming type declarations alone enforce values at runtime in plain JS where they do not without the runtime options.',
    ],
    prerequisites: ['props'],
    leadsTo: ['typescript-with-vue'],
    questions: [
      'What aspects of a prop can you validate, and when do the checks run?',
      'Why must object and array prop defaults be returned from a function?',
      'How does prop validation differ between plain JS and TypeScript with defineProps?',
      'When would you write a custom validator function for a prop?',
      'Does declaring a prop type in TypeScript enforce it at runtime? Explain.',
    ],
  },
  {
    slug: 'component-events',
    name: 'Component Events',
    category: 'components',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Emit events child to parent.',
    explanation: [
      'A child communicates upward by calling emit with an event name and optional payload, and the parent listens with v-on, completing the events-up half of Vue’s data-flow model.',
      'In script setup you declare emitted events with defineEmits, which documents the component’s output contract and, with TypeScript, types the payloads — mirroring how defineProps documents inputs.',
      'Events are the sanctioned way for a child to request a change to data it does not own: rather than mutating a prop, it emits, and the parent decides whether and how to update.',
      'Interviewers pair this with props to test the full contract; declaring emits also stops the event from accidentally falling through as a native listener on the root element via attribute fallthrough.',
      'A common mistake is reaching for provide/inject or a store for simple parent-child communication that emits handle cleanly, or forgetting that emitted event names are case-sensitive and conventionally kebab-cased in templates.',
    ],
    prerequisites: ['components-basics', 'v-on-events'],
    leadsTo: ['v-model-components'],
    questions: [
      'How does a child component send data back up to its parent?',
      'What does defineEmits do, and why declare your events explicitly?',
      'Why emit an event instead of mutating a prop when a child needs to change parent data?',
      'How does declaring emits interact with attribute fallthrough of listeners?',
      'What are the naming conventions for emitted events in template versus script?',
    ],
  },
  {
    slug: 'v-model-components',
    name: 'v-model on Components',
    category: 'components',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Custom two-way binding via modelValue + update:modelValue.',
    explanation: [
      'Putting v-model on a component is sugar for passing a `modelValue` prop and listening for an `update:modelValue` event, so the child receives the value as a prop and emits the new value to request a change.',
      'This makes the two-way binding explicit and lawful: the parent still owns the state (props down), and the child never mutates the prop — it emits an update (events up) that the parent applies.',
      'Vue 3 supports multiple named v-models on one component (`v-model:title`, `v-model:count`), each expanding to its own prop and `update:name` event, which is how a single component exposes several synced values.',
      'Interviewers consider this a core mid-level topic because it ties together props, events, and the v-model sugar; a strong answer can implement a custom input component from scratch.',
      'A common mistake is mutating the modelValue prop inside the child or forgetting the exact event name `update:modelValue`, both of which silently break the binding.',
    ],
    prerequisites: ['v-model', 'component-events'],
    leadsTo: ['form-handling'],
    questions: [
      'What prop and event does v-model on a component desugar to?',
      'How do you support multiple v-models on a single component?',
      'Implement a custom text-input component that works with v-model.',
      'Why must the child emit instead of mutating modelValue directly?',
      'How does component v-model preserve the one-way data-flow contract?',
    ],
  },
  {
    slug: 'slots',
    name: 'Slots',
    category: 'components',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Content projection from parent into child.',
    explanation: [
      'Slots let a parent inject template content into a child’s designated outlet, so a child like a Card or Modal defines structure while the parent supplies the body — content projection, not data passing.',
      'A component can declare multiple named slots plus a default slot, letting a layout component expose header, body, and footer regions that parents fill independently.',
      'Slot content is compiled in the parent’s scope, so it has access to the parent’s data and not the child’s — the key scoping rule that distinguishes plain slots from scoped slots.',
      'Interviewers ask about slots to assess component-API design skills: knowing slots enable flexible, composable wrappers without prop explosion is a sign of mature component thinking.',
      'A common mistake is trying to read the child’s internal data inside default slot content (it is in parent scope) — needing that is exactly what scoped slots solve, and fallback content for empty slots is often overlooked.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['scoped-slots', 'component-design-patterns'],
    questions: [
      'What problem do slots solve that props do not?',
      'In whose scope is slot content compiled, parent or child?',
      'How do named slots work, and when would you use several?',
      'How do you provide fallback content for an empty slot?',
      'When does plain slot scoping become insufficient, requiring scoped slots?',
    ],
  },
  {
    slug: 'scoped-slots',
    name: 'Scoped Slots',
    category: 'components',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition: 'Child exposes data to parent-defined slot content.',
    explanation: [
      'A scoped slot lets the child pass data back up to the slot content the parent provides, by binding values on the slot outlet that the parent receives via `v-slot="slotProps"`.',
      'This inverts plain slots: the parent still writes the markup, but now it can render using the child’s internal data — the current row, the loading state, a toggle function — without the child dictating the markup.',
      'It is Vue’s render-prop equivalent and the foundation of headless components: the child owns behavior and state while the parent owns presentation, maximizing reuse.',
      'Interviewers treat this as a senior topic because it enables flexible patterns like a data-table that handles fetching and sorting while letting callers render each cell however they like.',
      'A common mistake is confusing slot props (data the child exposes) with component props (data the parent passes in), or not realizing scoped slots are how you build truly reusable, presentation-agnostic components.',
    ],
    prerequisites: ['slots'],
    leadsTo: ['headless-components', 'render-props-vue'],
    questions: [
      'How does a scoped slot let the child share data with parent-authored content?',
      'What is the syntax for receiving slot props in the parent?',
      'How do scoped slots relate to the render-props pattern in React?',
      'How would you design a headless list component using scoped slots?',
      'What is the difference between slot props and component props?',
    ],
  },
  {
    slug: 'dynamic-components',
    name: 'Dynamic Components',
    category: 'components',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition: 'Use <component :is> to swap components at runtime.',
    explanation: [
      'The built-in `<component :is="...">` renders whichever component you bind to `is`, letting you switch components at runtime by changing a single reactive value rather than chaining v-ifs.',
      'The `is` value can be a registered component name, an imported component object, or even a native element tag, which makes it ideal for tab interfaces, wizards, and content-type-driven rendering.',
      'By default switching components unmounts the old one and mounts the new, destroying its state — wrapping the dynamic component in `<keep-alive>` caches instances so toggling preserves their state.',
      'Interviewers ask this to see you can avoid v-if ladders and to lead into keep-alive and lazy loading, both of which build on dynamic components.',
      'A common mistake is expecting state to survive a swap without keep-alive, or binding a component that was never registered/imported so `is` resolves to nothing.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['keepalive', 'async-components'],
    questions: [
      'How does `<component :is>` decide what to render?',
      'What can you pass to the `is` prop besides a component name?',
      'What happens to a dynamic component’s state when you swap it, and how do you preserve it?',
      'When would you use dynamic components over a chain of v-if blocks?',
      'How do dynamic components combine with keep-alive and async loading?',
    ],
  },
  {
    slug: 'async-components',
    name: 'Async Components',
    category: 'components',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'defineAsyncComponent for code-split, lazily loaded components.',
    explanation: [
      'defineAsyncComponent wraps a loader function that returns a dynamic `import()`, so the component’s code is split into a separate chunk and fetched only when the component is first rendered.',
      'This is the primary code-splitting lever in Vue apps: heavy or rarely-used components (modals, editors, charts) stay out of the initial bundle, improving first-load performance.',
      'The API accepts options for a loading component, an error component, a delay before showing the loader, and a timeout, giving you control over the perceived loading experience.',
      'Interviewers connect this to route-level lazy loading and Suspense; a strong candidate explains how async components and `<Suspense>` coordinate to show fallback UI while chunks load.',
      'A common mistake is making components async indiscriminately (adding network round-trips and flashes of loading state for tiny components) instead of targeting genuinely large or deferrable code.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['suspense', 'lazy-loading-components'],
    questions: [
      'How does defineAsyncComponent enable code splitting?',
      'What loading-experience options does an async component accept?',
      'How do async components relate to route-level lazy loading?',
      'How does Suspense coordinate with async components?',
      'When is making a component async a mistake rather than an optimization?',
    ],
  },
  {
    slug: 'provide-inject',
    name: 'Provide / Inject',
    category: 'components',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition: 'Pass data deep without prop drilling.',
    explanation: [
      'provide/inject lets an ancestor expose a value under a key that any descendant can inject at any depth, bypassing the prop-drilling problem where intermediate components forward props they do not use.',
      'It is dependency injection scoped to the component subtree: the provider declares what is available and descendants opt in, with the nearest provider winning if a key is provided at multiple levels.',
      'To stay reactive, you provide a ref or reactive object, not a plain snapshot; descendants then react to changes, and you can provide readonly wrappers to prevent children mutating shared state.',
      'Interviewers ask this to gauge architecture sense — when prop drilling is genuinely painful (theme, locale, form context) versus when a store or simple props would be clearer.',
      'A common mistake is providing a plain non-reactive value and wondering why updates do not propagate, or overusing provide/inject for app-wide state where Pinia gives better tooling and traceability.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['dependency-injection-composition', 'provide-inject-state'],
    questions: [
      'What problem does provide/inject solve compared to passing props?',
      'How do you keep provided values reactive for descendants?',
      'What happens when the same key is provided at multiple levels of the tree?',
      'When would you choose provide/inject over a Pinia store, and vice versa?',
      'How do you prevent descendants from mutating provided shared state?',
    ],
  },
  {
    slug: 'component-registration',
    name: 'Component Registration',
    category: 'components',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition: 'Global vs local registration trade-offs.',
    explanation: [
      'Components must be registered before use: global registration via `app.component()` makes a component available everywhere, while local registration scopes it to the component that imports it.',
      'Global registration is convenient but costs tree-shaking — a globally registered component is included in the bundle even if no template ever uses it, whereas local registration lets unused components be dropped.',
      'In `<script setup>`, imported components are automatically locally registered just by importing them, which is the modern default and keeps dependencies explicit per file.',
      'Interviewers ask this to check bundle-awareness: a senior reserves global registration for truly ubiquitous components (icons, base UI primitives) and relies on local registration elsewhere.',
      'A common mistake is globally registering everything for convenience, bloating the bundle and obscuring which components a given file actually depends on.',
    ],
    prerequisites: ['components-basics'],
    leadsTo: ['recursive-components', 'bundle-optimization'],
    questions: [
      'What is the difference between global and local component registration?',
      'How does global registration affect tree-shaking and bundle size?',
      'How are components registered in `<script setup>`?',
      'When is global registration justified despite its downsides?',
      'Why does local registration make a component’s dependencies clearer?',
    ],
  },
  {
    slug: 'attribute-fallthrough',
    name: 'Attribute Fallthrough',
    category: 'components',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition: 'Non-prop attrs falling through to root; inheritAttrs.',
    explanation: [
      'When a parent passes an attribute that the child did not declare as a prop — class, style, id, event listeners — Vue automatically applies it to the child’s single root element, a behavior called attribute fallthrough.',
      'This is why you can put `class="..."` or `@click` on a custom button component and have it land on the underlying element without the component explicitly forwarding it.',
      'Setting `inheritAttrs: false` disables the automatic application so you can bind the collected attributes explicitly via `v-bind="$attrs"` onto a specific inner element — essential for wrapper components whose root is not the element that should receive them.',
      'Interviewers treat this as senior-signal because it surfaces in real component-library work: multi-root components have no single fallthrough target, so you must place `$attrs` deliberately.',
      'A common mistake is not knowing fallthrough exists (and being surprised a class lands somewhere unexpected) or forgetting that fragments/multi-root components require manual `$attrs` binding.',
    ],
    prerequisites: ['props'],
    leadsTo: ['component-design-patterns'],
    questions: [
      'What is attribute fallthrough, and which attributes does it apply to?',
      'Where do fallthrough attributes go on a single-root component?',
      'What does inheritAttrs: false do, and when do you need it?',
      'How do you forward $attrs to a specific inner element in a wrapper component?',
      'Why do multi-root components complicate attribute fallthrough?',
    ],
    overlooked: true,
  },
  {
    slug: 'recursive-components',
    name: 'Recursive Components',
    category: 'components',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition: 'Components that render themselves (trees/menus).',
    explanation: [
      'A recursive component renders instances of itself, which is the natural way to display recursive data structures like file trees, nested comments, or multi-level menus.',
      'For a component to refer to itself it needs a name — provided by its filename in SFCs or an explicit `name` option — so the template can reference its own tag.',
      'The critical requirement is a base case: the recursion must stop when a node has no children, or the component recurses infinitely and crashes the call stack just like any unbounded recursion.',
      'Interviewers use the tree-rendering task to test recursion comfort plus Vue specifics — passing the right slice of data to each level and keying nested v-for correctly.',
      'A common mistake is omitting the termination condition or mis-keying child lists, producing infinite loops or the same state-leak bugs that missing keys cause in flat lists.',
    ],
    prerequisites: ['components-basics', 'component-registration'],
    leadsTo: ['compound-components'],
    questions: [
      'How does a component reference itself for recursion?',
      'What is the most important thing to get right in a recursive component?',
      'How would you render an arbitrarily deep tree of comments?',
      'Why does the name option (or filename) matter for recursion?',
      'How do keys behave in nested v-for within a recursive component?',
    ],
  },

  // ───────────────────────────── 5. Composition API ──────────────────────────────
  {
    slug: 'setup-function',
    name: 'setup() Function',
    category: 'composition-api',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition: 'The Composition API entry point; returns state/methods.',
    explanation: [
      'setup() is the Composition API entry point that runs once before the component is created, where you declare reactive state, computeds, and methods and return what the template should access.',
      'It runs so early that `this` is not yet available and the component instance is not fully set up; instead setup receives props as its first argument and a context object (emit, attrs, slots) as the second.',
      'Anything returned from setup is exposed to the template, and refs returned are automatically unwrapped there so you write `count` rather than `count.value` in the markup.',
      'Interviewers ask about setup to confirm you understand the timing and the props/context arguments, even though most code now uses `<script setup>` sugar over the raw function.',
      'A common mistake is destructuring props in setup (losing reactivity) or expecting `this` to work; setup predates the instance, so you use the provided arguments and toRefs instead.',
    ],
    prerequisites: ['options-vs-composition-api'],
    leadsTo: ['script-setup', 'composables'],
    questions: [
      'When does setup() run relative to the component lifecycle, and why is `this` unavailable?',
      'What are the two arguments setup receives?',
      'What does returning a ref from setup do for the template?',
      'Why does destructuring props inside setup lose reactivity?',
      'How does setup relate to `<script setup>`?',
    ],
  },
  {
    slug: 'script-setup',
    name: '<script setup>',
    category: 'composition-api',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Compile-time sugar; the modern default authoring style.',
    explanation: [
      '`<script setup>` is compile-time sugar where the entire block body becomes the setup function, so top-level variables, imports, and functions are automatically exposed to the template without an explicit return.',
      'It provides compiler macros — defineProps, defineEmits, defineExpose, withDefaults — that need no import and give better TypeScript inference than the raw setup signature.',
      'It is more concise and more performant (the compiler can analyze bindings) and is the officially recommended default for Vue 3 SFCs, which is why nearly all modern Vue code uses it.',
      'Interviewers expect fluency here as the baseline modern style; a strong candidate contrasts it with plain setup and the Options API and explains why imported components auto-register.',
      'A common mistake is mixing patterns confusingly or not knowing that you need defineExpose to make members accessible via a template ref, since script-setup components are closed by default.',
    ],
    prerequisites: ['setup-function', 'single-file-components'],
    leadsTo: ['composables', 'typescript-with-vue'],
    questions: [
      'How does `<script setup>` expose values to the template without a return statement?',
      'What are defineProps, defineEmits, and defineExpose, and why do they not need importing?',
      'Why is `<script setup>` considered more performant and better for TypeScript?',
      'Why must you use defineExpose to access a child’s members via a template ref?',
      'How does `<script setup>` differ from the plain setup() function and the Options API?',
    ],
  },
  {
    slug: 'composables',
    name: 'Composables',
    category: 'composition-api',
    level: 'advanced',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition: 'Reusable stateful logic functions (useXxx).',
    explanation: [
      'A composable is a function, conventionally named `useSomething`, that encapsulates reactive state and logic using Composition API primitives and returns refs and functions for a component to consume.',
      'They are Vue’s primary logic-reuse mechanism, combining closures, reactivity, and lifecycle hooks: a composable can own refs, register onMounted/onUnmounted, and clean itself up, all reusable across components.',
      'Each call creates a fresh, isolated instance of the state by default, which is why useMouse or useFetch gives every component its own copy — to share one instance you define the state at module scope.',
      'Interviewers regard composables as the key senior Composition API topic because writing a correct one demonstrates mastery of refs, lifecycle ownership, and SSR-safe patterns at once.',
      'A common mistake is calling composables conditionally or inside callbacks (lifecycle hooks must register synchronously during setup) or accidentally sharing module-scoped state when per-instance state was intended.',
    ],
    prerequisites: ['script-setup', 'ref', 'lifecycle-hooks-composition'],
    leadsTo: ['composition-reuse-patterns', 'state-sharing-composables'],
    questions: [
      'What is a composable, and what makes it different from a plain utility function?',
      'How does a composable manage its own lifecycle hooks and cleanup?',
      'Why does each call to a composable usually get isolated state, and how do you share state instead?',
      'Why must composables be called synchronously at the top of setup?',
      'Walk through writing a useFetch composable that exposes data, error, and loading.',
    ],
  },
  {
    slug: 'lifecycle-hooks-composition',
    name: 'Lifecycle Hooks (Composition)',
    category: 'composition-api',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'onMounted, onUnmounted, etc. inside setup.',
    explanation: [
      'In the Composition API, lifecycle hooks are registered by calling functions like onMounted, onUpdated, and onUnmounted inside setup, passing the callback you want run at that phase.',
      'They map to the Options API hooks (mounted, updated, unmounted) but as imported functions, so you can register several of the same hook and colocate setup and teardown logic next to the state they concern.',
      'They must be called synchronously during setup so Vue can associate them with the current component instance; calling them after an await or inside a nested callback silently fails to register.',
      'Interviewers ask this to test that you fetch data and set up subscriptions in onMounted and, crucially, tear them down in onUnmounted to prevent the listener and timer leaks common in SPAs.',
      'A common mistake is forgetting onUnmounted cleanup for intervals, event listeners, and observers, which is exactly the source of the memory leaks that accumulate across mount/unmount cycles.',
    ],
    prerequisites: ['setup-function', 'lifecycle-hooks'],
    leadsTo: ['composables', 'data-fetching-patterns'],
    questions: [
      'How do you register lifecycle hooks in the Composition API?',
      'Why must lifecycle hooks be called synchronously inside setup?',
      'How do the Composition hooks map to the Options API lifecycle?',
      'Where do you fetch data and where do you clean up side effects?',
      'How does forgetting onUnmounted cleanup cause memory leaks in a long-lived SPA?',
    ],
  },
  {
    slug: 'dependency-injection-composition',
    name: 'Dependency Injection (Composition)',
    category: 'composition-api',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition: 'Typed provide/inject in the Composition API.',
    explanation: [
      'In the Composition API you call the provide and inject functions inside setup, where provide registers a value under a key and inject retrieves it from the nearest ancestor that provided it.',
      'Using an InjectionKey typed symbol gives full TypeScript inference, so injected values carry their type automatically instead of being unknown — the recommended pattern for typed DI.',
      'inject accepts a default value or factory for when no provider exists, which keeps a component usable standalone and makes the dependency optional rather than a hard crash.',
      'Interviewers ask this to see you can build typed, subtree-scoped services — a form context, a theme, a feature flag provider — without prop drilling and with proper TypeScript safety.',
      'A common mistake is providing plain values and losing reactivity, or not using an InjectionKey so the injected type degrades to unknown and the contract goes undocumented.',
    ],
    prerequisites: ['provide-inject', 'setup-function'],
    leadsTo: ['state-sharing-composables'],
    questions: [
      'How do you call provide and inject in the Composition API?',
      'How does an InjectionKey give type safety to inject?',
      'How do you make an injected dependency optional with a sensible default?',
      'How do you keep provided values reactive in Composition-API DI?',
      'When is typed provide/inject preferable to passing props or using a store?',
    ],
  },
  {
    slug: 'composition-reuse-patterns',
    name: 'Composition Reuse Patterns',
    category: 'composition-api',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition: 'Composing, returning refs, naming, SSR-safety.',
    explanation: [
      'Mature composable design follows conventions: name with the `use` prefix, accept refs or getters so inputs stay reactive, and return an object of refs (not a reactive object) so consumers can destructure without losing reactivity.',
      'Composables compose — one can call another, layering small focused pieces (useFetch built on useEventListener) into larger behavior, mirroring function composition rather than inheritance.',
      'SSR-safety matters: a composable must not touch window or document at module scope or during setup synchronously without guards, and side effects belong in lifecycle hooks so the server render stays pure.',
      'Interviewers use this to separate people who have written one composable from those who have designed a reusable library of them — return shape, reactive inputs, and cleanup ownership are the tells.',
      'A common mistake is returning a reactive object that breaks on destructure, accepting plain values instead of refs (losing reactivity to input changes), or running browser-only code that crashes during SSR.',
    ],
    prerequisites: ['composables'],
    leadsTo: ['headless-components'],
    questions: [
      'Why return an object of refs from a composable instead of a reactive object?',
      'How do you accept reactive inputs into a composable so it reacts to changes?',
      'What makes a composable SSR-safe, and what breaks it?',
      'How do composables compose with one another?',
      'What naming and structural conventions signal a well-designed composable?',
    ],
  },
  {
    slug: 'composition-vs-mixins',
    name: 'Composition API vs Mixins',
    category: 'composition-api',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition: 'Why composables beat mixins (no name clashes, explicit).',
    explanation: [
      'Mixins were the Vue 2 logic-reuse mechanism that merged options (data, methods, hooks) into a component, but they had three chronic problems composables solve: name collisions, unclear source of properties, and brittle implicit coupling.',
      'With mixins, two mixins exposing a property named `loading` silently clash, and when a template uses some property you cannot tell which mixin provided it — there is no explicit, traceable origin.',
      'Composables fix this because they are just function calls: you import them, call them, and bind their returned values to names you choose, so collisions are impossible and every value’s origin is visible at the call site.',
      'Interviewers treat this as senior-signal because it shows you understand why the Composition API was designed, not just how to use it — the answer is reuse without the mixin tax.',
      'A common mistake is dismissing mixins as merely outdated rather than articulating the concrete failures (implicit merging, name clashes, no clear data flow) that composables were built to eliminate.',
    ],
    prerequisites: ['composables'],
    leadsTo: ['composition-reuse-patterns'],
    questions: [
      'What concrete problems with mixins do composables solve?',
      'How do mixins handle name collisions, and how is that different with composables?',
      'Why is it hard to trace where a property came from with mixins?',
      'How does the explicitness of composables improve maintainability?',
      'Was the Composition API introduced just for reuse, or are there other motivations?',
    ],
  },
]
