import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ───────────────────────── 10. Performance & Optimization ─────────────────────────
  {
    slug: 'vue-performance-overview',
    name: 'Vue Performance Overview',
    category: 'performance',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Where Vue apps get slow — bundle size, over-rendering, large lists, over-reactivity — and the levers to fix each.',
    explanation: [
      'Performance work splits into load-time (bundle size, code splitting, hydration) and runtime (render frequency, list size, reactivity scope).',
      'The biggest wins usually come from lazy-loading routes/components, caching derived values with computed, and keeping reactive graphs small.',
      'It exists as a discipline because Vue’s reactivity makes correctness easy but can hide cost — needless re-renders and huge reactive objects are invisible until profiled.',
      'A common mistake is premature micro-optimization (e.g. `v-once` everywhere) instead of measuring with the devtools timeline/flame chart first.',
      'Senior nuance: profile to find the actual bottleneck (render count, component cost, hydration), then apply the targeted lever — computed caching, `v-memo`, virtualization, `shallowRef`/`markRaw`, or splitting bundles.',
    ],
    prerequisites: [],
    leadsTo: ['computed-caching', 'list-rendering-performance', 'v-once-v-memo', 'lazy-loading-components'],
    questions: [
      'How do you find out why a Vue app is slow?',
      'What are the main load-time vs runtime performance levers?',
      'Why can reactivity hide performance cost?',
      'What would you measure before optimizing?',
      'Give three high-impact Vue performance optimizations.',
    ],
  },
  {
    slug: 'lazy-loading-components',
    name: 'Lazy Loading Components',
    category: 'performance',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Deferring component/route code with dynamic imports so it loads only when needed.',
    explanation: [
      'Using `defineAsyncComponent(() => import(...))` or a dynamic-import route component splits that code into its own chunk fetched on demand.',
      'This shrinks the initial bundle so first paint and time-to-interactive improve, especially for heavy or rarely used views (modals, admin panels, charts).',
      'It exists because shipping the whole app upfront wastes bandwidth on code most users never reach in a session.',
      'A common mistake is lazy-loading tiny components (the request overhead outweighs the savings) or above-the-fold critical UI (causing a flash/spinner on first load).',
      'Senior nuance: pair async components with `<Suspense>` or loading/error component options, and group related chunks to balance request count against payload size.',
    ],
    prerequisites: ['async-components', 'route-meta-lazy-loading'],
    leadsTo: ['bundle-optimization'],
    questions: [
      'How do you lazy-load a component vs a route?',
      'What are the trade-offs of splitting too granularly?',
      'Which components are good vs bad candidates for lazy loading?',
      'How do you show a loading/error state for an async component?',
      'How does lazy loading affect first paint and TTI?',
    ],
  },
  {
    slug: 'v-once-v-memo',
    name: 'v-once & v-memo',
    category: 'performance',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Directives that skip re-rendering: `v-once` renders once, `v-memo` re-renders only when listed deps change.',
    explanation: [
      '`v-once` renders an element/subtree a single time and never updates it again — useful for truly static content.',
      '`v-memo="[dep]"` caches a subtree and only re-renders it when one of the listed dependencies changes, like a manual memoization of a render block.',
      'They exist for the rare hot path where a large subtree re-renders unnecessarily and computed/keys aren’t enough.',
      'A common mistake is sprinkling them prematurely; misused `v-memo` deps cause stale UI, and `v-once` on dynamic content shows wrong data.',
      'Senior nuance: these are last-resort, measured optimizations — reach for them only after profiling shows a specific subtree dominating render cost (e.g. giant tables).',
    ],
    prerequisites: ['virtual-dom', 'template-compilation'],
    leadsTo: [],
    overlooked: true,
    questions: [
      'What do `v-once` and `v-memo` do?',
      'When is `v-memo` worth using, and what is the risk?',
      'How can a wrong `v-memo` dependency list cause bugs?',
      'Why are these considered last-resort optimizations?',
      'How does `v-memo` differ from a computed property?',
    ],
  },
  {
    slug: 'computed-caching',
    name: 'Computed Caching',
    category: 'performance',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Why `computed` (cached, dependency-tracked) beats a method call for derived values rendered repeatedly.',
    explanation: [
      'A computed property caches its result and only recomputes when a tracked dependency changes; a method re-runs on every single render.',
      'For expensive derivations referenced multiple times in a template, computed avoids repeating the work each render.',
      'It exists to make derived state both declarative and efficient — you express the relationship and Vue handles when to recompute.',
      'A common mistake is using a method (or inline expression) for derived data in a frequently re-rendering component, recomputing needlessly.',
      'Senior nuance: computed should be pure (no side effects); avoid heavy work or async in getters, and remember caching is keyed on tracked reactive deps, not arbitrary inputs.',
    ],
    prerequisites: ['computed'],
    leadsTo: [],
    questions: [
      'Why use a computed instead of a method for derived data?',
      'When does a computed actually recompute?',
      'What makes a good computed getter (purity, no async)?',
      'Show a case where a method causes redundant work that computed fixes.',
      'Can a computed have side effects? Why is that a problem?',
    ],
  },
  {
    slug: 'list-rendering-performance',
    name: 'List Rendering Performance',
    category: 'performance',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Keeping large `v-for` lists fast with stable keys, virtualization, and avoiding index keys.',
    explanation: [
      'Stable, unique keys let the diff reuse and reorder existing nodes instead of recreating them; index keys break this when the list reorders or items are inserted/removed.',
      'For very large lists, virtualization (rendering only visible rows) is the real fix — the DOM, not Vue, is the bottleneck at thousands of nodes.',
      'These techniques exist because rendering and diffing thousands of nodes is expensive regardless of framework.',
      'A common mistake is using the array index as `key`, causing wrong DOM reuse (e.g. checkbox/input state attaching to the wrong row).',
      'Senior nuance: combine stable keys + a virtual-scroll library + `shallowRef`/frozen data for read-only big lists, and avoid per-item inline closures that allocate on every render.',
    ],
    prerequisites: ['v-for-key', 'virtual-dom'],
    leadsTo: ['reactivity-performance'],
    questions: [
      'Why is an array index a bad key, and what bug does it cause?',
      'When do you need list virtualization?',
      'How do stable keys help the diff algorithm?',
      'What reactivity tricks help with large read-only lists?',
      'Why avoid inline closures per list item in hot renders?',
    ],
  },
  {
    slug: 'reactivity-performance',
    name: 'Reactivity Performance',
    category: 'performance',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Bounding reactive cost with `shallowRef`, `markRaw`, and avoiding deep reactivity on large structures.',
    explanation: [
      'Deep reactivity wraps every nested property in a Proxy; on huge objects/arrays this adds memory and tracking overhead and widens the dependency set.',
      '`shallowRef`/`shallowReactive` track only the top level, and `markRaw` opts an object out of reactivity entirely (e.g. a large third-party instance, a map of geo data).',
      'These exist because not all state needs to be deeply reactive, and making it so can dominate render/update cost.',
      'A common mistake is storing large, frequently-replaced datasets as deep `reactive`, triggering broad re-tracking on every change.',
      'Senior nuance: choose the shallowest reactivity that satisfies the UI, freeze/`markRaw` immutable blobs, and replace shallow refs wholesale to trigger a single update instead of many fine-grained ones.',
    ],
    prerequisites: ['shallowref-shallowreactive', 'reactivity-render-link'],
    leadsTo: [],
    questions: [
      'When would you use `shallowRef` or `markRaw`?',
      'Why can deep reactivity hurt performance on large data?',
      'How do you store a large third-party object without reactivity overhead?',
      'What is the trade-off of shallow reactivity?',
      'How does narrowing the reactive graph reduce re-renders?',
    ],
  },
  {
    slug: 'bundle-optimization',
    name: 'Bundle Optimization',
    category: 'performance',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Reducing shipped JS via tree-shaking, route/component code-splitting, and analyzing the bundle.',
    explanation: [
      'Tree-shaking drops unused exports; code-splitting moves non-critical code into async chunks; analyzing the bundle reveals heavy dependencies to trim or lazy-load.',
      'Vue 3 is tree-shakable (you import only the APIs you use), and Vite makes splitting and analysis straightforward.',
      'It exists because initial JS payload directly drives load time and interactivity, especially on mobile.',
      'A common mistake is importing whole utility/icon libraries instead of specific functions, or shipping dev-only code to production.',
      'Senior nuance: measure with a bundle analyzer, set sensible manualChunks for vendor vs app code, and watch for duplicate dependency versions inflating the bundle.',
    ],
    prerequisites: ['lazy-loading-components', 'vite-vue'],
    leadsTo: [],
    questions: [
      'How do you reduce a Vue app’s bundle size?',
      'What is tree-shaking and how does Vue 3 enable it?',
      'How do you find what is bloating a bundle?',
      'What is a common import mistake that bloats bundles?',
      'How would you split vendor vs application code?',
    ],
  },

  // ───────────────────────── 11. Forms, Async & Data ─────────────────────────
  {
    slug: 'form-handling',
    name: 'Form Handling',
    category: 'forms-async',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Binding inputs with `v-model`, validating, and submitting forms in Vue.',
    explanation: [
      '`v-model` two-way binds inputs to reactive state; modifiers like `.number`, `.trim`, and `.lazy` shape the value/timing.',
      'Validation can be hand-rolled (computed error messages) or delegated to libraries (VeeValidate, Zod), and submission usually calls a store action or API.',
      'It exists because forms are the most common stateful UI and Vue’s binding makes the input ↔ state link declarative.',
      'A common mistake is mutating props for form fields in child components instead of using `v-model`/emit, or validating only on submit (poor UX).',
      'Senior nuance: validate on blur/inline, show errors near fields, disable the submit during async with loading state, and keep a single source of truth for the form model.',
    ],
    prerequisites: ['v-model'],
    leadsTo: ['v-model-components'],
    questions: [
      'How does `v-model` work on a form input under the hood?',
      'What do `.number`, `.trim`, `.lazy` modifiers do?',
      'How do you validate a form (inline vs on submit)?',
      'How do you handle async submission and prevent double submits?',
      'How do you build a reusable form field component with `v-model`?',
    ],
  },
  {
    slug: 'data-fetching-patterns',
    name: 'Data Fetching Patterns',
    category: 'forms-async',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Where and how to fetch data — lifecycle/watchers/composables — with loading, error, and refetch handling.',
    explanation: [
      'Fetching typically happens in `onMounted` (initial), in a `watch` on a route param/filter (refetch on change), or encapsulated in a `useXxx` composable that returns `{ data, error, loading }`.',
      'A composable centralizes the pattern so every screen handles loading/error consistently and the logic is testable in isolation.',
      'It exists because ad-hoc fetching scattered in components leads to inconsistent UX and duplicated boilerplate.',
      'A common mistake is fetching in `setup` synchronously and not handling race conditions when params change rapidly (stale responses overwriting fresh ones).',
      'Senior nuance: cancel/ignore stale requests (AbortController or a request token), debounce search-driven fetches, and consider a data library (TanStack Query, useFetch) for caching/dedupe.',
    ],
    prerequisites: ['lifecycle-hooks-composition', 'composables'],
    leadsTo: ['error-handling-vue'],
    questions: [
      'Where should you fetch initial data, and where should you refetch?',
      'How do you avoid a stale response overwriting a newer one?',
      'How would you build a reusable `useFetch` composable?',
      'How do you handle loading and error states consistently?',
      'When is a dedicated data-fetching library worth it?',
    ],
  },
  {
    slug: 'error-handling-vue',
    name: 'Error Handling in Vue',
    category: 'forms-async',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Catching component errors with `errorCaptured`/`onErrorCaptured`, a global handler, and async error patterns.',
    explanation: [
      '`onErrorCaptured`/`errorCaptured` lets an ancestor catch errors from descendants (a Vue "error boundary"), and `app.config.errorHandler` catches uncaught errors globally for logging.',
      'Async errors (rejected promises in event handlers/await) are not caught by these automatically — you handle them with try/catch in the async code.',
      'These exist to prevent one broken component from blanking the whole app and to centralize error reporting.',
      'A common mistake is assuming `errorCaptured` catches async/promise rejections in handlers — it does not; only render/lifecycle/watcher errors propagate to it.',
      'Senior nuance: combine a boundary component (fallback UI) + global handler (telemetry to Sentry) + disciplined async try/catch, and decide which errors are recoverable vs fatal.',
    ],
    prerequisites: ['lifecycle-hooks'],
    leadsTo: [],
    overlooked: true,
    questions: [
      'How do you build an error boundary in Vue?',
      'What does `onErrorCaptured` catch, and what does it NOT catch?',
      'How do you handle async/promise rejection errors?',
      'What is `app.config.errorHandler` for?',
      'How would you wire Vue errors to a service like Sentry?',
    ],
  },
  {
    slug: 'ssr-hydration',
    name: 'SSR & Hydration',
    category: 'forms-async',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Rendering Vue to HTML on the server, then "hydrating" it on the client to attach reactivity and listeners.',
    explanation: [
      'SSR renders the component tree to an HTML string on the server for fast first paint and SEO; the client then hydrates — reusing that DOM and attaching event listeners/reactivity instead of re-creating it.',
      'Hydration must produce the *same* tree the server rendered, or Vue throws a hydration mismatch and may discard/replace the markup.',
      'It exists to combine server rendering’s first-paint/SEO benefits with SPA interactivity.',
      'The classic mistake is rendering something nondeterministic (Date.now(), random, browser-only APIs, `window`) during SSR, causing a server/client mismatch.',
      'Senior nuance: guard browser-only code (`onMounted`, `import.meta.env.SSR`), keep module-scoped state out of shared server context (cross-request leakage), and understand partial/lazy hydration trade-offs (often via Nuxt).',
    ],
    prerequisites: ['mounting-process', 'virtual-dom'],
    leadsTo: ['nuxt-overview'],
    questions: [
      'What is hydration and how does it differ from a normal client mount?',
      'What causes a hydration mismatch and how do you avoid it?',
      'Why is module-scoped singleton state dangerous in SSR?',
      'What are the benefits of SSR over a pure SPA?',
      'How do you guard browser-only code during SSR?',
    ],
  },

  // ───────────────────────── 12. Testing ─────────────────────────
  {
    slug: 'testing-overview',
    name: 'Testing Overview',
    category: 'testing',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'The Vue testing pyramid: unit (logic/composables), component (mounted behavior), and end-to-end flows.',
    explanation: [
      'Unit tests cover pure logic and composables; component tests mount a component and assert rendered output/behavior; e2e tests drive the real app in a browser.',
      'The balance favors many fast unit/component tests and fewer slow e2e tests for critical paths.',
      'It exists to give confidence at the right cost — testing behavior users rely on, not implementation details.',
      'A common mistake is testing internal state/private methods instead of observable behavior, making tests brittle to refactors.',
      'Senior nuance: prefer Testing-Library-style queries (by role/text) over reaching into component internals, and reserve e2e for the few journeys that must never break.',
    ],
    prerequisites: [],
    leadsTo: ['component-testing', 'testing-composables', 'e2e-testing-vue'],
    questions: [
      'What are the layers of the testing pyramid for a Vue app?',
      'What should be a unit test vs a component test vs e2e?',
      'Why test behavior over implementation details?',
      'How do you keep tests resilient to refactors?',
      'Where do composables fit in the testing strategy?',
    ],
  },
  {
    slug: 'component-testing',
    name: 'Component Testing',
    category: 'testing',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Mounting components with Vue Test Utils / Testing Library and asserting rendered output and interactions.',
    explanation: [
      'You `mount`/`render` a component with props, interact (click, type), and assert the resulting DOM/emitted events — testing it the way a user/parent uses it.',
      'Vue Test Utils gives a wrapper API; @testing-library/vue encourages querying by accessible role/text.',
      'It exists to verify a component’s contract (props in, rendered UI + events out) without a full browser.',
      'A common mistake is asserting on internal `vm` state or CSS classes instead of visible output/behavior, coupling tests to implementation.',
      'Senior nuance: stub heavy children/async, use `await nextTick()`/`flushPromises()` for reactive/async updates, and test the public interface (props, slots, emits) not internals.',
    ],
    prerequisites: ['testing-overview', 'components-basics'],
    leadsTo: [],
    questions: [
      'How do you test that a component emits an event on click?',
      'Why query by role/text instead of internal state?',
      'How do you wait for reactive/async updates in a test?',
      'How do you handle child components and async deps in tests?',
      'What is the component "contract" you should be testing?',
    ],
  },
  {
    slug: 'testing-composables',
    name: 'Testing Composables',
    category: 'testing',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Testing reactive logic in composables, including those that use lifecycle or injection.',
    explanation: [
      'Pure composables (no lifecycle/inject) can be called directly in a test and their returned refs asserted after state changes + `nextTick`.',
      'Composables that use `onMounted`/`inject` need a host: mount a tiny test component that calls the composable, or use a helper like `withSetup`.',
      'This exists because composables are where the real logic lives, and testing them in isolation is faster and clearer than only via components.',
      'A common mistake is calling a lifecycle-using composable outside a component context, where the hooks silently do nothing or warn.',
      'Senior nuance: design composables to be testable — accept dependencies as args, return refs, and avoid hidden global side effects so they can be exercised in isolation.',
    ],
    prerequisites: ['composables', 'testing-overview'],
    leadsTo: [],
    questions: [
      'How do you test a pure composable directly?',
      'How do you test a composable that uses `onMounted` or `inject`?',
      'Why might lifecycle hooks "do nothing" in a test?',
      'How do you design composables to be testable?',
      'How do you assert reactive output changes?',
    ],
  },
  {
    slug: 'e2e-testing-vue',
    name: 'E2E Testing',
    category: 'testing',
    level: 'intermediate',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Driving the running app in a real browser (Cypress/Playwright) to verify whole user journeys.',
    explanation: [
      'E2E tests launch the built app and simulate a user: navigate, fill forms, click, and assert on the resulting UI and URLs.',
      'They catch integration issues (routing, store, API wiring) that unit/component tests can’t see.',
      'They exist to protect the few critical flows (login, checkout, core CRUD) end to end.',
      'A common mistake is over-relying on e2e for everything — they are slow and flaky; reserve them for high-value journeys.',
      'Senior nuance: stabilize with explicit waits/network stubbing, run against a seeded environment, and keep the suite small and deterministic to avoid flake.',
    ],
    prerequisites: ['testing-overview'],
    leadsTo: [],
    questions: [
      'What do e2e tests cover that component tests don’t?',
      'Why keep the e2e suite small?',
      'How do you reduce flakiness in e2e tests?',
      'How do you handle network calls in e2e?',
      'Which flows are worth e2e coverage?',
    ],
  },

  // ───────────────────────── 13. Patterns & Architecture ─────────────────────────
  {
    slug: 'component-design-patterns',
    name: 'Component Design Patterns',
    category: 'patterns',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Designing component APIs and composition — favoring composition over inheritance and clear prop/slot/emit contracts.',
    explanation: [
      'Good components have a small, predictable API: typed props in, events out, slots for content — and compose rather than inherit.',
      'Patterns like container/presentational, headless logic, and compound components emerge from applying that consistently.',
      'They exist to make components reusable, testable, and easy to reason about as an app scales.',
      'A common mistake is god-components (too many responsibilities/props) or leaking internal state, making them hard to reuse.',
      'Senior nuance: design for the consumer — minimal required props, sensible defaults, slots for flexibility, and composables for shared logic instead of inheritance/mixins.',
    ],
    prerequisites: ['props', 'slots', 'composables'],
    leadsTo: ['smart-dumb-components', 'compound-components', 'headless-components'],
    questions: [
      'How do you design a clean component API?',
      'Why composition over inheritance in Vue?',
      'What is a god-component and how do you avoid it?',
      'When do you expose behavior via slots vs props vs composables?',
      'How do you make a component reusable across contexts?',
    ],
  },
  {
    slug: 'smart-dumb-components',
    name: 'Smart vs Dumb Components',
    category: 'patterns',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Separating container (data/logic) components from presentational (props-in, events-out) components.',
    explanation: [
      '"Smart"/container components own data fetching and store access; "dumb"/presentational components just render props and emit events.',
      'This separation makes presentational components highly reusable and trivially testable, while logic concentrates in a few containers.',
      'It exists to decouple how data is obtained from how it is displayed.',
      'A common mistake is scattering store/API calls throughout presentational components, coupling them to data sources and hurting reuse.',
      'Senior nuance: with composables the line blurs — logic can live in a composable used by an otherwise presentational component — so treat it as a guideline, not dogma.',
    ],
    prerequisites: ['components-basics', 'props'],
    leadsTo: [],
    questions: [
      'What is the smart/dumb (container/presentational) split?',
      'What are the benefits for reuse and testing?',
      'How do composables change this pattern?',
      'Where should store/API access live?',
      'When is the split overkill?',
    ],
  },
  {
    slug: 'render-props-vue',
    name: 'Render Props (Scoped Slots)',
    category: 'patterns',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Vue’s render-prop equivalent: a component exposes data through a scoped slot for the parent to render.',
    explanation: [
      'Instead of a render-prop function (React), a Vue component passes data to a scoped slot, letting the parent decide the markup while the child owns the logic/state.',
      'This powers "data provider" components — a `<MouseTracker>` or `<FetchData>` that yields values for the parent to display.',
      'It exists to share behavior while keeping presentation fully in the consumer’s hands.',
      'A common mistake is overusing it where a plain composable would be simpler and avoid an extra component layer.',
      'Senior nuance: scoped slots and composables overlap; composables are usually preferred for pure logic reuse, while scoped slots shine when the provider also needs a place in the template.',
    ],
    prerequisites: ['scoped-slots'],
    leadsTo: ['headless-components'],
    questions: [
      'How do scoped slots act like render props?',
      'When would you use a renderless data-provider component?',
      'How does this compare to a composable?',
      'Give an example of a scoped-slot data provider.',
      'What are the downsides versus composables?',
    ],
  },
  {
    slug: 'higher-order-components-vue',
    name: 'Higher-Order Components',
    category: 'patterns',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 2,
    definition:
      'Wrapping a component to inject props/behavior — a pattern largely superseded by composables in Vue.',
    explanation: [
      'A HOC is a function that takes a component and returns a new one with extra props/behavior layered on.',
      'It was borrowed from React, but Vue’s composables achieve the same logic reuse more directly and with less wrapper nesting.',
      'It exists historically and occasionally for cross-cutting wrapping (e.g. adding analytics) where a wrapper component is genuinely useful.',
      'A common mistake is reaching for HOCs in Vue where a composable (or scoped slot) is clearer and avoids extra component instances and prop forwarding.',
      'Senior nuance: know HOCs exist and why composables generally replace them — interviewers test whether you reach for the idiomatic Vue tool over a React habit.',
    ],
    prerequisites: ['components-basics', 'composables'],
    leadsTo: [],
    overlooked: true,
    questions: [
      'What is a higher-order component?',
      'Why do composables usually replace HOCs in Vue?',
      'When might a wrapper component still be justified?',
      'What are the downsides of HOCs (wrapper hell, prop forwarding)?',
      'How would you migrate a HOC to a composable?',
    ],
  },
  {
    slug: 'compound-components',
    name: 'Compound Components',
    category: 'patterns',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'A set of components that work together sharing implicit state via provide/inject (e.g. Tabs/Tab).',
    explanation: [
      'A parent (e.g. `<Tabs>`) provides shared state/actions; children (`<Tab>`, `<TabPanel>`) inject it to coordinate without explicit props between siblings.',
      'This yields an expressive, declarative API where structure in the template conveys relationships.',
      'It exists to build flexible, composable widgets (tabs, accordions, menus, selects) with a clean consumer API.',
      'A common mistake is wiring siblings together with brittle prop drilling or refs instead of provide/inject.',
      'Senior nuance: define a typed injection key, guard against use outside the parent (throw a helpful error), and document the allowed child set.',
    ],
    prerequisites: ['provide-inject', 'slots'],
    leadsTo: [],
    questions: [
      'What is a compound component and when is it useful?',
      'How does provide/inject coordinate the parts?',
      'How do you make the injection type-safe?',
      'How do you guard a child used outside its parent?',
      'Give examples of UI built this way.',
    ],
  },
  {
    slug: 'headless-components',
    name: 'Headless Components',
    category: 'patterns',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Logic-only components/composables that expose behavior and state while leaving all markup to the consumer.',
    explanation: [
      'A headless pattern packages behavior (e.g. a combobox’s keyboard nav, state, a11y wiring) and exposes it via a scoped slot or composable, with zero opinion on styling/markup.',
      'Consumers get full control of the UI while reusing complex logic — the model behind libraries like Headless UI and VueUse.',
      'It exists to maximize reuse of hard logic across wildly different visual designs.',
      'A common mistake is baking styling/markup into a "reusable" component, which limits reuse to one look.',
      'Senior nuance: composables are the most idiomatic headless form in Vue; expose minimal, well-named reactive state + actions and let the consumer render — this is peak logic reuse.',
    ],
    prerequisites: ['scoped-slots', 'composables'],
    leadsTo: [],
    questions: [
      'What is a headless component and why is it powerful?',
      'How do composables enable the headless pattern?',
      'How do scoped slots fit headless components?',
      'Name real libraries built on this idea.',
      'What should a headless API expose?',
    ],
  },
  {
    slug: 'project-structure',
    name: 'Project Structure',
    category: 'patterns',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Organizing a Vue codebase — features, components, composables, stores, routes — for scale and clarity.',
    explanation: [
      'Common approaches are layer-based (components/, composables/, stores/, views/) or feature-based (group everything for a feature together); larger apps trend feature-based.',
      'Conventions for naming, where logic lives (composables/stores vs views), and a clear API boundary keep growth manageable.',
      'It exists because structure is what keeps a codebase navigable and onboarding fast as it scales.',
      'A common mistake is dumping all logic in views/components with no composable/store layer, leading to duplication and untestable code.',
      'Senior nuance: views orchestrate, stores/composables hold logic, components stay presentational; consistent boundaries (like this app’s) make changes predictable.',
    ],
    prerequisites: ['composables', 'pinia-basics'],
    leadsTo: [],
    questions: [
      'How would you structure a large Vue project?',
      'Layer-based vs feature-based organization — trade-offs?',
      'Where should business logic live?',
      'How do you keep views thin?',
      'What conventions help a team scale a Vue codebase?',
    ],
  },

  // ───────────────────────── 14. Ecosystem & Tooling ─────────────────────────
  {
    slug: 'vite-vue',
    name: 'Vite for Vue',
    category: 'ecosystem',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Vite is the default Vue build tool: native-ESM dev server with instant HMR and an optimized Rollup build.',
    explanation: [
      'In dev, Vite serves source over native ES modules (no bundling) for near-instant startup and fast hot module replacement; for production it bundles with Rollup.',
      'The official `@vitejs/plugin-vue` compiles `.vue` SFCs; config lives in `vite.config.ts`.',
      'It exists to fix slow webpack dev startup/HMR on large apps by leveraging browser-native ESM in development.',
      'A common mistake is conflating dev and build behavior — something working in dev (unbundled) can break in the bundled build (e.g. dynamic import paths, env handling).',
      'Senior nuance: understand `import.meta.env`, `define`, manualChunks, and that dev (esbuild/ESM) and prod (Rollup) pipelines differ, which explains certain build-only bugs.',
    ],
    prerequisites: [],
    leadsTo: ['bundle-optimization'],
    questions: [
      'How does Vite’s dev server differ from a bundler like webpack?',
      'Why is Vite HMR so fast?',
      'How are .vue files compiled in a Vite project?',
      'Why can something work in dev but break in the build?',
      'What do `import.meta.env` and `define` do?',
    ],
  },
  {
    slug: 'vue-devtools',
    name: 'Vue DevTools',
    category: 'ecosystem',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 5,
    definition:
      'Browser/standalone devtools to inspect the component tree, props/state, events, router, and Pinia.',
    explanation: [
      'DevTools shows the live component hierarchy with each component’s props/data/computed, lets you edit state, and includes a timeline for events and performance.',
      'It integrates with Vue Router (route inspection) and Pinia (store state/time-travel).',
      'It exists to make Vue’s otherwise-invisible reactive state and update flow observable while debugging.',
      'A common mistake is debugging with console.log when DevTools would directly reveal the reactive state and which component owns it.',
      'Senior nuance: the timeline/performance tab helps pinpoint excessive re-renders and slow components — the empirical basis for performance work.',
    ],
    prerequisites: ['vue-instance-app'],
    leadsTo: [],
    questions: [
      'What can you inspect and do with Vue DevTools?',
      'How does it help debug reactive state?',
      'How does it integrate with Router and Pinia?',
      'How would you use it to find unnecessary re-renders?',
      'When is DevTools better than console logging?',
    ],
  },
  {
    slug: 'typescript-with-vue',
    name: 'TypeScript with Vue',
    category: 'ecosystem',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Typing Vue: `defineProps<>()`/`defineEmits<>()`, typed refs, generics in `<script setup lang="ts">`.',
    explanation: [
      'In `<script setup lang="ts">` you type props/emits with generic `defineProps<Props>()` and `defineEmits<Emits>()`, get typed `ref<T>()`, and infer most things automatically.',
      'It exists to catch contract bugs at compile time — wrong prop types, missing required props, mistyped emit payloads — and to power editor autocomplete.',
      'A common mistake is fighting the compiler with `any`/casts instead of modeling props/emits/stores properly, throwing away the safety.',
      'Senior nuance: use `PropType`/generic component types, type Pinia stores (inferred), `withDefaults`, and understand `vue-tsc` for type-checking SFCs in CI — the same tool this project gates on.',
      'Interviewers increasingly expect comfortable typed Vue, since most new Vue codebases are TypeScript.',
    ],
    prerequisites: ['script-setup', 'props'],
    leadsTo: [],
    questions: [
      'How do you type props and emits in `<script setup>`?',
      'How do you type a `ref` and a Pinia store?',
      'What is `vue-tsc` and why use it over plain `tsc`?',
      'How do you provide default prop values with types?',
      'What are common TypeScript pitfalls in Vue and how do you avoid them?',
    ],
  },
  {
    slug: 'nuxt-overview',
    name: 'Nuxt Overview',
    category: 'ecosystem',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'The Vue meta-framework: file-based routing, SSR/SSG, server routes, auto-imports, and data fetching.',
    explanation: [
      'Nuxt builds on Vue + Vue Router + Vite to add file-based routing, server-side rendering/static generation, server API routes, and conventions like auto-imported composables/components.',
      'It exists to give Vue apps SEO, fast first paint, and full-stack capabilities without wiring SSR by hand.',
      'A common misconception is that Nuxt is "just SSR" — it also does SSG, hybrid rendering, and provides a server layer (`server/`), plus `useFetch`/`useAsyncData` for SSR-safe fetching.',
      'A frequent mistake is using browser-only APIs in universal code, hitting hydration mismatches — the same SSR pitfalls apply, just abstracted.',
      'Senior nuance: know rendering modes (SSR/SSG/ISR/hybrid), SSR-safe state (no module-scoped singletons), and when Nuxt’s conventions help vs add complexity over a plain Vite SPA.',
    ],
    prerequisites: ['ssr-hydration', 'vue-router-basics'],
    leadsTo: [],
    questions: [
      'What does Nuxt add on top of Vue?',
      'What rendering modes does Nuxt support?',
      'How does Nuxt handle SSR-safe data fetching?',
      'When would you choose Nuxt over a plain Vite SPA?',
      'What SSR pitfalls still apply in Nuxt?',
    ],
  },
  {
    slug: 'vue-style-options',
    name: 'Component Styling Options',
    category: 'ecosystem',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Styling SFCs: `<style scoped>`, CSS Modules, deep selectors, and `v-bind` in CSS.',
    explanation: [
      '`<style scoped>` adds a data-attribute so styles only affect the current component; CSS Modules expose hashed class names as a `$style` object; `:deep()` reaches into child components when needed.',
      'You can also bind reactive values into CSS with `v-bind()` in the style block.',
      'These exist to prevent global CSS leakage and keep component styles encapsulated and co-located.',
      'A common mistake is fighting scoped styles to override a child’s internals without `:deep()`, or overusing global styles that bleed across components.',
      'Senior nuance: understand how scoping attributes affect specificity, when `:deep()`/`:slotted()`/`:global()` are appropriate, and the trade-offs vs utility CSS (Tailwind) at scale.',
    ],
    prerequisites: ['single-file-components'],
    leadsTo: [],
    questions: [
      'How does `<style scoped>` actually work?',
      'When and how do you use `:deep()`?',
      'What are CSS Modules in Vue and when to use them?',
      'How do you bind a reactive value into CSS?',
      'Scoped styles vs utility CSS — trade-offs at scale?',
    ],
  },
]
