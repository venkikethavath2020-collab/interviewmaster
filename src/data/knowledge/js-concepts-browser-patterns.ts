import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ── 10. Browser & DOM ──────────────────────────────────────────────
  {
    slug: 'dom',
    name: 'DOM',
    category: 'browser-dom',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Tree representation of HTML exposed as live objects — querying, traversal, and mutation, including live vs static NodeLists.',
    explanation: [
      'The browser parses HTML into a tree of node objects and hands JavaScript a live API over it: every read or write through document goes through this object model, and some collections (like getElementsByClassName) are live views that change under you while querySelectorAll returns static snapshots.',
      'The DOM exists as the contract between markup and script; without it JavaScript would have no standard way to react to or modify the page, which is why every framework is ultimately a DOM-manipulation strategy.',
      'Interviewers ask about it because innerHTML vs textContent vs createElement reveals whether a candidate understands both performance (parsing, reflow) and security (innerHTML is the classic XSS sink).',
      'The most common mistake is treating the DOM as cheap plain data: candidates iterate live NodeLists while mutating them, or rebuild large subtrees with innerHTML where targeted node updates would avoid both re-parsing and injection risk.',
    ],
    prerequisites: ['what-is-javascript'],
    leadsTo: ['event-system', 'browser-rendering-pipeline'],
    questions: [
      'What are the tradeoffs between innerHTML, textContent, and createElement, including the security angle?',
      'What is the difference between a live and a static NodeList, and which APIs return which?',
      'Why can iterating a live HTMLCollection while removing nodes skip elements?',
      'How does the DOM differ from the HTML source text the server sent?',
      'Why is heavy DOM manipulation slow, and what strategies reduce the cost?',
    ],
  },
  {
    slug: 'bom',
    name: 'BOM',
    category: 'browser-dom',
    level: 'beginner',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Browser objects outside the document: window, location, navigator, history, and screen.',
    explanation: [
      'The Browser Object Model is everything the host exposes that is not the page itself: window is the global object and event target, location reads and drives the URL, navigator describes the environment, and history controls the session stack.',
      'It exists because scripts need to talk to the browser, not just the document — redirects, tab focus, user agent capabilities, and back-button behavior all live here rather than in the DOM spec.',
      'Interviewers use it as a quick conceptual filter: candidates who cannot separate window (the browser context) from document (the parsed page inside it) usually have a fuzzy mental model of the platform.',
      'A common mistake is assuming BOM APIs are standardized like the DOM; navigator sniffing and direct location string manipulation are frequent sources of brittle, browser-specific bugs.',
    ],
    prerequisites: ['dom'],
    leadsTo: ['history-api'],
    questions: [
      'What is the difference between window and document?',
      'What does the BOM include, and how is it different from the DOM?',
      'What are the differences between location.assign, location.replace, and setting location.href?',
      'Why is feature detection preferred over navigator.userAgent sniffing?',
    ],
  },
  {
    slug: 'event-system',
    name: 'Event Propagation',
    category: 'browser-dom',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Events travel capture → target → bubble; listener options (capture, once, passive) and preventDefault vs stopPropagation control behavior.',
    explanation: [
      'When an event fires, the browser computes the propagation path from window down to the target and dispatches in three phases: capturing down the tree, the target phase, then bubbling back up — listeners choose their phase via the capture option.',
      'This design exists so ancestors can observe or intercept descendant interactions without wiring every node, which is the mechanism delegation, analytics capture, and framework synthetic events are all built on.',
      'Interviewers ask it because the preventDefault vs stopPropagation distinction (cancel the browser default action vs stop other listeners from seeing the event) cleanly separates memorizers from people who have debugged real event bugs.',
      "The classic mistake is conflating the two, or not knowing that passive: true promises the listener won't call preventDefault — which lets the browser scroll immediately instead of waiting on your touch/wheel handler.",
      'Candidates also forget that some events (focus, blur, mouseenter) do not bubble, which silently breaks delegation built on them.',
    ],
    prerequisites: ['dom', 'callbacks'],
    leadsTo: ['event-delegation', 'custom-events'],
    questions: [
      'Walk through the three phases of DOM event propagation.',
      'What is the difference between preventDefault and stopPropagation, and when do you need each?',
      'What does passive: true unlock for scroll and touch listeners?',
      'How do the once and capture listener options change dispatch behavior?',
      'Which common events do not bubble, and how does that affect delegation?',
      'What is the difference between stopPropagation and stopImmediatePropagation?',
    ],
  },
  {
    slug: 'event-delegation',
    name: 'Event Delegation',
    category: 'browser-dom',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'One ancestor listener uses event.target to dispatch behavior — fewer listeners, and it works for dynamically added children.',
    explanation: [
      'Instead of attaching a handler to every child, you attach one listener to a shared ancestor and inspect event.target (usually via closest()) during the bubble phase to decide what was clicked.',
      'It exists because per-node listeners scale badly: thousands of handlers cost memory and setup time, and elements added after binding silently have no handler at all — delegation fixes both because the ancestor listener never needs rebinding.',
      'Interviewers love it as a design question ("10,000 rows need click handlers") because a good answer touches propagation, memory, dynamic content, and the limits of the technique in one go.',
      'The most common mistake is reading event.target naively: the click usually lands on a nested span or icon, so without closest() the matching logic fails, and delegation built on non-bubbling events like focus breaks unless you use capture or focusin.',
    ],
    prerequisites: ['event-system'],
    leadsTo: ['list-virtualization'],
    questions: [
      'You have a table with 10,000 rows that each need a click handler — design the solution.',
      'How does event delegation work under the hood, and what makes it possible?',
      'What is the difference between event.target and event.currentTarget?',
      'Which events do not bubble, and how do you delegate them anyway?',
      'Why does delegation keep working for rows added to the list after page load?',
      'What are the downsides or limits of event delegation?',
    ],
  },
  {
    slug: 'custom-events',
    name: 'Custom Events & EventTarget',
    category: 'browser-dom',
    level: 'intermediate',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'CustomEvent carries a detail payload, and any object can extend EventTarget — native pub/sub without a library.',
    explanation: [
      "dispatchEvent(new CustomEvent('name', { detail, bubbles: true })) pushes your own event through the same propagation machinery as native events, and since EventTarget became constructible, plain objects can host addEventListener/dispatchEvent without touching the DOM at all.",
      'This exists so components can announce things without holding references to their consumers — the emitter does not know or care who is listening, which is exactly the decoupling web components and micro-frontends rely on.',
      'Interviewers ask it to see whether candidates reach for the platform before reaching for a library: "decouple two widgets without a framework" has a one-line native answer.',
      'Common mistakes are forgetting bubbles: true (so the event never leaves the dispatching element and ancestor listeners hear nothing) and not knowing that composed: true is required to cross shadow DOM boundaries.',
    ],
    prerequisites: ['event-system'],
    leadsTo: ['observer-pattern', 'event-emitter'],
    questions: [
      'How would you decouple two widgets on a page without a framework or shared state?',
      'How do you create and dispatch a CustomEvent with a payload?',
      'Why might a custom event never reach a listener on a parent element?',
      'What does extending EventTarget give you, and when would you use it over a hand-rolled emitter?',
      'How do custom events interact with shadow DOM boundaries?',
    ],
  },
  {
    slug: 'storage-apis',
    name: 'Storage APIs',
    category: 'browser-dom',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'localStorage (sync, ~5MB, persistent) vs sessionStorage (per-tab) vs cookies (sent per-request, httpOnly capable) vs IndexedDB (async, structured).',
    explanation: [
      'The browser offers several origin-scoped stores with very different contracts: localStorage and sessionStorage are synchronous string key-value stores differing in lifetime and tab scope, cookies ride along on every matching HTTP request and can be hidden from JavaScript via httpOnly, and IndexedDB is an async transactional database for structured data.',
      'Multiple APIs exist because the needs differ: cookies were designed for server-visible state, web storage for simple client-only persistence, and IndexedDB for large or binary data that must not block the main thread.',
      'Interviewers ask "where do you store a JWT?" because the answer forces you through the whole tradeoff matrix — XSS readability of localStorage vs CSRF exposure of cookies vs lifetime and size limits.',
      'The most common mistakes are treating localStorage as free (it is synchronous, so large reads/writes jank the main thread, and it stores only strings, so objects need JSON round-trips) and forgetting it is shared by every script on the origin, including injected ones.',
    ],
    prerequisites: ['bom'],
    leadsTo: ['secure-token-storage'],
    questions: [
      'Where do you store a JWT on the client, and why?',
      'Compare localStorage, sessionStorage, cookies, and IndexedDB across lifetime, size, and access model.',
      'Why can large localStorage reads hurt performance?',
      'What does the httpOnly flag on a cookie actually protect against?',
      'When would you reach for IndexedDB instead of localStorage?',
      'How is storage scoped between origins, tabs, and windows?',
    ],
  },
  {
    slug: 'script-loading',
    name: 'Script Loading',
    category: 'browser-dom',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Parser-blocking default vs async (fetch in parallel, run when ready) vs defer (ordered, after parse) vs type=module (deferred by default).',
    explanation: [
      'A classic <script> halts HTML parsing while it downloads and executes; async downloads in parallel and executes the moment it arrives (in arbitrary order, possibly mid-parse), while defer downloads in parallel but queues execution in document order after parsing completes, and type="module" scripts defer by default.',
      'These attributes exist because parser-blocking scripts were the single biggest cause of slow first paint — the page could render nothing past the script tag until it finished.',
      'Interviewers ask it more than candidates expect because the right answer is use-case driven: independent third-party code like analytics fits async, application code that touches the DOM and has ordering dependencies fits defer.',
      'The common mistake is claiming async and defer are interchangeable — async scripts can execute in any order and before DOMContentLoaded-dependent markup exists, which is exactly the class of "works locally, breaks in prod" race bugs.',
    ],
    prerequisites: ['dom'],
    leadsTo: ['browser-rendering-pipeline'],
    questions: [
      'Explain the difference between async and defer on a script tag.',
      'Which loading strategy would you pick for an analytics snippet vs your app bundle, and why?',
      'How does a default script tag affect HTML parsing and first paint?',
      'How do type="module" scripts load compared to classic scripts?',
      'Why can an async script cause intermittent "element not found" errors?',
    ],
    overlooked: true,
  },
  {
    slug: 'browser-rendering-pipeline',
    name: 'Rendering Pipeline',
    category: 'browser-dom',
    level: 'senior',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'HTML→DOM and CSS→CSSOM combine into the render tree, then layout → paint → composite; reflow vs repaint vs composite-only changes, and layout thrashing.',
    explanation: [
      'Each frame the browser may recompute styles, run layout (geometry), paint (rasterize pixels into layers), and composite (assemble layers on the GPU); changing geometry properties like width or top forces the whole chain, while transform and opacity can be handled composite-only, off the main thread.',
      'Understanding the pipeline exists as a survival skill because the stages are forced synchronously when you interleave reads and writes: reading offsetHeight after a style write makes the browser run layout immediately, and doing that in a loop is layout thrashing.',
      'Interviewers ask "why animate transform instead of top?" because the answer proves you know which stage each property triggers — the difference between a 60fps animation and a janky one.',
      'The common mistake is blaming JavaScript execution time for jank when the real cost is forced synchronous layout; the fix is batching all reads before all writes, or letting rAF-aligned code do the writes.',
    ],
    prerequisites: ['dom'],
    leadsTo: ['requestanimationframe', 'debounce', 'core-web-vitals'],
    questions: [
      'Walk through what the browser does between receiving HTML and pixels appearing on screen.',
      'What is the difference between reflow, repaint, and a composite-only change?',
      'Why should you animate transform and opacity instead of top and width?',
      'What is layout thrashing — show code that causes it and the fix.',
      'Which DOM reads force a synchronous layout, and why is that expensive?',
      'How do CSS properties map to the pipeline stages they invalidate?',
    ],
  },
  {
    slug: 'observers',
    name: 'Intersection / Mutation / Resize Observers',
    category: 'browser-dom',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Async, batched observation APIs replacing scroll polling and DOM-diff hacks — lazy loading, infinite scroll, and element-level resize handling.',
    explanation: [
      'IntersectionObserver reports visibility changes between an element and a root (with thresholds and rootMargin), MutationObserver batches DOM tree changes into one async callback, and ResizeObserver fires when an element\'s box changes — all delivered off the hot path instead of synchronously per event.',
      'They exist because the old alternatives were pathological: scroll handlers calling getBoundingClientRect forced layout on every scroll tick, and the deprecated synchronous Mutation Events fired per-change and could re-enter your code mid-mutation.',
      'Interviewers ask "implement lazy-loaded images without scroll listeners" to check that you know the platform replaced the hack — a one-observer answer with rootMargin preloading signals real production experience.',
      'Common mistakes are forgetting to unobserve elements after one-shot work like lazy loading (leaking observations), and reacting to ResizeObserver callbacks with size mutations that retrigger the observer in a loop.',
    ],
    prerequisites: ['dom', 'callbacks'],
    leadsTo: ['list-virtualization'],
    questions: [
      'Implement lazy-loaded images without any scroll listeners.',
      'How does IntersectionObserver work, and what do threshold and rootMargin control?',
      'Why are observer APIs better than scroll handlers calling getBoundingClientRect?',
      'When would you use a MutationObserver, and how does it batch changes?',
      'How would you build infinite scroll with an IntersectionObserver sentinel?',
      'What cleanup do observers need to avoid leaks?',
    ],
  },
  {
    slug: 'history-api',
    name: 'History API & SPA Routing',
    category: 'browser-dom',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'pushState/replaceState/popstate change the URL without navigation — the foundation of client-side routers.',
    explanation: [
      'pushState and replaceState rewrite the address bar and the session history stack without triggering a network navigation, while the popstate event fires when the user moves through that stack with back/forward — a router is essentially these three primitives plus a path-to-component map.',
      'The API exists because SPAs needed real, shareable, back-button-friendly URLs; before it, client routing lived in the URL hash fragment, which servers never see and which pollutes the URL semantics.',
      'Interviewers ask "how does vue-router change the URL without reloading?" to test whether you know what your framework abstracts — and the follow-up is always why the server must be configured to serve index.html for unknown paths (deep links hit the server, which has no such route).',
      'The classic mistake is expecting popstate to fire on pushState calls — it only fires on user/history navigation, so routers must invoke their own render logic after pushing state.',
    ],
    prerequisites: ['bom'],
    leadsTo: ['dynamic-imports'],
    questions: [
      'How does a client-side router change the URL without a page reload?',
      'What is the difference between pushState and replaceState, and when do you use each?',
      'When does the popstate event fire — and when does it not?',
      'Why do SPA deep links 404 without server configuration, and what is the fix?',
      'Compare hash-based routing with History-API routing.',
    ],
  },
  {
    slug: 'websocket-sse',
    name: 'WebSocket & SSE',
    category: 'browser-dom',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Full-duplex sockets vs one-way server push over HTTP — plus reconnection, heartbeats, and fallback strategy.',
    explanation: [
      'A WebSocket upgrades an HTTP connection into a persistent two-way message channel, while Server-Sent Events keep an ordinary HTTP response open and stream text events one way, with the EventSource API handling reconnection and last-event-id resume automatically.',
      'Both exist because HTTP request/response cannot push: before them, real-time meant long-polling, which burns connections and adds latency on every message.',
      'Interviewers pose "chat app: WebSocket, SSE, or polling?" because the right answer is requirements-driven — bidirectional low-latency traffic justifies WebSocket complexity, while one-way feeds like notifications or tickers are simpler and more proxy-friendly over SSE.',
      'The common candidate mistake is ignoring operational reality: raw WebSockets do not reconnect themselves, so production code needs heartbeat/ping detection of dead connections and exponential-backoff reconnects — and candidates rarely mention that SSE gives much of this for free.',
    ],
    prerequisites: ['fetch-api'],
    leadsTo: ['concurrency-patterns'],
    questions: [
      'For a chat application, would you choose WebSocket, SSE, or polling — defend the decision.',
      'How does a WebSocket connection get established over HTTP?',
      'What does SSE give you for free that raw WebSockets do not?',
      'How do you detect and recover from a silently dead WebSocket connection?',
      'What are the limitations of SSE compared to WebSocket?',
    ],
  },
  {
    slug: 'service-workers',
    name: 'Service Workers',
    category: 'browser-dom',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'A programmable network proxy enabling offline caching strategies, background sync, and push — with lifecycle gotchas around waiting and activation.',
    explanation: [
      'A service worker is a separate-thread script registered for a scope that intercepts every fetch from its pages via a fetch event, letting you answer from the Cache API, the network, or any combination — cache-first for static assets, network-first or stale-while-revalidate for data.',
      'It exists to make the web installable and offline-capable: without an interceptable network layer, apps simply die without connectivity, and push notifications have no execution context when no tab is open.',
      'Interviewers probe the lifecycle because it bites every team once: a new worker version installs but sits waiting until all tabs controlled by the old one close, which is the canonical answer to "why is my deploy not visible?" — and why skipWaiting/clients.claim exist.',
      'The most common mistake is caching aggressively without a versioning and cleanup strategy in the activate event, shipping users a permanently stale app that no deploy can fix.',
    ],
    prerequisites: ['promises', 'web-workers'],
    leadsTo: ['storage-apis'],
    questions: [
      'When would you choose a cache-first vs network-first caching strategy?',
      'Walk through the service worker lifecycle: install, waiting, activate.',
      'Users report the new deploy is not visible — how does the waiting service worker explain this, and what is the fix?',
      'How does a service worker intercept and respond to network requests?',
      'What can a service worker do that a web worker cannot, and vice versa?',
    ],
    overlooked: true,
  },

  // ── 11. Performance ────────────────────────────────────────────────
  {
    slug: 'debounce',
    name: 'Debounce',
    category: 'performance',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Delay execution until input has stopped for N ms, using a closure-held timer — search-as-you-type, resize handlers.',
    explanation: [
      'A debounced function keeps a timer id in a closure: every call clears the pending timer and schedules a new one, so the wrapped function only runs after a full quiet period of N milliseconds with no calls.',
      'It exists because some events fire far faster than the work they trigger is worth — firing an API search on every keystroke wastes requests and creates race conditions, when only the final settled value matters.',
      'It is a top-five implementation question because a correct version exercises closures, timers, this forwarding, and argument capture in ten lines — and the leading/trailing edge follow-up separates users of lodash from people who understand the tool.',
      'The classic mistakes are losing this and arguments by not using apply, and creating a fresh debounced function on every render or call site so the shared timer state never persists and nothing is actually debounced.',
    ],
    prerequisites: ['closure', 'timers'],
    leadsTo: ['throttle'],
    questions: [
      'Implement debounce, correctly forwarding this and arguments.',
      'What is the difference between leading-edge and trailing-edge debounce?',
      'Debounce or throttle for a search-as-you-type input — why?',
      'Why does re-creating the debounced function on every render break it?',
      'How would you add a cancel method to your debounce implementation?',
      'What role does the closure play in how debounce works?',
    ],
  },
  {
    slug: 'throttle',
    name: 'Throttle',
    category: 'performance',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Guarantee at most one execution per time window — sampling scroll and mousemove streams.',
    explanation: [
      'A throttled function records when it last ran (or holds an open-window flag) in a closure and drops or defers calls arriving inside the window, guaranteeing a steady maximum execution rate no matter how fast events fire.',
      'It exists for continuous event streams where you need intermediate updates at a sane rate: unlike debounce, which goes silent until input stops, throttle keeps emitting — which is why it fits scroll position tracking and drag handlers.',
      'Interviewers pair it with debounce precisely to test that distinction: "debounce or throttle for infinite scroll?" has a right answer (throttle, or an rAF-based variant), because a debounced handler would not fire at all during a long continuous scroll.',
      'Common mistakes are implementing it as debounce with a different name, and forgetting the trailing call — without it the final scroll position is never processed and the UI sticks slightly out of date.',
    ],
    prerequisites: ['closure', 'timers'],
    leadsTo: ['debounce', 'requestanimationframe'],
    questions: [
      'Implement throttle with a configurable window.',
      'Debounce or throttle for an infinite-scroll listener — why?',
      'What happens to calls that arrive while the throttle window is closed?',
      'Why do most throttle implementations include a trailing invocation?',
      'How would you build a throttle based on requestAnimationFrame, and when is that better than a time window?',
    ],
  },
  {
    slug: 'requestanimationframe',
    name: 'requestAnimationFrame',
    category: 'performance',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'A pre-paint callback synced to the display refresh — the correct place for visual updates; pauses in background tabs.',
    explanation: [
      'rAF callbacks run as a batch right before the browser computes style, layout, and paint for a frame, synced to the display refresh rate, so DOM writes made there land in exactly the frame being produced instead of a frame late or wastefully often.',
      'It exists because setTimeout(16) animation is structurally wrong: timers have no relationship to the paint cycle, so they drift, double-fire within a frame, or miss frames — and they keep burning CPU in background tabs while rAF pauses automatically.',
      'Interviewers ask where rAF sits in the event loop because it occupies a slot neither macrotask nor microtask: it runs in the render phase, after microtask drain, before paint — placing it correctly is strong evidence of a complete event-loop model.',
      'The common mistake is reading layout and then writing styles inside the same rAF callback in a loop, recreating the layout-thrashing problem rAF was supposed to help avoid; reads belong before the write batch.',
    ],
    prerequisites: ['event-loop', 'browser-rendering-pipeline'],
    leadsTo: ['long-task-scheduling'],
    questions: [
      'Where does requestAnimationFrame fit in the event loop relative to microtasks and macrotasks?',
      'Why is rAF better than setTimeout(16) for animation?',
      'How would you build an rAF-based throttle for scroll handling?',
      'What happens to rAF callbacks when the tab goes to the background?',
      'How do you create a continuous animation loop with rAF, and how do you stop it cleanly?',
    ],
  },
  {
    slug: 'web-workers',
    name: 'Web Workers',
    category: 'performance',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Real threads without shared memory — message passing with structured clone — for CPU-bound work like parsing, search indexing, and image processing.',
    explanation: [
      'A worker runs a script on a genuinely separate thread with its own event loop and global scope; it communicates with the page only through postMessage, where data is structured-cloned (or transferred, for ArrayBuffers) rather than shared.',
      'Workers exist because the main thread is also the UI thread: any CPU-bound JavaScript freezes rendering and input for its whole duration, and no amount of async syntax fixes that — async moves waiting off the thread, not computation.',
      'Interviewers ask what a worker cannot access (the DOM, window, document, parent scope) to verify you understand the isolation model, and "when is postMessage cost worth it?" to check you know cloning large payloads has a real price.',
      'The common mistake is wrapping a 5ms task in a worker where messaging overhead exceeds the work, or conversely shipping megabytes through structured clone per message when transferable objects would move the buffer for free.',
    ],
    prerequisites: ['event-loop'],
    leadsTo: ['typed-arrays', 'service-workers'],
    questions: [
      'What can a web worker not access, and why?',
      'How does data travel between the main thread and a worker — what does structured clone imply?',
      'When is the postMessage overhead worth paying, and when is it not?',
      'Why does async/await not solve a CPU-bound main-thread freeze, while a worker does?',
      'What are transferable objects and when do they matter?',
    ],
  },
  {
    slug: 'long-task-scheduling',
    name: 'Long Tasks & Scheduling',
    category: 'performance',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Tasks over 50ms hurt INP; chunk work with setTimeout, scheduler.yield, or requestIdleCallback, and consult isInputPending.',
    explanation: [
      'The event loop runs one task to completion before handling anything else, so a single 300ms task means 300ms during which clicks, keystrokes, and rendering are all queued — the 50ms threshold defines a "long task" because it pushes input response past perceptible limits.',
      'Mitigation is cooperative scheduling: slice the work into chunks and yield the thread between them via setTimeout, the newer scheduler.yield (which resumes at the front of the queue), or requestIdleCallback for genuinely deferrable work, checking isInputPending to yield exactly when a user is waiting.',
      'Interviewers use "process 100k rows without freezing input" because it has three distinct answer tiers — chunk-and-yield, move it to a worker, or virtualize so the work never happens — and a senior candidate can compare all three.',
      'The common mistake is yielding with a promise chain or queueMicrotask: microtasks drain before the browser can render or dispatch input, so the loop is exactly as blocked as before.',
    ],
    prerequisites: ['event-loop-starvation'],
    leadsTo: ['web-workers', 'core-web-vitals'],
    questions: [
      'Process 100,000 rows on the client without freezing input — give three distinct strategies.',
      'What is a long task, and why is 50ms the threshold?',
      'Why does yielding with queueMicrotask or a promise chain not unblock rendering?',
      'Compare setTimeout, scheduler.yield, and requestIdleCallback as yielding mechanisms.',
      'How does isInputPending change a chunked processing loop?',
      'How do long tasks show up in INP, and how would you find them?',
    ],
    overlooked: true,
  },
  {
    slug: 'list-virtualization',
    name: 'List Virtualization',
    category: 'performance',
    level: 'senior',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Render only the viewport rows inside a full-height spacer — constant DOM size for unbounded data.',
    explanation: [
      'A virtualized list renders a spacer element sized to totalRows × rowHeight to keep the scrollbar honest, then computes from scrollTop which slice of rows intersects the viewport and renders only those (plus an overscan buffer), absolutely positioned at their offsets.',
      'It exists because the DOM cost of a list is paid per node whether visible or not: 50,000 rows means 50,000 elements participating in layout, style, and memory, which no amount of event delegation rescues — virtualization makes cost proportional to the viewport instead of the data.',
      'Interviewers ask for the windowing math (startIndex = floor(scrollTop / rowHeight), count = ceil(viewportHeight / rowHeight) + overscan) because deriving it on a whiteboard proves you understand the technique rather than the library name.',
      'The classic mistake is assuming fixed row heights; variable-height content requires measurement and offset caching, which is most of the real complexity, along with handling scroll anchoring and accessibility of off-screen content.',
    ],
    prerequisites: ['browser-rendering-pipeline', 'observers'],
    leadsTo: ['event-delegation'],
    questions: [
      'Design a 50,000-row table — walk through the windowing math.',
      'Why does the DOM get slow with huge lists even when handlers are delegated?',
      'How do you keep the scrollbar proportional when only 30 rows exist in the DOM?',
      'What breaks when rows have variable heights, and how do virtualization libraries handle it?',
      'What is overscan and why do virtualized lists render it?',
    ],
  },
  {
    slug: 'code-splitting-lazy-loading',
    name: 'Code Splitting & Lazy Loading',
    category: 'performance',
    level: 'senior',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Route/component-level chunks via dynamic import, with preload/prefetch hints, plus loading states and error boundaries for chunks.',
    explanation: [
      'Bundlers treat every dynamic import() as a split point, emitting that subtree as a separate chunk fetched over the network at call time — frameworks wrap this in lazy route/component definitions so the router triggers the fetch on navigation.',
      'It exists because a monolithic bundle bills every user up front for every feature: parse and compile time scale with shipped bytes, so the settings page code a user never visits still delays their first interaction.',
      'Interviewers pose "your bundle is 3MB — splitting strategy?" expecting a priority order (routes first, then heavy below-the-fold components and rarely-used libraries) plus the hint vocabulary: prefetch for likely-next idle-time fetching, preload for needed-now resources.',
      'The common mistake is splitting without handling the new failure modes — a chunk fetch is a network request that can fail (especially right after a deploy changes hashed filenames), so lazy boundaries need loading states, error fallbacks, and a retry or reload story.',
    ],
    prerequisites: ['dynamic-imports'],
    leadsTo: ['bundlers-module-resolution'],
    questions: [
      'Your bundle is 3MB — describe your code-splitting strategy and where you would split first.',
      'How does dynamic import() become a separate network chunk — what does the bundler do?',
      'What is the difference between prefetch and preload hints, and when do you use each?',
      'What new failure modes does lazy loading introduce, and how do you handle them?',
      'Users get chunk-load errors right after each deploy — what is happening and what is the fix?',
    ],
  },
  {
    slug: 'core-web-vitals',
    name: 'Core Web Vitals',
    category: 'performance',
    level: 'senior',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      "LCP, CLS, and INP — what each measures, JavaScript's role in each, and the measurement APIs (PerformanceObserver).",
    explanation: [
      'The three vitals each measure a distinct user experience: LCP is the render time of the largest viewport element (loading), CLS sums unexpected layout movement (visual stability), and INP captures the slowest interaction latencies across the whole visit (responsiveness).',
      'They exist to replace proxy metrics like onload, which said nothing about what users actually perceived; they are also a ranking factor, which is why product teams care and why frontend engineers get asked.',
      'JavaScript implicates each differently — render-blocking and hydration delay LCP, late content injection without reserved space causes CLS, and long main-thread tasks inflate INP — and field measurement flows through PerformanceObserver entries (largest-contentful-paint, layout-shift, event timing).',
      'The classic mistake when "INP regressed after a release" is profiling load performance: INP is interaction latency, so the investigation is long tasks in input handlers — oversized effects, synchronous re-renders, layout thrash — not bundle size.',
      'Candidates also routinely conflate lab data (Lighthouse, synthetic) with field data (real users via CrUX/RUM), which can disagree wildly.',
    ],
    prerequisites: ['browser-rendering-pipeline'],
    leadsTo: ['long-task-scheduling', 'script-loading'],
    questions: [
      'What do LCP, CLS, and INP each measure?',
      'INP regressed after a release — walk through your investigation.',
      'How does JavaScript affect each of the three vitals?',
      'How do you measure web vitals on real users in the field?',
      'What is the difference between lab and field data, and why might they disagree?',
      'Name two common causes of CLS and how to prevent them.',
    ],
  },

  // ── 12. Error Handling & Debugging ─────────────────────────────────
  {
    slug: 'error-types-try-catch',
    name: 'Errors & try/catch',
    category: 'errors-debugging',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'The Error hierarchy (TypeError, RangeError, …), throw semantics, finally and the catch binding — try/catch only catches same-stack throws.',
    explanation: [
      'A throw unwinds the call stack synchronously until a catch is found; the built-in subclasses (TypeError, RangeError, ReferenceError, SyntaxError) classify what went wrong, finally runs on every exit path including return and throw, and the catch binding is optional since ES2019.',
      'Structured error handling exists so failures can be intercepted at the right altitude instead of killing the program — but its mechanism is stack unwinding, which is exactly why it cannot reach asynchronous failures.',
      'Interviewers ask "why doesn\'t try/catch catch errors in setTimeout callbacks?" because the answer requires the full model: by the time the callback runs, the try block\'s stack frame is long gone — the callback executes on a fresh, empty stack, so the throw has nothing to unwind into.',
      'The common mistake beyond that is catch-and-ignore (swallowing errors with an empty catch), and not knowing that a return inside finally silently overrides both the try return value and any in-flight exception.',
    ],
    prerequisites: ['call-stack'],
    leadsTo: ['async-error-handling'],
    questions: [
      'Why does try/catch fail to catch an error thrown inside a setTimeout callback?',
      'Name the built-in Error subclasses and when the engine throws each.',
      'When does finally run, and what happens if it contains a return statement?',
      'What is the difference between throwing an Error instance and throwing a string, and why does it matter?',
      'How does a thrown error travel up the call stack to find a handler?',
    ],
  },
  {
    slug: 'custom-errors-cause',
    name: 'Custom Errors & cause',
    category: 'errors-debugging',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Extending Error with names and fields, error.cause chains, and AggregateError — designing error taxonomies callers can branch on.',
    explanation: [
      'Subclassing Error gives you typed failures (class ApiError extends Error) carrying domain fields like status codes, while the cause option ({ cause: originalError }) chains a low-level failure to the high-level error you rethrow, preserving the full story for logs.',
      'This exists because string matching on error messages is the alternative — brittle, unlocalizable, and impossible to branch on safely; a taxonomy lets callers write instanceof RateLimitError instead of /429/.test(e.message).',
      'Interviewers ask candidates to "design errors for an API client" because it is an API-design question disguised as an error question: it reveals whether you think about your callers, retryability, and what information survives across layer boundaries.',
      'The classic mistakes are catching and rethrowing a new error without cause (destroying the original stack trace), and forgetting that instanceof on custom errors can fail across realms or after some transpiler targets unless the prototype is set explicitly.',
    ],
    prerequisites: ['es6-classes', 'error-types-try-catch'],
    leadsTo: ['global-error-handlers'],
    questions: [
      'Design the error hierarchy for an API client so callers can branch on failure type.',
      'What does the error cause option do, and what problem does it solve when rethrowing?',
      'How do you correctly subclass Error so instanceof and stack traces work?',
      'What is AggregateError and which APIs produce it?',
      'Why is matching on error.message an anti-pattern, and what is the alternative?',
    ],
    overlooked: true,
  },
  {
    slug: 'global-error-handlers',
    name: 'Global Error Handlers',
    category: 'errors-debugging',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'window.onerror, the error event, and unhandledrejection — last-resort capture feeding monitoring systems.',
    explanation: [
      "Errors that escape every local handler surface in two global channels: the window error event catches uncaught synchronous throws (and, with capture, resource load failures), while unhandledrejection fires for promises rejected with no catch handler attached by the end of the microtask drain.",
      'These hooks exist because production failures you never see are failures you never fix — they are the foundation every error-monitoring SDK (Sentry-style) builds on, enriching captures with breadcrumbs, releases, and source-map-decoded stacks.',
      'Interviewers ask "how do monitoring SDKs catch everything?" to probe whether you know both channels exist and differ: candidates reliably know onerror and reliably forget unhandledrejection, which in an async-heavy codebase is where most real errors land.',
      'Common mistakes include treating global capture as an error-handling strategy rather than a safety net for telemetry, and not knowing that cross-origin scripts without crossorigin attributes report only "Script error." with all detail stripped.',
    ],
    prerequisites: ['async-error-handling'],
    leadsTo: ['custom-errors-cause'],
    questions: [
      'How do error-monitoring SDKs manage to capture every uncaught error in an app?',
      'What is the unhandledrejection event and when exactly does it fire?',
      'Why do some captured errors show only "Script error." with no stack, and what is the fix?',
      'What can the window error event tell you that try/catch cannot?',
      'Should global handlers replace local error handling? Argue the boundary between them.',
    ],
    overlooked: true,
  },
  {
    slug: 'devtools-debugging',
    name: 'DevTools Debugging',
    category: 'errors-debugging',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 5,
    definition:
      'Breakpoints (conditional, DOM, XHR), watch expressions, console utilities, source maps, and the performance/memory panels.',
    explanation: [
      'Beyond line breakpoints, DevTools can pause on conditions, on DOM subtree or attribute changes, on matching network requests, and on caught or uncaught exceptions — while source maps translate the running minified bundle back to your authored files so all of it works against production code.',
      'This tooling exists because console.log debugging cannot answer the hard questions: what the full call stack and closure state looked like at the moment of failure, who mutated this DOM node, or which code path issued an unexpected request.',
      'Interviewers ask "debug a bug you can\'t reproduce locally" because the answer reveals a real methodology: source-mapped production stacks from monitoring, conditional breakpoints keyed to the failing user\'s data shape, and the network/performance panels — rather than scattering logs and redeploying.',
      "The common mistake is not knowing the high-leverage tools exist: candidates who have never used a conditional breakpoint step through a loop five hundred times, and many have never opened the performance panel that would answer their jank question in one recording.",
    ],
    prerequisites: [],
    leadsTo: ['memory-profiling'],
    questions: [
      'A bug only reproduces in production — walk through your debugging toolkit and approach.',
      'What breakpoint types exist beyond plain line breakpoints, and when is each useful?',
      'How do source maps let you debug minified production code?',
      'How would you find out which code is mutating a specific DOM element?',
      'Which console methods beyond log do you actually use, and for what?',
    ],
  },

  // ── 13. Security ───────────────────────────────────────────────────
  {
    slug: 'xss',
    name: 'Cross-Site Scripting',
    category: 'security',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Injected script executing in users\' sessions (stored, reflected, or DOM-based); mitigated by escaping output, sanitizing HTML, and avoiding dangerous sinks.',
    explanation: [
      'XSS happens when user-controlled data reaches a sink that interprets it as code — innerHTML, document.write, javascript: URLs, eval — at which point the attacker\'s script runs with the victim\'s session, cookies, and DOM, indistinguishable from your own code.',
      'The three variants differ by delivery: stored XSS persists in your database and hits every viewer, reflected XSS bounces off a request parameter via a crafted link, and DOM-based XSS never touches the server — client code itself pipes location or input data into a sink.',
      'The defense is output-side, which is the point interviewers probe with "innerHTML with user data — exploit it, then fix it": escape text for its context (or use textContent), sanitize any genuinely-needed rich HTML with a library like DOMPurify, and layer CSP behind it as defense-in-depth.',
      'The classic candidate mistake is answering "validate input" — input validation is not the defense, because data can be safe in one context and lethal in another; encoding at the point of output for that specific context is what actually stops XSS.',
    ],
    prerequisites: ['dom'],
    leadsTo: ['csp', 'secure-token-storage'],
    questions: [
      'You render user data with innerHTML — demonstrate the vulnerability, then fix it.',
      'Explain stored vs reflected vs DOM-based XSS with an example of each.',
      'Why is input validation insufficient as an XSS defense — what is the real one?',
      'Which DOM APIs are dangerous sinks, and what are the safe alternatives?',
      'How do modern frameworks protect against XSS by default, and how do developers still bypass it?',
      'What can an attacker actually do once they have script execution on your origin?',
    ],
  },
  {
    slug: 'csrf',
    name: 'Cross-Site Request Forgery',
    category: 'security',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'The browser auto-attaches cookies to cross-site requests, enabling forged state-changing requests; mitigated by SameSite cookies, CSRF tokens, and origin checks.',
    explanation: [
      'CSRF exploits the fact that the browser attaches your session cookie to any matching request regardless of who initiated it: a hidden form on evil.com auto-submitting POST /transfer to your bank rides on the victim\'s real session, no credentials stolen.',
      'The attack works precisely because of ambient authority — which yields the interview insight that CSRF targets cookies but not Authorization headers: a bearer token must be explicitly attached by JavaScript, and the attacker\'s page cannot read or set your headers cross-origin.',
      'Defenses break the ambient-authority assumption: SameSite=Lax (now the default) stops cookies on cross-site POSTs, anti-CSRF tokens require a per-session secret the attacker cannot read (because of the same-origin policy), and server-side Origin header checks verify the request source.',
      'The common mistake is believing CORS prevents CSRF — the forged form POST is a simple request that goes through and executes server-side regardless; CORS only governs whether scripts can read responses, so a senior answer keeps these two mechanisms cleanly separate.',
    ],
    prerequisites: ['storage-apis'],
    leadsTo: ['cors'],
    questions: [
      'Walk through how a CSRF attack works end to end.',
      'Why does CSRF target cookie-based sessions but not Authorization headers?',
      'How does SameSite=Lax mitigate CSRF, and what does Strict add?',
      'How do anti-CSRF tokens work, and why can the attacker not just read one?',
      'Does CORS prevent CSRF? Explain precisely why or why not.',
    ],
  },
  {
    slug: 'cors',
    name: 'CORS',
    category: 'security',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Server-side relaxation of the same-origin policy: preflights, allowed origins and headers, and credentials mode.',
    explanation: [
      'The same-origin policy blocks scripts from reading cross-origin responses by default; CORS is the protocol by which a server opts out, declaring via Access-Control-Allow-* headers which origins, methods, and headers may read its responses.',
      'Requests beyond the "simple" set — custom headers like Authorization, methods like PUT/DELETE, non-form content types like application/json — trigger a preflight: the browser sends OPTIONS first and only releases the real request if the server approves.',
      'Interviewers ask "why can\'t the frontend fix a CORS error?" because the answer proves you understand the architecture: the policy is enforced by the browser but configured by the response headers, so the fix is always server-side — and crucially, CORS protects response reading, not the server (the request often still executes).',
      'The classic mistakes are exactly those two: trying to solve CORS with frontend code or request headers, and not knowing that credentials: include forbids the wildcard — Allow-Origin must echo the specific origin and set Allow-Credentials: true.',
    ],
    prerequisites: ['fetch-api'],
    leadsTo: ['csrf'],
    questions: [
      'What exactly triggers a CORS preflight request?',
      'Why can the frontend never fix a CORS error?',
      'What does the same-origin policy actually block, and what does CORS change?',
      'How does credentials mode interact with Access-Control-Allow-Origin: *?',
      'Does a CORS error mean the request never reached the server? Explain.',
      'Walk through the headers exchanged in a preflighted request.',
    ],
  },
  {
    slug: 'csp',
    name: 'Content Security Policy',
    category: 'security',
    level: 'senior',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'A response header whitelisting script, style, and connect sources — with nonces and hashes — as defense-in-depth against XSS.',
    explanation: [
      'CSP is a response header the browser enforces: script-src, style-src, connect-src and friends define where each resource type may load from, so even if an attacker injects a script tag, the browser refuses to execute anything outside the policy — including, with no unsafe-inline, all inline scripts.',
      'It exists as the layer behind escaping: output encoding prevents most XSS, but one missed sink in a large codebase is inevitable, and CSP turns that miss from full compromise into a blocked, reported violation.',
      'The rollout question interviewers ask ("legacy app full of inline scripts — plan it") has a known good answer: start with Content-Security-Policy-Report-Only to collect violations without breaking anything, migrate inline scripts to nonces ( per-response random values on each legitimate tag) or hashes, then enforce and ratchet tighter.',
      'The common mistakes are shipping unsafe-inline (which surrenders the script-injection protection CSP exists for) and deploying an enforcing policy without a report-only phase, breaking analytics, fonts, and third-party embeds in production at once.',
    ],
    prerequisites: ['xss'],
    leadsTo: ['supply-chain-security'],
    questions: [
      'Plan a CSP rollout for a legacy app full of inline scripts.',
      'How do nonces and hashes allow specific inline scripts under CSP?',
      'What does Report-Only mode do, and why start there?',
      'Why does unsafe-inline largely defeat the purpose of a CSP?',
      'Which directives matter most for XSS defense, and what does each control?',
      'How does CSP complement, rather than replace, output escaping?',
    ],
  },
  {
    slug: 'secure-token-storage',
    name: 'Token Storage',
    category: 'security',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'localStorage is readable by any XSS, while httpOnly cookies are CSRF-exposed but SameSite-protected — the canonical client-auth tradeoff.',
    explanation: [
      'The question is really "which attack class do you accept": a token in localStorage is readable by any script on the origin, so a single XSS exfiltrates it for offline reuse; a token in an httpOnly cookie is invisible to JavaScript but gets auto-attached to requests, reopening CSRF.',
      'The mature answer is httpOnly + Secure + SameSite cookies for the session, with CSRF tokens or origin checks covering what SameSite misses — because the XSS-steals-localStorage failure mode (silent, durable, off-site token reuse) is strictly worse than the mitigated-by-default CSRF side.',
      'Interviewers use this as a senior litmus test because it cannot be answered with a fact — it requires holding both attack models simultaneously and reasoning about residual risk, refresh-token handling, and token lifetime.',
      'The most common mistake is declaring httpOnly cookies "safe from XSS": XSS on a cookie-auth site can still issue authenticated requests as the victim from the compromised page — httpOnly prevents token theft and offline reuse, not in-session abuse, which is why CSP and escaping still matter.',
    ],
    prerequisites: ['storage-apis', 'xss', 'csrf'],
    leadsTo: ['cors'],
    questions: [
      'Where do you store a JWT on the client? Defend your choice against both XSS and CSRF.',
      'What does an attacker gain from a token in localStorage that they do not get from an httpOnly cookie under XSS?',
      'Which cookie flags matter for an auth cookie, and what does each one do?',
      'How would you design refresh-token storage and rotation for a SPA?',
      'Does httpOnly make you safe from XSS? Explain the residual risk.',
    ],
  },
  {
    slug: 'prototype-pollution',
    name: 'Prototype Pollution',
    category: 'security',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Attacker-controlled keys like __proto__ or constructor.prototype in merges and parsers poison properties on every object.',
    explanation: [
      'Because property lookup walks the prototype chain, writing to Object.prototype affects every plain object in the program — and a recursive merge that copies user-supplied keys will happily follow a key named __proto__ (or constructor.prototype) straight there, planting properties like isAdmin: true on everything.',
      'The vulnerability exists at the intersection of two innocent features: prototype delegation and generic deep-merge/parse utilities; that is why it has repeatedly surfaced as CVEs in lodash, qs, and countless config mergers rather than in obviously sketchy code.',
      'Interviewers ask "why is a recursive merge of user JSON dangerous?" because it tests whether your prototype-chain knowledge extends to its security consequence — a strong differentiator, since most candidates have never connected the two.',
      'The mitigations are concrete: create lookup objects with Object.create(null) so there is no chain to poison, denylist or skip __proto__/constructor/prototype keys in merges, use Map for user-keyed data, and optionally Object.freeze(Object.prototype) as a backstop.',
      'The common mistake is assuming JSON.parse is the dangerous step — parsing is safe (it creates an own property named __proto__); the pollution happens when merge or assignment logic later traverses that key as an accessor.',
    ],
    prerequisites: ['prototype-chain'],
    leadsTo: ['supply-chain-security'],
    questions: [
      'Why is recursively merging user-supplied JSON into an object dangerous?',
      'Walk through how a __proto__ key in a payload ends up affecting unrelated objects.',
      'What mitigations prevent prototype pollution — name at least three.',
      'Why is Object.create(null) a defense here?',
      'Is JSON.parse itself vulnerable to prototype pollution? Explain precisely where the danger lies.',
    ],
    overlooked: true,
  },
  {
    slug: 'clickjacking',
    name: 'Clickjacking',
    category: 'security',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Invisible iframes capture clicks intended for the attacker\'s page; mitigated by frame-ancestors and X-Frame-Options.',
    explanation: [
      'The attacker loads your authenticated app in an iframe styled with opacity: 0 and positions it so your real "Delete account" or "Pay" button sits exactly under their fake "Win a prize" button — the victim\'s genuine click lands on your genuine UI, with their genuine session.',
      'The attack exists because framing another site is a legitimate, default-allowed capability, and clicks pass through to whatever is visually on top in the frame stack regardless of opacity.',
      'The defense is refusing to be framed: the CSP frame-ancestors directive (the modern control, with origin granularity) or the legacy X-Frame-Options header tells the browser to not render your pages inside frames you have not approved.',
      'Interviewers ask it as a quick header-knowledge check, and the common mistake is offering JavaScript frame-busting (top !== self checks) as the answer — it is bypassable with the sandbox attribute and race tricks, which is exactly why the declarative headers exist.',
    ],
    prerequisites: ['dom'],
    leadsTo: ['csp'],
    questions: [
      'How do you stop your app from being framed by other sites?',
      'Walk through how a clickjacking attack works with an invisible iframe.',
      'Compare frame-ancestors and X-Frame-Options — which wins, and which is current?',
      'Why is JavaScript frame-busting insufficient as a defense?',
      'What kinds of actions make a page a worthwhile clickjacking target?',
    ],
  },
  {
    slug: 'supply-chain-security',
    name: 'Supply-Chain Security',
    category: 'security',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Compromised npm packages, typosquatting, and install scripts; defended with lockfiles, audits, provenance, and minimal dependencies.',
    explanation: [
      'Your app executes every line of every transitive dependency with full privileges — at install time via lifecycle scripts on developer machines and CI (with their tokens and secrets), and at runtime in users\' browsers — so one hijacked maintainer account anywhere in the tree is code execution everywhere.',
      'The attack surface is structural to the npm model: deep transitive graphs, semver ranges that auto-adopt new versions, typosquatted lookalike names, and incidents like event-stream and ua-parser-js proved it is exploited in practice, not hypothetically.',
      'Interviewers ask the blast-radius question to see whether you can reason in layers: committed lockfiles pin exact resolved versions, npm ci enforces them, --ignore-scripts neuters install-time payloads, audit/Socket-style scanning and provenance (signed builds linking package to source) cover the rest, and the cheapest control is simply fewer dependencies.',
      'The common mistake is equating supply-chain security with running npm audit — audit only knows published CVEs, while a freshly hijacked package version is by definition not in the database yet; pinning plus a deliberate upgrade cadence is what actually limits that window.',
    ],
    prerequisites: ['bundlers-module-resolution'],
    leadsTo: ['csp'],
    questions: [
      'A transitive dependency of yours was hijacked — what is the blast radius, and what defenses limit it?',
      'What can a malicious package do at install time vs at runtime?',
      'How do lockfiles and npm ci defend against supply-chain attacks?',
      'Why is npm audit alone insufficient protection?',
      'What is package provenance and what attack does it address?',
      'What policies would you set for adding new dependencies to a team codebase?',
    ],
  },

  // ── 14. Design Patterns in JavaScript ──────────────────────────────
  {
    slug: 'module-pattern',
    name: 'Module Pattern',
    category: 'design-patterns',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Closure-based encapsulation exposing a public API over private state — the ancestor of ES modules.',
    explanation: [
      'An IIFE (or any factory function) declares private variables in its scope and returns an object of functions that close over them; the returned API can read and mutate the private state, but no outside code can reach it — true privacy enforced by scope, not convention.',
      'The pattern existed because pre-2015 JavaScript had no module system or private fields: it was the only way to keep library internals off the global object, and ES modules later standardized exactly this shape — file-scoped privates with explicit exports.',
      'Interviewers ask "private state without classes or # fields?" because the answer proves closures are operational knowledge: you should be able to write a counter or once-initializer whose state is genuinely unreachable.',
      'The common mistake is returning the state object itself (or a mutable reference into it) alongside the API, which silently breaks encapsulation — privacy only holds for what stays captured and unexported.',
    ],
    prerequisites: ['iife', 'closure'],
    leadsTo: ['singleton-pattern'],
    questions: [
      'How do you create private state without classes or # private fields?',
      'Implement a counter module whose count cannot be modified from outside.',
      'How does the module pattern relate to what ES modules give you today?',
      'What role does the closure play in the module pattern?',
      'How can the pattern accidentally leak its private state?',
    ],
  },
  {
    slug: 'factory-pattern',
    name: 'Factory Pattern',
    category: 'design-patterns',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Functions returning configured objects — no new keyword, easy composition, and natural closure-based privacy.',
    explanation: [
      'A factory is a plain function that builds and returns an object, often closing over its arguments for private state; callers never use new, there is no prototype chain to manage, and the factory can return different shapes or implementations depending on input.',
      'It earns its place in JavaScript specifically because functions and object literals are cheap: you get encapsulation without classes, no this-binding hazards (methods can be closures), and trivial composition by spreading or delegating to other factories.',
      'Interviewers ask "factory vs constructor vs class — when each?" to test judgment rather than syntax: factories win for privacy, this-free safety, and runtime polymorphism; classes win when instanceof checks, shared prototype methods at scale, or framework conventions matter.',
      'The honest tradeoff candidates miss is memory: a factory creates fresh function objects per instance while prototype methods are shared — irrelevant for dozens of objects, measurable for hundreds of thousands.',
    ],
    prerequisites: ['object-creation', 'closure'],
    leadsTo: ['inheritance-patterns'],
    questions: [
      'Factory vs constructor vs class in JavaScript — when would you pick each?',
      'How does a factory provide private state that a plain class cannot?',
      'Why are factories immune to the lost-this problem that plagues class methods?',
      'What is the memory tradeoff between factory-created methods and prototype methods?',
      'Write a factory that returns different implementations based on its arguments.',
    ],
  },
  {
    slug: 'singleton-pattern',
    name: 'Singleton',
    category: 'design-patterns',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'One shared instance via module-level state or lazy initialization — noting that ES modules are singletons by design.',
    explanation: [
      'A singleton guarantees a single shared instance: classically a lazy getInstance() that creates on first call and returns the cached instance thereafter — but in modern JavaScript, simply exporting an instance (or module-level state) from an ES module achieves it, because each module is evaluated exactly once and every importer receives the same bindings.',
      'The pattern exists for genuinely shared resources — one config, one connection pool, one event bus — where multiple instances would mean inconsistent state.',
      'Interviewers ask "why are modules already singletons?" to check you know the module cache is doing the work, and the senior follow-up is the caveat: that guarantee is per-realm and per-resolution, so duplicated packages in node_modules or multiple bundles can quietly give you two "singletons".',
      'The common mistake is reaching for it reflexively: a singleton is shared mutable global state, which couples consumers and makes tests order-dependent — dependency injection of one instance usually serves better than a hard-coded global one.',
    ],
    prerequisites: ['module-pattern'],
    leadsTo: ['es-modules'],
    questions: [
      'Implement a lazy singleton in JavaScript.',
      'Why are ES modules effectively singletons already?',
      'When can the module-singleton guarantee break down?',
      'What are the downsides of singletons, particularly for testing?',
      'When is a singleton genuinely the right choice over dependency injection?',
    ],
  },
  {
    slug: 'observer-pattern',
    name: 'Observer vs Pub/Sub',
    category: 'design-patterns',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Subjects notifying subscribers directly vs decoupling through a broker or event channel — the spine of reactivity and event systems.',
    explanation: [
      'In the observer pattern, a subject keeps its own list of subscribers and calls them on change — observers know which subject they watch (DOM listeners, Vue watchers); in pub/sub, an intermediary broker routes named events so publishers and subscribers never reference each other (a global event bus, Redis channels).',
      'The distinction matters architecturally: observer gives direct, traceable coupling suited to local reactivity, while pub/sub buys maximal decoupling at the cost of discoverability — nobody can statically see who handles an event.',
      'Interviewers ask for the difference plus real examples because most candidates use both daily without knowing it: addEventListener is observer, a Vuex/mitt-style event bus is pub/sub, and reactive state systems are observers wired automatically through dependency tracking.',
      "The common mistakes are treating the two terms as synonyms, and — in implementation questions — forgetting unsubscription, which is precisely how observer references become the memory leaks interviewers follow up about.",
    ],
    prerequisites: ['callbacks', 'custom-events'],
    leadsTo: ['event-emitter', 'proxy-reflect'],
    questions: [
      'What is the difference between the observer pattern and pub/sub — give a real example of each from tools you use.',
      'Implement a subject with subscribe, unsubscribe, and notify.',
      'How does the observer pattern relate to reactivity in frameworks like Vue?',
      'How does the observer pattern lead to memory leaks, and what prevents them?',
      'What do you lose in debuggability when moving from direct observation to a pub/sub broker?',
    ],
  },
  {
    slug: 'event-emitter',
    name: 'EventEmitter (implement)',
    category: 'design-patterns',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'on/off/once/emit over a Map of listener sets — the most common "implement a pattern" coding question.',
    explanation: [
      'The core is a Map from event name to a Set of listeners: on() adds, off() deletes, emit() iterates the listeners with the payload, and once() wraps the listener in a self-removing function — with the wrapper trick being that off() for a once-listener must still work, so you map original to wrapper.',
      'It is the pub/sub mechanic underneath Node streams, socket clients, and countless app event buses, which is why interviewers treat implementing it as a proxy for day-to-day architectural fluency.',
      'The question\'s real teeth are the edge cases: the classic "what if a listener unsubscribes during emit?" is answered by iterating a snapshot (or relying on Set\'s deletion-tolerant iteration semantics) so mutation mid-emit cannot skip or double-fire listeners.',
      'Common mistakes are using an array with indexOf-based removal that breaks with duplicate listeners, implementing once() so it cannot be removed via off(), and letting one throwing listener abort delivery to the rest.',
      'Senior candidates also mention the leak angle: long-lived emitters holding listeners from short-lived components is the EventEmitter version of the forgotten-listener memory leak.',
    ],
    prerequisites: ['observer-pattern', 'map-vs-object'],
    leadsTo: ['custom-events'],
    questions: [
      'Implement an EventEmitter with on, off, emit, and once.',
      'What goes wrong if a listener unsubscribes during emit, and how do you make it safe?',
      'How do you implement once() so the listener can still be removed with off()?',
      'Why is a Map of Sets a better backing store than an object of arrays?',
      'Should emit continue if one listener throws? Defend your choice.',
      'How do EventEmitters cause memory leaks in long-lived applications?',
    ],
  },
  {
    slug: 'strategy-pattern',
    name: 'Strategy Pattern',
    category: 'design-patterns',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'Interchangeable algorithms behind one interface — typically an object map of functions — replacing if/else ladders in validation, pricing, and sorting.',
    explanation: [
      'Instead of branching on a type string, you define each algorithm as a function with a shared signature and select it by key from a plain object: strategies[paymentType](order) replaces the six-branch if/else, and adding a payment type becomes adding an entry, not editing a conditional.',
      'In JavaScript the pattern is almost free because functions are first-class — no interfaces or classes required — which is why it appears everywhere from Array.prototype.sort comparators to validation schema rules and webpack configuration.',
      'Interviewers pose it as a refactoring exercise because it measures design instinct: recognizing that a growing conditional over a discriminator is data masquerading as control flow, and that the open/closed fix is a lookup table.',
      'The common mistakes are forgetting the unknown-key case (a missing strategy should fail loudly or fall back deliberately, not throw "undefined is not a function") and over-applying the pattern to a stable two-branch conditional that an if statement served perfectly well.',
    ],
    prerequisites: ['higher-order-functions'],
    leadsTo: ['factory-pattern'],
    questions: [
      'Refactor a six-branch if/else over a type string into the strategy pattern.',
      'How do first-class functions make the strategy pattern trivial in JavaScript?',
      'How should a strategy lookup handle an unknown key?',
      'Where does the strategy pattern show up in APIs you already use?',
      'When is an if/else actually better than introducing strategies?',
    ],
  },
]
