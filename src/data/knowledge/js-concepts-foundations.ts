import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ───────────────────────── 1. Fundamentals & Engine ─────────────────────────
  {
    slug: 'what-is-javascript',
    name: 'What is JavaScript',
    category: 'fundamentals-engine',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 5,
    definition:
      'Single-threaded, garbage-collected, JIT-compiled, multi-paradigm dynamic language with an event-loop concurrency model.',
    explanation: [
      'JavaScript executes on one thread with one call stack, but the host (browser or Node) performs I/O, timers, and networking off-thread and feeds completed work back through the event loop as queued callbacks.',
      'This design exists because the language was built to manipulate a shared, mutable DOM — a single thread eliminates data races on the page without locks.',
      'Interviewers open with it to test whether you can reconcile "single-threaded" with "handles thousands of concurrent requests": the thread never waits, it only schedules.',
      'The classic candidate mistake is answering "JavaScript is interpreted" — modern engines parse to bytecode and JIT-compile hot paths to machine code, so the honest answer is "both, in tiers".',
      'A strong answer also names what the language is NOT: no threads in the language core (workers are host-provided), no manual memory management, no blocking I/O primitives.',
    ],
    prerequisites: [],
    leadsTo: ['ecmascript', 'javascript-engines', 'dom'],
    questions: [
      'JavaScript is single-threaded — how can it handle thousands of concurrent network requests without blocking?',
      'Is JavaScript an interpreted or a compiled language? Defend your answer for a modern engine.',
      'What does "multi-paradigm" mean for JavaScript, and which paradigms do you actually use day to day?',
      'Why was JavaScript designed as single-threaded, and what problems would threads introduce in the browser?',
      'What parts of "JavaScript" are actually provided by the host environment rather than the language?',
    ],
  },
  {
    slug: 'ecmascript',
    name: 'ECMAScript & TC39',
    category: 'fundamentals-engine',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'The language specification JS implements; versions (ES5/ES6/ES2020+) are yearly spec releases driven by the TC39 proposal process.',
    explanation: [
      'ECMAScript is the written contract; JavaScript engines are competing implementations of it, and host APIs like the DOM or fetch live in entirely separate specs.',
      'Since ES2015, TC39 ships a release every year built from proposals that advance through stages 0–4, with stage 3 features often available behind flags or in transpilers before standardization.',
      'The spec exists so the same code behaves identically across V8, SpiderMonkey, and JavaScriptCore — without it, the web of the late 90s (browser-specific forks everywhere) returns.',
      'Interviewers use this to gauge how current you are: a senior should name recent additions they actually use (optional chaining, structuredClone, Array.at, top-level await) rather than stopping at ES6.',
      'The common mistake is conflating JavaScript features with Web APIs — saying "fetch is ES6" or "setTimeout is part of JavaScript" signals fuzzy boundaries.',
    ],
    prerequisites: ['what-is-javascript'],
    leadsTo: ['javascript-engines'],
    questions: [
      'What is the difference between JavaScript and ECMAScript?',
      'How does a feature go from idea to part of the language? Describe the TC39 stage process.',
      'Which ES2020+ features do you use daily, and what did they replace in your code?',
      'Is setTimeout part of ECMAScript? Where is it actually specified?',
      'Why did the spec move from big numbered releases (ES5, ES6) to yearly versions?',
    ],
  },
  {
    slug: 'javascript-engines',
    name: 'JavaScript Engines',
    category: 'fundamentals-engine',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Programs (V8, SpiderMonkey, JavaScriptCore) that parse, compile and execute JS; each browser/runtime embeds one.',
    explanation: [
      'An engine takes source text through tokenizing and parsing into an AST, compiles that to bytecode, executes it, and progressively optimizes hot code to machine code — all while managing the heap and GC.',
      'The engine is only the language core; the embedder (Chrome, Node, Deno) supplies the event loop, timers, and I/O APIs around it, which is why "Node and Chrome both use V8" yet behave differently.',
      'Engines exist as separate, swappable components precisely so runtimes can embed them: Node embeds V8, Safari uses JavaScriptCore, Firefox uses SpiderMonkey, Bun uses JavaScriptCore.',
      'Interviewers ask "what happens between loading a script and it running" to see if you can name concrete phases instead of hand-waving "the browser runs it".',
      'A frequent mistake is attributing the event loop to the engine — V8 has no event loop of its own; libuv (Node) and the browser implement it.',
    ],
    prerequisites: ['ecmascript'],
    leadsTo: ['v8-architecture', 'jit-compilation'],
    questions: [
      'Walk me through what happens between a script being downloaded and its first line executing.',
      'Name three JavaScript engines and where each one is embedded.',
      'Is the event loop part of the JavaScript engine? Who actually implements it?',
      'Node.js and Chrome both run V8 — why do they expose different global APIs?',
      'What are the major phases an engine puts your source code through?',
    ],
  },
  {
    slug: 'v8-architecture',
    name: 'V8 Architecture',
    category: 'fundamentals-engine',
    level: 'senior',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Parser → AST → Ignition (bytecode interpreter) → Maglev/TurboFan (optimizing JIT), plus Orinoco GC.',
    explanation: [
      'V8 parses source into an AST, Ignition compiles that to compact bytecode and starts interpreting immediately, while profiling counters identify hot functions for the optimizing tiers (Maglev for fast mid-tier, TurboFan for aggressive optimization).',
      'The tiered design balances startup latency against peak speed: interpreting bytecode starts fast and uses little memory, while machine code from TurboFan is generated only where the time is actually spent.',
      'Orinoco, the garbage collector, runs largely concurrent and parallel to execution, splitting the heap into a young generation (cheap scavenges) and old generation (mark-sweep-compact).',
      'This is a senior/staff filter question: interviewers want to hear "bytecode first, optimize hot paths with type feedback, deoptimize when assumptions break" rather than a memorized diagram.',
      'The common mistake is claiming V8 compiles everything to machine code up front — most code only ever runs as bytecode, and that is by design.',
    ],
    prerequisites: ['javascript-engines'],
    leadsTo: ['jit-compilation', 'hidden-classes-inline-caches', 'garbage-collection'],
    questions: [
      'Walk through what V8 does with your code from source text to execution.',
      'What is Ignition, and why does V8 interpret bytecode instead of compiling everything to machine code immediately?',
      'How does V8 decide which functions to optimize, and what information does it use?',
      'What is the difference between bytecode and optimized machine code in V8, and when does each run?',
      'What is Orinoco and how does it avoid pausing your application for long GC cycles?',
      'Why does V8 have multiple compiler tiers (Ignition, Maglev, TurboFan) instead of one?',
    ],
  },
  {
    slug: 'jit-compilation',
    name: 'JIT Compilation & Deoptimization',
    category: 'fundamentals-engine',
    level: 'senior',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'Hot functions are optimized with assumptions (types, shapes); violated assumptions trigger deoptimization back to bytecode.',
    explanation: [
      'The JIT watches type feedback gathered while bytecode runs — "this parameter was always a small integer, this object always had shape X" — and compiles machine code specialized to those assumptions, with guard checks at the entry points.',
      'When a guard fails (a string suddenly arrives where numbers were expected), the engine deoptimizes: it throws away the machine code mid-flight and falls back to bytecode, which is correct but slower.',
      'JIT exists because a dynamic language has no static types to compile against; speculative specialization is how JS gets within range of statically typed performance.',
      'Interviewers ask this to probe whether your performance intuitions are mechanical or folkloric — e.g., why monomorphic functions and stable object shapes matter in hot paths.',
      'The classic mistake is "optimizing" by micro-rewriting syntax; the real lever is type and shape stability — passing mixed types through a hot function silently costs deopts and megamorphic lookups.',
    ],
    prerequisites: ['v8-architecture'],
    leadsTo: ['hidden-classes-inline-caches'],
    questions: [
      'What is a deoptimization, what triggers it, and what does it cost?',
      'Why can adding properties to objects in different orders make code measurably slower?',
      'What assumptions does the optimizing compiler bake into machine code, and how are they protected?',
      'How would you detect that a hot function in production is repeatedly deoptimizing?',
      'Why do JS engines compile speculatively instead of ahead of time?',
      'What coding patterns keep a hot function JIT-friendly?',
    ],
  },
  {
    slug: 'hidden-classes-inline-caches',
    name: 'Hidden Classes & Inline Caches',
    category: 'fundamentals-engine',
    level: 'expert',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Engines assign shared "shapes" to objects with identical layouts; call sites cache lookups per shape (monomorphic → megamorphic).',
    explanation: [
      'Instead of dictionary lookups for every property access, the engine gives objects created with the same property layout a shared hidden class (shape), so `obj.x` compiles down to "check shape, read fixed offset".',
      'Inline caches remember which shapes a given call site has seen: one shape is monomorphic (fastest), a few is polymorphic, and too many makes it megamorphic — at which point the engine gives up on caching and falls back to slow lookups.',
      'Operations like `delete obj.x` or adding properties in inconsistent orders force objects into mismatched or dictionary-mode shapes, silently destroying these optimizations across every call site that touches them.',
      'This is a staff-level signal question: almost no candidate can explain WHY constructors that initialize all fields in a fixed order are fast, and interviewers use it to separate mechanical understanding from cargo-culted advice.',
      'The common mistake is treating this as trivia — in real code it shows up as "the same function is 10x slower on one data source", because that source produces objects with heterogeneous shapes.',
    ],
    prerequisites: ['jit-compilation', 'prototype'],
    leadsTo: ['memory-profiling'],
    questions: [
      'Why is `delete obj.x` considered a performance smell, and what should you do instead?',
      'What is a hidden class (shape), and how does it turn property access into a fixed-offset read?',
      'Explain monomorphic vs polymorphic vs megamorphic call sites and how a site degrades.',
      'Why does initializing all object properties in the constructor, in the same order, matter for performance?',
      'Two arrays of "the same" objects from different APIs run through one function at very different speeds — what engine-level cause would you investigate?',
    ],
    overlooked: true,
  },
  {
    slug: 'strict-mode',
    name: 'Strict Mode',
    category: 'fundamentals-engine',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      "`'use strict'` opt-in (automatic in modules/classes): undeclared-assignment throws, `this` is undefined in plain calls, bans `with`, duplicate params.",
    explanation: [
      'Strict mode turns a set of silent legacy failures into thrown errors: assigning to an undeclared variable becomes a ReferenceError instead of an accidental global, writes to non-writable properties throw instead of no-oping, and `with` and duplicate parameter names are banned outright.',
      'Its most interview-relevant change is `this`: in a plain function call, `this` is `undefined` under strict mode instead of being silently boxed to the global object — which is why "lost this" bugs throw loudly in modern code.',
      'It exists because ES5 needed to fix dangerous semantics without breaking the old web; an opt-in directive let new code get the corrected behavior while legacy scripts kept running.',
      'ES modules and class bodies are strict automatically, so in any modern codebase you are effectively always in strict mode — interviewers ask this to check you know which world your code runs in.',
      'The common mistake is reciting "strict mode makes JS stricter" without a single concrete example, or not knowing that the directive is per-function/per-script and unnecessary inside modules.',
    ],
    prerequisites: ['this-keyword'],
    leadsTo: ['es-modules'],
    questions: [
      'What does strict mode change about the value of `this` in a plain function call?',
      'Why do ES modules not need the `\'use strict\'` directive?',
      'Name three silent failures that strict mode converts into thrown errors.',
      'How can you tell whether a given piece of code is running in strict mode?',
      'Why was strict mode introduced as an opt-in directive instead of just changing the language?',
    ],
  },

  // ───────────────────────────── 2. Memory Model ──────────────────────────────
  {
    slug: 'primitive-vs-reference',
    name: 'Primitives vs References',
    category: 'memory-model',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Primitives copy by value; objects copy by reference — two variables can point at one heap object.',
    explanation: [
      'Assigning a primitive copies the value itself, but assigning an object copies only the reference — both variables then point at the same heap allocation, so a mutation through one is visible through the other.',
      'Function arguments follow the same rule: JS is always pass-by-value, but for objects the value being passed IS the reference, which is why a function can mutate your object yet reassigning the parameter inside it changes nothing outside.',
      'This split exists because primitives are small, immutable, and cheap to copy, while objects are arbitrarily large — copying them on every assignment would be ruinously expensive.',
      'Interviewers ask it because it is the root cause of an enormous bug class: shared state mutated at a distance, React/Vue change detection not firing, "why did editing b change a".',
      'The classic candidate mistake is saying "objects are pass-by-reference" — true pass-by-reference would let a function reassign the caller\'s variable, which JS cannot do.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['stack-vs-heap', 'shallow-vs-deep-copy', 'object-immutability'],
    questions: [
      'I assigned `const b = a` and mutated `b`, and `a` changed too — explain exactly why.',
      'Is JavaScript pass-by-value or pass-by-reference? Prove your answer with a code example.',
      'Why can a function mutate an object you pass in, but reassigning the parameter has no effect outside?',
      'How do you compare two objects for equality, given that `===` compares references?',
      'Why does `"a" === "a"` hold but `{} === {}` is false?',
      'How does this distinction explain why frameworks want immutable state updates?',
    ],
  },
  {
    slug: 'stack-vs-heap',
    name: 'Stack vs Heap',
    category: 'memory-model',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Call frames and primitives live on the stack (LIFO, fixed-size); objects, arrays, functions, closures live on the heap (GC-managed).',
    explanation: [
      'The stack holds call frames — each function invocation pushes a frame with its locals and pops it on return — making allocation and deallocation a pointer bump, while the heap holds dynamically sized objects whose lifetimes the GC must track.',
      'The stack is fast precisely because it is rigid: fixed-size, strictly LIFO, and bounded (hence stack overflow on runaway recursion); anything that must outlive its function call cannot live there.',
      'That is exactly why closures force captured variables onto the heap — the inner function may run long after its parent frame is gone, so the engine moves the captured environment to GC-managed memory.',
      'Interviewers ask "where does a closure\'s variable live" because it links three topics at once: memory layout, closures, and garbage collection; a correct answer shows the mental model is real.',
      'The common mistake is over-literal bookkeeping ("all primitives are always on the stack") — engines store object-held primitives inside heap objects and may optimize freely; the model is about lifetimes, not a physical law.',
    ],
    prerequisites: ['primitive-vs-reference'],
    leadsTo: ['garbage-collection', 'execution-context', 'call-stack'],
    questions: [
      'Where does a variable captured by a closure live after the outer function returns, and why must it live there?',
      'Why is stack allocation so much cheaper than heap allocation?',
      'What causes a stack overflow, and why does the heap not have an equivalent hard limit?',
      'When you write `const user = { name: "x" }`, what exactly is on the stack and what is on the heap?',
      'Why does the heap need a garbage collector while the stack does not?',
    ],
  },
  {
    slug: 'garbage-collection',
    name: 'Garbage Collection',
    category: 'memory-model',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Reachability-based reclamation: generational scavenging for young objects, mark-sweep-compact for old; roots are globals, stack, active closures.',
    explanation: [
      'The GC starts from roots — globals, the current stack, active closure environments — and marks everything reachable by following references; whatever is left unmarked is garbage and gets reclaimed.',
      'V8 splits the heap generationally: most objects die young, so the small "new space" is scavenged cheaply and often, while survivors get promoted to old space, which is collected with concurrent mark-sweep-compact at a higher per-object cost.',
      'Reachability — not reference counting — is the criterion, which is why circular references between two abandoned objects are collected fine: neither is reachable from a root.',
      'Interviewers ask it because every memory leak is a reachability story: the question "how does GC decide what to free" sets up "so what keeps things alive that shouldn\'t be".',
      'Common mistakes: claiming JS uses reference counting, believing setting a variable to null forces collection, or thinking you can trigger GC from code — you can only make objects unreachable and let the engine decide.',
    ],
    prerequisites: ['stack-vs-heap'],
    leadsTo: ['memory-leaks', 'weakmap-weakset', 'closure'],
    questions: [
      'How does the garbage collector decide what memory to free?',
      'Can you force garbage collection from JavaScript code? What is the closest you can get?',
      'Why are circular references not a problem for a mark-and-sweep collector?',
      'What are GC roots in a browser tab, and why do active closures count as roots?',
      'What is generational collection, and what is the cost of an object being promoted to old space?',
      'Does setting `obj = null` free the object\'s memory? Explain precisely.',
    ],
  },
  {
    slug: 'memory-leaks',
    name: 'Memory Leaks',
    category: 'memory-model',
    level: 'senior',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Unintended reachability: forgotten timers/listeners, detached DOM nodes, unbounded caches, closures pinning large objects.',
    explanation: [
      'A leak in a GC language is never "forgot to free" — it is an object that is still reachable from a root even though the program no longer needs it, so the collector is correctly refusing to touch it.',
      'The canonical sources are mechanical: a setInterval or event listener that was never removed keeps its callback alive, the callback\'s closure keeps everything it captured alive, and a detached DOM subtree held by one stray variable keeps the whole tree in memory.',
      'Unbounded module-level caches and Maps are the slow-burn variant: each entry is individually justified, but nothing ever evicts, so the heap only grows.',
      'Interviewers frame it as "the app gets slow after hours of use — debug it" because it tests reachability understanding, DevTools fluency, and SPA component-lifecycle hygiene all at once.',
      'The classic mistake in long-lived SPAs is mounting listeners, observers, and subscriptions in components without a teardown path — every unmount/remount cycle then stacks another live copy.',
    ],
    prerequisites: ['garbage-collection', 'closure'],
    leadsTo: ['memory-profiling', 'weakmap-weakset'],
    questions: [
      'A single-page app slows down and bloats after a few hours of use — describe how you would diagnose and fix it.',
      'Name three common sources of memory leaks in frontend code and the fix for each.',
      'How can a closure cause a memory leak even though closures are a normal language feature?',
      'What is a detached DOM node, and how does one stray reference retain an entire subtree?',
      'How do you design a cache so it cannot become a leak?',
      'Why do leaks in a garbage-collected language even exist, given the GC frees unused memory?',
    ],
  },
  {
    slug: 'weakmap-weakset',
    name: 'WeakMap & WeakSet',
    category: 'memory-model',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      "Collections whose keys are weakly held — entries don't prevent GC; ideal for object-keyed metadata and caches.",
    explanation: [
      'A regular Map holds strong references to its keys, so using objects as cache keys pins them in memory forever; a WeakMap holds keys weakly, meaning once the key object is unreachable elsewhere, the whole entry is eligible for collection.',
      'This makes WeakMap the right tool for attaching metadata to objects you do not own — DOM nodes, request objects, third-party instances — without altering them or extending their lifetime.',
      'The weakness has a hard consequence: entries can vanish whenever GC runs, so WeakMaps are deliberately non-iterable and have no size — exposing those would make garbage collection observable and non-deterministic.',
      'Interviewers use "Map vs WeakMap for a cache" to check whether you connect data structures to GC behavior; the senior answer notes WeakMap keys must be objects and the cache evicts itself when keys die.',
      'The common mistake is misreading the direction of weakness — the KEY is weakly held, not the value; a WeakMap does nothing for you if your keys are strings or if something else still strongly holds the key.',
    ],
    prerequisites: ['garbage-collection'],
    leadsTo: ['weakref-finalizationregistry', 'memoization'],
    questions: [
      'When would you choose a WeakMap over a Map for a cache, and what do you give up?',
      'Why can\'t you iterate a WeakMap or ask for its size?',
      'How would you associate private metadata with DOM elements without leaking memory when they are removed?',
      'Why must WeakMap keys be objects rather than strings or numbers?',
      'In a WeakMap, what exactly is weakly held — the key, the value, or both? What follows from that?',
    ],
    overlooked: true,
  },
  {
    slug: 'weakref-finalizationregistry',
    name: 'WeakRef & FinalizationRegistry',
    category: 'memory-model',
    level: 'expert',
    importance: 2,
    interviewFrequency: 1,
    realWorldUsage: 2,
    definition:
      'Explicit weak references and post-GC callbacks; last-resort tools for caches interacting with GC.',
    explanation: [
      'A WeakRef wraps an object without keeping it alive: `ref.deref()` returns the object if it still exists or undefined if the GC has reclaimed it, making collection directly observable from code for the first time.',
      'FinalizationRegistry registers a callback to run sometime after a target object is collected — useful for releasing external resources (file handles, WASM memory, cross-worker handles) tied to a JS object\'s lifetime.',
      'Both exist for a narrow class of problems — value-holding caches that should release under memory pressure, JS wrappers around native resources — where WeakMap\'s key-side weakness is not enough.',
      'The spec itself warns against them: GC timing is non-deterministic and engine-dependent, finalizers may run late or (on process exit) never, so no correctness logic can depend on them.',
      'Interviewers ask "when would you ever need a WeakRef" mostly to see if you know the answer is "almost never" — the common mistake is treating finalizers as reliable destructors like C++ RAII, which they are explicitly not.',
    ],
    prerequisites: ['weakmap-weakset'],
    leadsTo: [],
    questions: [
      'When would you legitimately reach for a WeakRef, and what simpler tools should you rule out first?',
      'What does `WeakRef.prototype.deref()` return, and why must callers always handle the empty case?',
      'Why can you not rely on a FinalizationRegistry callback ever running?',
      'How would you build a cache that lets the GC evict values under memory pressure?',
      'Why do the spec authors explicitly discourage using these APIs for program correctness?',
    ],
    overlooked: true,
  },
  {
    slug: 'memory-profiling',
    name: 'Memory Profiling',
    category: 'memory-model',
    level: 'senior',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 4,
    definition:
      'Heap snapshots, allocation timelines, retainer chains in DevTools to locate leaks and churn.',
    explanation: [
      'The core workflow is comparative: snapshot the heap, perform the suspect action and its undo (open and close a dialog), snapshot again, and diff — objects that survive the round trip when they should not are your leak candidates.',
      'For each retained object, the retainer chain shows the path of references from a GC root down to it; the fix is always to break the cheapest link in that chain (remove the listener, clear the map entry, null the field).',
      'Allocation timelines and sampling profilers answer a different question — not "what is retained" but "what is churning", i.e., code allocating so fast that GC pauses become a performance problem.',
      'Interviewers ask for the heap-snapshot walkthrough because leak theory is common but tool fluency is rare; describing the three-snapshot technique and reading "Detached" entries signals real production debugging.',
      'The common mistake is staring at total heap size or shallow sizes — what matters is retained size (what would be freed if this object died) and growth across repeated user actions, not any single number.',
    ],
    prerequisites: ['memory-leaks'],
    leadsTo: ['devtools-debugging'],
    questions: [
      'Walk me through finding a memory leak with DevTools heap snapshots, step by step.',
      'What is a retainer chain, and how do you use it to decide what code to fix?',
      'What is the difference between shallow size and retained size in a heap snapshot?',
      'How would you confirm in DevTools that detached DOM nodes are being leaked?',
      'When would you reach for an allocation timeline instead of a heap snapshot?',
    ],
    overlooked: true,
  },

  // ───────────────────── 3. Variables, Types & Coercion ──────────────────────
  {
    slug: 'var-let-const',
    name: 'var vs let vs const',
    category: 'types-coercion',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Function-scoped, hoisted-to-undefined `var` vs block-scoped, TDZ-guarded `let`/`const`; const = binding immutability, not value.',
    explanation: [
      '`var` is function-scoped and initialized to undefined during hoisting, so it leaks out of blocks and reads as undefined before its line; `let`/`const` are block-scoped and hoisted but uninitialized, so early access throws (the temporal dead zone).',
      'The interview-famous consequence is the loop: `for (var i ...)` with setTimeout logs the final value N times because every callback closes over one shared `i`, while `let` creates a fresh binding per iteration, so each closure captures its own value.',
      '`const` locks the binding, not the value — a const object can still be mutated freely; only reassignment of the variable itself is forbidden, which is why `Object.freeze` exists as a separate tool.',
      '`let`/`const` were added because var\'s function scoping plus silent hoisting produced an entire bug class (loop closures, accidental shadowing, use-before-declare reading as undefined) that block scoping eliminates.',
      'Interviewers lead with this because it fans out into hoisting, TDZ, closures, and scope in one question; the most common candidate failure is saying "const makes the value immutable".',
    ],
    prerequisites: ['scope'],
    leadsTo: ['scope', 'hoisting', 'temporal-dead-zone'],
    questions: [
      'What does `for (var i = 0; i < 3; i++) setTimeout(() => console.log(i))` print, why, and how do you fix it?',
      'Is an object declared with `const` immutable? What exactly does const guarantee?',
      'Explain the scoping difference between var and let with a concrete code example.',
      'Are let and const hoisted? Reconcile your answer with the ReferenceError they throw before declaration.',
      'Why does the language still support var, and when (if ever) would you write it today?',
      'What changes when `let` is used in a for-loop header versus `var`, at the binding level?',
    ],
  },
  {
    slug: 'data-types',
    name: 'Data Types',
    category: 'types-coercion',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Seven primitives (string, number, boolean, null, undefined, symbol, bigint) plus object; dynamic typing per value, not per variable.',
    explanation: [
      'JavaScript has exactly seven primitive types — string, number, boolean, null, undefined, symbol, bigint — and one compound type, object, under which functions, arrays, dates, and regexes all fall.',
      'Typing is dynamic and attached to values, not variables: a variable can hold a number now and a string later, which is why type errors surface at the use site, often far from the assignment that caused them.',
      'Primitives are immutable; the reason `"abc".toUpperCase()` works at all is that the engine temporarily wraps the primitive in an object (autoboxing) to provide methods, then discards the wrapper.',
      'Interviewers ask candidates to list the primitives because it is a cheap completeness check — most forget symbol or bigint — and because `typeof null === "object"` (a fossilized 1995 bug) is the standard follow-up.',
      'The common mistake is calling arrays or functions separate types: `typeof []` is "object" and arrays are objects with numeric keys; only `typeof fn === "function"` gets special treatment.',
    ],
    prerequisites: [],
    leadsTo: [
      'primitive-vs-reference',
      'null-vs-undefined',
      'truthy-falsy',
      'typeof-instanceof',
      'ieee-754-floating-point',
    ],
    questions: [
      'List all the primitive types in JavaScript. Which two do candidates usually forget?',
      'Why does `typeof null` return "object", and how do you reliably test for null?',
      'How can a string primitive have methods like `.toUpperCase()` if primitives are not objects?',
      'Is an array a distinct type in JavaScript? How do you reliably detect one?',
      'What does it mean that JavaScript is dynamically typed at the value level rather than the variable level?',
    ],
  },
  {
    slug: 'null-vs-undefined',
    name: 'null vs undefined',
    category: 'types-coercion',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'undefined = not assigned (the default state); null = intentional absence assigned by code; `null == undefined` only loosely.',
    explanation: [
      'undefined is the language\'s own default — uninitialized variables, missing properties, absent function arguments, and functions without a return all produce it — whereas null almost never arises naturally; it appears because some code deliberately assigned "no value here".',
      'That convention gives you a debugging signal: undefined usually means "nobody ever set this" (possibly a bug), null means "somebody decided this is empty" (a represented state).',
      'They behave differently under conversion: `Number(null)` is 0 but `Number(undefined)` is NaN, JSON.stringify keeps null but drops undefined-valued properties, and default parameters trigger on undefined but not on null.',
      'The pair `null == undefined` being true while `null === undefined` is false underlies the one widely accepted use of loose equality: `x == null` checks both absent states in a single expression.',
      'Interviewers ask "when would you assign null on purpose" to test whether you treat the distinction as a convention you can apply in API design; the common mistake is calling them interchangeable and then being unable to explain the JSON or default-parameter differences.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['optional-chaining-nullish', 'equality-comparison'],
    questions: [
      'When would you deliberately assign null rather than leaving a value undefined?',
      'Why is `null == undefined` true while `null === undefined` is false, and where is that loose check actually idiomatic?',
      'How do JSON.stringify and default function parameters treat null and undefined differently?',
      'A field in your API response can be missing, null, or set — how do you model and check each case?',
      'What are all the ways a variable or expression ends up as undefined without anyone assigning it?',
    ],
  },
  {
    slug: 'truthy-falsy',
    name: 'Truthy & Falsy',
    category: 'types-coercion',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      "Eight falsy values (false, 0, -0, 0n, '', null, undefined, NaN); everything else is truthy, including [] and {}.",
    explanation: [
      'Boolean contexts (if, &&, ||, !) run ToBoolean on the value, and the falsy list is closed and memorizable: false, 0, -0, 0n, empty string, null, undefined, NaN — every other value, including [] and {} and "0", is truthy.',
      'The trap interviewers love is that truthiness and loose equality use different algorithms: `[]` is truthy, yet `[] == false` is true, because `==` converts the array to "" and then to 0 instead of calling ToBoolean.',
      'This matters daily because shorthand guards like `if (value)` silently reject legitimate values — 0 for a quantity, "" for an optional string — producing bugs where valid input is treated as missing.',
      'Truthiness exists as ergonomic shorthand for "is there something here", and the modern fix for its falsy-zero problem is precise checks (`value != null`) or nullish coalescing rather than `||` defaults.',
      'The common candidate mistake is assuming empty array/object are falsy (they are objects, hence truthy) or being unable to recite the falsy list when asked.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['type-coercion'],
    questions: [
      'List every falsy value in JavaScript.',
      'Is an empty array truthy? Then why does `[] == false` evaluate to true?',
      'Give a real bug caused by `if (value)` guarding a field where 0 or "" is a legitimate value.',
      'How do `||` and `??` differ when applied to a falsy-but-valid value like 0?',
      'What algorithm runs when a value appears in an `if` condition, and how does it differ from `==` comparison?',
    ],
  },
  {
    slug: 'type-coercion',
    name: 'Type Coercion',
    category: 'types-coercion',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Implicit conversion rules: ToPrimitive (valueOf/toString), numeric conversion in comparisons, string conversion with `+`.',
    explanation: [
      'Coercion is rule-based, not random: objects are first reduced to primitives via ToPrimitive (try valueOf, then toString, order depending on hint), then the operator decides — binary `+` concatenates if either side is a string, while `-`, `*`, `/`, and relational operators force numbers.',
      'That asymmetry of `+` explains the classics: `"5" - 1` is 4 (numeric coercion) but `"5" + 1` is "51" (string wins), and `[] + {}` is "[object Object]" because both sides become strings via toString.',
      'Coercion exists because a dynamically typed scripting language chose to keep mixed-type expressions running rather than throwing; the cost is that mistakes propagate as wrong values (NaN, "undefined" strings) instead of errors at the source.',
      'Interviewers use coercion puzzles less for trivia than as a proxy: a candidate who can walk `[] + {}` step by step through ToPrimitive demonstrably reasons from rules rather than memory.',
      'The common mistake is hand-waving "JavaScript is weird" instead of naming the algorithm — and in real code, relying on implicit coercion where an explicit `Number(x)`, `String(x)`, or template literal would document intent.',
    ],
    prerequisites: ['truthy-falsy'],
    leadsTo: ['equality-comparison'],
    questions: [
      'Explain step by step why `[] + {}` and `{} + []` can produce different results.',
      'What does `"5" - 1` evaluate to, and why does `"5" + 1` behave differently?',
      'Describe the ToPrimitive algorithm — when is valueOf tried before toString?',
      'How does an object end up coerced in a comparison like `obj > 1`? What methods get called?',
      'Where in real application code does implicit coercion cause bugs, and what is your rule for avoiding them?',
      'What does `1 + true + "1"` evaluate to? Walk through each operator.',
    ],
  },
  {
    slug: 'equality-comparison',
    name: '== vs === vs Object.is',
    category: 'types-coercion',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      '`===` no coercion; `==` coerces (only idiomatic use: `x == null`); Object.is differs on NaN and ±0.',
    explanation: [
      'Strict equality `===` compares type first and never converts; loose equality `==` runs the Abstract Equality algorithm, coercing operands toward numbers (with special cases for null/undefined) until types match.',
      'The canonical walkthrough is `[] == ![]`: `![]` is false (arrays are truthy), false coerces to 0, `[]` goes ToPrimitive to "" and then to 0, so 0 == 0 — true; reproducing this chain is the whole point of the question.',
      '`Object.is` patches the two IEEE-754 edge cases `===` inherits: `Object.is(NaN, NaN)` is true (=== says false) and `Object.is(0, -0)` is false (=== says true) — which is why frameworks use it for change detection.',
      'For objects, all three compare references, not contents — two structurally identical objects are never equal — which is the equality fact that matters most in day-to-day code.',
      'Interviewers ask this because it tests coercion mastery and engineering judgment together; the common mistakes are banning `==` absolutely while not recognizing the idiomatic `x == null`, and checking NaN with `x === NaN` instead of `Number.isNaN(x)`.',
    ],
    prerequisites: ['type-coercion'],
    leadsTo: [],
    questions: [
      'Walk through why `[] == ![]` evaluates to true, step by step.',
      'How do `==`, `===`, and `Object.is` each treat NaN and -0?',
      'What is the one widely accepted idiomatic use of `==`, and what exactly does it check?',
      'How do you reliably check whether a value is NaN, and why does `x === NaN` fail?',
      'Why are two objects with identical contents not equal under any of the three operators, and how do you compare them?',
      'When would a framework prefer Object.is over === for detecting state changes?',
    ],
  },
  {
    slug: 'typeof-instanceof',
    name: 'typeof & instanceof',
    category: 'types-coercion',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      "typeof returns a primitive-type string (with the null quirk); instanceof walks the prototype chain against a constructor's prototype.",
    explanation: [
      'typeof is a static-ish operator answering "what kind of primitive is this": it returns one of a fixed set of strings, lumps all objects (including arrays and null) under "object", special-cases functions, and is uniquely safe on undeclared identifiers.',
      'instanceof answers a completely different question — "does Constructor.prototype appear anywhere on this object\'s prototype chain" — so it works for class hierarchies but says nothing about primitives (`"x" instanceof String` is false).',
      'The `typeof null === "object"` quirk is a 1995 implementation artifact (null\'s type tag matched objects) that can never be fixed without breaking the web — and it is the single most asked trivia follow-up here.',
      'instanceof has real failure modes seniors should know: it lies across realms (an array from an iframe fails `instanceof Array`, hence `Array.isArray`), and `Symbol.hasInstance` lets classes customize it entirely.',
      'The common mistake is treating these as interchangeable type checks — robust code uses typeof for primitives, Array.isArray for arrays, and instanceof only for own-realm class instances.',
    ],
    prerequisites: ['data-types', 'prototype-chain'],
    leadsTo: [],
    questions: [
      'Why does `typeof null` return "object", and what should you use to test for null?',
      'How does instanceof actually work? Describe the lookup it performs.',
      'Why does `[] instanceof Array` fail for an array created in an iframe, and what is the correct check?',
      'Why is `"hello" instanceof String` false while `typeof "hello"` is "string"?',
      'When is typeof the only safe way to probe a name that might not be declared?',
      'How can a class customize what instanceof returns for it?',
    ],
  },
  {
    slug: 'ieee-754-floating-point',
    name: 'Floating Point & Safe Integers',
    category: 'types-coercion',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'All numbers are 64-bit IEEE-754 doubles: 0.1 + 0.2 !== 0.3; integers safe only to 2^53−1.',
    explanation: [
      'JavaScript has a single number type — the 64-bit IEEE-754 double — so 0.1 and 0.2 are stored as the nearest representable binary fractions; their stored errors survive the addition, yielding 0.30000000000000004 instead of 0.3.',
      'This is not a JavaScript bug but binary arithmetic: 0.1 has no finite base-2 representation for the same reason 1/3 has none in base 10, and every language using doubles (Python, Java\'s double, C) reproduces it.',
      'The 53-bit mantissa also caps exact integers at Number.MAX_SAFE_INTEGER (2^53 − 1 = 9007199254740991); beyond it, adjacent integers collapse — the practical symptom is 64-bit database IDs silently corrupting in JSON.parse.',
      'The professional answers are: compare with a tolerance (Math.abs(a − b) < Number.EPSILON-scaled), do money in integer cents or a decimal library, use Number.isSafeInteger as a guard, and BigInt or string IDs past 2^53.',
      'Interviewers ask 0.1 + 0.2 because it is a two-part trap: most candidates know the output, but few can explain the binary-representation cause or name the safe-integer boundary — stopping at "floating point is weird" is the common failure.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['bigint'],
    questions: [
      'Why does 0.1 + 0.2 !== 0.3, at the level of binary representation?',
      'How do you correctly compare two floating-point results for equality?',
      'What is Number.MAX_SAFE_INTEGER, where does the value come from, and what happens above it?',
      'Your backend sends 64-bit numeric IDs and some arrive corrupted after JSON.parse — explain the cause and two fixes.',
      'How would you handle money amounts in a JavaScript application, and why not plain floats?',
      'What does Number.EPSILON represent and how is it used?',
    ],
    overlooked: true,
  },
  {
    slug: 'bigint',
    name: 'BigInt',
    category: 'types-coercion',
    level: 'intermediate',
    importance: 2,
    interviewFrequency: 2,
    realWorldUsage: 2,
    definition:
      'Arbitrary-precision integers (`10n`); cannot mix with Number in arithmetic; not JSON-serializable.',
    explanation: [
      'BigInt is a separate primitive type for integers of arbitrary size, written with an `n` suffix (`9007199254740993n`) or `BigInt()`, existing precisely because doubles lose integer precision past 2^53 − 1.',
      'It refuses implicit mixing: `1n + 1` throws a TypeError rather than silently choosing a precision, so you must convert explicitly at the boundary — a deliberate design choice that surprises people used to coercion everywhere else.',
      'JSON.stringify throws on BigInt values, which is the practical pain point: APIs with 64-bit IDs typically transport them as strings, converting to BigInt only when arithmetic is genuinely needed.',
      'Division truncates (`7n / 2n` is `3n`) because BigInts are integers, and they are slower than Numbers — so the rule is to use them only where exactness above 2^53 matters: IDs, timestamps in nanoseconds, crypto, financial ledgers.',
      'Interviewers ask "when do you need BigInt on the frontend" to see if you connect it to the safe-integer limit; the common mistake is not knowing mixing throws, or reaching for BigInt when string IDs (never used arithmetically) would do.',
    ],
    prerequisites: ['ieee-754-floating-point'],
    leadsTo: [],
    questions: [
      'When do you actually need BigInt in a frontend application?',
      'What happens when you write `1n + 1`, and why did TC39 choose that behavior?',
      'How do you transport 64-bit integer IDs through JSON given that BigInt is not serializable?',
      'What does `7n / 2n` evaluate to, and why?',
      'Compare BigInt with storing large IDs as strings — when is each the right call?',
    ],
  },
  {
    slug: 'symbol',
    name: 'Symbol',
    category: 'types-coercion',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Unique, non-enumerable-by-default property keys; well-known symbols (Symbol.iterator) customize language behavior.',
    explanation: [
      'Every `Symbol()` call returns a value guaranteed unique — even with identical descriptions — making symbols collision-proof property keys: code can attach data to objects it does not own without ever clashing with string keys or other libraries.',
      'Symbol-keyed properties are invisible to for...in, Object.keys, and JSON.stringify (though discoverable via Object.getOwnPropertySymbols), which makes them ideal for metadata that should not pollute normal enumeration.',
      'The deeper purpose is the well-known symbols: Symbol.iterator makes any object work with for...of and spread, Symbol.hasInstance customizes instanceof, Symbol.toPrimitive hooks coercion — they are the language\'s sanctioned extension points.',
      'Symbols were how TC39 retrofitted protocols onto a language full of existing objects: a string key like "iterator" could have collided with user data, a symbol cannot.',
      'Interviewers ask "what problem do Symbols solve" to separate feature-listers from problem-understanders; the common mistakes are calling symbols "private" (they are merely hidden from enumeration, not access) and being unable to name a single well-known symbol.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['iterables-iterators', 'property-descriptors'],
    questions: [
      'What problem do Symbols solve that string property keys cannot?',
      'Name a well-known symbol and describe exactly what behavior it customizes.',
      'Are symbol-keyed properties truly private? How would you discover and read one?',
      'Why does `Symbol("a") === Symbol("a")` evaluate to false, and what does Symbol.for change about that?',
      'How does Symbol.iterator connect an ordinary object to for...of and the spread operator?',
      'Why did TC39 introduce symbols rather than reserving special string keys for new protocols?',
    ],
  },
  {
    slug: 'template-literals',
    name: 'Template Literals',
    category: 'types-coercion',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 5,
    definition: 'Backtick strings with interpolation and multi-line support.',
    explanation: [
      'Backtick strings evaluate `${expression}` placeholders — any expression, not just variables — and coerce each result to a string, while preserving literal newlines for multi-line text without `\\n` escapes.',
      'They replaced the error-prone `"a" + b + "c"` concatenation style, which suffered from operator-precedence surprises (`"total: " + 1 + 2`) and unreadable quoting; interpolation makes the string\'s shape visible at a glance.',
      'Interpolation is still plain ToString coercion — objects become "[object Object]" unless they define toString or Symbol.toPrimitive — so template literals do not remove the need to think about conversion.',
      'The senior-level point interviewers fish for is the tag syntax: prefixing a function (`` tag`...` ``) hands it the raw string parts and evaluated values, which is the foundation of styled-components and SQL sanitizers.',
      'The common mistake is treating interpolation as automatically safe for HTML — `` `<div>${userInput}</div>` `` is exactly as XSS-prone as concatenation; escaping is still your job.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['tagged-templates'],
    questions: [
      'Beyond interpolation, what can template literals do that quoted strings cannot?',
      'What does interpolating a plain object into a template literal produce, and how do you control it?',
      'How do template literals fix the classic `"total: " + 1 + 2` concatenation bug?',
      'Is building HTML with template literals and user input safe? Why or why not?',
      'What is a tagged template, and how does the tag syntax relate to ordinary template literals?',
    ],
  },
  {
    slug: 'tagged-templates',
    name: 'Tagged Templates',
    category: 'types-coercion',
    level: 'advanced',
    importance: 2,
    interviewFrequency: 1,
    realWorldUsage: 2,
    definition:
      'A function receiving the string parts and values of a template literal — powers styled-components, sql`` sanitizers, i18n.',
    explanation: [
      'Writing `` tag`a ${x} b ${y} c` `` calls `tag(["a ", " b ", " c"], x, y)`: the function receives the literal string segments as a frozen array (with a `.raw` view of unescaped text) plus each interpolated value separately, before anything is concatenated.',
      'That separation is the superpower — because static text and dynamic values arrive distinguished, the tag can treat them differently: an sql tag parameterizes the values to kill injection, an html tag escapes them to kill XSS, and the literal parts pass through untouched.',
      'styled-components is the famous application: the tag receives your CSS segments and interpolated functions, stores them, and resolves the functions against props at render time — library behavior built purely from a language feature.',
      'The strings array is identical (same reference) across calls from the same call site, enabling per-template caching — a subtlety that distinguishes people who have built a tag from those who have only read about one.',
      'Interviewers use it as a depth probe ("how does styled-components use the language itself?"); the common mistake is thinking a tag must return a string — it can return anything: a component, a query object, a DOM tree.',
    ],
    prerequisites: ['template-literals', 'higher-order-functions'],
    leadsTo: [],
    questions: [
      'What arguments does a tag function receive when you write tag`Hello ${name}`?',
      'How does an sql`` tagged template prevent injection in a way string concatenation cannot?',
      'How does styled-components use tagged templates to turn CSS text into components?',
      'What is the `.raw` property on the strings array, and when do you need it?',
      'Must a tag function return a string? Give an example where it returns something else.',
      'Implement a simple `html` tag that escapes interpolated values to prevent XSS.',
    ],
    overlooked: true,
  },
  {
    slug: 'optional-chaining-nullish',
    name: 'Optional Chaining & Nullish Coalescing',
    category: 'types-coercion',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      '`?.` short-circuits on null/undefined; `??` defaults only on null/undefined (unlike `||` which trips on all falsy).',
    explanation: [
      '`a?.b` evaluates to undefined and short-circuits the REST of the chain if `a` is null or undefined — `a?.b.c` does not throw when `a` is missing — and the same guard works for calls (`fn?.()`) and index access (`arr?.[i]`).',
      '`??` is the precision counterpart to `||`: it falls back only when the left side is null/undefined, so legitimate falsy values like 0, "" and false survive — `count ?? 10` keeps a real 0 where `count || 10` would destroy it.',
      'The pair exists because real data is optional at every level (API responses, config, partial state), and the pre-2020 idiom `a && a.b && a.b.c` was verbose and falsy-buggy in exactly the `||` way.',
      'Crucially, `?.` only guards against null/undefined on its immediate left — if the property exists but holds the wrong type, you still throw, and chaining `?.` everywhere converts crashes into silently missing data that surfaces as a confusing bug elsewhere.',
      'Interviewers ask "?? vs ||" because it has a precise checkable answer (the falsy-vs-nullish boundary); the common mistake is sprinkling `?.` defensively over data that should be guaranteed, hiding real contract violations instead of failing fast.',
    ],
    prerequisites: ['null-vs-undefined'],
    leadsTo: [],
    questions: [
      'What is the difference between `??` and `||`, and give a concrete value where the choice changes the result?',
      'In `a?.b.c`, what happens when `a` is undefined, and what happens when `a.b` is undefined?',
      'How do you optionally call a function that may not exist, and optionally index into a possibly-missing array?',
      'When does liberal use of `?.` hide real bugs, and where should you let the code throw instead?',
      'What did `a && a.b && a.b.c` get wrong that `a?.b?.c` fixes?',
      'Why does `value || defaultVal` mishandle a stored 0, and what is the correct modern idiom?',
    ],
  },
]
