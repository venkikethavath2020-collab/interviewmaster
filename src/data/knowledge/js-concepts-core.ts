import type { ConceptNode } from '../../types/knowledge'

export const concepts: ConceptNode[] = [
  // ─────────────────────────────────────────────
  // 4. Execution Model
  // ─────────────────────────────────────────────
  {
    slug: 'execution-context',
    name: 'Execution Context',
    category: 'execution-model',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'The environment a piece of code runs in — variable environment, scope-chain reference, and this — created in two phases: memory allocation, then execution.',
    explanation: [
      'Every time the engine runs a script or invokes a function it builds an execution context: a record containing the variable environment, a reference to the outer lexical environment, and the resolved this binding.',
      'Creation happens in two passes — a memory/creation phase that registers declarations (the mechanism behind hoisting) and an execution phase that runs statements line by line.',
      'Without this two-phase model there would be no hoisting, no TDZ, and no closures, since closures are simply functions holding references to the environments of contexts that have already finished executing.',
      'Interviewers ask about it because it is the root concept that makes hoisting, scope chain, this, and closures explainable as one coherent system rather than four memorized tricks.',
      'The most common mistake is describing it vaguely as "where code runs" without naming the two phases or distinguishing the global context from per-call function contexts — each invocation gets a fresh context.',
    ],
    prerequisites: ['scope', 'stack-vs-heap'],
    leadsTo: ['call-stack', 'hoisting', 'closure', 'this-keyword'],
    questions: [
      'What happens, step by step, when a JavaScript file starts executing?',
      'Explain the two phases of execution context creation. What is registered in each?',
      'What exactly lives inside an execution context?',
      'How does the global execution context differ from a function execution context?',
      'How does the creation phase of an execution context explain hoisting?',
      'When you call the same function twice, do both calls share an execution context?',
    ],
  },
  {
    slug: 'call-stack',
    name: 'Call Stack',
    category: 'execution-model',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'LIFO stack of execution contexts; only the top frame runs, overflow means unbounded recursion, and async callbacks always run on an empty stack.',
    explanation: [
      'The engine pushes a new execution context onto the stack on every call and pops it on return; only the top frame is ever executing, which is what "single-threaded" concretely means.',
      'Each frame consumes a fixed-size region, so unbounded recursion exhausts it and throws RangeError: Maximum call stack size exceeded — the stack trace you read in errors is literally a snapshot of these frames.',
      'The stack is also the hinge of asynchrony: the event loop only dispatches queued callbacks when the stack is empty, which is why a setTimeout callback runs in a brand-new, empty stack.',
      'Interviewers use it to test whether you can explain why try/catch around setTimeout cannot catch errors thrown inside the callback — the registering frame is long gone when the callback executes.',
      'The classic mistake is conflating the call stack with the task queues, or claiming async callbacks "interrupt" running code; nothing preempts the stack mid-frame.',
    ],
    prerequisites: ['execution-context'],
    leadsTo: ['event-loop', 'recursion'],
    questions: [
      'Why can a try/catch block not catch an error thrown inside a setTimeout callback?',
      'What is a stack trace and how is it produced?',
      'What causes a "Maximum call stack size exceeded" error and how do you fix it?',
      'Why do async callbacks always start executing on an empty call stack?',
      'How does the call stack relate to JavaScript being single-threaded?',
      'Walk through the stack frames for a function that calls two nested helpers.',
    ],
  },
  {
    slug: 'scope',
    name: 'Scope',
    category: 'execution-model',
    level: 'beginner',
    importance: 5,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Where a binding is visible: global, function, or block — determined by code position (lexical), not by call site.',
    explanation: [
      'Scope is resolved lexically: the engine decides what a name refers to from where the code is written, at parse time, not from where or how a function is later called.',
      'JavaScript has three scope kinds — global, function (var, function declarations), and block (let, const, class) — and ES6 block scoping exists precisely because function-only var scoping caused loop-variable and leakage bugs.',
      'Without well-defined scope every name would be global, making encapsulation, libraries, and closures impossible; scope is the boundary that makes private state expressible at all.',
      'Interviewers ask it as a gateway question: if a candidate cannot state that var ignores blocks while let respects them, deeper questions on closures and TDZ are pointless.',
      'The most common mistake is confusing lexical scope with dynamic this — scope is fixed at author time, while this is decided at call time; candidates regularly merge the two.',
    ],
    prerequisites: ['var-let-const'],
    leadsTo: ['scope-chain', 'closure'],
    questions: [
      'What is the difference between function scope and block scope?',
      'What is the scope of a loop variable declared with var versus let?',
      'What does it mean that JavaScript scope is lexical?',
      'Can a block create a new scope for var declarations? Why not?',
      'How is scope different from the this binding?',
    ],
  },
  {
    slug: 'scope-chain',
    name: 'Scope Chain',
    category: 'execution-model',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'The lookup path through nested lexical environments, fixed at definition time; it ends at the global scope, and a miss throws ReferenceError.',
    explanation: [
      'When an identifier is not found in the current environment record, the engine follows the outer-environment reference upward, one level at a time, until it finds the binding or reaches global and throws ReferenceError.',
      'The chain is wired when functions are defined, not when they are called — a function nested three levels deep carries those three outer references forever, regardless of who invokes it.',
      'This definition-time linkage is exactly what closures exploit: a returned inner function keeps its chain alive, so the "dead" outer scopes remain reachable on the heap.',
      'Interviewers ask it to verify you understand variable resolution as a concrete walk rather than magic, and to set up closure and shadowing follow-ups.',
      'The common mistake is claiming the chain depends on the call stack ("whoever called me") — that is dynamic scoping, which JavaScript does not have for variables.',
    ],
    prerequisites: ['scope', 'execution-context'],
    leadsTo: ['lexical-environment', 'closure'],
    questions: [
      'How is a variable resolved when it is not found in the local scope?',
      'Is the scope chain determined at definition time or at call time? Prove it with an example.',
      'What happens when a variable lookup reaches the end of the scope chain without a match?',
      'How does variable shadowing interact with the scope chain?',
      'How does the scope chain make closures possible?',
    ],
  },
  {
    slug: 'lexical-environment',
    name: 'Lexical Environment',
    category: 'execution-model',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 3,
    definition:
      'The spec structure — an environment record of bindings plus a reference to the outer environment — that is the machinery behind scope and closures.',
    explanation: [
      'In the specification, every scope is a Lexical Environment: an environment record holding the actual bindings, plus a pointer to the enclosing environment — scopes and the scope chain are just this linked structure.',
      'A closure does not copy values; it stores a reference to the entire lexical environment, so reads always see the current value of a captured variable, not a snapshot.',
      'This is why all three setTimeout callbacks in a var loop log the final value: they share one environment record with one i, whereas let creates a fresh record per iteration.',
      'Interviewers reach for this term to separate candidates who memorized "closure = function remembers variables" from those who can explain the underlying mechanism precisely.',
      'The classic mistake is the snapshot misconception — saying closures "capture the value at creation time" — which immediately fails the loop output-prediction question.',
    ],
    prerequisites: ['scope-chain'],
    leadsTo: ['closure'],
    questions: [
      'What exactly does a closure capture — values or the environment? Demonstrate the difference.',
      'What are the two parts of a lexical environment in the spec?',
      'Why do let loop iterations each see a different variable while var iterations share one?',
      'How do environment records relate to the scope chain?',
      'When a function reads a captured variable that changed after the closure was created, what does it see and why?',
    ],
  },
  {
    slug: 'hoisting',
    name: 'Hoisting',
    category: 'execution-model',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 3,
    definition:
      'Declarations are registered during context creation: var becomes undefined, function declarations become fully usable, let/const are registered but uninitialized.',
    explanation: [
      'Nothing physically moves: during the creation phase of an execution context the engine scans for declarations and registers them before any line runs, so names exist scope-wide from the start.',
      'The registration differs by declaration kind — function declarations are initialized with the whole function object (callable above their definition), var is initialized to undefined, and let/const are registered but left uninitialized, producing the temporal dead zone.',
      'Hoisting exists as a consequence of two-phase execution, and it usefully enables mutual recursion and "API at the top, helpers below" file organization.',
      'Interviewers love it for cheap output-prediction questions that expose whether a candidate understands the creation phase or just repeats "declarations move to the top".',
      'The most common mistake is claiming let and const are not hoisted — they are; they are simply not initialized, which is precisely why typeof on them throws inside the TDZ while typeof on a truly undeclared name does not.',
    ],
    prerequisites: ['execution-context'],
    leadsTo: ['temporal-dead-zone', 'var-let-const', 'function-declaration-vs-expression'],
    questions: [
      'Predict the output when a function is called above its declaration — for a function declaration versus a function expression assigned to var.',
      'Are let and const hoisted? Justify your answer with observable behavior.',
      'What does the engine actually do during "hoisting" — does code move?',
      'Why does var x log undefined before its declaration instead of throwing?',
      'How are function declarations hoisted differently from var declarations?',
      'What output does a var-based counter logged before and after assignment produce, and why?',
    ],
  },
  {
    slug: 'temporal-dead-zone',
    name: 'Temporal Dead Zone',
    category: 'execution-model',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'The region from scope entry to a let/const declaration where any access throws ReferenceError — the binding is hoisted but uninitialized.',
    explanation: [
      'When a block is entered, its let/const bindings are created immediately but flagged uninitialized; every access before the declaration line executes throws ReferenceError — that window is the TDZ.',
      'It was added deliberately: var silently yielding undefined before assignment hid real bugs, so TC39 made early access to modern bindings fail loudly instead.',
      'The zone is temporal, not spatial — what matters is whether the declaration has executed yet, so a function defined above the declaration but called after it works fine.',
      'Interviewers use the typeof probe: typeof x throws inside a TDZ but returns "undefined" for a completely undeclared name, a distinction that cleanly separates surface knowledge from real understanding.',
      'The common mistake is concluding from the error that let/const are not hoisted at all, or forgetting that const class fields and default parameters can also hit TDZ errors (e.g. a parameter defaulting to a later parameter).',
    ],
    prerequisites: ['hoisting'],
    leadsTo: ['var-let-const'],
    questions: [
      'Why does typeof x throw for a let variable before its declaration but not for an undeclared variable?',
      'What exactly is the temporal dead zone and when does it start and end?',
      'Why did TC39 introduce the TDZ instead of giving let the var behavior?',
      'Why is the dead zone "temporal" rather than based on code position? Give an example.',
      'Can default function parameters trigger a TDZ error? How?',
    ],
  },
  {
    slug: 'closure',
    name: 'Closure',
    category: 'execution-model',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'A function retaining access to its defining scope after that scope has finished executing; captured variables live on the heap as long as the function is reachable.',
    explanation: [
      'A closure is a function bundled with a reference to its defining lexical environment; when the outer function returns, the engine keeps that environment alive on the heap because the inner function still points at it.',
      'It captures variables by reference, not by value — every call through the closure reads and writes the live binding, which is why a returned counter keeps incrementing real state.',
      'Closures are JavaScript\'s primary privacy and state mechanism: debounce timers, memoization caches, module-pattern private state, React hooks, and event handlers all depend on them; without closures, callbacks could carry no context.',
      'It is the single most-asked JS interview topic because one question tests scope, the heap, garbage collection, and async timing simultaneously — usually via the var-in-loop setTimeout bug (all callbacks share one binding; fix with let, an IIFE, or passing the value as an argument).',
      'Beyond the snapshot misconception, the senior-level mistake is ignoring the memory cost: a long-lived closure pins its whole captured environment, so a tiny event handler can keep a large parsed payload from ever being collected.',
    ],
    prerequisites: ['scope-chain', 'garbage-collection'],
    leadsTo: ['memoization', 'debounce', 'throttle', 'currying-partial-application', 'module-pattern'],
    questions: [
      'What is a closure? Describe a real place you used one in production code.',
      'A for loop with var registers three setTimeout callbacks — what do they log, and what are three different fixes?',
      'Implement a counter factory with truly private state using a closure.',
      'How can a closure cause a memory leak? How would you detect and fix it?',
      'Do closures capture values or variables? Show code that proves your answer.',
      'How do debounce and memoize rely on closures internally?',
    ],
  },
  {
    slug: 'iife',
    name: 'IIFE & Module Pattern Roots',
    category: 'execution-model',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 2,
    definition:
      'Immediately-invoked function expression: pre-ESM scope isolation and private state via closures.',
    explanation: [
      'An IIFE wraps a function in parentheses to force it to parse as an expression and invokes it immediately, creating a one-shot private scope whose internals never touch the global object.',
      'Before ES modules, every script shared one global namespace, so libraries wrapped themselves in IIFEs to hide helpers and expose only a deliberate public API — the module pattern is exactly this plus a returned object.',
      'Inside, closures keep the "private" variables alive for any exposed functions, giving encapsulation a decade before # private fields existed.',
      'Interviewers ask about IIFEs to probe historical depth and to confirm you understand that ES modules (each file its own scope) and block-scoped let/const are what made them mostly obsolete.',
      'The common mistake is calling IIFEs pointless legacy: they still appear as the var-loop fix, for one-time initialization expressions, and as the wrapper format bundlers emit around your code.',
    ],
    prerequisites: ['closure'],
    leadsTo: ['module-pattern', 'es-modules'],
    questions: [
      'Why were IIFEs everywhere in pre-2015 JavaScript codebases?',
      'What language features replaced most IIFE use cases, and how?',
      'Why does an IIFE need the wrapping parentheses to work?',
      'How does an IIFE create private state? Sketch the module pattern.',
      'Where would you still legitimately reach for an IIFE today?',
    ],
  },
  {
    slug: 'this-keyword',
    name: 'this',
    category: 'execution-model',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'A call-site-determined binding: new > bind > implicit (obj.method) > default (undefined in strict mode); arrow functions capture this lexically instead.',
    explanation: [
      'Unlike variables, this is not resolved through the scope chain — it is set fresh in each execution context based on how the function was called, following a strict precedence: new binding, then explicit bind/call/apply, then implicit receiver (obj.method()), then default (undefined in strict mode, globalThis otherwise).',
      'Arrow functions are the exception that proves the rule: they have no own this and simply read it from the enclosing scope like any closed-over variable, which is why they suit callbacks but not object methods.',
      'The design exists so one function on a prototype can serve thousands of instances — the receiver is supplied per call — but the cost is the "lost this" bug: extracting obj.method into a bare reference (a callback, an event handler) severs the receiver, leaving this undefined.',
      'It is a top-three interview topic because a single snippet reveals whether you reason from the call site; candidates who reason from where the function is defined get every variant wrong.',
      'The senior follow-up is class fields: an arrow class field binds this per instance (handler-safe but per-instance memory and awkward to mock), while a prototype method is shared but needs binding before being passed around.',
    ],
    prerequisites: ['execution-context', 'function-declaration-vs-expression'],
    leadsTo: ['call-apply-bind', 'arrow-functions', 'es6-classes'],
    questions: [
      'List the four this-binding rules and their precedence when several apply.',
      'Why does passing obj.method as a callback break, and what are three fixes?',
      'What is this inside an arrow function defined as a class field versus a prototype method?',
      'What is this in a plain function call in strict mode versus sloppy mode?',
      'Predict this for: fn(), obj.fn(), fn.call(x), new fn() — same function each time.',
      'Why can bind and call not change this of an arrow function?',
    ],
  },
  {
    slug: 'call-apply-bind',
    name: 'call / apply / bind',
    category: 'execution-model',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 4,
    definition:
      'Explicit this-binding: call (argument list) and apply (argument array) invoke immediately; bind returns a new, permanently bound function.',
    explanation: [
      'These three Function.prototype methods let you override the implicit receiver: call and apply invoke now (differing only in args-as-list versus args-as-array), while bind defers — it returns a new function whose this (and optionally leading arguments) are locked in.',
      'They exist because functions are first-class values that travel without their receivers; borrowing methods, fixing callbacks, and partial application all need a way to reattach this explicitly.',
      'A bound function cannot be re-bound — the first bind wins permanently — and even call/apply cannot override it; only new can still supersede the bound this.',
      '"Implement bind from scratch" is a canonical interview exercise because a correct answer must compose this capture, argument prepending via closure, and ideally the new-operator edge case.',
      'Common mistakes: confusing which of call/apply takes an array (mnemonic: Apply = Array), forgetting bind does not invoke, and trying to fix an arrow function with bind, which silently does nothing for this.',
    ],
    prerequisites: ['this-keyword'],
    leadsTo: ['currying-partial-application'],
    questions: [
      'Implement Function.prototype.bind from scratch, including partial application.',
      'Can you re-bind a function that is already bound? What actually happens?',
      'When would you choose apply over call? Does spread syntax make apply obsolete?',
      'How does bind enable partial application? Give a practical example.',
      'What does new do when applied to a bound function?',
      'Use call to borrow an array method for an array-like object.',
    ],
  },

  // ─────────────────────────────────────────────
  // 5. Functions
  // ─────────────────────────────────────────────
  {
    slug: 'function-declaration-vs-expression',
    name: 'Declarations vs Expressions',
    category: 'functions',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Declarations hoist fully; expressions (including arrows) follow their variable\'s hoisting rules; named expressions aid stack traces.',
    explanation: [
      'A function declaration is a statement registered with its full function object during the creation phase, so it is callable anywhere in its scope; a function expression is a value, created only when the assignment line actually runs.',
      'That means calling an expression early fails by its variable\'s rules — TypeError ("x is not a function") for var, ReferenceError via TDZ for const/let — while a declaration above its definition just works.',
      'The distinction exists because functions are first-class values: expressions let you pass, return, and conditionally create functions, which statements cannot do.',
      'Interviewers use it as a fast composite probe of hoisting, TDZ, and value-versus-statement thinking in a single output-prediction snippet.',
      'A common gap is not knowing why named function expressions matter — the name appears in stack traces and enables self-reference, while the name is scoped only inside the function itself.',
    ],
    prerequisites: ['hoisting'],
    leadsTo: ['arrow-functions', 'this-keyword'],
    questions: [
      'Which can you call before its definition — a declaration or an expression — and what does the engine do differently?',
      'What error do you get calling a var-assigned function expression before assignment, and why that error specifically?',
      'What are the benefits of naming a function expression?',
      'How do conditionally-created functions favor expressions over declarations?',
      'Where is the name of a named function expression in scope?',
    ],
  },
  {
    slug: 'arrow-functions',
    name: 'Arrow Functions',
    category: 'functions',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Compact functions with no own this, arguments, or prototype; this is captured lexically at definition — so they cannot be constructors.',
    explanation: [
      'An arrow function does not create its own this, arguments, super, or new.target — it resolves them lexically through the scope chain, exactly like a closed-over variable, and the binding is fixed at definition.',
      'They were introduced largely to kill the var self = this dance: a callback written as an arrow simply sees the this of the method that created it.',
      'Because there is no internal [[Construct]] and no .prototype object, new on an arrow throws — they are values for passing around, not blueprints.',
      'Interviewers focus on where arrows are wrong: object-literal methods (this becomes the outer scope, not the object), prototype methods needing dynamic this, and event handlers that rely on this being the element.',
      'The classic mistake is treating arrows as universal syntax sugar and only discovering in production that bind/call cannot fix an arrow\'s this and that arguments belongs to the enclosing function.',
    ],
    prerequisites: ['this-keyword'],
    leadsTo: ['es6-classes'],
    questions: [
      'When should you NOT use an arrow function? Give at least three concrete cases.',
      'What happens when you access arguments inside an arrow function?',
      'Why can an arrow function not be used as a constructor?',
      'What is this inside an arrow function defined in an object literal method position?',
      'How do arrow functions eliminate the old var self = this pattern?',
      'Can call or bind change this of an arrow function? Why not?',
    ],
  },
  {
    slug: 'default-rest-spread',
    name: 'Default, Rest & Spread',
    category: 'functions',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Defaults evaluate per call; rest collects trailing arguments into an array; spread expands iterables and objects — shallowly.',
    explanation: [
      'Default parameter expressions are evaluated lazily on each call where the argument is undefined (only undefined — null passes through), so a default like [] creates a fresh array every call, unlike Python.',
      'Rest (...args in a parameter list) gathers the remaining arguments into a real array; spread (...x in a call or literal) does the inverse, expanding an iterable or object\'s own enumerable properties into place.',
      'Together they replaced the arguments object and apply for variadic code, giving readable signatures and immutable-style copies ({ ...state, key: value }).',
      'Interviewers like the pair because the same three dots mean opposite things by position, and because object spread copying only one level deep sets up the shallow-versus-deep-copy follow-up.',
      'The common mistake is assuming spread clones deeply — nested objects are still shared references — and forgetting that defaults do not trigger for null.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['shallow-vs-deep-copy', 'destructuring'],
    questions: [
      'Spread and rest use the same syntax — what is the difference and how do you tell them apart?',
      'Is object spread a deep copy? Show what goes wrong with nested data.',
      'When does a default parameter value kick in — for undefined, null, or any falsy value?',
      'Are default parameter expressions evaluated once or per call? Why does it matter for object defaults?',
      'Rewrite a function using the arguments object to use rest parameters instead.',
    ],
  },
  {
    slug: 'callbacks',
    name: 'Callback Functions',
    category: 'functions',
    level: 'beginner',
    importance: 4,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Functions passed as arguments to be invoked later — the primitive underneath events, timers, and array methods.',
    explanation: [
      'A callback works because functions are first-class values: you hand a function to other code, which invokes it later — synchronously (array methods run their callback during the call) or asynchronously (timers and events run it on a future tick).',
      'They are the only way a single-threaded language can express "do this when that finishes" — without callbacks there are no event listeners, no timers, and nothing for promises to be built on.',
      'Handing your function away is an inversion of control: the host decides when, how many times, and with what arguments your code runs, and you must trust it — this trust problem is exactly what promises were designed to repair.',
      'Interviewers use callbacks to check async fundamentals before going deeper, and to see whether you distinguish synchronous callbacks from asynchronous ones.',
      'Common mistakes: assuming every callback is async, and invoking instead of passing — writing onClick(handler()) and shipping the return value rather than the function.',
    ],
    prerequisites: ['function-declaration-vs-expression'],
    leadsTo: ['higher-order-functions', 'callback-hell'],
    questions: [
      'What is inversion of control in the context of callbacks, and why is it a problem?',
      'Is the callback passed to Array.prototype.map synchronous or asynchronous? How do you know?',
      'What bug does fn(callback()) introduce compared to fn(callback)?',
      'What is an error-first callback and why did Node standardize that convention?',
      'How do closures let callbacks carry context with them?',
    ],
  },
  {
    slug: 'higher-order-functions',
    name: 'Higher-Order Functions',
    category: 'functions',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Functions that take or return other functions — the basis of array methods, decorators, middleware, and hooks.',
    explanation: [
      'A higher-order function either accepts functions as input (map, filter, addEventListener) or returns one (bind, debounce, a configured validator); both directions work because functions are ordinary heap values.',
      'A returned function closes over the factory\'s arguments, which is how wrappers like withLogging(fn) or once(fn) attach behavior around an existing function without modifying it.',
      'This is the backbone of real-world JS architecture: Express/Koa middleware, React higher-order components and hooks, Vue composables, and Redux middleware are all HOFs by another name.',
      'Interviewers typically ask you to write a wrapper (timing, logging, retry, once) because a correct answer must forward this and arguments, return the inner result, and use a closure — three fundamentals in five lines.',
      'The common implementation mistake is exactly that forwarding: dropping the return value or losing this by calling fn(...args) instead of fn.apply(this, args) inside the wrapper.',
    ],
    prerequisites: ['callbacks', 'closure'],
    leadsTo: ['currying-partial-application', 'function-composition'],
    questions: [
      'Write a function that wraps any other function with execution-time logging.',
      'Implement once(fn) so the wrapped function can only execute a single time.',
      'What makes higher-order functions possible in JavaScript at the language level?',
      'Give examples of higher-order functions in a framework you use daily.',
      'When wrapping a method, how do you preserve its this and arguments correctly?',
    ],
  },
  {
    slug: 'pure-functions',
    name: 'Pure Functions & Side Effects',
    category: 'functions',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Same inputs always produce the same output with no observable side effects; purity enables memoization, easy testing, and predictable state management.',
    explanation: [
      'A pure function depends only on its arguments and communicates only through its return value — no mutation of inputs or shared state, no I/O, no reads of changing globals like Date.now().',
      'Purity is what makes a function safe to cache (memoization is only correct for pure functions), trivial to unit-test without mocks, safe to run in any order, and safe to re-execute.',
      'Frameworks lean on it structurally: Redux reducers and Vue/React computed render paths assume purity so that change detection can compare references and replay state transitions deterministically.',
      'Interviewers ask "why do React/Redux insist on purity" to see if you connect the definition to its payoffs — referential transparency, time-travel debugging, memoized components.',
      'The subtle mistake is hidden impurity: a function that sorts or splices its argument array mutates caller state while still returning the "right" value, and candidates routinely miss that mutation of inputs breaks purity even with deterministic output.',
    ],
    prerequisites: ['higher-order-functions'],
    leadsTo: ['memoization', 'object-immutability'],
    questions: [
      'What are the two conditions a function must satisfy to be pure?',
      'Why do React and Redux insist on pure reducers and render functions?',
      'Is a function that calls Date.now() or Math.random() pure? Why not?',
      'Spot the impurity: a function that sorts the array it receives and returns it.',
      'Why is purity a prerequisite for safe memoization?',
    ],
  },
  {
    slug: 'recursion',
    name: 'Recursion',
    category: 'functions',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'A function calling itself with a base case; each call adds a stack frame, so depth limits and iterative/stack conversions matter.',
    explanation: [
      'A recursive function reduces a problem toward a base case, with every in-flight call holding its own stack frame for parameters and locals — depth equals memory.',
      'It is the natural tool for self-similar data (DOM trees, nested menus, file systems, JSON), where the iterative version needs an explicit stack to track what recursion tracks for free.',
      'The hard limit is the call stack: engines allow roughly ten to twenty thousand frames before RangeError, and JavaScript engines do not reliably implement tail-call optimization despite it being in the ES2015 spec, so deep recursion must be converted to a loop with an explicit stack or queue.',
      'Interviewers pair "solve it recursively" with "now do it iteratively" precisely to test whether you understand the frame cost rather than just the pattern; flattening nested structures is the standard vehicle.',
      'The common mistakes are a missing or unreachable base case (instant overflow) and recursing on the same arguments without making progress toward termination.',
    ],
    prerequisites: ['call-stack'],
    leadsTo: ['generators'],
    questions: [
      'Flatten an arbitrarily nested array recursively, then rewrite it iteratively with an explicit stack.',
      'What limits recursion depth in JavaScript and what error do you hit?',
      'Does JavaScript optimize tail calls in practice? What does that mean for deep recursion?',
      'Walk through the stack frames of a recursive factorial(4) call.',
      'When is recursion clearly better than iteration, and when is it clearly worse?',
    ],
  },
  {
    slug: 'currying-partial-application',
    name: 'Currying & Partial Application',
    category: 'functions',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'Transforming f(a,b,c) into f(a)(b)(c) via closures that accumulate arguments; partial application fixes some arguments and returns a function for the rest.',
    explanation: [
      'A curried function takes its arguments one call at a time, each intermediate call returning a closure that has accumulated the arguments so far; only when enough are collected does the original function fire.',
      'Partial application is the looser cousin — fix some arguments now (bind(null, ...preset) or an arrow) and get back a function awaiting the rest; currying is just systematic, one-at-a-time partial application.',
      'The practical payoff is building specialized functions from general ones — log(level)(message), a configured fetcher, reusable event handlers — and clean composition pipelines of unary functions.',
      'It is a beloved interview exercise because a generic curry(fn) implementation requires fn.length, recursion, closures, and argument accumulation in a handful of lines, and the sum(1)(2)(3) family tests the same mechanics.',
      'Common mistakes: confusing the two terms, sharing one accumulator array across branches of a curried chain (each call must build a fresh argument list), and forgetting that rest-parameter functions report length 0, breaking arity-based curry.',
    ],
    prerequisites: ['closure', 'higher-order-functions'],
    leadsTo: ['call-apply-bind', 'function-composition'],
    questions: [
      'Implement a generic curry(fn) that works for any arity.',
      'Make sum(1)(2)(3)() return 6 while sum(1)(2)(3)(4)() returns 10 — explain the termination trick.',
      'What is the difference between currying and partial application?',
      'How would you partially apply a function using bind?',
      'Why must each step of a curried chain create a new arguments array rather than mutate a shared one?',
      'Where has currying genuinely helped you in application code?',
    ],
  },
  {
    slug: 'function-composition',
    name: 'Function Composition',
    category: 'functions',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Combining small functions into pipelines (compose/pipe) where data flows through successive transformations.',
    explanation: [
      'Composition wires unary functions together so the output of one becomes the input of the next: pipe(f, g, h)(x) is h(g(f(x))), and compose is the same chain read right-to-left.',
      'Both are one-line reduces over the function list — reduce carries the intermediate value through each function — which is exactly the implementation interviewers expect.',
      'The point is building complex transformations from small, individually testable pieces instead of one tangled procedure; it is the idea behind lodash/fp flow, RxJS pipe, and middleware chains.',
      'Interviewers use "implement pipe(...fns)" as a compact test of reduce, rest parameters, closures, and higher-order thinking together.',
      'The common stumbles are reversing compose vs pipe order and writing pipelines of functions whose input/output shapes do not line up — composition presumes each stage feeds the next a compatible value.',
    ],
    prerequisites: ['higher-order-functions'],
    leadsTo: ['pure-functions'],
    questions: [
      'Implement pipe(...fns) and then compose(...fns) — what is the only difference?',
      'How does reduce make a one-line implementation of pipe possible?',
      'Why does composition favor unary, pure functions?',
      'Refactor a chain of nested calls h(g(f(x))) into a readable pipeline.',
      'Where does the composition idea show up in libraries you already use?',
    ],
  },
  {
    slug: 'memoization',
    name: 'Memoization',
    category: 'functions',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 4,
    definition:
      'Caching results keyed by arguments in a closure-held Map; trades memory for repeat-call speed — eviction and key strategy are the senior follow-ups.',
    explanation: [
      'A memoized function keeps a Map in a closure: on each call it derives a key from the arguments, returns the cached result on a hit, and computes-then-stores on a miss — the cache is invisible to callers.',
      'It is only correct for pure functions; memoizing anything whose result depends on external state serves stale answers, which is the first thing a strong candidate states unprompted.',
      'The senior depth is in the key and the lifetime: JSON.stringify keys break on argument order, functions, and cycles; reference-keyed caches pin objects in memory unless you use a WeakMap; unbounded caches are memory leaks, so real implementations add LRU eviction or max size.',
      'It is a standard interview implementation because ten lines exercise closures, Maps, key design, and this/argument forwarding, with natural follow-ups about multi-argument keys and eviction.',
      'The same idea, under other names, is everywhere in frameworks — Vue computed properties, React useMemo, reselect selectors — so interviewers also check you can connect the pattern to tools you use.',
    ],
    prerequisites: ['closure', 'pure-functions'],
    leadsTo: ['weakmap-weakset', 'debounce'],
    questions: [
      'Implement memoize(fn) for a single-argument function, then extend it to multiple arguments.',
      'What are the pitfalls of using JSON.stringify to build cache keys?',
      'When is memoization actively harmful?',
      'How does a WeakMap-based cache prevent the memory leak a Map-based one creates?',
      'Why must a memoized function be pure?',
      'What eviction strategy would you add to an unbounded memo cache, and why?',
    ],
  },
  {
    slug: 'generators',
    name: 'Generators',
    category: 'functions',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 2,
    definition:
      'Pausable functions (function* / yield) producing iterators; the foundation for lazy sequences and, historically, async control flow.',
    explanation: [
      'Calling a generator function runs none of its body; it returns an iterator, and each .next() executes until the next yield, where the function suspends with its entire local state — locals, loop counters, instruction pointer — preserved on the heap.',
      'yield is two-way: it emits a value out, and the argument of the next .next(value) becomes the result of the yield expression inside, which is the channel that made generator-based async (co, redux-saga) possible before async/await was syntax over the same idea.',
      'Generators are the easiest way to implement the iterator protocol, so they power custom for...of iteration, lazy and infinite sequences, and pagination cursors that compute values only on demand.',
      'Interviewers ask "what does yield actually pause" to distinguish candidates who grasp suspended execution contexts from those who think the function somehow runs in the background — it runs only inside .next() calls.',
      'Common mistakes: expecting the body to start executing at call time, and forgetting a generator is single-use — once done, it stays done, and you need a fresh one to iterate again.',
    ],
    prerequisites: ['iterables-iterators'],
    leadsTo: ['async-await'],
    questions: [
      'What exactly does yield pause, and where does the function\'s state live while suspended?',
      'Implement an infinite auto-incrementing ID generator.',
      'What does passing a value to .next(value) do?',
      'How do generators relate to async/await under the hood?',
      'Use a generator to make a custom object iterable with for...of.',
      'What happens if you iterate the same generator object twice?',
    ],
  },

  // ─────────────────────────────────────────────
  // 6. Objects & Prototypes
  // ─────────────────────────────────────────────
  {
    slug: 'object-creation',
    name: 'Object Creation Patterns',
    category: 'objects-prototypes',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Literals, Object.create, constructors, classes, and factories — and when each fits.',
    explanation: [
      'JavaScript offers several creation paths that differ mainly in what prototype the result gets and where shared behavior lives: literals link to Object.prototype, Object.create(proto) links to exactly what you pass, and constructors/classes link instances to Fn.prototype.',
      'Factories — plain functions returning objects — sidestep new and this entirely and get privacy for free via closures, at the cost of per-instance copies of methods.',
      'Object.create(null) produces a truly prototype-less object, making it a safe dictionary: no inherited toString, no constructor, and immunity to prototype-pollution lookups on attacker-controlled keys.',
      'Interviewers use creation patterns to see if you choose deliberately — literal for one-offs, class for many instances sharing methods, factory for closure-private state — instead of defaulting to class for everything.',
      'The common gap is not knowing what Object.create actually does (create an object with a given prototype, no constructor run) or why a null-prototype map matters for security.',
    ],
    prerequisites: ['data-types'],
    leadsTo: ['factory-pattern', 'constructor-functions-new'],
    questions: [
      'Why would you ever use Object.create(null)?',
      'Compare object literals, classes, and factory functions — when does each fit?',
      'What prototype does each creation pattern give the resulting object?',
      'How do factories achieve private state without classes or # fields?',
      'What does Object.create(proto) do that new Constructor() does not?',
    ],
  },
  {
    slug: 'destructuring',
    name: 'Destructuring',
    category: 'objects-prototypes',
    level: 'beginner',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 5,
    definition:
      'Pattern-based extraction from objects and arrays with renaming, defaults, nesting, and rest.',
    explanation: [
      'Destructuring mirrors literal syntax on the left side of an assignment: the engine reads properties (by key) or iterates (by position) and binds them to new variables in one statement.',
      'The full grammar composes — renaming ({ id: userId }), defaults that fire only on undefined, arbitrary nesting, and rest to sweep the leftovers — and it works in parameter lists, which is how options-object APIs stay readable.',
      'It exists to kill repetitive const x = obj.a.b.c access chains, and array destructuring works on any iterable, enabling swap-without-temp and tuple-style returns like React\'s useState.',
      'Interviewers hand candidates a nested API response to destructure because real responses have missing branches — the follow-up is always why it throws on a null intermediate and how defaults only catch undefined.',
      'Common mistakes: reading { a: b } as "a renamed from b" instead of "property a into variable b", and expecting a default to rescue a null value — defaults trigger on undefined alone.',
    ],
    prerequisites: ['object-creation'],
    leadsTo: ['default-rest-spread'],
    questions: [
      'Destructure a nested API response with renaming and defaults for missing fields.',
      'In { a: b } = obj, which identifier becomes the variable?',
      'Why does destructuring throw when an intermediate property is null, and how do you guard it?',
      'Do destructuring defaults apply for null values?',
      'How does parameter destructuring improve functions that take an options object?',
      'Swap two variables without a temporary using destructuring.',
    ],
  },
  {
    slug: 'property-descriptors',
    name: 'Property Descriptors & Accessors',
    category: 'objects-prototypes',
    level: 'advanced',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'The writable/enumerable/configurable flags plus get/set accessors behind every property; Object.defineProperty — the mechanism under Vue 2 reactivity and freeze.',
    explanation: [
      'Every property is more than a value: data properties carry writable, enumerable, and configurable flags, while accessor properties replace the value with get/set functions — Object.defineProperty sets any of this, and properties it defines default all flags to false.',
      'These flags explain everyday mysteries: non-enumerable properties (like most built-in methods) are invisible to for...in, Object.keys, and spread; non-configurable ones cannot be deleted or redefined; Object.freeze is essentially flipping writable and configurable off for every own property.',
      'Accessors are interception: Vue 2 rewrote every data property into get/set pairs to track reads and trigger re-renders on writes — which is exactly why it could not detect added or deleted keys, the limitation Proxies later fixed in Vue 3.',
      'Interviewers use descriptors as a senior filter because most candidates have never looked beneath obj.x = 1; connecting the flags to freeze semantics and Vue 2\'s caveats signals real depth.',
      'The common gap is assuming all properties behave like literal-assigned ones (all flags true) and being surprised that defineProperty\'s false defaults silently create hidden, read-only properties.',
    ],
    prerequisites: ['object-creation'],
    leadsTo: ['proxy-reflect', 'object-immutability'],
    questions: [
      'How did Vue 2 detect state changes, and what changes could it fundamentally not see?',
      'Why don\'t for...in or Object.keys show some properties of built-in objects?',
      'What are the three flags on a data property and what does each control?',
      'What does Object.freeze actually do in terms of descriptors?',
      'What are the default flag values when you use Object.defineProperty, and why is that a trap?',
      'Define a read-only, non-enumerable property on an object.',
    ],
    overlooked: true,
  },
  {
    slug: 'prototype',
    name: 'Prototype',
    category: 'objects-prototypes',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 3,
    definition:
      'Every object has an internal [[Prototype]] link used for delegation; functions additionally carry a .prototype object used by new.',
    explanation: [
      'Two distinct things share the name: every object has an internal [[Prototype]] link (read via Object.getPrototypeOf or the legacy __proto__ accessor) that property lookup delegates to, and functions carry a separate .prototype property — an ordinary object that only matters when the function is invoked with new.',
      'new Fn() sets the new instance\'s [[Prototype]] to Fn.prototype; the function\'s .prototype is the template for future instances, never the function\'s own prototype.',
      'Delegation is JavaScript\'s memory model for shared behavior: a thousand instances hold one method via their prototype link instead of a thousand copies, and classes are syntax over exactly this.',
      'The "__proto__ vs .prototype" question is an interview staple because confusing them is the single most common prototype error, instantly revealing whether the candidate has a mental model or memorized vocabulary.',
      'A second senior point: mutating Object.prototype or reassigning prototypes of live objects deoptimizes and leaks behavior everywhere — prototypes are for reading shared behavior, not runtime patching.',
    ],
    prerequisites: ['object-creation'],
    leadsTo: ['prototype-chain'],
    questions: [
      'What is the difference between __proto__ and .prototype?',
      'Do arrow functions or object methods have a .prototype property? Why not?',
      'What is the [[Prototype]] of an object created with a literal? With Object.create(null)?',
      'How do a constructor\'s .prototype and its instances\' [[Prototype]] relate?',
      'What is the modern, correct way to read and set an object\'s prototype?',
    ],
  },
  {
    slug: 'prototype-chain',
    name: 'Prototype Chain',
    category: 'objects-prototypes',
    level: 'intermediate',
    importance: 5,
    interviewFrequency: 5,
    realWorldUsage: 3,
    definition:
      'Property reads delegate up the chain until found or null; writes shadow locally; methods are shared, not copied.',
    explanation: [
      'A property read checks the object\'s own properties first, then walks [[Prototype]] links upward — instance → Fn.prototype → Object.prototype → null — returning the first match or undefined at the end.',
      'Reads and writes are asymmetric: assignment does not climb the chain but creates an own property that shadows the inherited one, which is why "writing through" an instance never modifies the shared prototype method.',
      'This walk is the engine of the language: method lookup on arrays and strings, instanceof (which checks whether a constructor\'s prototype appears in the chain), and class inheritance are all chain traversals.',
      'Interviewers ask you to narrate obj.foo resolution because it forces precision about own-versus-inherited — hasOwnProperty checks only own properties while the in operator searches the whole chain.',
      'Beyond confusing read and write behavior, the senior-level connection candidates miss is security: attacker-controlled keys like __proto__ in naive deep merges poison Object.prototype for every object in the program — prototype pollution.',
    ],
    prerequisites: ['prototype'],
    leadsTo: ['constructor-functions-new', 'typeof-instanceof', 'prototype-pollution'],
    questions: [
      'Walk through exactly how obj.foo is resolved when foo is not an own property.',
      'What is the difference between hasOwnProperty and the in operator?',
      'What happens when you assign to a property that exists on the prototype — does it modify the prototype?',
      'How does instanceof use the prototype chain?',
      'What is the full prototype chain of [] and where does it end?',
      'How does the prototype chain enable prototype pollution attacks?',
    ],
  },
  {
    slug: 'constructor-functions-new',
    name: 'Constructors & new',
    category: 'objects-prototypes',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 3,
    definition:
      'new creates an object linked to Fn.prototype, binds this to it, runs the body, and returns the object — unless the body explicitly returns another object.',
    explanation: [
      'new performs four steps: create a fresh object, set its [[Prototype]] to Fn.prototype, invoke the function with this bound to that object, and return the object — unless the body explicitly returns an object, which overrides everything (primitive returns are ignored).',
      'It exists to combine allocation, prototype linking, and initialization in one operation, so every instance shares methods through the prototype while owning its own data — the pattern classes formalize.',
      'Forgetting new is the classic failure: the function runs as a plain call, this is undefined in strict mode (TypeError on first assignment) or globalThis in sloppy mode (silent global leaks), which is why classes throw when called without new and why new.target exists for detection.',
      '"Implement new as a function" is a canonical question because five lines must demonstrate Object.create, this binding via apply, and the object-return override rule — the full object model in miniature.',
      'The most common candidate mistake is omitting the return-override rule, or not knowing that new has the highest precedence among the this-binding rules, beating even bind.',
    ],
    prerequisites: ['prototype-chain', 'this-keyword'],
    leadsTo: ['es6-classes'],
    questions: [
      'What exactly does the new operator do? List every step.',
      'Implement new as a function: myNew(Fn, ...args).',
      'What happens if a constructor explicitly returns an object? A primitive?',
      'What goes wrong when you call a constructor function without new, in strict versus sloppy mode?',
      'What is new.target and what problem does it solve?',
      'How does new interact with a function created by bind?',
    ],
  },
  {
    slug: 'es6-classes',
    name: 'Classes',
    category: 'objects-prototypes',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 5,
    definition:
      'Syntax over prototypes: constructor, methods on the prototype, static members, #private fields, and super dispatch.',
    explanation: [
      'A class declaration compiles down to the constructor-plus-prototype pattern: the constructor becomes the function, methods land non-enumerable on .prototype, static members attach to the constructor itself, and extends wires two prototype chains (instances and constructors).',
      'It is not a new object model — instances still resolve methods through the same prototype chain — but it adds real semantics: mandatory new, strict mode, hard-private #fields enforced by the engine, and super for parent dispatch.',
      'Class fields are evaluated per instance, which creates the interview-favorite handler tradeoff: handleClick = () => {} auto-binds this per instance (safe to pass to listeners, but one function object per instance and invisible to the prototype), while a prototype method is shared but loses this when extracted.',
      '"Are JS classes real classes?" is asked to check you can say "syntax over prototypes" and then prove it — typeof a class is "function", and its methods live on .prototype.',
      'Common mistakes: calling methods that were passed as callbacks without binding, forgetting super(...) must run before this in a derived constructor, and assuming pre-#-era "private" underscore conventions were enforced.',
    ],
    prerequisites: ['constructor-functions-new'],
    leadsTo: ['inheritance-patterns'],
    questions: [
      'Are JavaScript classes real classes? What are they at runtime?',
      'Class field arrow function versus prototype method for an event handler — tradeoffs of each?',
      'Why must a derived class call super() before touching this?',
      'How are #private fields different from the underscore convention?',
      'Where do static members live, and how do you access them from an instance method?',
      'Translate a small class with one method into the equivalent constructor-function code.',
    ],
  },
  {
    slug: 'inheritance-patterns',
    name: 'Inheritance & Composition',
    category: 'objects-prototypes',
    level: 'advanced',
    importance: 4,
    interviewFrequency: 4,
    realWorldUsage: 3,
    definition:
      'extends wires prototype chains; composition (mixins, delegation, plain functions) usually beats deep hierarchies.',
    explanation: [
      'extends links both chains — Child.prototype delegates to Parent.prototype for instance methods, and Child itself delegates to Parent for statics — so inheritance in JS is always delegation, never copying.',
      'Deep hierarchies fail in practice: the fragile-base-class problem (parent changes ripple into every descendant), the gorilla-banana problem (you inherit everything whether you want it or not), and single inheritance forcing awkward taxonomies like Bird → FlyingBird vs Penguin.',
      'Composition assembles capabilities instead: factory functions merging behavior objects, mixin functions, or simply holding collaborators as properties — JavaScript is unusually good at this because objects and functions compose freely without class ceremony.',
      'The modern ecosystem voted with its feet — React dropped class inheritance for hooks, Vue 3 introduced composables — and interviewers expect you to defend "composition over inheritance" with a concrete example rather than recite the slogan.',
      'The nuanced mistake is absolutism: shallow inheritance is fine for genuine is-a relationships (extending Error or a framework base class); the senior answer is knowing where the line is, not banning extends.',
    ],
    prerequisites: ['es6-classes'],
    leadsTo: ['factory-pattern'],
    questions: [
      'Defend "composition over inheritance" with a concrete JavaScript example.',
      'What problems show up in deep class hierarchies, and how does composition avoid them?',
      'Implement a mixin pattern that adds shared behavior to multiple classes.',
      'What does extends actually wire up between two classes?',
      'When is class inheritance still the right choice in JavaScript?',
      'How did React hooks and Vue composables change the inheritance-vs-composition conversation?',
    ],
  },
  {
    slug: 'object-immutability',
    name: 'Immutability',
    category: 'objects-prototypes',
    level: 'intermediate',
    importance: 3,
    interviewFrequency: 3,
    realWorldUsage: 4,
    definition:
      'const vs Object.freeze vs structural sharing; the shallow-freeze caveat; immutable updates power change detection.',
    explanation: [
      'Three different mechanisms get conflated: const locks a binding (you cannot reassign the variable, but the object behind it stays mutable), Object.freeze locks an object\'s own properties, and immutable-update style never mutates at all — it builds new objects that share unchanged branches.',
      'freeze is shallow: nested objects remain writable, so true deep immutability requires recursive freezing — and in sloppy mode writes to frozen objects fail silently rather than throwing, hiding bugs.',
      'The reason stores demand immutable updates is change detection by reference: if state can only change by replacement, oldState === newState is a complete, O(1) dirty check — the foundation of Redux, React.memo, and efficient re-rendering.',
      'Interviewers ask "is a const object frozen?" and "does freeze deep-freeze?" as paired traps, then follow with why ===-based change detection breaks the moment someone mutates nested state in place.',
      'The everyday mistake is the in-place mutation that keeps the reference identical — state.items.push(x) — so memoized components and selectors never see the change.',
    ],
    prerequisites: ['primitive-vs-reference'],
    leadsTo: ['shallow-vs-deep-copy', 'pure-functions'],
    questions: [
      'Does Object.freeze deep-freeze an object? How would you achieve deep immutability?',
      'A const-declared object — what can and cannot change?',
      'Why do Redux and similar stores require immutable updates?',
      'What happens when you write to a frozen object in strict versus sloppy mode?',
      'Write an immutable update for a nested property using spread — and explain its shallow-copy pitfall.',
      'How does immutability make change detection an O(1) reference check?',
    ],
  },
  {
    slug: 'shallow-vs-deep-copy',
    name: 'Shallow vs Deep Copy',
    category: 'objects-prototypes',
    level: 'intermediate',
    importance: 4,
    interviewFrequency: 5,
    realWorldUsage: 5,
    definition:
      'Spread and Object.assign copy one level; structuredClone handles depth, cycles, and Dates; the JSON round-trip loses functions, undefined, and turns Dates into strings.',
    explanation: [
      'Spread and Object.assign copy only the top level — nested objects are copied as references, so mutating copy.nested still hits the original; this is the direct consequence of primitives-by-value, objects-by-reference.',
      'The folkloric deep copy, JSON.parse(JSON.stringify(x)), silently drops functions, undefined, and symbols, stringifies Dates, turns Maps and Sets into {}, and throws outright on circular references.',
      'structuredClone is the modern built-in answer: true deep copy with cycle support, preserving Dates, Maps, Sets, and typed arrays — though it still throws on functions and DOM nodes and does not copy prototypes or getters.',
      'This is a five-star interview topic because it chains naturally — reference semantics, JSON pitfalls, then "implement deepClone with cycle handling," where the expected trick is a WeakMap of already-cloned objects consulted before each recursive clone.',
      'The dated answer is the mistake: candidates who still offer the JSON trick with no caveats, or who claim spread is "a copy" without specifying one level deep, fail the senior bar; many also still do not know structuredClone exists.',
    ],
    prerequisites: ['primitive-vs-reference'],
    leadsTo: ['object-immutability'],
    questions: [
      'What are the pitfalls of JSON.parse(JSON.stringify(obj)) as a deep copy?',
      'Implement deepClone with circular-reference handling.',
      'What does structuredClone handle that the JSON trick cannot — and what can it still not copy?',
      'Why does mutating a nested property of a spread-copied object affect the original?',
      'You need to clone state containing Dates, a Map, and a cycle — what do you use and why?',
      'Is Object.assign a deep or shallow copy? Prove it with two lines of code.',
    ],
  },
  {
    slug: 'proxy-reflect',
    name: 'Proxy & Reflect',
    category: 'objects-prototypes',
    level: 'expert',
    importance: 3,
    interviewFrequency: 2,
    realWorldUsage: 3,
    definition:
      'Traps for fundamental object operations (get/set/has/deleteProperty); Reflect provides the default behaviors — the engine of Vue 3 reactivity.',
    explanation: [
      'A Proxy wraps a target with a handler whose traps intercept the language\'s fundamental internal operations — property reads, writes, has (the in operator), deleteProperty, even function calls and construction — running your code where the engine would normally act directly.',
      'Reflect is its mirror: Reflect.get, Reflect.set and friends perform the exact default behavior, so a trap typically does its side work and delegates to Reflect, preserving correct semantics for getters, receivers, and return values.',
      'This is precisely how Vue 3 reactivity works: reactive() returns a Proxy whose get trap records which effect read which key (dependency tracking) and whose set trap re-runs those effects — and because traps see every operation, it detects added keys, deletions, and index writes that Vue 2\'s defineProperty approach structurally could not.',
      'Interviewers use it as an expert-signal question: "how does Vue 3 know you changed state?" separates framework users from candidates who understand the machinery, with validation proxies and negative-index arrays as the standard build exercises.',
      'Common mistakes: forgetting to forward via Reflect inside traps (breaking getters and invariants), not knowing the proxy and target are distinct identities (proxy !== target, which is why Vue needs toRaw), and the catch-all claim that proxies are free — every trapped operation pays an interception cost.',
    ],
    prerequisites: ['property-descriptors'],
    leadsTo: ['observer-pattern'],
    questions: [
      'How does Vue 3 know you changed a piece of state? Walk through the get and set traps.',
      'Build a validation proxy that rejects writes of the wrong type.',
      'What is Reflect for, and why should traps delegate to it?',
      'Why could Vue 2 not detect added properties while Vue 3 can?',
      'Name five operations a Proxy can trap beyond get and set.',
      'Why is proxy !== target a problem, and how do frameworks expose the raw object?',
    ],
    overlooked: true,
  },
]
